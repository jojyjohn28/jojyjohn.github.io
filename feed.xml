<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://jojyjohn28.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://jojyjohn28.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-01-13T15:55:12-05:00</updated><id>https://jojyjohn28.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design.
</subtitle><entry><title type="html">Whole Genome Sequencing — Day 4: Genome Annotation &amp;amp; Functional Potential</title><link href="https://jojyjohn28.github.io/blog/wgs-day4-annotation-function/" rel="alternate" type="text/html" title="Whole Genome Sequencing — Day 4: Genome Annotation &amp;amp; Functional Potential" /><published>2026-01-13T00:00:00-05:00</published><updated>2026-01-13T00:00:00-05:00</updated><id>https://jojyjohn28.github.io/blog/wgs-day4-annotation-function</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/wgs-day4-annotation-function/"><![CDATA[<h1 id="-whole-genome-sequencing--day-4">🧬 Whole Genome Sequencing — Day 4</h1>

<p>Genome Annotation &amp; Functional Potential</p>

<p>👉In Day 1, we cleaned raw reads.
👉In Day 2, we assembled genomes and evaluated quality and topology.
👉In Day 3, we placed genomes in a taxonomic and phylogenetic context.</p>

<p>Today, in Day 4, <strong>we answer the most biologically meaningful question:</strong></p>

<p><strong>What can this genome actually do?</strong></p>

<p>This step transforms assembled genomes into functional blueprints by identifying genes, protein domains, pathways, and metabolic capabilities.</p>

<h2 id="-goal-of-day-4">🎯 Goal of Day 4</h2>

<p>Identify genes, pathways, and metabolic potential encoded in genomes</p>

<p>Specifically, we aim to:</p>

<p>✔ Predict protein-coding genes
✔ Assign functional annotations
✔ Reconstruct metabolic pathways
✔ Compare functional potential across many genomes</p>

<p>This workflow is designed for isolate genomes and MAG collections, and scales cleanly to hundreds of genomes.</p>

<h1 id="-annotation-strategy-a-layered-approach">🧪 Annotation Strategy: A Layered Approach</h1>

<p>Genome annotation works best when done in layers, each answering a different question:
| Layer | Question |
| —————— | —————————————- |
| Gene prediction | Where are the genes? |
| Rapid annotation | What are they likely doing? |
| Domain annotation | What conserved functions do they encode? |
| Pathway annotation | How do genes connect into metabolism? |
| Trait screening | What ecological traits are present? |</p>

<h2 id="-why-genome-annotation-matters">🧠 Why Genome Annotation Matters</h2>

<p>A genome sequence without annotation is just a long string of nucleotides.</p>

<p>Genome annotation enables us to:</p>

<p>● Interpret ecological strategies</p>

<p>● Predict metabolic roles</p>

<p>● Identify functional redundancy or specialization</p>

<p>● Link taxonomy to ecosystem function</p>

<p>● Good annotation is the foundation for:</p>

<p>● Comparative genomics</p>

<p>● Metabolic modeling</p>

<p>● Transcriptomic integration</p>

<p>● Functional redundancy analyses</p>

<h2 id="-annotation-strategy-overview">🧪 Annotation Strategy Overview</h2>

<p>Genome annotation is best approached in layers, not with a single tool.</p>

<p>Layered annotation strategy used here:</p>

<p>1️⃣ Gene prediction – Where are the genes?
2️⃣ Rapid annotation – What are they likely doing?
3️⃣ Domain-based annotation – What functional motifs do proteins contain?
4️⃣ Pathway-level reconstruction – How do genes connect into metabolism?</p>

<h2 id="-tools-used-in-day-4">🧰 Tools Used in Day 4</h2>

<p>🔹 Prokka</p>

<p>Rapid, genome-scale annotation pipeline for bacterial and archaeal genomes.</p>

<p>🔹 Prodigal</p>

<p>Accurate gene prediction engine used internally by many annotation tools.</p>

<p>🔹 InterProScan</p>

<p>Protein domain and motif identification across multiple databases.</p>

<p>🔹 DRAM</p>

<p>High-level metabolic and pathway-centric genome annotation.</p>

<p>Each tool complements the others rather than replacing them.</p>

<h1 id="-step-1-gene-prediction-with-prodigal">🧬 Step 1: Gene Prediction with Prodigal</h1>

<p>Gene prediction identifies open reading frames (ORFs) and defines the basic gene catalog.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>prodigal <span class="se">\</span>
  <span class="nt">-i</span> genome.fasta <span class="se">\</span>
  <span class="nt">-a</span> genome.faa <span class="se">\</span>
  <span class="nt">-d</span> genome.fna <span class="se">\</span>
  <span class="nt">-o</span> genome.genes.gbk <span class="se">\</span>
  <span class="nt">-p</span> single
</code></pre></div></div>

<p>Key outputs</p>

<p>● faa → predicted proteins</p>

<p>● fna → nucleotide CDS</p>

<p>● gbk → gene coordinates</p>

<p>Prodigal is:
✔ Fast
✔ Accurate
✔ Well-suited for isolates and high-quality MAGs</p>

<h1 id="-step-2-rapid-genome-annotation-with-prokka">🧬 Step 2: Rapid Genome Annotation with Prokka</h1>

<p>Prokka wraps gene prediction and functional assignment into a single, fast pipeline.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>prokka genome.fasta <span class="se">\</span>
  <span class="nt">--outdir</span> prokka_out <span class="se">\</span>
  <span class="nt">--prefix</span> genome <span class="se">\</span>
  <span class="nt">--cpus</span> 16
</code></pre></div></div>

<p>What Prokka provides</p>

<p>● Gene names and product descriptions</p>

<p>● rRNA, tRNA, tmRNA prediction</p>

<p>● GFF, GenBank, and protein FASTA files</p>

<p>Prokka is ideal for:
✔ First-pass annotation
✔ NCBI submissions
✔ Comparative gene counts</p>

<h1 id="-step-3-protein-domain-annotation-with-interproscan">🧬 Step 3: Protein Domain Annotation with InterProScan</h1>

<p>While Prokka assigns gene names, InterProScan identifies conserved protein domains, which is critical for:</p>

<p>● Novel proteins</p>

<p>● Hypothetical genes</p>

<p>● Functional inference beyond BLAST hits</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>interproscan.sh <span class="se">\</span>
  <span class="nt">-i</span> genome.faa <span class="se">\</span>
  <span class="nt">-o</span> genome.interpro.tsv <span class="se">\</span>
  <span class="nt">-f</span> TSV <span class="se">\</span>
  <span class="nt">-dp</span> <span class="se">\</span>
  <span class="nt">--cpu</span> 32
</code></pre></div></div>

<p>Why domain-based annotation matters</p>

<p>● Domains are more conserved than full-length proteins</p>

<p>● Enables functional inference for unknown genes</p>

<p>● Links proteins to GO terms and enzyme classes</p>

<h1 id="-step-4-metabolic--pathway-annotation-with-dram">🧬 Step 4: Metabolic &amp; Pathway Annotation with DRAM</h1>

<p>DRAM integrates multiple databases to reconstruct genome-scale metabolic potential.
<strong>DRAM annotation</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DRAM.py annotate <span class="se">\</span>
  <span class="nt">-i</span> genome.fasta <span class="se">\</span>
  <span class="nt">-o</span> dram_out <span class="se">\</span>
  <span class="nt">--threads</span> 32
</code></pre></div></div>

<p>DRAM distillation (summaries)</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DRAM.py distill <span class="se">\</span>
  <span class="nt">-i</span> dram_out/annotations.tsv <span class="se">\</span>
  <span class="nt">-o</span> dram_out/genome_summaries
</code></pre></div></div>

<p>DRAM excels at
✔ Central carbon metabolism
✔ Nitrogen, sulfur, hydrogen pathways
✔ CAZymes and polysaccharide utilization
✔ Transporters and redox systems</p>

<p>This moves annotation from gene lists → biological interpretation.
🧠 Focus Areas: Core Metabolic Pathways</p>

<p>DRAM distillation gives details on core metabolic potential, including:</p>

<p>🔹 Energy metabolism</p>

<p>Glycolysis</p>

<p>TCA cycle</p>

<p>Oxidative phosphorylation</p>

<p>Fermentation pathways</p>

<p>🔹 Carbon utilization</p>

<p>Sugar transporters</p>

<p>Polysaccharide degradation</p>

<p>Organic acid metabolism</p>

<p>🔹 Nitrogen &amp; sulfur metabolism</p>

<p>Nitrate/nitrite reduction</p>

<p>Amino acid biosynthesis</p>

<p>Sulfur assimilation</p>

<p><strong>These pathways define how organisms:</strong></p>

<p>● Acquire energy</p>

<p>● Use resources</p>

<p>● Interact with their environment</p>

<h1 id="-scaling-annotation-to-100-genomes">📊 Scaling Annotation to 100+ Genomes</h1>

<p>For large genome sets:</p>

<p>✔ Run Prokka and Prodigal in batch mode
✔ Use DRAM summaries for comparison tables
✔ Aggregate pathway presence/absence matrices
✔ Combine with Day 3 taxonomy and phylogeny</p>

<p>This enables:</p>

<p>● Functional clustering</p>

<p>● Ecological stratification</p>

<p>● Trait-based comparisons</p>

<p><img src="/assets/img/dram.png" alt="dram annotation" /></p>

<h1 id="-trait-based-screening-from-annotation-to-ecology">🔍 Trait-Based Screening: From Annotation to Ecology</h1>

<p>Once annotation is complete, we can screen genomes for specific ecological traits, such as:</p>

<p>● Biofilm formation</p>

<p>● EPS (exopolysaccharide) production</p>

<p>● Motility</p>

<p>● Stress response</p>

<p>● Host or particle association</p>

<p>This is especially powerful for comparative genomics and functional redundancy analyses.</p>

<p><strong>🧬 Example 1: Screening for Biofilm-Related Genes (InterProScan)</strong></p>

<p>Step 1: Create a reference keyword list</p>

<p>Create a simple table (CSV or Excel) with keywords or domain IDs:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Keyword
biofilm
adhesion
fimbrial
pilus
curli
polysaccharide-binding
</code></pre></div></div>

<p>This can also include:</p>

<p>● Pfam IDs</p>

<p>● GO terms</p>

<p>● InterPro IDs</p>

<p>Step 2: Filter InterProScan results using pandas</p>

<p>You can use filter.py for filtering the annotation file with reference file.
All codes can be found at :
https://github.com/jojyjohn28/whole-genome-sequencing-analysis
This produces a genome-specific list of candidate biofilm genes.</p>

<p><strong>🧬 Example 2: Screening for EPS-Related Genes (DRAM / KEGG)</strong></p>

<p>DRAM outputs include KEGG IDs, CAZymes, and functional descriptions, making them ideal for EPS screening.</p>

<p>Step 1: Prepare a reference list</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Keyword
exopolysaccharide
capsule
glycosyltransferase
alginate
cellulose
levan
</code></pre></div></div>

<h1 id="-merging-functional-traits-with-quantitative-data">🧬 Merging Functional Traits with Quantitative Data</h1>

<p>Trait screening becomes even more powerful when merged with abundance or expression data.</p>

<p>A generalized version of workflow is given in /scripts as merge.py</p>

<h1 id="-how-keywords-are-used-for-functional-screening">🔑 How Keywords Are Used for Functional Screening</h1>

<p>Many ecological traits (e.g., biofilm formation, EPS production, adhesion, motility) are not represented by a single gene, but by groups of genes, domains, or pathways.
Keyword-based screening provides a flexible, transparent way to identify such traits across genomes.</p>

<p>Below are two common and complementary approaches.</p>

<p>🧭 Option 1: Build Keyword-Based References from Databases</p>

<p>Keywords can be translated into reference identifiers using established databases.</p>

<p>🔹 InterPro / Pfam (protein domains)</p>

<p>Use keywords on the InterPro or Pfam websites to identify relevant domains:</p>

<p>Examples:</p>

<p>● biofilm → adhesion domains, pili, fimbriae</p>

<p>● EPS → glycosyltransferases, polysaccharide biosynthesis proteins</p>

<p>You can then screen InterProScan outputs using:</p>

<p>● InterPro IDs</p>

<p>● Pfam accessions</p>

<p>● Domain descriptions</p>

<p>This approach is robust for trait-level inference, even when gene names are ambiguous.</p>

<p>🔹 KEGG (pathways &amp; enzymes)</p>

<p>KEGG keywords can be used to identify:</p>

<p>● Pathway modules (e.g., polysaccharide biosynthesis)</p>

<p>● Enzyme classes (e.g., glycosyltransferases)</p>

<p>● KO identifiers linked to EPS or biofilm pathways</p>

<p>DRAM outputs already integrate KEGG annotations, making KEGG-based screening especially convenient.</p>

<h1 id="️-option-2-create-a-keyword-filter-list-and-screen-locally">🗂️ Option 2: Create a Keyword Filter List and Screen Locally</h1>

<p>For large genome sets, a practical approach is to create a simple keyword reference file and use it to filter annotation outputs.</p>

<p>Example keyword list (trait_keywords.txt):</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>biofilm
adhesion
pilus
fimbrial
exopolysaccharide
capsule
glycosyltransferase
alginate
cellulose
</code></pre></div></div>

<p>This file can be reused across:</p>

<p>● InterProScan outputs</p>

<p>● DRAM annotations</p>

<p>● Prokka product descriptions</p>

<h1 id="-example-keyword-based-screening-in-r">📊 Example: Keyword-Based Screening in R</h1>

<p>You can use screening_traits.R as minimal R workflow for screening annotation tables using keywords.</p>

<p>What this does</p>

<p>● Searches annotation descriptions for any keyword match</p>

<p>● Returns a genome-specific list of candidate trait genes</p>

<p>● Scales easily to hundreds of genomes</p>

<h1 id="-best-practices">🧠 Best Practices</h1>

<p>● Use keywords + domain IDs when possible</p>

<p>● Keep keyword lists transparent and version-controlled</p>

<p>● Treat results as hypothesis-generating, not definitive proof</p>

<p>● Combine with pathway context (DRAM) and expression data when available</p>

<p>This keyword-driven approach bridges the gap between raw annotation tables and ecological interpretation, making it especially powerful for comparative genomics and other downstream analyses.</p>

<h2 id="-key-takeaways-from-day-4">🧠 Key Takeaways from Day 4</h2>

<p>● Annotation works best in layers
● Prodigal ensures accurate gene prediction
● Prokka provides rapid functional context
● InterProScan enables trait-level screening
● DRAM connects genes into metabolic pathways
● Simple keyword-based filtering can reveal powerful ecological insights</p>

<h2 id="-coming-up-day-5">🔜 Coming Up: Day 5</h2>

<p>Day 5 — Comparative Genomics &amp; Functional Comparisons</p>

<p>We’ll integrate taxonomy (Day 3) and function (Day 4) to compare genomes across environments, clades, and ecological strategies.</p>

<p>All codes can be found at :
https://github.com/jojyjohn28/whole-genome-sequencing-analysis</p>

<p><strong>These functional screening results can be combined with genome taxonomy and phylogeny and used as annotation layers in iTOL.</strong>
For example, the figure below illustrates biofilm-related gene screening across 200+ genomes, where the presence or absence of biofilm-associated traits is overlaid onto the phylogenomic tree as metadata strips or heatmaps.
This integration allows rapid visualization of how functional traits are distributed across taxonomic lineages and evolutionary clades, enabling direct comparison of functional potential within and between groups.</p>

<p><img src="/assets/img/d3.png" alt="phylo and function" /></p>]]></content><author><name></name></author><summary type="html"><![CDATA[Day 4 of the WGS series: gene prediction and functional annotation using Prokka, Prodigal, InterProScan, and DRAM to reconstruct metabolic pathways and genome-scale functional potential.]]></summary></entry><entry><title type="html">Whole Genome Sequencing — Day 3: Taxonomy, Phylogeny &amp;amp; Genome Similarity</title><link href="https://jojyjohn28.github.io/blog/wgs-day3-taxonomy-phylogeny/" rel="alternate" type="text/html" title="Whole Genome Sequencing — Day 3: Taxonomy, Phylogeny &amp;amp; Genome Similarity" /><published>2026-01-12T00:00:00-05:00</published><updated>2026-01-12T00:00:00-05:00</updated><id>https://jojyjohn28.github.io/blog/wgs-day3-taxonomy-phylogeny</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/wgs-day3-taxonomy-phylogeny/"><![CDATA[<h1 id="-whole-genome-sequencing--day-3">🧬 Whole Genome Sequencing — Day 3</h1>

<p><strong>Taxonomy, Phylogeny &amp; Genome Similarity</strong></p>

<p>In <strong>Day 1</strong>, we focused on raw read quality control.
In <strong>Day 2</strong>, we assembled genomes and evaluated assembly quality and topology.</p>

<p>Today, in Day 3, we move from sequence reconstruction to biological identity.</p>

<p><strong>👉Who is this genome related to?</strong>
<strong>👉Where does it sit in the tree of life?</strong>
<strong>👉Is it a known species or something new?</strong></p>

<h2 id="-goal-of-day-3">🎯 Goal of Day 3</h2>

<p>Place genomes in an evolutionary and taxonomic context</p>

<p>Specifically, we aim to:</p>

<p>✔ Assign standardized genome-based taxonomy
✔ Construct phylogenomic trees
✔ Test species and genus boundaries
✔ Support NCBI submissions, MRAs, and comparative studies</p>

<p>This workflow is designed for dozens to hundreds of genomes, including:</p>

<p>• Isolate genomes</p>

<p>• MAG collections</p>

<p>• Mixed genome datasets</p>

<h1 id="-why-genome-based-taxonomy">🧠 Why Genome-Based Taxonomy?</h1>

<p>Historically, microbial taxonomy relied heavily on 16S rRNA genes. While useful, 16S alone:</p>

<p>• Lacks species-level resolution</p>

<p>• Is often fragmented or missing in MAGs</p>

<p>• Cannot resolve closely related taxa</p>

<p>Genome-based taxonomy solves these issues by using:</p>

<p>• Hundreds of conserved marker genes</p>

<p>• Whole-genome similarity metrics</p>

<p>• Curated, standardized reference frameworks</p>

<p>This is now the recommended approach for modern microbial genomics.</p>

<h1 id="-core-analyses-in-day-3">🧪 Core Analyses in Day 3</h1>

<p>1️⃣ Genome Taxonomy Assignment</p>

<p>Assign taxonomy using conserved marker genes rather than single loci.</p>

<p>2️⃣ Phylogenomic Tree Construction</p>

<p>Infer evolutionary relationships using genome-scale alignments.</p>

<p>3️⃣ Species &amp; Genus Boundary Testing</p>

<p>Evaluate novelty and relatedness using:</p>

<p>• ANI (nucleotide-level)</p>

<p>• AAI (protein-level)</p>

<p>• dDDH (digital DNA–DNA hybridization)</p>

<p>Using multiple metrics together provides robust taxonomic conclusions.</p>

<h1 id="-tools-used">🧰 Tools Used</h1>

<p>• GTDB-Tk – Genome-based taxonomy and phylogeny</p>

<p>• iTOL – Interactive phylogenomic tree visualization</p>

<p>• FastANI – Species-level genome similarity</p>

<p>• CompareM – Amino acid identity across genomes</p>

<p>• GGDC – Digital DNA–DNA hybridization (web-based)</p>

<p>• NCBI Datasets CLI – Reference genome retrieval</p>

<p>Each tool contributes a different layer of confidence.</p>

<h1 id="-gtdb-tk-genome-based-taxonomy">🧬 GTDB-Tk: Genome-Based Taxonomy</h1>

<p>GTDB-Tk (Genome Taxonomy Database Toolkit) assigns taxonomy based on a curated set of conserved marker genes and reference phylogenies.</p>

<p>1️⃣ Installing GTDB-Tk (Conda)</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> gtdbtk_env <span class="nt">-c</span> bioconda <span class="nt">-c</span> conda-forge gtdbtk
conda activate gtdbtk_env
gtdbtk <span class="nt">--version</span>
</code></pre></div></div>

<p>For large projects, installing in a custom path is recommended due to database size.</p>

<p>2️⃣ Downloading the GTDB Reference Database</p>

<p>GTDB-Tk requires the full GTDB reference database (&gt;100 GB).</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wget https://data.gtdb.ecogenomic.org/releases/release214/214.1/gtdbtk/gtdbtk_r214_data.tar.gz
<span class="nb">tar</span> <span class="nt">-xzf</span> gtdbtk_r214_data.tar.gz <span class="nt">-C</span> /project/bcampb7/camplab/databases/
</code></pre></div></div>

<p>Set the environment variable:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">GTDBTK_DATA_PATH</span><span class="o">=</span>/project/bcampb7/camplab/databases/gtdbtk_r214_data
<span class="nb">echo</span> <span class="s2">"export GTDBTK_DATA_PATH=/project/bcampb7/camplab/databases/gtdbtk_r214_data"</span> <span class="o">&gt;&gt;</span> ~/.bashrc
gtdbtk check_install
</code></pre></div></div>

<p>3️⃣ GTDB-Tk Standard Workflow</p>

<p>(Identify → Align → Classify)</p>

<p>Identify conserved marker genes</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gtdbtk identify <span class="se">\</span>
  <span class="nt">--genome_dir</span> genomes/ <span class="se">\</span>
  <span class="nt">--out_dir</span> gtdbtk/identify <span class="se">\</span>
  <span class="nt">--extension</span> fa <span class="se">\</span>
  <span class="nt">--cpus</span> 32
</code></pre></div></div>

<p>Align marker genes</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gtdbtk align <span class="se">\</span>
  <span class="nt">--identify_dir</span> gtdbtk/identify <span class="se">\</span>
  <span class="nt">--out_dir</span> gtdbtk/align <span class="se">\</span>
  <span class="nt">--cpus</span> 32
</code></pre></div></div>

<p>Classify genomes</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gtdbtk classify <span class="se">\</span>
  <span class="nt">--genome_dir</span> genomes/ <span class="se">\</span>
  <span class="nt">--align_dir</span> gtdbtk/align <span class="se">\</span>
  <span class="nt">--out_dir</span> gtdbtk/classify <span class="se">\</span>
  <span class="nt">--cpus</span> 32 <span class="se">\</span>
  <span class="nt">--mash_db</span> gtdbtk/mash_db
</code></pre></div></div>

<p>📄 Key output file</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gtdbtk.bac120.summary.tsv
</code></pre></div></div>

<p>This table provides:</p>

<p>• GTDB taxonomy (domain → species)</p>

<p>• Closest reference genomes</p>

<p>• Novelty indicators</p>

<p>• This file becomes the starting point for ANI, AAI, and dDDH analyses.\</p>

<p>• This file is also needed for running Denovo work flow in next step</p>

<p>Below is an example screenshort of gtdbtk.bac120.summary.tsv</p>

<p><img src="/assets/img/clsfy.png" alt="custom taxonomy" /></p>

<p>4️⃣ Full de_novo_wf (Automatic Tree Building)</p>

<p>For phylogenomic tree construction across many genomes:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gtdbtk de_novo_wf <span class="se">\</span>
  <span class="nt">--genome_dir</span> genomes/ <span class="se">\</span>
  <span class="nt">--out_dir</span> gtdbtk/de_novo <span class="se">\</span>
  <span class="nt">--extension</span> fa <span class="se">\</span>
  <span class="nt">--bacteria</span> <span class="se">\</span>
  <span class="nt">--cpus</span> 32 <span class="se">\</span>
  <span class="nt">--outgroup_taxon</span> p__Chloroflexota <span class="se">\</span>
  <span class="nt">--skip_gtdb_refs</span>
</code></pre></div></div>

<p>Notes</p>

<p>Choose an outgroup not present in your dataset</p>

<p>–skip_gtdb_refs reduces tree size for large studies</p>

<p>📄 Output tree:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gtdbtk.bac120.decorated.tree
</code></pre></div></div>

<p>🌳 Tree Visualization with iTOL</p>

<p>Convert GTDB-Tk trees for iTOL:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gtdbtk convert_to_itol <span class="se">\</span>
  <span class="nt">--input_tree</span> gtdbtk/de_novo/gtdbtk.bac120.decorated.tree <span class="se">\</span>
  <span class="nt">--output_tree</span> phylogeny_itol/OUTPUT_TREE
</code></pre></div></div>

<p>Upload to 👉 https://itol.embl.de/</p>

<p><img src="/assets/img/itol.png" alt="itol" /></p>

<p>What iTOL allows you to do</p>

<p>✔ Color branches by taxonomy
✔ Add metadata strips (sample type, site, season)
✔ Add heatmaps (genome size, gene counts, FRed, CAZymes)
✔ Collapse or expand clades
✔ Export publication-quality figures (SVG, PDF)</p>

<p>For more deatils on GTDBTk and iTol please see https://jojyjohn28.github.io/blog/gtdbtk-tree-custom/
A video tutorial will be coming soon too. please stay tuned.</p>

<h1 id="-genome-similarity-metrics-batch-mode">🧮 Genome Similarity Metrics (Batch Mode)</h1>

<p>Taxonomy alone is not sufficient for species-level conclusions.
We therefore apply three complementary genome similarity metrics.</p>

<p>🔢 ANI — Average Nucleotide Identity (FastANI)</p>

<p>✔ Purpose: Species-level resolution</p>

<p>✔ Typical cutoff: ~95–96%</p>

<p>✔ FastANI is fast and suitable for large genome collections.</p>

<p>🔬 AAI — Average Amino Acid Identity (CompareM)</p>

<p>✔ Purpose: Genus- and higher-level resolution</p>

<p>Typical cutoffs:</p>

<p>✔ Species: ~95%</p>

<p>✔ Genus: ~65–75%</p>

<p>✔ AAI is especially useful when ANI is very low or ambiguous.</p>

<p>🧬 dDDH — Digital DNA–DNA Hybridization (GGDC)</p>

<p>Purpose: Classical species validation
Species cutoff: 70%</p>

<p>GGDC is currently web-based:
👉 https://ggdc.dsmz.de/</p>

<p>Used primarily for:</p>

<p>✔ Novel species descriptions</p>

<p>✔ MRAs</p>

<p>✔ Formal taxonomic studies</p>

<p>🧠 Why Use ANI + AAI + dDDH Together?</p>

<p>Each metric captures different evolutionary signals:</p>

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>Resolution</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>ANI</td>
      <td>Species</td>
    </tr>
    <tr>
      <td>AAI</td>
      <td>Genus and above</td>
    </tr>
    <tr>
      <td>dDDH</td>
      <td>Classical species confirmation</td>
    </tr>
  </tbody>
</table>

<p>✔ Agreement across all three = strong taxonomic confidence
✔ Disagreement flags borderline or novel taxa</p>

<p>For more deatails about each analysis and codes can be find at :
https://jojyjohn28.github.io/blog/novel-species-a-step-by-step-guide/</p>

<h1 id="-key-takeaways-from-day-3">🧠 Key Takeaways from Day 3</h1>

<p>● Genome-based taxonomy is now the standard
● GTDB-Tk provides reproducible, scalable classification
● Phylogenomic trees add essential evolutionary context
● Species delineation requires multiple similarity metrics
● This workflow scales cleanly to 100+ genomes</p>

<h1 id="-coming-up-day-4">🔜 Coming Up: Day 4</h1>

<p>Day 4 — Genome Annotation &amp; Functional Potential</p>

<p>We move from who the genome is to what the genome can do — covering gene prediction, functional annotation, and metabolic reconstruction.</p>

<p>All codes can be found at :
https://github.com/jojyjohn28/whole-genome-sequencing-analysis</p>

<p><img src="/assets/img/tr.png" alt="tree taxonomy" /></p>]]></content><author><name></name></author><summary type="html"><![CDATA[Day 3 of the WGS series: genome-based taxonomy using GTDB-Tk, phylogenomic tree construction, and species/genus boundary testing using ANI, AAI, and dDDH across large genome sets.]]></summary></entry><entry><title type="html">Whole Genome Sequencing — Day 2: Genome Assembly, Quality Assessment, and Topology</title><link href="https://jojyjohn28.github.io/blog/wgs-day2-genome-assembly/" rel="alternate" type="text/html" title="Whole Genome Sequencing — Day 2: Genome Assembly, Quality Assessment, and Topology" /><published>2026-01-08T00:00:00-05:00</published><updated>2026-01-08T00:00:00-05:00</updated><id>https://jojyjohn28.github.io/blog/wgs-day2-genome-assembly</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/wgs-day2-genome-assembly/"><![CDATA[<h2 id="-whole-genome-sequencing--day-2">🧬 Whole Genome Sequencing — Day 2</h2>

<p>Genome Assembly, Quality Assessment, and Topology</p>

<p>In Day 1, we focused on raw read quality control and preprocessing.
Today, we move to the next critical step in whole genome analysis:</p>

<p><strong>👉 Genome assembly and assembly evaluation</strong></p>

<p>This step transforms high-quality sequencing reads into contiguous genomic sequences that form the foundation for taxonomy, annotation, and comparative genomics.</p>

<h2 id="-from-reads-to-genomes-what-are-reads-contigs-and-sequences">🧩 From Reads to Genomes: What Are Reads, Contigs, and Sequences?</h2>

<p>Before diving into assembly tools, it’s important to clarify a few core concepts that often cause confusion.</p>

<p><strong>🔹 What are raw reads?</strong></p>

<p>Reads are short (Illumina) or long (Nanopore/PacBio) DNA fragments directly produced by sequencing machines.
They represent small, unordered pieces of the genome — like shattered fragments of a book.</p>

<p>• Illumina: typically 100–300 bp</p>

<p>• Nanopore/PacBio: thousands to millions of bp</p>

<p>*<em>On their own, reads do not represent genes or genomes.</em></p>

<p><strong>🔹 What is a contig?</strong></p>

<p>A contig (contiguous sequence) is created by assembling overlapping reads into a longer continuous DNA sequence.</p>

<p>• Built computationally using assemblers (SPAdes, MEGAHIT, Flye, etc.)</p>

<p>• Represents a reconstructed region of the genome</p>

<p>• Can range from a few kb to several Mb</p>

<p><strong>👉 In draft assemblies, a genome may consist of many contigs.</strong></p>

<p><strong>🔹 What does “sequence” mean in genome assembly?</strong></p>

<p>In practice, a sequence usually refers to:</p>

<p>• a contig, or</p>

<p>• a scaffold, or</p>

<p>• a complete chromosome (single contig)</p>

<p>So when we say “one sequence represents the chromosome”, we mean:
✔ the entire chromosome is assembled into one contig, often circular in bacteria.</p>

<p>##🧠 Assembling a Broken Book</p>

<p>Think of genome assembly like this:</p>

<p>• 📄 Reads → torn sentences</p>

<p>• 📚 Contigs → reconstructed paragraphs</p>

<p>• 📖 Genome → the full book</p>

<p>*<em>Short-read data often leaves gaps (many contigs), while long reads can reconstruct the whole book in one piece.</em>
<img src="/assets/img/day2.png" alt="Genome assembly" /></p>

<p>##🔧 Assembly Strategies Overview</p>

<p>Different sequencing technologies require different assembly approaches:</p>

<table>
  <thead>
    <tr>
      <th>Data type</th>
      <th>Typical tools</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Illumina short reads</td>
      <td>SPAdes, MEGAHIT, Shovill</td>
    </tr>
    <tr>
      <td>Nanopore / PacBio long reads</td>
      <td>Flye</td>
    </tr>
    <tr>
      <td>Hybrid (short + long)</td>
      <td>SPAdes hybrid, Unicycler</td>
    </tr>
  </tbody>
</table>

<h2 id="-short-read-genome-assembly-illumina">🧱 Short-Read Genome Assembly (Illumina)</h2>

<p><strong>1️⃣ SPAdes</strong></p>

<p>SPAdes is widely used for bacterial isolate assemblies and supports multiple modes.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>spades.py <span class="se">\</span>
  <span class="nt">-1</span> sample_R1.fastq.gz <span class="se">\</span>
  <span class="nt">-2</span> sample_R2.fastq.gz <span class="se">\</span>
  <span class="nt">-o</span> spades_out <span class="se">\</span>
  <span class="nt">--threads</span> 32 <span class="se">\</span>
  <span class="nt">--memory</span> 128
</code></pre></div></div>

<p>Best for:
✔ Isolates
✔ Moderate genome sizes
✔ High accuracy</p>

<p><strong>2️⃣ MEGAHIT</strong></p>

<p>MEGAHIT is optimized for speed and large datasets.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>megahit <span class="se">\</span>
  <span class="nt">-1</span> sample_R1.fastq.gz <span class="se">\</span>
  <span class="nt">-2</span> sample_R2.fastq.gz <span class="se">\</span>
  <span class="nt">-o</span> megahit_out <span class="se">\</span>
  <span class="nt">--num-cpu-threads</span> 32
</code></pre></div></div>

<p>Best for:
✔ Large datasets
✔ Rapid exploratory assemblies
✔ Metagenomes or many isolates</p>

<p><strong>3️⃣ Shovill (Pipeline wrapper)</strong></p>

<p>Shovill is a fast, practical wrapper around SPAdes with automatic preprocessing.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>shovill <span class="se">\</span>
  <span class="nt">--R1</span> sample_R1.fastq.gz <span class="se">\</span>
  <span class="nt">--R2</span> sample_R2.fastq.gz <span class="se">\</span>
  <span class="nt">--outdir</span> shovill_out <span class="se">\</span>
  <span class="nt">--cpus</span> 32
</code></pre></div></div>

<p>For more details about Shovil Assembler ; Please read https://jojyjohn28.github.io/blog/genome-assembly-day/
Why I like Shovill:
✔ Simple
✔ Opinionated defaults
✔ Ideal for batch isolate assembly</p>

<h2 id="-long-read-assembly-nanopore--pacbio">🧬 Long-Read Assembly (Nanopore / PacBio)</h2>

<p><strong>4️⃣ Flye</strong></p>

<p>Flye is designed specifically for long noisy reads and can resolve complete circular chromosomes.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>flye <span class="se">\</span>
  <span class="nt">--nano-raw</span> sample_nanopore.fastq.gz <span class="se">\</span>
  <span class="nt">--out-dir</span> flye_out <span class="se">\</span>
  <span class="nt">--threads</span> 32
</code></pre></div></div>

<p>Flye often outputs:</p>

<p>✔ Single contig bacterial chromosomes</p>

<p>✔ Assembly graphs indicating circular topology</p>

<p>✔ Coverage estimates</p>

<p>For more details; Please read https://jojyjohn28.github.io/blog/genome-assembly-day/</p>

<p><strong>5️⃣ EPI2ME: Real-Time Analysis for Oxford Nanopore Data</strong></p>

<p><strong>EPI2ME is Oxford Nanopore Technologies’ cloud-based (and increasingly local) analysis platform designed to process Nanopore sequencing data in real time.</strong></p>

<p><strong>Why EPI2ME is useful</strong></p>

<p>•Runs during sequencing (live basecalling + analysis)</p>

<p>•Minimal setup — ideal for quick checks and teaching</p>

<p><strong>Provides:</strong></p>

<p>•Read quality summaries</p>

<p>•Taxonomic classification</p>

<p>•Assembly workflows (selected pipelines)</p>

<p>•AMR and functional screens (workflow-dependent)</p>

<p><strong>Limitations</strong></p>

<p>•Less customizable than command-line workflows</p>

<p>•Not ideal for large comparative projects</p>

<p>•Cloud dependency (unless using EPI2ME Labs locally)</p>

<p><strong>Best practice: Treat EPI2ME as a first look, not a replacement for reproducible, command-line assembly pipelines (Flye, SPAdes, etc.).</strong></p>

<h2 id="-hybrid-assembly-combining-short-and-long-reads">🧬 Hybrid Assembly: Combining Short and Long Reads</h2>

<p>Modern bacterial genome assembly often benefits from hybrid assembly, which integrates:</p>

<p>•Short reads (Illumina) → high base accuracy</p>

<p>•Long reads (ONT / PacBio) → long-range continuity</p>

<p>Together, they solve problems that each technology alone cannot.</p>

<p><strong>Why hybrid assembly matters</strong>
| Challenge | Short reads | Long reads | Hybrid |
| ——————— | ———– | ———- | —— |
| Repeats | ❌ | ✅ | ✅ |
| Structural resolution | ❌ | ✅ | ✅ |
| Base accuracy | ✅ | ❌ | ✅ |
| Circular genomes | ❌ | ✅ | ✅ |</p>

<h2 id="-common-hybrid-assembly-strategies">🔧 Common Hybrid Assembly Strategies</h2>

<p>1️⃣ Short-read–first (polish later)</p>

<p>•Assemble long reads with Flye</p>

<p>•Polish using Illumina reads (e.g., Pilon)</p>

<p>•Best when long-read coverage is high</p>

<p>2️⃣ True hybrid assemblers</p>

<p>• SPAdes (–hybrid)</p>

<p>• Unicycler (highly recommended for bacteria)_ my favorite</p>

<p>Example (Unicycler):</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>unicycler <span class="se">\</span>
  <span class="nt">-1</span> reads_R1.fastq.gz <span class="se">\</span>
  <span class="nt">-2</span> reads_R2.fastq.gz <span class="se">\</span>
  <span class="nt">-l</span> nanopore.fastq.gz <span class="se">\</span>
  <span class="nt">-o</span> unicycler_out <span class="se">\</span>
  <span class="nt">-t</span> 32
</code></pre></div></div>

<p><strong>🧠 Choosing the Right Strategy</strong>
| Data available | Recommended approach |
| ———————— | ————————– |
| Illumina only | SPAdes / Shovill |
| ONT only (high coverage) | Flye |
| Illumina + ONT | Unicycler or SPAdes hybrid |
| Training / rapid checks | EPI2ME |</p>

<p><img src="/assets/img/ha.png" alt="Hybrid Genome assembly" />
In this image Left panel: fragmented / problematic assembly and Right panel: improved assembly after hybrid approach. The assembly graph is visulized on Bandage.</p>

<h2 id="-assembly-evaluation-with-quast">📊 Assembly Evaluation with QUAST</h2>

<p>Once assemblies are generated, QUAST is used to evaluate their quality.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>quast.py <span class="se">\</span>
  spades_out/contigs.fasta <span class="se">\</span>
  megahit_out/final.contigs.fa <span class="se">\</span>
  shovill_out/contigs.fa <span class="se">\</span>
  flye_out/assembly.fasta <span class="se">\</span>
  <span class="nt">-o</span> quast_results
</code></pre></div></div>

<p>QUAST reports include:</p>

<p>• Genome size</p>

<p>• Number of contigs</p>

<p>• N50 / L50</p>

<p>• GC content</p>

<p>• Presence of Ns</p>

<p>If you have many genomes and plan to submit them to NCBI or a Microbial Resource Announcement, you can extract QUAST results as a summary table using Python 🐍
Example Python script,quast_to_table.py, can be find at day 2 of https://github.com/jojyjohn28/whole-genome-sequencing-analysis</p>

<p><strong>📈 GC Content Visualization (R)</strong></p>

<p>GC content is a quick diagnostic for contamination or misassemblies.
Eaxmple R script GC_vis.R also can be find at day 2 of https://github.com/jojyjohn28/whole-genome-sequencing-analysis</p>

<p><img src="/assets/img/gc.png" alt="GC content" /></p>

<h2 id="-genome-topology-linear-vs-circular">🔁 Genome Topology: Linear vs Circular</h2>

<p>Determining topology is essential for:</p>

<p>• NCBI submission</p>

<p>• Genome announcements</p>

<p>• Biological interpretation</p>

<p><strong>General rules:</strong></p>

<p>• Short-read assemblies → treat as linear/draft</p>

<p>• Long-read Flye assemblies → may be circular</p>

<p><strong>To validate circularity:</strong></p>

<p>•Inspect Flye logs</p>

<p>• Check for overlap at contig ends</p>

<p>• Confirm no duplicated terminal regions
More details and step by step instruction are published at : https://jojyjohn28.github.io/blog/genome-topology-and-genome-report/</p>

<h2 id="-assembly-graph-inspection-with-bandage">🧠 Assembly Graph Inspection with Bandage</h2>

<p>Bandage allows visual inspection of assembly graphs.
Assembly graph visulization using bandage can be find at : https://jojyjohn28.github.io/blog/genome-visualization/</p>

<h2 id="-key-takeaways-from-day-2">🧠 Key Takeaways from Day 2</h2>

<p>● No single assembler fits all data
● Always evaluate assemblies with QUAST
● GC content is a powerful sanity check
● Long reads enable topology resolution
● Bandage helps see what statistics cannot</p>

<p><strong>All batch scripts are available in the project repository.</strong>
https://github.com/jojyjohn28/whole-genome-sequencing-analysis/day2</p>

<p><strong>In Day 3</strong>, we will move from assemblies to taxonomy, phylogeny, and species delineation using GTDB-Tk, ANI, AAI, and dDDH.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Day 2 of the WGS series: short- and long-read genome assembly using SPAdes, MEGAHIT, Shovill, and Flye, followed by assembly evaluation with QUAST, GC content visualization, topology assessment, and graph inspection with Bandage.]]></summary></entry><entry><title type="html">Whole Genome Analysis — Day 1: From Raw Reads to Clean, Assembly-Ready Data</title><link href="https://jojyjohn28.github.io/blog/wgs-day1-raw-reads-qc/" rel="alternate" type="text/html" title="Whole Genome Analysis — Day 1: From Raw Reads to Clean, Assembly-Ready Data" /><published>2026-01-07T00:00:00-05:00</published><updated>2026-01-07T00:00:00-05:00</updated><id>https://jojyjohn28.github.io/blog/wgs-day1-raw-reads-qc</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/wgs-day1-raw-reads-qc/"><![CDATA[<h2 id="-whole-genome-analysis--day-1">🧬 Whole Genome Analysis — Day 1</h2>

<h3 id="from-raw-reads-to-clean-assembly-ready-data">From Raw Reads to Clean, Assembly-Ready Data</h3>

<p>Every genome project starts at the same place: <strong>raw sequencing files</strong>.<br />
Before assembly, annotation, or comparative genomics, it is critical to confirm that the data you received is <strong>complete, uncorrupted, and of sufficient quality</strong>.</p>

<p>In this post, I walk through <strong>Day 1 of a whole-genome sequencing (WGS) workflow</strong>, covering both <strong>Illumina (short reads)</strong> and <strong>Oxford Nanopore (long reads)</strong>.</p>

<h2 id="-input-data-formats">📂 Input Data Formats</h2>

<p>Typical WGS inputs include:</p>

<h3 id="illumina-paired-end">Illumina (paired-end)</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sample_R1.fastq.gz
sample_R2.fastq.gz
</code></pre></div></div>

<h2 id="-oxford-nanopore">📂 Oxford Nanopore</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sample_nanopore.fastq.gz
</code></pre></div></div>

<p><strong>These files often arrive from sequencing facilities or collaborators and should never be trusted blindly.</strong></p>

<h2 id="-step-0--file-integrity-check-md5sum">🔐 Step 0 — File Integrity Check (md5sum)</h2>

<p>Before opening or processing FASTQ files, verify they were transferred correctly.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">md5sum</span> <span class="k">*</span>.fastq.gz
</code></pre></div></div>

<p>Compare the output against checksums provided by the sequencing facility.</p>

<p><strong>✅ Why this matters</strong></p>

<p>• Prevents silent file corruption</p>

<p>• Saves hours of debugging downstream</p>

<p>• Essential for reproducible genomics</p>

<h2 id="-step-1--quality-control-of-illumina-reads-fastqc">🔍 Step 1 — Quality Control of Illumina Reads (FastQC)</h2>

<p>What is FastQC?</p>

<p>FastQC provides a quick visual summary of sequencing quality for FASTQ files.
It works on raw or trimmed Illumina data and produces an interactive HTML report.</p>

<p><strong>Installation</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-c</span> bioconda fastqc
</code></pre></div></div>

<p>or on HPC systems you can use module load.</p>

<p><strong>Run FastQC</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>fastqc_raw
fastqc <span class="nt">-o</span> fastqc_raw <span class="k">*</span>.fastq.gz
</code></pre></div></div>

<h2 id="-scaling-fastqc-for-100-genomes">🔁 Scaling FastQC for 100+ Genomes</h2>

<p>Running FastQC on one or two samples is easy.
But real projects often involve hundreds of genomes or metagenomes, each with paired-end reads.</p>

<p>Instead of running FastQC manually for each sample, you can automate it using either:</p>

<p>• a simple Bash loop (local or login node)</p>

<p>• a SLURM batch script (recommended for HPC environments)</p>

<p><strong>🧪 Option 1 — FastQC Using a Bash Loop</strong></p>

<p>This approach works well for:</p>

<p>• local machines</p>

<p>• small to medium datasets</p>

<p>• quick exploratory QC</p>

<p><strong>✅ Advantages</strong></p>

<p>• Simple</p>

<p>• Transparent</p>

<p>• Easy to debug</p>

<p><strong>⚠️ Limitations</strong></p>

<p>• Runs sequentially</p>

<p>• Not ideal for very large datasets</p>

<p><strong>🖥️ Option 2 — FastQC as a SLURM Batch Job (HPC-friendly)</strong></p>

<p>For large projects (100+ samples), always prefer batch execution.</p>

<p><strong>⚙️ Why Batch Processing Matters</strong></p>

<p>When working with large datasets, batch processing:</p>

<p>• avoids overloading login nodes</p>

<p>• scales efficiently across cores</p>

<p>• ensures reproducibility</p>

<p>• allows unattended execution</p>

<p>💡 Tip: Combine FastQC with MultiQC later to summarize all reports in one dashboard.</p>

<p><strong>All example scripts are available in the project repository.</strong>
https://github.com/jojyjohn28/whole-genome-sequencing-analysis</p>

<h2 id="-interpreting-fastqc-reports">📊 Interpreting FastQC Reports</h2>

<p>Key modules to inspect:</p>

<p>• Basic Statistics – total reads, GC %, read length</p>

<p>• Per Base Sequence Quality – look for quality drops at read ends</p>

<p>• Per Sequence Quality Scores – distribution of read quality</p>

<p>• Adapter Content – presence of sequencing adapters</p>

<p>• Overrepresented Sequences – contamination or adapters</p>

<p>• Sequence Duplication Levels – PCR artifacts or bias</p>

<p>Understanding the plot</p>

<p>• Red line → median quality</p>

<p>• Yellow box → interquartile range (25–75%)</p>

<p>• Whiskers → 10–90%</p>

<p>• Blue line → mean quality</p>

<p>• Green / Orange / Red background → good / warning / poor quality</p>

<p>📌 Raw data often looks messy — that’s normal.</p>

<p>See the plots below as an example to learn (source-Google)</p>

<p><img src="/assets/img/fastqc.png" alt="FastQC quality report example" /></p>

<p>Left: Raw reads with severe quality drop toward read ends
Right: Trimmed reads with stabilized base quality
(These plots clearly show why trimming is required before assembly.)</p>

<p><strong>If you have many genomes you can use standalone Python script that parses FastQC outputs and generates a summary table you can use for reporting, plotting, or downstream filtering.</strong>
For more details please see Day 1 in https://github.com/jojyjohn28/whole-genome-sequencing-analysis look for fastqc_to_table.py and fastqc_to_table.md.</p>

<h2 id="-step-2--quality-control-of-nanopore-reads-nanoplot">🔬 Step 2 — Quality Control of Nanopore Reads (NanoPlot)</h2>

<p>Illumina-style QC tools are not suitable for Nanopore data.</p>

<p><strong>What is NanoPlot?</strong></p>

<p>NanoPlot visualizes:</p>

<p>• Read length distribution</p>

<p>• Quality vs read length</p>

<p>• Yield over time</p>

<p>• N50 / N90 statistics</p>

<p><strong>Installation</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-c</span> bioconda nanoplot
</code></pre></div></div>

<p><strong>Run NanoPlot</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NanoPlot <span class="se">\</span>
  <span class="nt">--fastq</span> sample_nanopore.fastq.gz <span class="se">\</span>
  <span class="nt">--outdir</span> nanoplot_results <span class="se">\</span>
  <span class="nt">--threads</span> 8
</code></pre></div></div>

<p><strong>For loop/batch script please visit the project repository (Day1).</strong>
https://github.com/jojyjohn28/whole-genome-sequencing-analysis</p>

<p><strong>What to look for</strong></p>

<p>• Wide read length distribution (expected)</p>

<p>• No extreme quality collapse</p>

<p>• Reasonable N50 for your library type</p>

<h2 id="️-step-3--read-trimming-illumina">✂️ Step 3 — Read Trimming (Illumina)</h2>

<p>Raw Illumina reads almost always contain:</p>

<p>• Adapter contamination</p>

<p>• Low-quality bases at ends</p>

<p>Two commonly used tools are Trimmomatic and Cutadapt.</p>

<p><strong>🔧 Trimmomatic</strong>
Installation</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-c</span> bioconda trimmomatic
</code></pre></div></div>

<p><strong>General Default Command</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>trimmomatic PE <span class="nt">-threads</span> 16 <span class="se">\</span>
  sample_R1.fastq.gz sample_R2.fastq.gz <span class="se">\</span>
  sample_R1.paired.fastq.gz sample_R1.unpaired.fastq.gz <span class="se">\</span>
  sample_R2.paired.fastq.gz sample_R2.unpaired.fastq.gz <span class="se">\</span>
  ILLUMINACLIP:TruSeq3-PE.fa:2:30:10 <span class="se">\</span>
  LEADING:3 <span class="se">\</span>
  TRAILING:3 <span class="se">\</span>
  SLIDINGWINDOW:4:15 <span class="se">\</span>
  MINLEN:50
</code></pre></div></div>

<p><strong>Key Trimming Options</strong></p>

<p>• ILLUMINACLIP → adapter removal</p>

<p>• SLIDINGWINDOW → dynamic quality trimming</p>

<p>• LEADING / TRAILING → end trimming</p>

<p>• MINLEN → drop short reads</p>

<p><strong>✂️ Cutadapt (Alternative)</strong></p>

<p>Cutadapt provides more flexible adapter handling and is increasingly popular.
<strong>General Default Command</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cutadapt <span class="se">\</span>
  <span class="nt">-a</span> AGATCGGAAGAGC <span class="se">\</span>
  <span class="nt">-A</span> AGATCGGAAGAGC <span class="se">\</span>
  <span class="nt">-q</span> 20,20 <span class="se">\</span>
  <span class="nt">-m</span> 50 <span class="se">\</span>
  <span class="nt">-o</span> sample_R1.trimmed.fastq.gz <span class="se">\</span>
  <span class="nt">-p</span> sample_R2.trimmed.fastq.gz <span class="se">\</span>
  sample_R1.fastq.gz sample_R2.fastq.gz
</code></pre></div></div>

<h2 id="-step-4--post-trimming-qc">🔁 Step 4 — Post-Trimming QC</h2>

<p>Always rerun FastQC after trimming:</p>

<p><strong>You should observe:</strong></p>

<p>• Cleaner base quality profiles</p>

<p>• Reduced adapter content</p>

<p>• More uniform read lengths</p>

<h2 id="-output-of-day-1">📁 Output of Day 1</h2>

<p>By the end of Day 1, you should have:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Illumina:
sample_R1.trimmed.fastq.gz
sample_R2.trimmed.fastq.gz

Nanopore:
sample_nanopore.fastq.gz (QC-validated)
</code></pre></div></div>

<h2 id="-why-day-1-matters">🧠 Why Day 1 Matters</h2>

<p>All downstream steps assume:</p>

<p>• Accurate base calls</p>

<p>• Minimal sequencing artifacts</p>

<p>• Reliable read lengths</p>

<p>Skipping or rushing QC can:</p>

<p>• Break assemblies</p>

<p>• Inflate errors</p>

<p>• Bias comparative genomics</p>

<p><strong>Clean input = trustworthy genomes</strong></p>]]></content><author><name></name></author><summary type="html"><![CDATA[Day 1 of a whole-genome analysis series covering raw Illumina and Nanopore reads, integrity checks, quality control with FastQC and NanoPlot, and read trimming using Trimmomatic and Cutadapt.]]></summary></entry><entry><title type="html">Exploring the Pan-Genome with panX: A Practical Workflow for DARPA Isolates</title><link href="https://jojyjohn28.github.io/blog/panx-pangenome-analysis/" rel="alternate" type="text/html" title="Exploring the Pan-Genome with panX: A Practical Workflow for DARPA Isolates" /><published>2026-01-01T00:00:00-05:00</published><updated>2026-01-01T00:00:00-05:00</updated><id>https://jojyjohn28.github.io/blog/PanX_pangenome_analysis</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/panx-pangenome-analysis/"><![CDATA[<p><strong>🧬 Exploring the Pan-Genome with panX</strong></p>

<p>Today’s post walks through the complete setup and execution of <strong>panX</strong>, a powerful tool for analyzing bacterial pan-genomes.</p>

<p>One of my Master’s students is working on a project involving <strong>DARPA isolate genomes</strong>, focusing on:</p>

<ul>
  <li>Abiotic stress adaptation</li>
  <li>Niche classification (generalist vs specialist)</li>
  <li>Functional redundancy</li>
</ul>

<p>As part of the analysis, she selected <strong>20 isolates</strong> and classified them using:</p>

<ul>
  <li>Genome size</li>
  <li>Total gene count</li>
  <li>CAZyme profiles</li>
  <li>Transporters</li>
  <li>Growth on carbon sources</li>
</ul>

<p>But after carbon utilization assays, we realized there was still a missing piece:<br />
👉 <strong>The pangenome perspective</strong><br />
So we turned to <strong>panX</strong>.</p>

<p>panX (Neher et al., 2016) integrates:</p>

<ul>
  <li>Ortholog clustering</li>
  <li>Gene presence/absence matrix</li>
  <li>Phylogenetic inference</li>
  <li>Interactive web visualization</li>
</ul>

<p>I often use <strong>Anvi’o</strong>, but because of time constraints, panX was the best choice:<br />
✔ Fast<br />
✔ Lightweight<br />
✔ Clear cluster outputs<br />
✔ Better suited for quick exploratory analysis</p>

<hr />

<h5 id="️-step-1--cloning-and-setup">⚙️ Step 1 — Cloning and Setup</h5>

<p>Clone the official repository:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/neherlab/pan-genome-analysis.git
<span class="nb">cd </span>pan-genome-analysis
</code></pre></div></div>

<h5 id="-step-2--install-panx-dependencies-conda">🧱 Step 2 — Install panX Dependencies (Conda)</h5>

<p>The documentation suggests Miniconda2, but Miniconda3 works perfectly.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh
<span class="nb">source</span> ~/miniconda3/etc/profile.d/conda.sh
</code></pre></div></div>

<p>Create the panX environment:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">env </span>create <span class="nt">-f</span> panX-environment.yml
conda activate panX
</code></pre></div></div>

<p>***If you see dependency conflicts: conda env create -f panX-environment.yml –solver=libmamba
Test your installation:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sh run-TestSet.sh
</code></pre></div></div>

<h5 id="-step-3--preparing-genomes-with-prokka">🧬 Step 3 — Preparing Genomes with Prokka</h5>

<p>panX requires GenBank (.gbk) files.
So I annotated all genomes using Prokka.
Install Prokka:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> prokka_env <span class="nt">-c</span> bioconda <span class="nt">-c</span> conda-forge prokka
conda activate prokka_env
</code></pre></div></div>

<p>Batch annotation:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for </span>f <span class="k">in</span> <span class="k">*</span>.fa<span class="p">;</span> <span class="k">do
  </span><span class="nv">base</span><span class="o">=</span><span class="si">$(</span><span class="nb">basename</span> <span class="s2">"</span><span class="nv">$f</span><span class="s2">"</span> .fa<span class="si">)</span>
  prokka <span class="s2">"</span><span class="nv">$f</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">--outdir</span> ../prokka_out/<span class="k">${</span><span class="nv">base</span><span class="k">}</span> <span class="se">\</span>
    <span class="nt">--prefix</span> <span class="k">${</span><span class="nv">base</span><span class="k">}</span> <span class="se">\</span>
    <span class="nt">--cpus</span> 8 <span class="se">\</span>
    <span class="nt">--force</span> <span class="se">\</span>
    <span class="nt">--rnammer</span> off
<span class="k">done</span>
</code></pre></div></div>

<p>Collect all .gbk files:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> ../panx_input
find ../prokka_out <span class="nt">-name</span> <span class="s2">"*.gbk"</span> <span class="nt">-exec</span> <span class="nb">cp</span> <span class="o">{}</span> ../panx_input/ <span class="se">\;</span>
</code></pre></div></div>

<h5 id="-step-4--running-panx">🚀 Step 4 — Running panX</h5>

<p>Move into the panX project directory:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> ~/pan-genome-analysis/projects
<span class="nb">mkdir </span>DARPA_20
<span class="nb">cp</span> /project/bcampb7/camplab/Alisha/pangenome_analysis/panx_input/<span class="k">*</span>.gbk DARPA_20/
<span class="nb">cd </span>DARPA_20
</code></pre></div></div>

<p>Run the full pipeline:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python ../../scripts/run-pipeline.py <span class="se">\</span>
    <span class="nt">-fn</span> <span class="nb">.</span> <span class="se">\</span>
    <span class="nt">-sl</span> all <span class="se">\</span>
    <span class="nt">-st</span> 13456 <span class="se">\</span>
    <span class="nt">--threads</span> 32 <span class="se">\</span>
    <span class="nt">--diamond</span>
</code></pre></div></div>

<p>You will get output folders like:
● 1_parsed_input/
● 2_clusters/
● 3_alignments/
● 4_trees/
● 5_statistics/
● summary/</p>

<h5 id="-step-5--interactive-visualization-optional">🌐 Step 5 — Interactive Visualization (Optional)</h5>

<p>panX includes a full browser-based viewer using Node.js.
Clone visualization module:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/neherlab/pan-genome-visualization
<span class="nb">cd </span>pan-genome-visualization
npm <span class="nb">install</span> <span class="nt">--legacy-peer-deps</span>
git submodule update <span class="nt">--init</span> <span class="nt">--recursive</span>
</code></pre></div></div>

<p>Build and launch:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">DATA_ROOT_URL</span><span class="o">=</span>/dataset/
npm run build
npm start
</code></pre></div></div>

<p>Open:👉 http://localhost:8000
<strong>📂 Linking Your Dataset to the Viewer</strong>
Edit:
public/dataset/all_downloads_table.json
{
“panX link”: “gbk”,
“species name”: “GBK dataset”,
“source”: “Local”,
“gene cluster json”: “dataset/gbk/geneCluster.json”,
“metadata table”: “dataset/gbk/metainfo.tsv”,
“strain/species tree”: “dataset/gbk/strain_tree.nwk”
}
Refresh the page → your dataset appears in the menu.</p>

<h5 id="-step-6--extracting-core-and-pan-genes-without-visualization">🧬 Step 6 — Extracting Core and Pan Genes Without Visualization</h5>

<p><strong>Often on HPC (like Palmetto) you only need:</strong></p>

<p>● Core gene count
● Pan gene count
● Accessory genes per genome
● PanX provides allclusters_final.tsv which we can parse directly.</p>

<p>👉 Here is the Python script:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#!/usr/bin/env python3
</span><span class="kn">import</span> <span class="n">pandas</span> <span class="k">as</span> <span class="n">pd</span>
<span class="kn">from</span> <span class="n">collections</span> <span class="kn">import</span> <span class="n">Counter</span>

<span class="n">infile</span> <span class="o">=</span> <span class="sh">"</span><span class="s">allclusters_final.tsv</span><span class="sh">"</span>
<span class="n">outfile</span> <span class="o">=</span> <span class="sh">"</span><span class="s">core_pan_gene_counts_per_genome.tsv</span><span class="sh">"</span>

<span class="n">clusters</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">with</span> <span class="nf">open</span><span class="p">(</span><span class="n">infile</span><span class="p">)</span> <span class="k">as</span> <span class="n">f</span><span class="p">:</span>
    <span class="k">for</span> <span class="n">line</span> <span class="ow">in</span> <span class="n">f</span><span class="p">:</span>
        <span class="n">genes</span> <span class="o">=</span> <span class="p">[</span><span class="n">x</span><span class="p">.</span><span class="nf">strip</span><span class="p">()</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">line</span><span class="p">.</span><span class="nf">strip</span><span class="p">().</span><span class="nf">split</span><span class="p">(</span><span class="sh">"</span><span class="se">\t</span><span class="sh">"</span><span class="p">)</span> <span class="k">if</span> <span class="n">x</span><span class="p">.</span><span class="nf">strip</span><span class="p">()]</span>
        <span class="n">clusters</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">genes</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">genome_name</span><span class="p">(</span><span class="n">g</span><span class="p">):</span> <span class="k">return</span> <span class="n">g</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="sh">"</span><span class="s">|</span><span class="sh">"</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>

<span class="n">cluster_genomes</span> <span class="o">=</span> <span class="p">[</span><span class="nf">list</span><span class="p">(</span><span class="nf">set</span><span class="p">(</span><span class="nf">genome_name</span><span class="p">(</span><span class="n">g</span><span class="p">)</span> <span class="k">for</span> <span class="n">g</span> <span class="ow">in</span> <span class="n">cl</span><span class="p">))</span> <span class="k">for</span> <span class="n">cl</span> <span class="ow">in</span> <span class="n">clusters</span><span class="p">]</span>
<span class="n">all_genomes</span> <span class="o">=</span> <span class="nf">sorted</span><span class="p">({</span><span class="n">g</span> <span class="k">for</span> <span class="n">cl</span> <span class="ow">in</span> <span class="n">cluster_genomes</span> <span class="k">for</span> <span class="n">g</span> <span class="ow">in</span> <span class="n">cl</span><span class="p">})</span>

<span class="n">core_clusters</span> <span class="o">=</span> <span class="p">[</span><span class="n">cl</span> <span class="k">for</span> <span class="n">cl</span> <span class="ow">in</span> <span class="n">cluster_genomes</span> <span class="k">if</span> <span class="nf">len</span><span class="p">(</span><span class="n">cl</span><span class="p">)</span> <span class="o">==</span> <span class="nf">len</span><span class="p">(</span><span class="n">all_genomes</span><span class="p">)]</span>
<span class="n">genome_counter</span> <span class="o">=</span> <span class="nc">Counter</span><span class="p">(</span><span class="n">g</span> <span class="k">for</span> <span class="n">cl</span> <span class="ow">in</span> <span class="n">cluster_genomes</span> <span class="k">for</span> <span class="n">g</span> <span class="ow">in</span> <span class="n">cl</span><span class="p">)</span>

<span class="n">summary</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nc">DataFrame</span><span class="p">({</span>
    <span class="sh">"</span><span class="s">Genome</span><span class="sh">"</span><span class="p">:</span> <span class="n">all_genomes</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">Pan_genes</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span><span class="n">genome_counter</span><span class="p">[</span><span class="n">g</span><span class="p">]</span> <span class="k">for</span> <span class="n">g</span> <span class="ow">in</span> <span class="n">all_genomes</span><span class="p">],</span>
<span class="p">})</span>
<span class="n">summary</span><span class="p">[</span><span class="sh">"</span><span class="s">Core_genes</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">core_clusters</span><span class="p">)</span>
<span class="n">summary</span><span class="p">[</span><span class="sh">"</span><span class="s">Accessory_genes</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">summary</span><span class="p">[</span><span class="sh">"</span><span class="s">Pan_genes</span><span class="sh">"</span><span class="p">]</span> <span class="o">-</span> <span class="n">summary</span><span class="p">[</span><span class="sh">"</span><span class="s">Core_genes</span><span class="sh">"</span><span class="p">]</span>
<span class="n">summary</span><span class="p">[</span><span class="sh">"</span><span class="s">Core_fraction(%)</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="p">(</span><span class="n">summary</span><span class="p">[</span><span class="sh">"</span><span class="s">Core_genes</span><span class="sh">"</span><span class="p">]</span> <span class="o">/</span> <span class="n">summary</span><span class="p">[</span><span class="sh">"</span><span class="s">Pan_genes</span><span class="sh">"</span><span class="p">]</span> <span class="o">*</span> <span class="mi">100</span><span class="p">).</span><span class="nf">round</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span>

<span class="n">summary</span><span class="p">.</span><span class="nf">to_csv</span><span class="p">(</span><span class="n">outfile</span><span class="p">,</span> <span class="n">sep</span><span class="o">=</span><span class="sh">"</span><span class="se">\t</span><span class="sh">"</span><span class="p">,</span> <span class="n">index</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">✅ Saved:</span><span class="sh">"</span><span class="p">,</span> <span class="n">outfile</span><span class="p">)</span>
</code></pre></div></div>

<p>Run with:python core_pan_from_list.py
Output file: core_pan_gene_counts_per_genome.tsv
<strong>Contains:</strong></p>

<p>● Core genes
● Pan genes
● Accessory genes
● Core gene % per genome</p>

<p><strong>Perfect for downstream comparative genomics.</strong></p>

<p>For more details on PanX visit :https://pangenome.org/</p>

<p><img src="/assets/img/panx.png" alt="ncbi_submission" /></p>]]></content><author><name></name></author><summary type="html"><![CDATA[5 complete walkthrough using panX to analyze the pan-genome of 20 DARPA isolates—annotation, pipeline execution, interactive visualization, and core/pan gene extraction.]]></summary></entry><entry><title type="html">Extracting Full-Length 16S rRNA from Bacterial Genomes Using Barrnap</title><link href="https://jojyjohn28.github.io/blog/barrnap-16s-extraction/" rel="alternate" type="text/html" title="Extracting Full-Length 16S rRNA from Bacterial Genomes Using Barrnap" /><published>2025-12-29T00:00:00-05:00</published><updated>2025-12-29T00:00:00-05:00</updated><id>https://jojyjohn28.github.io/blog/barrnap-16s-extraction</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/barrnap-16s-extraction/"><![CDATA[<h4 id="-extracting-full-length-16s-rrna-from-bacterial-genomes-using-barrnap">🧬 Extracting Full-Length 16S rRNA from Bacterial Genomes Using Barrnap</h4>

<p>Whole-genome sequencing has transformed microbial taxonomy, with genome-wide metrics such as ANI, AAI, and dDDH now serving as the gold standard for species delineation.
However, the 16S rRNA gene remains an essential taxonomic anchor, especially for linking isolate genomes to classical taxonomy and amplicon-based studies.</p>

<p>In this short post, I walk through a simple and reproducible workflow to:</p>

<ol>
  <li>
    <p>Extract full-length 16S rRNA genes from an assembled bacterial genome</p>
  </li>
  <li>
    <p>Validate taxonomic placement using NCBI BLAST</p>
  </li>
</ol>

<p><strong>🔧 Tool Overview: Barrnap</strong></p>

<p>Barrnap (BAsic Rapid Ribosomal RNA Predictor) is a fast and lightweight tool that identifies ribosomal RNA genes (5S, 16S, and 23S) in bacterial and archaeal genomes using HMM-based models.</p>

<p>Why Barrnap?</p>

<p>● Extremely fast (seconds per genome)</p>

<p>● Accurate for bacterial and archaeal rRNAs</p>

<p>● Ideal for complete genomes and high-quality drafts</p>

<p>● Outputs standard GFF annotations</p>

<p><strong>🔧 Installation (Linux / Conda)</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-c</span> bioconda barrnap
</code></pre></div></div>

<p>▶️ You’ll also need BEDTools for sequence extraction:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-c</span> bioconda bedtools
</code></pre></div></div>

<h4 id="️-step-1-predict-rrna-genes-with-barrnap">▶️ Step 1: Predict rRNA genes with Barrnap</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>barrnap <span class="nt">--kingdom</span> bac genome.fasta <span class="o">&gt;</span> genome_rrna.gff
</code></pre></div></div>

<p>**Replace genome.fasta with your genome file</p>

<p><strong>What this step does</strong></p>

<p>● Scans the genome for rRNA genes using bacterial HMM profiles</p>

<p>● Identifies coordinates for 5S, 16S, and 23S rRNA genes</p>

<p>● Outputs results in GFF format with genomic locations</p>

<h4 id="-step-2-extract-the-16s-rrna-sequence">🧬 Step 2: Extract the 16S rRNA sequence</h4>

<p>Once rRNA coordinates are available, extract the sequence directly from the genome using bedtools:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bedtools getfasta <span class="se">\</span>
  <span class="nt">-fi</span> genome.fasta <span class="se">\</span>
  <span class="nt">-bed</span> genome_rrna.gff <span class="se">\</span>
  <span class="nt">-fo</span> genome_rrna.fasta
</code></pre></div></div>

<p>The output FASTA typically contains:</p>

<p>● One or more rRNA sequences</p>

<p>● Full-length or near full-length 16S rRNA genes (≈1,500 bp)</p>

<p>If multiple rRNA operons are present, all will be extracted.
hint : I always select the longest one to proceed</p>

<h4 id="-step-3-identify-the-closest-relatives-using-ncbi-blast">🔍 Step 3: Identify the closest relatives using NCBI BLAST</h4>

<p>The extracted 16S rRNA sequence can now be compared against public databases using NCBI BLAST.</p>

<p><strong>Option A: Web-based BLAST (quickest)</strong></p>

<ol>
  <li>
    <p>Go to: https://blast.ncbi.nlm.nih.gov</p>
  </li>
  <li>
    <p>Select BLASTn</p>
  </li>
  <li>
    <p>Paste the 16S sequence from PC_D3_3_rrna.fasta</p>
  </li>
  <li>
    <p>Database: nt</p>
  </li>
  <li>
    <p>Program selection: Highly similar sequences (megablast)</p>
  </li>
</ol>

<p><strong>Option B: Command-line BLAST (reproducible)</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>blastn <span class="se">\</span>
  <span class="nt">-query</span> genome_rrna.fasta <span class="se">\</span>
  <span class="nt">-db</span> nt <span class="se">\</span>
  <span class="nt">-out</span> genome_16S_blast.tsv <span class="se">\</span>
  <span class="nt">-outfmt</span> 6 <span class="se">\</span>
  <span class="nt">-max_target_seqs</span> 10 <span class="se">\</span>
  <span class="nt">-evalue</span> 1e-20
</code></pre></div></div>

<p>**To run BLAST from the command line, you need the NCBI BLAST+ toolkit installed and access to a nucleotide database (e.g., nt). BLAST+ can be installed via Conda, and the nt database can either be downloaded locally or accessed through a preconfigured BLAST database path on an HPC system.</p>

<h4 id="step-4--how-to-interpret-blast-results">Step 4 📊 How to interpret BLAST results</h4>

<p>Key BLAST columns to focus on:</p>

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>What it means</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>% identity</td>
      <td>Sequence similarity to reference</td>
    </tr>
    <tr>
      <td>Alignment length</td>
      <td>Length of the matched region</td>
    </tr>
    <tr>
      <td>Query coverage</td>
      <td>Fraction of 16S gene aligned</td>
    </tr>
    <tr>
      <td>E-value</td>
      <td>Statistical significance</td>
    </tr>
    <tr>
      <td>Subject description</td>
      <td>Closest named organism</td>
    </tr>
  </tbody>
</table>

<p><strong>Typical interpretation guidelines</strong></p>

<p>● ≥99% identity → likely same species</p>

<p>● 98.7–99% identity → borderline species-level similarity</p>

<p>● &lt;98.7% identity → distinct species</p>

<p>● &lt;95% identity → possibly different genus</p>

<p>*<em>It is important to note that 16S alone is not sufficient for species designation, but it provides strong supporting evidence when combined with genome-wide metrics.</em></p>

<h4 id="-why-include-16s-in-a-genome-based-study">🧠 Why include 16S in a genome-based study?</h4>

<p>Even in the genomics era, 16S rRNA sequences are still valuable for:</p>

<p>● Connecting isolate genomes to historical literature</p>

<p>● Comparing isolates to amplicon-based surveys</p>

<p>● Supporting taxonomic claims in Genome Resource Announcements</p>

<p>● Providing a familiar reference for non-genomics audiences</p>

<p><strong>In practice, 16S supports taxonomy, while ANI, AAI, and dDDH define it.</strong></p>

<h4 id="-take-home-messages">✅ Take-home messages</h4>

<p>● Barrnap provides a fast and reliable way to extract rRNA genes from genomes</p>

<p>● Full-length 16S rRNA sequences remain an important taxonomic reference</p>

<p>● BLAST results should be interpreted alongside genome-wide comparisons</p>

<p>● This workflow takes minutes and fits naturally into any isolate genome analysis</p>

<p><img src="/assets/img/br.png" alt="16s-from-genome" /></p>]]></content><author><name></name></author><summary type="html"><![CDATA[A quick and reproducible workflow to extract 16S rRNA genes from assembled genomes and validate taxonomy using NCBI BLAST.]]></summary></entry><entry><title type="html">From Genome Assembly to Novel Species: A Step-by-Step Genomic Workflow</title><link href="https://jojyjohn28.github.io/blog/novel-species-a-step-by-step-guide/" rel="alternate" type="text/html" title="From Genome Assembly to Novel Species: A Step-by-Step Genomic Workflow" /><published>2025-12-23T00:00:00-05:00</published><updated>2025-12-23T00:00:00-05:00</updated><id>https://jojyjohn28.github.io/blog/novel-species-a-step-by-step-guide</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/novel-species-a-step-by-step-guide/"><![CDATA[<h4 id="-from-genome-assembly-to-novel-species-a-step-by-step-genomic-workflow">🧬 From Genome Assembly to Novel Species: A Step-by-Step Genomic Workflow</h4>

<p><strong>Case study: Winogradskyella sp. PC_D3_3</strong></p>

<p>Today’s post steps slightly away from community profiling and focuses on a question every microbial genomicist eventually faces:</p>

<p><strong>How do we confidently determine whether a newly sequenced genome represents a novel bacterial species?</strong></p>

<p>Using a newly assembled long-read genome (Winogradskyella sp. PC_D3_3), I walk through <strong>a complete, reproducible workflow</strong>—from assembly quality checks to taxonomic placement, genomic similarity metrics (ANI, AAI, dDDH), and functional interpretation.</p>

<p>This is the same logic used for <strong>NCBI submissions, Genome Resource Announcements, and formal species descriptions</strong>.</p>

<h4 id="step-1--long-read-sequence-quality-assessment-with-nanoplot">Step 1: 🔍 Long-Read Sequence Quality Assessment with NanoPlot</h4>

<p>Before genome assembly, assessing the quality of long-read sequencing data is critical to ensure sufficient read length, coverage, and overall data reliability. For Oxford Nanopore Technologies (ONT) reads, I used NanoPlot to visualize read length distributions, quality scores, and cumulative yield.</p>

<p><strong>Why NanoPlot?</strong></p>

<p>● NanoPlot provides a rapid overview of key long-read metrics, including:</p>

<p>● Read length distribution (mean, N50, max length)</p>

<p>● Per-read quality score distribution</p>

<p>● Cumulative sequencing yield</p>

<p>● Read length vs. quality relationships</p>

<p>These metrics help determine whether the dataset is suitable for de novo assembly and whether additional filtering or re-sequencing is required.
<strong>🔧 Installation (Linux / Conda)</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-c</span> bioconda nanoplot
</code></pre></div></div>

<p>▶️ Running NanoPlot</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NanoPlot <span class="se">\</span>
  <span class="nt">--fastq</span> PC_D3_3.fastq.gz <span class="se">\</span>
  <span class="nt">--outdir</span> nanoplot_PC_D3_3 <span class="se">\</span>
  <span class="nt">--threads</span> 8
</code></pre></div></div>

<p><strong>📊 Interpreting the Output</strong></p>

<p>Key outputs generated by NanoPlot include:</p>

<p>● Read length histogram — confirms the presence of long reads necessary for resolving repeats</p>

<p>● Read quality distribution — ensures sufficient base-calling accuracy</p>

<p>● Cumulative yield plot — verifies total sequencing depth</p>

<p>● Length vs. quality scatter plots — identifies potential low-quality long reads</p>

<h4 id="-step-2-long-read-genome-assembly">🧪 Step 2: Long-Read Genome Assembly</h4>

<p>The genome was assembled using Oxford Nanopore long reads and Flye, which is well suited for circular bacterial chromosomes.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python flye <span class="se">\</span>
  <span class="nt">--nano-raw</span> PC_D3_3.fastq.gz <span class="se">\</span>
  <span class="nt">--threads</span> 32 <span class="se">\</span>
  <span class="nt">--out-dir</span> lr
</code></pre></div></div>

<p><strong>Assembly summary:</strong></p>

<p>● Genome size: 4.21 Mb</p>

<p>● Contigs: 1</p>

<p>● Coverage: ~170×</p>

<p>● Topology: Circular</p>

<p>Flye produced a single, high-coverage contig, consistent with a complete bacterial chromosome.</p>

<h4 id="-step-3-assembly-quality-assessment">📊 Step 3: Assembly Quality Assessment</h4>

<p><strong>QUAST</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>quast.py PC_D3_3.fasta <span class="nt">-o</span> quast_output
</code></pre></div></div>

<p><strong>Key statistics:</strong></p>

<p>● Contigs: 1</p>

<p>● N50: 4,212,977 bp</p>

<p>● GC content: 33.32%</p>

<p>● Ns: 0</p>

<p><strong>CheckM</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>checkm lineage_wf <span class="nt">-x</span> fa lr checkm_out
</code></pre></div></div>

<p>● Completeness: 99.28%</p>

<p>● Contamination: 1.09%</p>

<p>● Strain heterogeneity: 0%</p>

<p>✔ High completeness and low contamination support downstream taxonomic claims.</p>

<h4 id="-step-4-genome-annotation-overview">🧬 Step 4: Genome Annotation Overview</h4>

<p><strong>Prokka</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>prokka PC_D3_3.fasta <span class="nt">--outdir</span> prokka
</code></pre></div></div>

<p>Prokka gives overall annotations.</p>

<p>● CDS: 3,748</p>

<p>● rRNA: 12</p>

<p>● tRNA: 48</p>

<p>● tmRNA: 1</p>

<p><strong>DRAM (functional overview)</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DRAM.py annotate <span class="nt">-i</span> PC_D3_3.fasta <span class="nt">-o</span> dram
DRAM.py distill <span class="nt">-i</span> dram/annotations.tsv <span class="nt">-o</span> dram/genome_summaries
</code></pre></div></div>

<p><strong>Why DRAM?</strong>
It provides:</p>

<p>● CAZyme profiles</p>

<p>● Central metabolism</p>

<p>● Transporters</p>

<p>● Secondary metabolites</p>

<p>● Polysaccharide utilization potential</p>

<p><strong>This goes far beyond simple gene counts.</strong></p>

<h4 id="-step-5-taxonomic-placement-with-gtdb-tk">🧭 Step 5: Taxonomic Placement with GTDB-Tk</h4>

<p>To assign standardized taxonomy and place the genome within the Genome Taxonomy Database (GTDB) framework, I used GTDB-Tk, which classifies genomes based on a curated set of conserved marker genes and reference phylogenies.</p>

<p><strong>1️⃣ Identify marker genes</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gtdbtk identify <span class="se">\</span>
  <span class="nt">--genome_dir</span> /scratch/jojyj/win_dec11/lr <span class="se">\</span>
  <span class="nt">--out_dir</span> /scratch/jojyj/win_dec11/gtdbtk/identify <span class="se">\</span>
  <span class="nt">--extension</span> fasta <span class="se">\</span>
  <span class="nt">--cpus</span> 32
</code></pre></div></div>

<p><strong>What this does:</strong>
This step scans the genome for GTDB’s conserved bacterial marker genes and prepares them for downstream phylogenetic placement.</p>

<p><strong>2️⃣ Align marker genes</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gtdbtk align <span class="se">\</span>
  <span class="nt">--identify_dir</span> /scratch/jojyj/win_dec11/gtdbtk/identify <span class="se">\</span>
  <span class="nt">--out_dir</span> /scratch/jojyj/win_dec11/gtdbtk/align <span class="se">\</span>
  <span class="nt">--cpus</span> 32
</code></pre></div></div>

<p>Identified marker genes are aligned against GTDB reference alignments, enabling consistent phylogenetic comparison across genomes.</p>

<p><strong>3️⃣ Classify the genome</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gtdbtk classify <span class="se">\</span>
  <span class="nt">--genome_dir</span> /scratch/jojyj/win_dec11/lr <span class="se">\</span>
  <span class="nt">--align_dir</span> /scratch/jojyj/win_dec11/gtdbtk/align <span class="se">\</span>
  <span class="nt">--out_dir</span> /scratch/jojyj/win_dec11/gtdbtk/classify <span class="se">\</span>
  <span class="nt">-x</span> fasta <span class="se">\</span>
  <span class="nt">--cpus</span> 32 <span class="se">\</span>
  <span class="nt">--mash_db</span> /scratch/jojyj/win_dec11/gtdbtk
</code></pre></div></div>

<p>This step assigns taxonomy to the genome by placing it into the GTDB reference tree using both phylogenetic placement and Mash-based genome distance estimates.</p>

<p><strong>📄 Key Output: GTDB-Tk Classification Table</strong></p>

<p>The classification table produced here is critical for the next steps.
File of interest:</p>

<pre><code class="language-pgsql">gtdbtk.bac120.summary.tsv
</code></pre>

<p>(or classify.csv, depending on GTDB-Tk version)</p>

<p>This table contains:</p>

<p>● GTDB taxonomy (domain → species)</p>

<p>● Closest GTDB reference genomes</p>

<p>● ANI-based placement confidence</p>

<p>● Novelty flags (e.g., no species assignment)</p>

<h4 id="how-this-table-is-used-next-important">How this table is used next (important)</h4>

<p>The GTDB-Tk classification output is typically used to:</p>

<p>● Identify closest reference genomes</p>

<p>● Extract GTDB/NCBI accession IDs</p>

<p>● Download ~10–20 closely related genomes using the NCBI CLI</p>

<p>● Perform ANI, AAI, and dDDH comparisons</p>

<h4 id="-step-6-download-closely-related-genomes-ncbi-cli">📥 Step 6: Download Closely Related Genomes (NCBI CLI)</h4>

<p>To test novelty, we compare PC_D3_3 against closely related reference genomes.
<strong>Install NCBI datasets (Linux)</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-c</span> conda-forge ncbi-datasets-cli
</code></pre></div></div>

<p>Download reference genomes</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>datasets download genome taxon <span class="s2">"Winogradskyella"</span> <span class="se">\</span>
  <span class="nt">--assembly-level</span> <span class="nb">complete</span> <span class="se">\</span>
  <span class="nt">--filename</span> winogradskyella_refs.zip
</code></pre></div></div>

<p>Here I used the classify table created using GTDBtk classify.
Unzip and retain ~10–20 representative genomes.</p>

<h4 id="-step-7-ani-analysis-fastani">🧮 Step 7: ANI Analysis (FastANI)</h4>

<p>Average Nucleotide Identity (ANI) comparisons between Winogradskyella sp. PC_D3_3 and representative reference genomes:</p>

<p>Install FastANI</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-c</span> bioconda fastani
</code></pre></div></div>

<p><strong>Run ANI</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>fastANI <span class="se">\</span>
  <span class="nt">-q</span> PC_D3_3.fasta <span class="se">\</span>
  <span class="nt">-r</span> reference_genomes/<span class="k">*</span>.fna <span class="se">\</span>
  <span class="nt">-o</span> ani_results.txt
</code></pre></div></div>

<p><strong>my output</strong>
| Reference genome | ANI (%) |
| ———————- | ——- |
| <em>W. psychrotolerans</em> B | 85.82 |
| <em>W. thalassocola</em> | 85.38 |
| <em>W. pacifica</em> | 84.96 |
| <em>W. marina</em> | 81.85 |</p>

<p><strong>Observed ANI range:</strong>
81.2% – 85.8%
All ANI values fall well below the 95–96% species threshold, supporting designation as a novel species.</p>

<p><strong>📌 Interpretation</strong></p>

<p>● Species cutoff: 95–96% ANI</p>

<p>● All values fall far below this threshold</p>

<p>● <strong>➡ PC_D3_3 is not any known Winogradskyella species</strong></p>

<h4 id="-step-8-aai-analysis-comparem">🧬 Step 8: AAI Analysis (CompareM)</h4>

<p>Average Amino Acid Identity is also used compare between the closely related genomes.</p>

<p>Install CompareM</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-c</span> bioconda comparem
</code></pre></div></div>

<p>Run AAI</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>comparem aai_wf PC_D3_3.faa refs/ aai_out
</code></pre></div></div>

<table>
  <thead>
    <tr>
      <th>Reference genome</th>
      <th>Mean AAI (%)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><em>W. thalassocola</em></td>
      <td>87.11</td>
    </tr>
    <tr>
      <td><em>W. pacifica</em></td>
      <td>86.86</td>
    </tr>
    <tr>
      <td><em>W. psychrotolerans</em></td>
      <td>86.49</td>
    </tr>
    <tr>
      <td><em>W. undariae</em></td>
      <td>82.37</td>
    </tr>
  </tbody>
</table>

<p>AAI values are above genus-level cutoffs but below species-level thresholds, confirming genus-level placement with species-level novelty.</p>

<p>Observed AAI range:
81.7% – 87.1%</p>

<p>📌 Interpretation</p>

<p>Species cutoff: ~95%</p>

<p>Genus cutoff: ~65–75%</p>

<p>➡ Confirms same genus, novel species</p>

<h4 id="-step-9-digital-dnadna-hybridization-ggdc">🧬 Step 9: Digital DNA–DNA Hybridization (GGDC)</h4>

<p>dDDH was calculated using the DSMZ GGDC web platform (Formula 2).
Digital DNA–DNA hybridization (dDDH) estimates:
| Reference genome | dDDH (%) |
| ———————- | ——– |
| <em>W. psychrotolerans</em> B | 29.0 |
| <em>W. thalassocola</em> | 28.8 |
| <em>W. pacifica</em> | 27.8 |
| <em>W. costae</em> | 25.9 |</p>

<p>All values are far below the 70% dDDH species cutoff, with near-zero probabilities of species identity.</p>

<p>Observed dDDH values:
23–29%</p>

<p>📌 Species cutoff: 70%
📌 Probability of species identity: 0–7%</p>

<p>➡ Definitive evidence for novel species status</p>

<h4 id="takeaway">Takeaway</h4>

<p><strong>Consistent evidence from ANI, AAI, and dDDH analyses confirms that Winogradskyella sp. PC_D3_3 represents a novel species, motivating careful cross-device synchronization of genome data, scripts, and analyses.</strong></p>

<h4 id="-functional-interpretation-dram-highlights">🧠 Functional Interpretation (DRAM Highlights)</h4>

<p>Winogradskyella sp. PC_D3_3 is:</p>

<p>● A polysaccharide-degrading marine heterotroph</p>

<p>● Rich in CAZymes (alginate lyases, amylases, pectinases)</p>

<p>● Equipped with diverse ABC and Mce transporters</p>

<p>● Motile (MotB), suggesting particle association</p>

<p>● Carries CRISPR–Cas9, indicating viral pressure</p>

<p>● Strongly adapted to macroalgal and particulate organic matter</p>

<p>This functional profile aligns with a particle-attached, algal-detritus–associated lifestyle in coastal systems.</p>

<h4 id="-take-home-messages">🧠 Take-Home Messages</h4>

<p>● Never rely on taxonomy alone—use ANI, AAI, and dDDH together</p>

<p>● Long-read assemblies dramatically simplify genome interpretation</p>

<p>● DRAM adds ecological meaning beyond taxonomy</p>

<p>● Genomic novelty is strongest when multiple metrics converge</p>

<p>todays figure shows non weighted histogram of read length of current novel genome (after log transformation) from nanoplot
<img src="/assets/img/np.png" alt="novel-genomme" /></p>]]></content><author><name></name></author><summary type="html"><![CDATA[How do we confidently determine whether a newly sequenced genome represents a novel bacterial species?]]></summary></entry><entry><title type="html">Using Syncthing, tmux, and Git to Sync My Bioinformatics Workflow Across Devices</title><link href="https://jojyjohn28.github.io/blog/syncthing-tmux-git-workflow/" rel="alternate" type="text/html" title="Using Syncthing, tmux, and Git to Sync My Bioinformatics Workflow Across Devices" /><published>2025-12-19T00:00:00-05:00</published><updated>2025-12-19T00:00:00-05:00</updated><id>https://jojyjohn28.github.io/blog/syncthing-tmux-git-workflow</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/syncthing-tmux-git-workflow/"><![CDATA[<h4 id="️-using-syncthing-tmux-and-git-to-sync-my-workflow-across-devices">🖥️ Using Syncthing, tmux, and Git to Sync My Workflow Across Devices</h4>

<p>Managing files and analysis sessions across multiple Linux machines can quickly become chaotic—especially when juggling bioinformatics scripts, results, manuscripts, figures, and a personal website at the same time.</p>

<p>Over the years, I tried:</p>

<p>● manual copying</p>

<p>● cloud storage</p>

<p>● external drives</p>

<p>● SSH-based syncing</p>

<p>Eventually, I realized that no single tool solves everything. Instead, I needed a small set of tools, each doing one job extremely well.</p>

<p><strong>My current setup is simple, reliable, and fully Linux-native:</strong></p>

<p>● Syncthing → real-time file synchronization</p>

<p>● tmux → persistent and shareable terminal sessions</p>

<p>● Git + GitHub → version control and publishing</p>

<p>Together, they let me move seamlessly between desktop, laptop, and HPC systems without losing context.</p>

<h4 id="-1-syncthing--real-time-file-synchronization">🔷 1. Syncthing — real-time file synchronization</h4>

<p>What Syncthing does</p>

<p>Syncthing keeps selected folders identical across machines, automatically and continuously.</p>

<p>● No USB drives</p>

<p>● No cloud storage</p>

<p>● No manual copying</p>

<p>● No “which version is correct?” confusion</p>

<p><strong>📦 Installing Syncthing</strong></p>

<p>On Ubuntu / Debian-based systems:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt <span class="nb">install </span>syncthing
</code></pre></div></div>

<p>Verify:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>syncthing <span class="nt">--version</span>
</code></pre></div></div>

<h4 id="-starting-syncthing-automatically-on-boot">🔁 Starting Syncthing automatically on boot</h4>

<p>I want Syncthing to start every time I log in, without manual intervention.</p>

<p>Enable and start the user service:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>systemctl <span class="nt">--user</span> <span class="nb">enable </span>syncthing.service
systemctl <span class="nt">--user</span> start syncthing.service
</code></pre></div></div>

<p>Check status:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>systemctl <span class="nt">--user</span> status syncthing.service
</code></pre></div></div>

<p>Now Syncthing:</p>

<p>● Starts automatically on login</p>

<p>● Runs quietly in the background</p>

<p>● Syncs even when the browser UI is closed</p>

<h4 id="-accessing-the-syncthing-interface">🌐 Accessing the Syncthing interface</h4>

<p>Open a browser and go to:</p>

<pre><code class="language-txt">http://localhost:8384
</code></pre>

<p>This is where I:</p>

<p>● Add devices</p>

<p>● Select folders to sync</p>

<p>● Monitor sync status</p>

<h4 id="-my-synced-folder-structure">📂 My synced folder structure</h4>

<p>I use one top-level folder across all machines:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/Jojy_Research_Sync/
</code></pre></div></div>

<p><strong>Inside it:</strong> I have many subfolder some examples are below</p>

<p>● scripts/ – Bash, SLURM, Snakemake, R, Python</p>

<p>● Projects/ – intermediate outputs, figures</p>

<p>● notes/ – daily research notes</p>

<p>● website_assets/ – content used by my blog</p>

<p><strong>These are files I edit frequently—sometimes switching machines multiple times in a day.</strong></p>

<h4 id="-2-tmux--persistent-and-shared-terminal-sessions">🔷 2. tmux — persistent and shared terminal sessions</h4>

<p>While Syncthing synchronizes files, <strong>it does not preserve live terminal state.</strong>
For that, I use tmux.
<strong>📦 Installing tmux</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt <span class="nb">install </span>tmux
</code></pre></div></div>

<p>Verify:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tmux <span class="nt">-V</span>
</code></pre></div></div>

<p><strong>🧠 Creating a persistent session</strong></p>

<p>To start a named tmux session:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tmux new <span class="nt">-s</span> shared
</code></pre></div></div>

<h4 id="-from-your-laptop-open-the-shared-tmux-session-desktop-hosted">✅ From your laptop: open the shared tmux session (desktop-hosted)</h4>

<p>tmux sessions live on the machine where they were created (your desktop or an HPC node).
So on your laptop you don’t “open” it locally — you SSH into that same machine and then attach to the session.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh &lt;user&gt;@&lt;DESKTOP_IP&gt;
tmux attach <span class="nt">-t</span> shared
</code></pre></div></div>

<p>If the session doesn’t exist yet (first time), create it:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh &lt;user&gt;@&lt;DESKTOP_IP&gt;
tmux new <span class="nt">-s</span> shared
</code></pre></div></div>

<p><strong>🔍 Find your local IP address (most common case)</strong></p>

<p>On the machine hosting the tmux session (desktop or server), run:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip a <span class="c">#Run on yur terminal</span>
</code></pre></div></div>

<h4 id="-sharing-the-same-session-across-devices">👥 Sharing the same session across devices</h4>

<p>If I SSH into the same machine from another device, I can attach to the same session.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tmux attach <span class="nt">-t</span> shared
</code></pre></div></div>

<p>This lets me:</p>

<p>● Resume work from a different device</p>

<p>● Monitor long-running commands</p>

<p>● Continue exactly where I left off</p>

<p>⚠️ tmux sessions are host-specific (not shared across different machines).</p>

<p><strong>🧩 tmux commands I use daily</strong>
| Command | Action |
| —————————– | ————– |
| <code class="language-plaintext highlighter-rouge">tmux new -s shared</code> | Create session |
| <code class="language-plaintext highlighter-rouge">tmux ls</code> | List sessions |
| <code class="language-plaintext highlighter-rouge">tmux attach -t shared</code> | Reattach |
| <code class="language-plaintext highlighter-rouge">tmux kill-session -t shared</code> | End session |</p>

<h4 id="-3-git--version-control-and-publishing">🔷 3. Git — version control and publishing</h4>

<p><strong>Syncthing is not a replacement for Git.</strong></p>

<p>I use Git + GitHub for:</p>

<p>● My website repository</p>

<p>● Analysis pipelines</p>

<p>● Code meant to be shared or published</p>

<p><strong>📦 Installing Git</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt <span class="nb">install </span>git
</code></pre></div></div>

<p><strong>🔄 My daily Git workflow*</strong></p>

<p>When starting work on any machine:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git pull
</code></pre></div></div>

<p>This ensures:</p>

<p>● I’m working on the latest committed version</p>

<p>● No conflicts with work done on another device</p>

<p><strong>When something is finalized:</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Update analysis / figures / blog post"</span>
git push
</code></pre></div></div>

<h4 id="-how-syncthing-tmux-and-git-work-together">🔗 How Syncthing, tmux, and Git work together</h4>

<table>
  <thead>
    <tr>
      <th>Tool</th>
      <th>Role</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Syncthing</td>
      <td>Sync active working files</td>
    </tr>
    <tr>
      <td>tmux</td>
      <td>Preserve live terminal sessions</td>
    </tr>
    <tr>
      <td>Git</td>
      <td>Track history and publish work</td>
    </tr>
  </tbody>
</table>

<p>Typical workflow:</p>

<p>● Syncthing syncs drafts and scripts across devices</p>

<p>● tmux keeps my analysis session alive</p>

<p>● Git records finalized work and publishes it</p>

<p>Each tool does one job well, without overlap.</p>

<h4 id="-why-this-is-important-for-me">✅ Why this is important for me</h4>

<p>This setup:</p>

<p>● Reduces cognitive overhead</p>

<p>● Prevents accidental overwrites</p>

<p>● Supports HPC + local workflows</p>

<p>● Keeps everything Linux-native and reproducible</p>

<p><strong>Most importantly, it lets me focus on science, not file management.</strong></p>

<p><img src="/assets/img/syn.png" alt="deskto-laptop" /></p>]]></content><author><name></name></author><summary type="html"><![CDATA[How I synchronize files, terminal sessions, and version-controlled projects across my Linux desktop and laptop.]]></summary></entry><entry><title type="html">Size-Fractionated Microbiome Analysis — Day 3: Species-Level Profiling with mOTUs</title><link href="https://jojyjohn28.github.io/blog/size-fractionated-microbiome-analysis-day3/" rel="alternate" type="text/html" title="Size-Fractionated Microbiome Analysis — Day 3: Species-Level Profiling with mOTUs" /><published>2025-12-18T00:00:00-05:00</published><updated>2025-12-18T00:00:00-05:00</updated><id>https://jojyjohn28.github.io/blog/size-fractionated-microbiome-analysis-day3</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/size-fractionated-microbiome-analysis-day3/"><![CDATA[<h4 id="-size-fractionated-microbiome-analysis--day-3">🌊 Size-Fractionated Microbiome Analysis — Day 3</h4>

<p>In Day 1 (https://jojyjohn28.github.io/blog/size-fractionated-microbiome-analysis-day1/), we focused on metagenome preprocessing, and in Day 2 (https://jojyjohn28.github.io/blog/size-fractionated-microbiome-analysis-day2/), we explored taxonomic profiling using Kaiju.</p>

<p><strong>Today, in Day 3, I introduce mOTUs v3, a marker-gene–based approach for species-level profiling of prokaryotes directly from short reads.</strong></p>

<p>This method is particularly powerful for read-based analyses, where genomes are not assembled or binned, and is equally applicable to DNA (total community) and RNA (active community) datasets.</p>

<h4 id="-primary-tool-motus-v3">🧬 Primary tool: mOTUs v3</h4>

<p>mOTUs profiles microbial communities using 40 universal single-copy marker genes, providing consistent, genome-informed species resolution.</p>

<p><strong>Why mOTUs?</strong></p>

<p>Uses conserved marker genes instead of whole-genome alignment</p>

<p>● Quantifies both known taxa (linked to reference genomes) and unknown mOTUs (derived from metagenomes)</p>

<p>● Produces directly comparable profiles across DNA and RNA datasets</p>

<p>● Outputs normalized relative abundances suitable for downstream statistics and visualization</p>

<p>This makes mOTUs ideal for size-fractionated microbiome studies, where consistent taxonomic resolution across multiple datasets is critical.</p>

<h4 id="️-installation-and-database-setup">🛠️ Installation and database setup</h4>

<p>Install mOTUs</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-c</span> bioconda motus
</code></pre></div></div>

<p>Verify installation:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>motus <span class="nt">--version</span>
</code></pre></div></div>

<p>mOTUs ships with prebuilt marker-gene databases, so no separate database construction is required.
for more details refer : https://motu-tool.org/tutorial.html</p>

<h4 id="-running-motus-on-paired-end-reads">🧪 Running mOTUs on paired-end reads</h4>

<p>Example command</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>motus profile <span class="se">\</span>
  <span class="nt">-f</span> Control_23_S13_R1.fq <span class="se">\</span>
  <span class="nt">-r</span> Control_23_S13_R2.fq <span class="se">\</span>
  <span class="nt">-o</span> Control_23_S13_motus.tsv
</code></pre></div></div>

<p>General usage</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Total community (DNA)</span>
motus profile <span class="se">\</span>
  <span class="nt">-f</span> reads/dna/S1_R1.fq.gz <span class="se">\</span>
  <span class="nt">-r</span> reads/dna/S1_R2.fq.gz <span class="se">\</span>
  <span class="nt">-o</span> motus/S1_dna.motus.tsv

<span class="c"># Active community (RNA)</span>
motus profile <span class="se">\</span>
  <span class="nt">-f</span> reads/rna/S1_R1.fq.gz <span class="se">\</span>
  <span class="nt">-r</span> reads/rna/S1_R2.fq.gz <span class="se">\</span>
  <span class="nt">-o</span> motus/S1_rna.motus.tsv
</code></pre></div></div>

<p>The output table contains consensus taxonomy and relative abundance per sample.</p>

<h4 id="-batch-processing-on-hpc-example">🚀 Batch processing on HPC (example)</h4>

<p>Below is the batch script used to run mOTUs across Chesapeake Bay metagenomes.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --job-name=motus_mg_CP</span>
<span class="c">#SBATCH --nodes=1</span>
<span class="c">#SBATCH --cpus-per-task=32</span>
<span class="c">#SBATCH --mem=1500G</span>
<span class="c">#SBATCH --time=72:00:00</span>
<span class="c">#SBATCH -o motus_%A_%a.out</span>
<span class="c">#SBATCH -e motus_%A_%a.err</span>
<span class="c">#SBATCH -A camplab</span>
<span class="c">#SBATCH -p camplab</span>

<span class="nb">set</span> <span class="nt">-euo</span> pipefail

<span class="nb">cd</span> /project/bcampb7/camplab/AL_JJ_oct23

<span class="nv">input_dir</span><span class="o">=</span><span class="s2">"/project/bcampb7/camplab/0_Raw_Data/Metagenome_estury/CB_DNA_trimmomatic"</span>
<span class="nv">output_dir</span><span class="o">=</span><span class="s2">"/project/bcampb7/camplab/AL_JJ_oct23/motus_output/cp"</span>

<span class="nb">mkdir</span> <span class="nt">-p</span> <span class="s2">"</span><span class="k">${</span><span class="nv">output_dir</span><span class="k">}</span><span class="s2">"</span>/<span class="o">{</span>profiles,logs<span class="o">}</span>

<span class="nb">source</span> ~/.bashrc
conda activate /home/jojyj/.conda/envs/motus_env

<span class="k">for </span>R1 <span class="k">in</span> <span class="s2">"</span><span class="k">${</span><span class="nv">input_dir</span><span class="k">}</span><span class="s2">"</span>/<span class="k">*</span>_R1.fastq<span class="p">;</span> <span class="k">do
    </span><span class="nv">sample</span><span class="o">=</span><span class="si">$(</span><span class="nb">basename</span> <span class="s2">"</span><span class="k">${</span><span class="nv">R1</span><span class="p">/_R1.fastq/</span><span class="k">}</span><span class="s2">"</span><span class="si">)</span>
    <span class="nv">R2</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">input_dir</span><span class="k">}</span><span class="s2">/</span><span class="k">${</span><span class="nv">sample</span><span class="k">}</span><span class="s2">_R2.fastq"</span>

    motus profile <span class="se">\</span>
        <span class="nt">-f</span> <span class="s2">"</span><span class="nv">$R1</span><span class="s2">"</span> <span class="se">\</span>
        <span class="nt">-r</span> <span class="s2">"</span><span class="nv">$R2</span><span class="s2">"</span> <span class="se">\</span>
        <span class="nt">-o</span> <span class="s2">"</span><span class="k">${</span><span class="nv">output_dir</span><span class="k">}</span><span class="s2">/profiles/</span><span class="k">${</span><span class="nv">sample</span><span class="k">}</span><span class="s2">_motus.txt"</span> <span class="se">\</span>
        <span class="nt">-n</span> 32 <span class="se">\</span>
        2&gt; <span class="s2">"</span><span class="k">${</span><span class="nv">output_dir</span><span class="k">}</span><span class="s2">/logs/</span><span class="k">${</span><span class="nv">sample</span><span class="k">}</span><span class="s2">.log"</span>
<span class="k">done</span>
</code></pre></div></div>

<h4 id="-merging-motus-profiles-across-samples">🧩 Merging mOTUs profiles across samples</h4>

<p>Motus do have an built-in motus merge command (for me it failed for this dataset due to a error so instead of rerunning all my metegenomes;I used a robust bash-based merging approach, which is transparent and easy to repeat.)</p>

<p><strong>Step 1 — Navigate to profiles directory</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>path/to/output/folder
</code></pre></div></div>

<p><strong>Step 2 — Fix headers (add sample name)</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for </span>f <span class="k">in</span> <span class="k">*</span>_motus.txt<span class="p">;</span> <span class="k">do
    </span><span class="nv">sample</span><span class="o">=</span><span class="si">$(</span><span class="nb">basename</span> <span class="s2">"</span><span class="nv">$f</span><span class="s2">"</span> _motus.txt<span class="si">)</span>
    <span class="nb">awk</span> <span class="nt">-v</span> <span class="nv">s</span><span class="o">=</span><span class="s2">"</span><span class="nv">$sample</span><span class="s2">"</span> <span class="s1">'NR==3{print "#consensus_taxonomy\t"s; next} {print}'</span> <span class="s2">"</span><span class="nv">$f</span><span class="s2">"</span> <span class="o">&gt;</span> tmp <span class="o">&amp;&amp;</span> <span class="nb">mv </span>tmp <span class="s2">"</span><span class="nv">$f</span><span class="s2">"</span>
<span class="k">done</span>
</code></pre></div></div>

<p><strong>Step 3 — Create clean tables for merging</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> merged_clean

<span class="k">for </span>f <span class="k">in</span> <span class="k">*</span>_motus.txt<span class="p">;</span> <span class="k">do
    </span><span class="nv">sample</span><span class="o">=</span><span class="si">$(</span><span class="nb">basename</span> <span class="s2">"</span><span class="nv">$f</span><span class="s2">"</span> _motus.txt<span class="si">)</span>
    <span class="nb">tail</span> <span class="nt">-n</span> +3 <span class="s2">"</span><span class="nv">$f</span><span class="s2">"</span> <span class="o">&gt;</span> merged_clean/<span class="s2">"</span><span class="nv">$sample</span><span class="s2">"</span>.tsv
<span class="k">done</span>
</code></pre></div></div>

<p><strong>Step 4 — Merge all samples</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>merged_clean

<span class="nv">first</span><span class="o">=</span><span class="si">$(</span><span class="nb">ls</span> <span class="k">*</span>.tsv | <span class="nb">sort</span> | <span class="nb">head</span> <span class="nt">-n</span> 1<span class="si">)</span>
<span class="nb">cut</span> <span class="nt">-f1</span> <span class="s2">"</span><span class="nv">$first</span><span class="s2">"</span> <span class="o">&gt;</span> clades.txt

<span class="k">for </span>f <span class="k">in</span> <span class="si">$(</span><span class="nb">ls</span> <span class="k">*</span>.tsv | <span class="nb">sort</span><span class="si">)</span><span class="p">;</span> <span class="k">do
    </span><span class="nb">cut</span> <span class="nt">-f2</span> <span class="s2">"</span><span class="nv">$f</span><span class="s2">"</span> <span class="o">&gt;</span> <span class="s2">"</span><span class="k">${</span><span class="nv">f</span><span class="p">%.tsv</span><span class="k">}</span><span class="s2">.ab"</span>
<span class="k">done

</span><span class="nb">paste </span>clades.txt <span class="k">*</span>.ab <span class="o">&gt;</span> merged_raw.tsv
<span class="nb">sed</span> <span class="s1">'1s/#consensus_taxonomy/clade_name/'</span> merged_raw.tsv <span class="o">&gt;</span> motus_merged_profiles.tsv
</code></pre></div></div>

<p>The resulting file (motus_merged_profiles.tsv) is analysis-ready.</p>

<h5 id="-generating-the-final-order-level-abundance-table-from-motus-profiles">🧬 Generating the final order-level abundance table from mOTUs profiles</h5>

<p>mOTUs outputs taxonomic assignments as consensus lineage strings, typically spanning multiple ranks (kingdom → species). For downstream ecological visualization (e.g., stacked bar plots and heatmaps), I summarized the data at the order level, which provides a good balance between resolution and interpretability for estuarine microbiomes.</p>

<p><strong>Overview of the approach</strong></p>

<ol>
  <li>
    <p>Start from the merged mOTUs profile table (motus_merged_profiles.tsv)</p>
  </li>
  <li>
    <p>Parse the consensus taxonomy lineage to extract the order-level annotation</p>
  </li>
  <li>
    <p>Collapse species- and genus-level entries into their corresponding orders</p>
  </li>
  <li>
    <p>Sum relative abundances across all mOTUs belonging to the same order</p>
  </li>
  <li>
    <p>Produce a sample × order matrix for visualization and statistical analysis</p>
  </li>
</ol>

<p><strong>This approach ensures that:</strong></p>

<p>● Taxonomy is consistent across samples</p>

<p>● Unknown or poorly resolved taxa are handled transparently</p>

<p>● The resulting table is directly compatible with ggplot2, heatmaps, and multivariate analyses</p>

<h4 id="-step-by-step-order-level-table-construction-in-r">🧩 Step-by-step: Order-level table construction in R</h4>

<p>I used “R” for this goal:</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">library</span><span class="p">(</span><span class="n">tidyverse</span><span class="p">)</span><span class="w"> </span><span class="c1">#Load required packages</span><span class="w">
</span><span class="n">motus</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">read_tsv</span><span class="p">(</span><span class="s2">"motus_merged_profiles.tsv"</span><span class="p">)</span><span class="w"> </span><span class="c1">#Read merged mOTUs table</span><span class="w">
</span><span class="c1">#The first column (clade_name) contains the consensus taxonomy lineage, followed by one column per sample.</span><span class="w">
</span><span class="n">motus_long</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">motus</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">pivot_longer</span><span class="p">(</span><span class="w">
    </span><span class="n">cols</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="o">-</span><span class="n">clade_name</span><span class="p">,</span><span class="w">
    </span><span class="n">names_to</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"Sample"</span><span class="p">,</span><span class="w">
    </span><span class="n">values_to</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"Abundance"</span><span class="w">
  </span><span class="p">)</span><span class="w">
</span><span class="c1">#Reshape to long format</span><span class="w">
</span><span class="c1">#Extract order-level taxonomy from lineage strings</span><span class="w">
</span><span class="c1">#mOTUs taxonomy strings typically follow this structure:</span><span class="w">
</span><span class="c1">#k__Bacteria|p__Proteobacteria|c__Gammaproteobacteria|o__Alteromonadales|...</span><span class="w">
</span><span class="c1">#We extract the o__ component and clean it for plotting.</span><span class="w">
</span><span class="n">motus_long</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">motus_long</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">mutate</span><span class="p">(</span><span class="w">
    </span><span class="n">order</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">str_extract</span><span class="p">(</span><span class="n">clade_name</span><span class="p">,</span><span class="w"> </span><span class="s2">"o__[^|]+"</span><span class="p">),</span><span class="w">
    </span><span class="n">order</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">str_remove</span><span class="p">(</span><span class="n">order</span><span class="p">,</span><span class="w"> </span><span class="s2">"^o__"</span><span class="p">),</span><span class="w">
    </span><span class="n">order</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">if_else</span><span class="p">(</span><span class="nf">is.na</span><span class="p">(</span><span class="n">order</span><span class="p">),</span><span class="w"> </span><span class="s2">"Unclassified"</span><span class="p">,</span><span class="w"> </span><span class="n">order</span><span class="p">)</span><span class="w">
  </span><span class="p">)</span><span class="w">
</span><span class="c1">#Summarize abundances at the order level</span><span class="w">
</span><span class="n">order_level</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">motus_long</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">group_by</span><span class="p">(</span><span class="n">order</span><span class="p">,</span><span class="w"> </span><span class="n">Sample</span><span class="p">)</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">summarise</span><span class="p">(</span><span class="w">
    </span><span class="n">Abundance</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nf">sum</span><span class="p">(</span><span class="n">Abundance</span><span class="p">),</span><span class="w">
    </span><span class="n">.groups</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"drop"</span><span class="w">
  </span><span class="p">)</span><span class="w">
</span><span class="c1">#At this stage, each sample has a relative abundance profile summarized by order.</span><span class="w">
</span></code></pre></div></div>

<p>This table was used as the final order-level input for:</p>

<p>● Stacked bar plots (Day 3)</p>

<p>● Heatmaps and multivariate analyses (later days)</p>

<p>● network and co-occurance modelling in coming months</p>

<h4 id="-visualization-in-r">📊 Visualization in R</h4>

<p>Stacked bar plots (order level, bay-wise)</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">library</span><span class="p">(</span><span class="n">tidyverse</span><span class="p">)</span><span class="w">
</span><span class="n">library</span><span class="p">(</span><span class="n">readxl</span><span class="p">)</span><span class="w">

</span><span class="n">df</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">read_excel</span><span class="p">(</span><span class="w">
  </span><span class="s2">"/home/jojy-john/Jojy_Research_Sync/Fl_vs_Pa/total_and_active_community/order_level_summed.xlsx"</span><span class="w">
</span><span class="p">)</span><span class="w">

</span><span class="n">df_long</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">df</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">pivot_longer</span><span class="p">(</span><span class="w">
    </span><span class="n">cols</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="o">-</span><span class="n">order</span><span class="p">,</span><span class="w">
    </span><span class="n">names_to</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"Sample"</span><span class="p">,</span><span class="w">
    </span><span class="n">values_to</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"Abundance"</span><span class="w">
  </span><span class="p">)</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">mutate</span><span class="p">(</span><span class="w">
    </span><span class="n">Bay</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">case_when</span><span class="p">(</span><span class="w">
      </span><span class="n">str_detect</span><span class="p">(</span><span class="n">Sample</span><span class="p">,</span><span class="w"> </span><span class="s2">"^CP"</span><span class="p">)</span><span class="w"> </span><span class="o">~</span><span class="w"> </span><span class="s2">"Chesapeake"</span><span class="p">,</span><span class="w">
      </span><span class="n">str_detect</span><span class="p">(</span><span class="n">Sample</span><span class="p">,</span><span class="w"> </span><span class="s2">"^DE"</span><span class="p">)</span><span class="w"> </span><span class="o">~</span><span class="w"> </span><span class="s2">"Delaware"</span><span class="p">,</span><span class="w">
      </span><span class="kc">TRUE</span><span class="w"> </span><span class="o">~</span><span class="w"> </span><span class="s2">"Unknown"</span><span class="w">
    </span><span class="p">),</span><span class="w">
    </span><span class="n">order</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">str_trim</span><span class="p">(</span><span class="n">order</span><span class="p">),</span><span class="w">
    </span><span class="n">order</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">ifelse</span><span class="p">(</span><span class="n">order</span><span class="w"> </span><span class="o">%in%</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="s2">"Other"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Others"</span><span class="p">),</span><span class="w"> </span><span class="s2">"Other"</span><span class="p">,</span><span class="w"> </span><span class="n">order</span><span class="p">)</span><span class="w">
  </span><span class="p">)</span><span class="w">

</span><span class="n">top_orders</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">df_long</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">filter</span><span class="p">(</span><span class="n">order</span><span class="w"> </span><span class="o">!=</span><span class="w"> </span><span class="s2">"Other"</span><span class="p">)</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">group_by</span><span class="p">(</span><span class="n">order</span><span class="p">)</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">summarise</span><span class="p">(</span><span class="n">total</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nf">sum</span><span class="p">(</span><span class="n">Abundance</span><span class="p">),</span><span class="w"> </span><span class="n">.groups</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"drop"</span><span class="p">)</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">arrange</span><span class="p">(</span><span class="n">desc</span><span class="p">(</span><span class="n">total</span><span class="p">))</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">slice_head</span><span class="p">(</span><span class="n">n</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">15</span><span class="p">)</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">pull</span><span class="p">(</span><span class="n">order</span><span class="p">)</span><span class="w">

</span><span class="n">df_long2</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">df_long</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">mutate</span><span class="p">(</span><span class="n">order2</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">ifelse</span><span class="p">(</span><span class="n">order</span><span class="w"> </span><span class="o">%in%</span><span class="w"> </span><span class="n">top_orders</span><span class="p">,</span><span class="w"> </span><span class="n">order</span><span class="p">,</span><span class="w"> </span><span class="s2">"Other"</span><span class="p">))</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">group_by</span><span class="p">(</span><span class="n">order2</span><span class="p">,</span><span class="w"> </span><span class="n">Sample</span><span class="p">,</span><span class="w"> </span><span class="n">Bay</span><span class="p">)</span><span class="w"> </span><span class="o">%&gt;%</span><span class="w">
  </span><span class="n">summarise</span><span class="p">(</span><span class="n">Abundance</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nf">sum</span><span class="p">(</span><span class="n">Abundance</span><span class="p">),</span><span class="w"> </span><span class="n">.groups</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"drop"</span><span class="p">)</span><span class="w">

</span><span class="n">ggplot</span><span class="p">(</span><span class="n">df_long2</span><span class="p">,</span><span class="w"> </span><span class="n">aes</span><span class="p">(</span><span class="n">x</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">Sample</span><span class="p">,</span><span class="w"> </span><span class="n">y</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">Abundance</span><span class="p">,</span><span class="w"> </span><span class="n">fill</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">order2</span><span class="p">))</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">geom_bar</span><span class="p">(</span><span class="n">stat</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"identity"</span><span class="p">,</span><span class="w"> </span><span class="n">position</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"fill"</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">facet_wrap</span><span class="p">(</span><span class="o">~</span><span class="n">Bay</span><span class="p">,</span><span class="w"> </span><span class="n">scales</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"free_x"</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">labs</span><span class="p">(</span><span class="w">
    </span><span class="n">x</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"Samples"</span><span class="p">,</span><span class="w">
    </span><span class="n">y</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"Relative abundance"</span><span class="p">,</span><span class="w">
    </span><span class="n">fill</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"Order"</span><span class="w">
  </span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">theme_minimal</span><span class="p">(</span><span class="n">base_size</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">13</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">theme</span><span class="p">(</span><span class="w">
    </span><span class="n">axis.text.x</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">element_text</span><span class="p">(</span><span class="n">angle</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">60</span><span class="p">,</span><span class="w"> </span><span class="n">hjust</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">1</span><span class="p">,</span><span class="w"> </span><span class="n">face</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"bold"</span><span class="p">),</span><span class="w">
    </span><span class="n">strip.text</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">element_text</span><span class="p">(</span><span class="n">face</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"bold"</span><span class="p">),</span><span class="w">
    </span><span class="n">legend.title</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">element_text</span><span class="p">(</span><span class="n">face</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"bold"</span><span class="p">)</span><span class="w">
  </span><span class="p">)</span><span class="w">
</span></code></pre></div></div>

<h4 id="-strengths-of-motus-in-this-study">✅ Strengths of mOTUs in this study</h4>

<p>● Species-level resolution without MAGs</p>

<p>● Consistent taxonomy across DNA and RNA</p>

<p>● Robust handling of unknown taxa</p>

<p>● Clean, normalized outputs for statistical analysis</p>

<h4 id="-kaiju-vs-motus-consistency-across-methods">🔄 Kaiju vs mOTUs: Consistency across methods</h4>

<p>To assess methodological consistency, I compared Kaiju (protein-level classification) and mOTUs (marker-gene–based profiling) outputs at higher taxonomic ranks. <strong>Despite relying on fundamentally different approaches, both methods recovered highly similar dominant taxa across samples, particularly when comparisons were made at the order level.</strong></p>

<p><img src="/assets/img/motu.png" alt="total_community_motu" /></p>]]></content><author><name></name></author><summary type="html"><![CDATA[Day 3 of the size-fractionated microbiome series: mOTUs-based species-level profiling, batch processing, manual merging of profiles, and visualization.]]></summary></entry><entry><title type="html">Genome Topology and Genome Announcement Reports: A Practical, Reviewer-Safe Workflow</title><link href="https://jojyjohn28.github.io/blog/genome-topology-and-genome-report/" rel="alternate" type="text/html" title="Genome Topology and Genome Announcement Reports: A Practical, Reviewer-Safe Workflow" /><published>2025-12-17T00:00:00-05:00</published><updated>2025-12-17T00:00:00-05:00</updated><id>https://jojyjohn28.github.io/blog/genome-topology-and_genome_report</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/genome-topology-and-genome-report/"><![CDATA[<h4 id="-genome-topology-and-genome-announcement-reports">🧬 Genome Topology and Genome Announcement Reports</h4>

<p>Today, I’m stepping slightly back from the Size-Fractionated Microbiome Analysis series. With the winter break approaching, I wanted to wrap up a few pending tasks, including finalizing genome submissions to NCBI. We recently received the final queries and accession numbers for most of our genomes. So toaday I am documenting how to answer last few NCBI submission questions and how to prepare a complete genome summary table suitable for a Genome Resource Announcement manuscript.</p>

<p><strong>How to answer NCBI questions without over-claiming</strong></p>

<p>When submitting genomes to NCBI (GenBank / Genome / BioProject), two questions almost always appear and often cause confusion:</p>

<p>[A] Is this a draft assembly or does one sequence represent the chromosome?
[B] If this is a circular chromosome, are the ends of the sequence contiguous around the circle with no overlap and no gap?</p>

<p>These questions are not asking you to prove perfection — they are asking you to state clearly what you are claiming and what you are not.</p>

<p>This post documents a practical, conservative, and reviewer-safe workflow that I use to:</p>

<p>● Answer NCBI topology questions confidently</p>

<p>● Avoid over-claiming circularity</p>

<p>● Generate a final genome summary table suitable for Genome Resource Announcements</p>

<h4 id="-core-principle-very-important">🔑 Core principle (very important)</h4>

<p><strong>Only assert circularity when the assembly method and validation steps truly support it.</strong></p>

<p>● Draft genomes and linear submissions are fully acceptable to NCBI.</p>

<p>● Over-claiming circularity is not</p>

<h4 id="step-1--determine-whether-a-genome-is-draft-or-single-contig">Step 1 — Determine whether a genome is draft or single-contig</h4>

<p>For each genome FASTA:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">grep</span> <span class="nt">-c</span> <span class="s2">"^&gt;"</span> genome.fa
</code></pre></div></div>

<p>I have many genomes and I copied all into one folder and used a batch script as follows</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for </span>f <span class="k">in</span> <span class="k">*</span>.fa<span class="p">;</span> <span class="k">do
    </span><span class="nb">echo</span> <span class="s2">"=== </span><span class="nv">$f</span><span class="s2"> ==="</span>
    <span class="nb">grep</span> <span class="nt">-c</span> <span class="s2">"^&gt;"</span> <span class="s2">"</span><span class="nv">$f</span><span class="s2">"</span>
<span class="k">done</span>
</code></pre></div></div>

<p><strong>Interpretation</strong>
| Result | Meaning | NCBI Answer |
| ——— | ——————– | ——————- |
| &gt;1 contig | Draft assembly | [A] Draft assembly |
| 1 contig | Candidate chromosome | Requires validation |
If a genome has multiple contigs, do not discuss circularity.</p>

<h4 id="step-2--validate-circularity-for-single-contig-genomes">Step 2 — Validate circularity for single-contig genomes</h4>

<p>This step applies only to genomes with exactly one contig.</p>

<p><strong>2a. Check assembler evidence (Flye / Unicycler)</strong></p>

<p>Inspect assembly_info.txt</p>

<p>Look for circular=true</p>

<p>If circularity is not explicitly stated, do not assume it</p>

<p>Illumina-only assemblies cannot reliably establish circularity.
As I used long read and hybrid assembly, I made a custom script to extract the inforamtion either from the assembly_info.txt or from the assembly. I also confirmed the results with step 2b</p>

<p><strong>2b. Check for overlap between contig ends</strong></p>

<p>NCBI requires no overlap and no gap at the contig ends.
<strong>Extract first and last 1 kb</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>seqkit subseq <span class="nt">-r</span> 1:1000 genome.fa <span class="o">&gt;</span> first1k.fa
seqkit subseq <span class="nt">-r</span> <span class="nt">-999</span>:-1 genome.fa <span class="o">&gt;</span> last1k.fa
</code></pre></div></div>

<p>You need seqkit tool for this and this can be insttaled as follows:
seqkit is a fast, lightweight toolkit for manipulating FASTA/FASTQ files. In this workflow, it is used to extract the first and last regions of contigs for circularity checks.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-c</span> bioconda seqkit
</code></pre></div></div>

<p><strong>Align ends</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nucmer <span class="nt">--maxmatch</span> first1k.fa last1k.fa <span class="nt">-p</span> ends
show-coords <span class="nt">-rcl</span> ends.delta
</code></pre></div></div>

<p>for this step you need the tool nucmer.
MUMmer is a whole-genome alignment package. Here, nucmer is used to align the start and end of contigs to detect overlapping duplicated regions, which is critical before claiming circularity.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-c</span> bioconda mummer
</code></pre></div></div>

<p>This installs nucmer and show-coords, which are used for end-to-end contig validation.</p>

<p><strong>Interpretation</strong>
| Result | Meaning | Action |
| —————- | —————- | ———————————- |
| Strong alignment | Overlap present | Must trim before claiming circular |
| No alignment | Not circularized | Treat as linear |</p>

<p><strong>If no alignments are shown (only headers):</strong></p>

<p>→ The genome is not circularized
→ Do not answer YES to question [B]
<strong>🚫 Do not infer circularity for Illumina-only assemblies</strong></p>

<h4 id="-genome-resource-announcements">🔁 Genome Resource Announcements</h4>

<p>Although all of the analyses described below had been completed previously, the final genome FASTA files were subsequently renamed, filtered, and curated, and a subset of genomes was reassembled. Therefore, to generate a consistent and accurate summary table for the Genome Resource Announcement, I decided to define a clear work plan and rerun the relevant steps in a streamlined, reproducible workflow.</p>

<p><strong>Workflow overview</strong>
| Tool | Purpose | Fields |
| —————- | ——————- | ————————— |
| QUAST | Assembly statistics | Size, GC, contigs, N50 |
| CheckM2 | Quality metrics | Completeness, contamination |
| Prokka | Annotation | Protein-coding genes |
| Flye / Unicycler | Topology evidence | Circular / Linear |
| NUCmer | End validation | Overlap detection |</p>

<h4 id="step-1-assembly-statistics-with-quast">Step 1— Assembly statistics with QUAST</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> quast_out
quast.py <span class="k">*</span>.fa <span class="nt">-o</span> quast_out <span class="nt">--threads</span> 16
</code></pre></div></div>

<p>I used batch script as follows</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --job-name=quast_all</span>
<span class="c">#SBATCH --partition=camplab</span>
<span class="c">#SBATCH --nodes=1</span>
<span class="c">#SBATCH --ntasks=1</span>
<span class="c">#SBATCH --cpus-per-task=16</span>
<span class="c">#SBATCH --mem=64G</span>
<span class="c">#SBATCH --time=12:00:00</span>
<span class="c">#SBATCH --output=quast_%j.out</span>
<span class="c">#SBATCH --error=quast_%j.err</span>

module load quast

<span class="nv">GENOME_DIR</span><span class="o">=</span><span class="s2">"/project/dkarig/ecocoat/NCBI_oct22/fixed_genomes_dec11"</span>
<span class="nv">OUTDIR</span><span class="o">=</span><span class="s2">"/project/dkarig/ecocoat/NCBI_oct22/quast_dec11"</span>

<span class="nb">mkdir</span> <span class="nt">-p</span> <span class="s2">"</span><span class="nv">$OUTDIR</span><span class="s2">"</span>

<span class="c"># Run QUAST on all genomes together</span>
quast.py <span class="se">\</span>
  <span class="nt">--threads</span> 16 <span class="se">\</span>
  <span class="nt">--min-contig</span> 500 <span class="se">\</span>
  <span class="nt">-o</span> <span class="s2">"</span><span class="nv">$OUTDIR</span><span class="s2">"</span> <span class="se">\</span>
  <span class="s2">"</span><span class="nv">$GENOME_DIR</span><span class="s2">"</span>/<span class="k">*</span>.fa

<span class="nb">echo</span> <span class="s2">"QUAST completed at </span><span class="si">$(</span><span class="nb">date</span><span class="si">)</span><span class="s2">"</span>
</code></pre></div></div>

<p>save this file as run_quast_all.sh and run as follows</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x run_quast_all.sh
sbatch run_quast_all.sh
</code></pre></div></div>

<p>Final file is report.tsv:</p>

<p>● Genome size</p>

<p>● GC (%)</p>

<p>● number of Contigs</p>

<p>● N50</p>

<h4 id="step-2--completeness-and-contamination-with-checkm2">Step 2 — Completeness and contamination with CheckM2</h4>

<p>CheckM was used to assess genome quality by estimating completeness and contamination based on lineage-specific single-copy marker genes. The lineage workflow automatically assigns each genome to an appropriate taxonomic lineage and reports standardized quality metrics, which were exported as a tab-delimited summary table and incorporated directly into the final genome summary table for the Genome Resource Announcement.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --job-name=checkm_all</span>
<span class="c">#SBATCH --partition=camplab</span>
<span class="c">#SBATCH --nodes=1</span>
<span class="c">#SBATCH --ntasks=1</span>
<span class="c">#SBATCH --cpus-per-task=24</span>
<span class="c">#SBATCH --mem=96G</span>
<span class="c">#SBATCH --time=24:00:00</span>
<span class="c">#SBATCH --output=checkm_%j.out</span>
<span class="c">#SBATCH --error=checkm_%j.err</span>

<span class="c"># Load CheckM module or activate conda env</span>
conda activate checkm_env

<span class="nv">GENOME_DIR</span><span class="o">=</span><span class="s2">"/project/dkarig/ecocoat/NCBI_oct22/all_genomes_dec15"</span>
<span class="nv">OUTDIR</span><span class="o">=</span><span class="s2">"/project/dkarig/ecocoat/NCBI_oct22/checkm_dec15"</span>

<span class="nb">mkdir</span> <span class="nt">-p</span> <span class="s2">"</span><span class="nv">$OUTDIR</span><span class="s2">"</span>

<span class="c"># Run CheckM lineage workflow</span>
checkm lineage_wf <span class="se">\</span>
  <span class="nt">-x</span> fa <span class="se">\</span>
  <span class="nt">-t</span> 24 <span class="se">\</span>
  <span class="nt">--reduced_tree</span> <span class="se">\</span>
  <span class="s2">"</span><span class="nv">$GENOME_DIR</span><span class="s2">"</span> <span class="se">\</span>
  <span class="s2">"</span><span class="nv">$OUTDIR</span><span class="s2">"</span>

<span class="c"># Generate a clean summary table (TSV)</span>
checkm qa <span class="se">\</span>
  <span class="s2">"</span><span class="nv">$OUTDIR</span><span class="s2">"</span>/lineage.ms <span class="se">\</span>
  <span class="s2">"</span><span class="nv">$OUTDIR</span><span class="s2">"</span> <span class="se">\</span>
  <span class="nt">-o</span> 2 <span class="se">\</span>
  <span class="nt">-f</span> <span class="s2">"</span><span class="nv">$OUTDIR</span><span class="s2">/checkm_summary.tsv"</span>

<span class="nb">echo</span> <span class="s2">"CheckM completed at </span><span class="si">$(</span><span class="nb">date</span><span class="si">)</span><span class="s2">"</span>
</code></pre></div></div>

<p>Save this as run_checkm_all.sh and run as follows</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x run_checkm_all.sh
sbatch run_checkm_all.sh
</code></pre></div></div>

<p><strong>the key output file you need is checkm_summary.tsv</strong>
This includes (per genome):</p>

<p>● Completeness (%)</p>

<p>● Contamination (%)</p>

<p>● Strain heterogeneity</p>

<p>● Marker lineage</p>

<h4 id="step-3--gene-prediction-with-prokka">Step 3 — Gene prediction with Prokka</h4>

<p>This is for finding number of predicted genes and my strtegy is below</p>

<p>● Run Prokka once per genome (recommended; Prokka is per-genome by design)</p>

<p>● Store outputs in per-genome subfolders</p>

<p>● Parse Prokka outputs to create one summary TSV with:</p>

<p>● number of CDS (protein-coding genes)</p>

<p>● optionally rRNA, tRNA counts</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --job-name=prokka_all</span>
<span class="c">#SBATCH --partition=camplab</span>
<span class="c">#SBATCH --nodes=1</span>
<span class="c">#SBATCH --ntasks=1</span>
<span class="c">#SBATCH --cpus-per-task=16</span>
<span class="c">#SBATCH --mem=64G</span>
<span class="c">#SBATCH --time=24:00:00</span>
<span class="c">#SBATCH --output=prokka_%j.out</span>
<span class="c">#SBATCH --error=prokka_%j.err</span>

module load prokka

<span class="nv">GENOME_DIR</span><span class="o">=</span><span class="s2">"/project/dkarig/ecocoat/NCBI_oct22/all_genomes_dec15"</span>
<span class="nv">OUTDIR</span><span class="o">=</span><span class="s2">"/project/dkarig/ecocoat/NCBI_oct22/prokka_dec15"</span>

<span class="nb">mkdir</span> <span class="nt">-p</span> <span class="s2">"</span><span class="nv">$OUTDIR</span><span class="s2">"</span>

<span class="k">for </span>fa <span class="k">in</span> <span class="s2">"</span><span class="nv">$GENOME_DIR</span><span class="s2">"</span>/<span class="k">*</span>.fa<span class="p">;</span> <span class="k">do
    </span><span class="nv">base</span><span class="o">=</span><span class="si">$(</span><span class="nb">basename</span> <span class="s2">"</span><span class="nv">$fa</span><span class="s2">"</span> .fa<span class="si">)</span>
    <span class="nb">echo</span> <span class="s2">"Running Prokka on </span><span class="nv">$base</span><span class="s2">"</span>

    prokka <span class="se">\</span>
      <span class="nt">--outdir</span> <span class="s2">"</span><span class="nv">$OUTDIR</span><span class="s2">/</span><span class="nv">$base</span><span class="s2">"</span> <span class="se">\</span>
      <span class="nt">--prefix</span> <span class="s2">"</span><span class="nv">$base</span><span class="s2">"</span> <span class="se">\</span>
      <span class="nt">--cpus</span> 16 <span class="se">\</span>
      <span class="nt">--force</span> <span class="se">\</span>
      <span class="s2">"</span><span class="nv">$fa</span><span class="s2">"</span>
<span class="k">done

</span><span class="nb">echo</span> <span class="s2">"Prokka completed at </span><span class="si">$(</span><span class="nb">date</span><span class="si">)</span><span class="s2">"</span>
</code></pre></div></div>

<p>save as run_prokka_all.sh and run as follows</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x run_prokka_all.sh
sbatch run_prokka_all.sh
</code></pre></div></div>

<p>this will generate
For each genome (example ABV4_150):</p>

<pre><code class="language-pgsql">prokka_dec15/ABV4_150/
├── ABV4_150.gff
├── ABV4_150.tbl
├── ABV4_150.txt   ← summary stats (VERY IMPORTANT)
├── ABV4_150.faa
├── ABV4_150.ffn
└── ABV4_150.log
</code></pre>

<p>The file *.txt contains lines like:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">CDS</span><span class="pi">:</span> <span class="m">4123</span>
<span class="na">rRNA</span><span class="pi">:</span> <span class="m">9</span>
<span class="na">tRNA</span><span class="pi">:</span> <span class="m">67</span>
</code></pre></div></div>

<h4 id="step-31-generate-a-summary-tsv-for-predicted-genes">step 3.1 Generate a summary TSV for predicted genes</h4>

<p>Script: prokka_summary.sh</p>

<p>This extracts CDS counts (protein-coding genes) for all genomes.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>

<span class="nv">PROKKA_DIR</span><span class="o">=</span><span class="s2">"/project/dkarig/ecocoat/NCBI_oct22/prokka_dec15"</span>
<span class="nv">OUT_TSV</span><span class="o">=</span><span class="s2">"prokka_gene_summary.tsv"</span>

<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"Genome</span><span class="se">\t</span><span class="s2">CDS</span><span class="se">\t</span><span class="s2">rRNA</span><span class="se">\t</span><span class="s2">tRNA"</span> <span class="o">&gt;</span> <span class="s2">"</span><span class="nv">$OUT_TSV</span><span class="s2">"</span>

<span class="k">for </span>d <span class="k">in</span> <span class="s2">"</span><span class="nv">$PROKKA_DIR</span><span class="s2">"</span>/<span class="k">*</span><span class="p">;</span> <span class="k">do
    </span><span class="nv">genome</span><span class="o">=</span><span class="si">$(</span><span class="nb">basename</span> <span class="s2">"</span><span class="nv">$d</span><span class="s2">"</span><span class="si">)</span>
    <span class="nv">txt</span><span class="o">=</span><span class="s2">"</span><span class="nv">$d</span><span class="s2">/</span><span class="nv">$genome</span><span class="s2">.txt"</span>

    <span class="k">if</span> <span class="o">[[</span> <span class="nt">-f</span> <span class="s2">"</span><span class="nv">$txt</span><span class="s2">"</span> <span class="o">]]</span><span class="p">;</span> <span class="k">then
        </span><span class="nv">cds</span><span class="o">=</span><span class="si">$(</span><span class="nb">grep</span> <span class="s2">"^CDS:"</span> <span class="s2">"</span><span class="nv">$txt</span><span class="s2">"</span> | <span class="nb">awk</span> <span class="s1">'{print $2}'</span><span class="si">)</span>
        <span class="nv">rrna</span><span class="o">=</span><span class="si">$(</span><span class="nb">grep</span> <span class="s2">"^rRNA:"</span> <span class="s2">"</span><span class="nv">$txt</span><span class="s2">"</span> | <span class="nb">awk</span> <span class="s1">'{print $2}'</span><span class="si">)</span>
        <span class="nv">trna</span><span class="o">=</span><span class="si">$(</span><span class="nb">grep</span> <span class="s2">"^tRNA:"</span> <span class="s2">"</span><span class="nv">$txt</span><span class="s2">"</span> | <span class="nb">awk</span> <span class="s1">'{print $2}'</span><span class="si">)</span>
        <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"</span><span class="nv">$genome</span><span class="se">\t</span><span class="nv">$cds</span><span class="se">\t</span><span class="nv">$rrna</span><span class="se">\t</span><span class="nv">$trna</span><span class="s2">"</span> <span class="o">&gt;&gt;</span> <span class="s2">"</span><span class="nv">$OUT_TSV</span><span class="s2">"</span>
    <span class="k">fi
done

</span><span class="nb">echo</span> <span class="s2">"Summary written to </span><span class="nv">$OUT_TSV</span><span class="s2">"</span>
</code></pre></div></div>

<p>Run it:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x prokka_summary.sh
./prokka_summary.sh
</code></pre></div></div>

<h4 id="step-4--gene-prediction-with-prokka">Step 4 — Gene prediction with Prokka</h4>

<p>Follow the steps from first section</p>

<h4 id="-step-5-merge-individual-outputs-into-a-final-genome-summary-table">🧩 Step 5: Merge individual outputs into a final genome summary table</h4>

<p>At this stage, all analyses have been completed independently, and the results are available as separate tabular files:</p>

<p>● prokka_out.tsv → predicted gene counts</p>

<p>● topology.tsv → genome topology (Circular / Linear / Undetermined)</p>

<p>● checkm_summary.tsv → completeness and contamination</p>

<p>● quast_report.tsv → genome size, GC content, contigs, N50</p>

<p>● NCBI_list_of_genomes.tsv → genome identifiers and accession numbers</p>

<p><strong>Each file contains a shared identifier column (Genome), which allows them to be merged programmatically into a single, consistent summary table.</strong></p>

<p>To avoid manual errors and ensure reproducibility, I used pandas in Python to merge all tables based on this common column.</p>

<p>🐍 Example: merging genome-level tables using pandas</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">pandas</span> <span class="k">as</span> <span class="n">pd</span>

<span class="c1"># Load individual result tables
</span><span class="n">prokka</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nf">read_csv</span><span class="p">(</span><span class="sh">"</span><span class="s">prokka_out.tsv</span><span class="sh">"</span><span class="p">,</span> <span class="n">sep</span><span class="o">=</span><span class="sh">"</span><span class="se">\t</span><span class="sh">"</span><span class="p">)</span>
<span class="n">topology</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nf">read_csv</span><span class="p">(</span><span class="sh">"</span><span class="s">topology.tsv</span><span class="sh">"</span><span class="p">,</span> <span class="n">sep</span><span class="o">=</span><span class="sh">"</span><span class="se">\t</span><span class="sh">"</span><span class="p">)</span>
<span class="n">checkm</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nf">read_csv</span><span class="p">(</span><span class="sh">"</span><span class="s">checkm_summary.tsv</span><span class="sh">"</span><span class="p">,</span> <span class="n">sep</span><span class="o">=</span><span class="sh">"</span><span class="se">\t</span><span class="sh">"</span><span class="p">)</span>
<span class="n">quast</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nf">read_csv</span><span class="p">(</span><span class="sh">"</span><span class="s">quast_report.tsv</span><span class="sh">"</span><span class="p">,</span> <span class="n">sep</span><span class="o">=</span><span class="sh">"</span><span class="se">\t</span><span class="sh">"</span><span class="p">)</span>
<span class="n">ncbi</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nf">read_csv</span><span class="p">(</span><span class="sh">"</span><span class="s">NCBI_list_of_genomes.tsv</span><span class="sh">"</span><span class="p">,</span> <span class="n">sep</span><span class="o">=</span><span class="sh">"</span><span class="se">\t</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># Ensure consistent column names
</span><span class="k">for</span> <span class="n">df</span> <span class="ow">in</span> <span class="p">[</span><span class="n">prokka</span><span class="p">,</span> <span class="n">topology</span><span class="p">,</span> <span class="n">checkm</span><span class="p">,</span> <span class="n">quast</span><span class="p">,</span> <span class="n">ncbi</span><span class="p">]:</span>
    <span class="n">df</span><span class="p">.</span><span class="n">columns</span> <span class="o">=</span> <span class="n">df</span><span class="p">.</span><span class="n">columns</span><span class="p">.</span><span class="nb">str</span><span class="p">.</span><span class="nf">strip</span><span class="p">()</span>

<span class="c1"># Sequentially merge tables using the common 'Genome' column
</span><span class="n">final_table</span> <span class="o">=</span> <span class="p">(</span>
    <span class="n">quast</span>
    <span class="p">.</span><span class="nf">merge</span><span class="p">(</span><span class="n">prokka</span><span class="p">,</span> <span class="n">on</span><span class="o">=</span><span class="sh">"</span><span class="s">Genome</span><span class="sh">"</span><span class="p">,</span> <span class="n">how</span><span class="o">=</span><span class="sh">"</span><span class="s">left</span><span class="sh">"</span><span class="p">)</span>
    <span class="p">.</span><span class="nf">merge</span><span class="p">(</span><span class="n">checkm</span><span class="p">,</span> <span class="n">on</span><span class="o">=</span><span class="sh">"</span><span class="s">Genome</span><span class="sh">"</span><span class="p">,</span> <span class="n">how</span><span class="o">=</span><span class="sh">"</span><span class="s">left</span><span class="sh">"</span><span class="p">)</span>
    <span class="p">.</span><span class="nf">merge</span><span class="p">(</span><span class="n">topology</span><span class="p">,</span> <span class="n">on</span><span class="o">=</span><span class="sh">"</span><span class="s">Genome</span><span class="sh">"</span><span class="p">,</span> <span class="n">how</span><span class="o">=</span><span class="sh">"</span><span class="s">left</span><span class="sh">"</span><span class="p">)</span>
    <span class="p">.</span><span class="nf">merge</span><span class="p">(</span><span class="n">ncbi</span><span class="p">,</span> <span class="n">on</span><span class="o">=</span><span class="sh">"</span><span class="s">Genome</span><span class="sh">"</span><span class="p">,</span> <span class="n">how</span><span class="o">=</span><span class="sh">"</span><span class="s">left</span><span class="sh">"</span><span class="p">)</span>
<span class="p">)</span>

<span class="c1"># Save final table
</span><span class="n">final_table</span><span class="p">.</span><span class="nf">to_csv</span><span class="p">(</span><span class="sh">"</span><span class="s">final_genome_summary_table.tsv</span><span class="sh">"</span><span class="p">,</span> <span class="n">sep</span><span class="o">=</span><span class="sh">"</span><span class="se">\t</span><span class="sh">"</span><span class="p">,</span> <span class="n">index</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Final genome summary table successfully created.</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>This approach ensures that:</p>

<p>● All genome-level metadata are synchronized</p>

<p>● Missing values are handled transparently</p>

<p>● The workflow is fully reproducible</p>

<h4 id="-final-genome-summary-table-format">📋 Final genome summary table format</h4>

<p>The merged output is used directly as Table 1 in the Genome Resource Announcement manuscript.</p>

<p>Table 1. Genome assembly and annotation statistics
| Genome | Assembly type | Genome size (bp) | GC (%) | Coverage (×) | Contigs | N50 (bp) | Predicted genes | Completeness (%) | Contamination (%) | Topology | Accession |
| —— | ————- | —————- | —— | ———— | ——- | ——– | ————— | —————- | —————– | ——– | ——— |</p>

<p>● Assembly type: Illumina / Hybrid / Long-read</p>

<p>● Topology: Circular / Linear / Undetermined</p>

<p>● Accession: “pending” during submission, updated post-acceptance</p>

<p>● genome statistics : N50, GC content, Completness, Contamination, Genome size, and Number of contigs</p>

<h4 id="-take-home-messages">✅ Take-home messages</h4>

<p>● Do not over-claim genome circularity. Only assert circular topology when supported by long-read or hybrid assemblies and explicit validation.</p>

<p>● Draft and linear genomes are fully acceptable for NCBI submission and Genome Resource Announcements.</p>

<p>● Topology should be assigned conservatively using assembly evidence, not biological expectation.</p>

<p>● A single, reproducible summary table simplifies NCBI submissions and serves directly as a manuscript-ready resource.</p>

<p>● Clear documentation of methods and assumptions makes genome submissions reviewer-safe and future-proof.</p>

<p>This workflow has worked smoothly for large-scale genome submissions and Genome Resource Announcements.</p>

<p>The image is showing GC content of all the genomes.</p>

<p><img src="/assets/img/gc.png" alt="genomes_are_ready" /></p>]]></content><author><name></name></author><summary type="html"><![CDATA[A step-by-step guide to answering NCBI genome topology questions and generating a complete, automated genome report table for Genome Resource Announcements.]]></summary></entry></feed>