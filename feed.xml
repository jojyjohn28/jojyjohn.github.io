<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://jojyjohn28.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://jojyjohn28.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-03-20T10:34:21-04:00</updated><id>https://jojyjohn28.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design.
</subtitle><entry><title type="html"></title><link href="https://jojyjohn28.github.io/blog/2026-02-11-day6-specialized-functions/" rel="alternate" type="text/html" title="" /><published>2026-03-20T10:34:21-04:00</published><updated>2026-03-20T10:34:21-04:00</updated><id>https://jojyjohn28.github.io/blog/2026-02-11-day6-specialized-functions</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/2026-02-11-day6-specialized-functions/"><![CDATA[<h1 id="day-6-specialized-genomic-functions">Day 6: Specialized Genomic Functions</h1>

<table>
  <tbody>
    <tr>
      <td><strong>Estimated Time:</strong> 8-12 hours</td>
      <td><strong>Difficulty:</strong> Advanced</td>
      <td><strong>Prerequisites:</strong> Day 5 (Annotation)</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="-table-of-contents">📚 Table of Contents</h2>

<ul>
  <li><a href="#introduction">Introduction</a></li>
  <li><a href="#secondary-metabolites">Secondary Metabolites</a></li>
  <li><a href="#antimicrobial-resistance">Antimicrobial Resistance</a></li>
  <li><a href="#carbohydrate-active-enzymes">Carbohydrate-Active Enzymes</a></li>
  <li><a href="#prophages--viruses">Prophages &amp; Viruses</a></li>
  <li><a href="#crispr-systems">CRISPR Systems</a></li>
  <li><a href="#mobile-genetic-elements">Mobile Genetic Elements</a></li>
  <li><a href="#protein-domains">Protein Domains</a></li>
  <li><a href="#comparative-analysis">Comparative Analysis</a></li>
  <li><a href="#best-practices">Best Practices</a></li>
</ul>

<hr />

<h2 id="-introduction">🎯 Introduction</h2>

<p>Welcome to Day 6! After basic annotation (Day 5), it’s time to discover <strong>hidden genomic treasures</strong> - specialized functions that make organisms unique.</p>

<p><strong>Today’s discoveries:</strong></p>

<ol>
  <li>🧪 <strong>Secondary metabolites</strong> - Antibiotics, toxins, signaling molecules</li>
  <li>💊 <strong>Antimicrobial resistance</strong> - AMR genes and mechanisms</li>
  <li>🍬 <strong>CAZymes</strong> - Carbohydrate degradation capabilities</li>
  <li>🦠 <strong>Prophages</strong> - Integrated viral sequences</li>
  <li>✂️ <strong>CRISPR systems</strong> - Bacterial immune systems</li>
  <li>🔄 <strong>Mobile elements</strong> - Transposons, insertion sequences</li>
  <li>🧬 <strong>Protein domains</strong> - Functional motifs and structures</li>
</ol>

<h3 id="why-this-matters">Why This Matters</h3>

<p><strong>Basic annotation tells you:</strong></p>

<ul>
  <li>This is a kinase</li>
  <li>This is a transporter</li>
</ul>

<p><strong>Specialized analysis reveals:</strong></p>

<ul>
  <li>This organism produces penicillin</li>
  <li>This strain is resistant to carbapenems</li>
  <li>This microbe can degrade cellulose</li>
  <li>This genome contains CRISPR-Cas9</li>
</ul>

<hr />

<h2 id="-secondary-metabolites">🧪 Secondary Metabolites</h2>

<h3 id="antismash-biosynthetic-gene-cluster-detection">antiSMASH: Biosynthetic Gene Cluster Detection</h3>

<p><strong>antiSMASH</strong> (antibiotics &amp; Secondary Metabolite Analysis SHell) is the gold standard for BGC detection.</p>

<h4 id="installation">Installation</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create environment</span>
conda create <span class="nt">-n</span> antismash <span class="nt">-c</span> bioconda antismash
conda activate antismash

<span class="c"># Download databases (~15 GB)</span>
download-antismash-databases
</code></pre></div></div>

<h4 id="basic-usage">Basic Usage</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda activate antismash

<span class="c"># Single genome</span>
antismash <span class="se">\</span>
    <span class="nt">--output-dir</span> antismash_output/genome1 <span class="se">\</span>
    <span class="nt">--genefinding-tool</span> prodigal <span class="se">\</span>
    <span class="nt">--cpus</span> 8 <span class="se">\</span>
    genome1.gbk

<span class="c"># With all features enabled</span>
antismash <span class="se">\</span>
    <span class="nt">--output-dir</span> antismash_output/genome1 <span class="se">\</span>
    <span class="nt">--genefinding-tool</span> prodigal <span class="se">\</span>
    <span class="nt">--knownclusterblast</span> <span class="se">\</span>
    <span class="nt">--subclusterblast</span> <span class="se">\</span>
    <span class="nt">--clusterblast</span> <span class="se">\</span>
    <span class="nt">--smcog-trees</span> <span class="se">\</span>
    <span class="nt">--cpus</span> 8 <span class="se">\</span>
    genome1.gbk
</code></pre></div></div>

<p><strong>Input formats:</strong></p>

<ul>
  <li>GenBank (.gbk) - recommended</li>
  <li>FASTA (.fa) - requires gene finding</li>
</ul>

<h4 id="antismash-output">antiSMASH Output</h4>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>antismash_output/genome1/
├── genome1.gbk                    # Annotated genome
├── genome1.json                   # Machine-readable results
├── index.html                     # Interactive visualization
├── regions.js                     # BGC regions
├── knownclusterblast/            # Hits to known BGCs
└── svg/                          # Cluster visualizations
</code></pre></div></div>

<p><strong>BGC types detected:</strong></p>

<ul>
  <li>Polyketides (PKS)</li>
  <li>Non-ribosomal peptides (NRPS)</li>
  <li>Terpenes</li>
  <li>Bacteriocins</li>
  <li>Siderophores</li>
  <li>RiPPs (Ribosomally synthesized and Post-translationally modified Peptides)</li>
</ul>

<h4 id="parse-antismash-results">Parse antiSMASH Results</h4>

<h2 id="please-use-parse_antismashpy-from-day-6-">please use parse_antismash.py from <strong><a href="https://github.com/jojyjohn28/metagenome-analysis-series/tree/main/day6-specialized-functions/">Day 6 →</a></strong></h2>

<h3 id="big-scape-bgc-similarity-analysis">BiG-SCAPE: BGC Similarity Analysis</h3>

<p><strong>BiG-SCAPE</strong> compares BGCs across multiple genomes to identify families.</p>

<h4 id="installation-1">Installation</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> bigscape <span class="nt">-c</span> bioconda bigscape
conda activate bigscape
</code></pre></div></div>

<h4 id="usage">Usage</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Collect all BGC GenBank files from antiSMASH</span>
<span class="nb">mkdir </span>bigscape_input
<span class="nb">cp </span>antismash_output/<span class="k">*</span>/regions/<span class="k">*</span>.gbk bigscape_input/

<span class="c"># Run BiG-SCAPE</span>
bigscape <span class="se">\</span>
    <span class="nt">-i</span> bigscape_input <span class="se">\</span>
    <span class="nt">-o</span> bigscape_output <span class="se">\</span>
    <span class="nt">--pfam_dir</span> ~/pfam_database <span class="se">\</span>
    <span class="nt">--cores</span> 8 <span class="se">\</span>
    <span class="nt">--mode</span> auto

<span class="c"># Visualize</span>
firefox bigscape_output/network_files/index.html
</code></pre></div></div>

<p><strong>BiG-SCAPE output:</strong></p>

<ul>
  <li>BGC families (grouped by similarity)</li>
  <li>Network visualization</li>
  <li>Comparative analysis across genomes</li>
</ul>

<hr />

<h2 id="-antimicrobial-resistance">💊 Antimicrobial Resistance</h2>

<h3 id="card-rgi-comprehensive-amr-database">CARD-RGI: Comprehensive AMR Database</h3>

<p><strong>RGI</strong> (Resistance Gene Identifier) uses CARD database for AMR detection.</p>

<h4 id="installation-2">Installation</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> rgi <span class="nt">-c</span> bioconda rgi
conda activate rgi

<span class="c"># Download CARD database</span>
rgi load <span class="nt">--card_json</span> ~/card_database/card.json

<span class="c"># Verify</span>
rgi database <span class="nt">--version</span>
</code></pre></div></div>

<h4 id="usage-1">Usage</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda activate rgi

<span class="c"># Predict AMR from proteins</span>
rgi main <span class="se">\</span>
    <span class="nt">--input_sequence</span> proteins.faa <span class="se">\</span>
    <span class="nt">--input_type</span> protein <span class="se">\</span>
    <span class="nt">--output_file</span> rgi_output <span class="se">\</span>
    <span class="nt">--clean</span> <span class="se">\</span>
    <span class="nt">--num_threads</span> 8

<span class="c"># From nucleotides (slower but more sensitive)</span>
rgi main <span class="se">\</span>
    <span class="nt">--input_sequence</span> genome.fa <span class="se">\</span>
    <span class="nt">--input_type</span> contig <span class="se">\</span>
    <span class="nt">--output_file</span> rgi_output <span class="se">\</span>
    <span class="nt">--clean</span> <span class="se">\</span>
    <span class="nt">--num_threads</span> 8 <span class="se">\</span>
    <span class="nt">--include_loose</span>
</code></pre></div></div>

<p><strong>RGI output columns:</strong></p>

<ul>
  <li>ORF_ID</li>
  <li>ARO (Antibiotic Resistance Ontology)</li>
  <li>Drug Class</li>
  <li>Resistance Mechanism</li>
  <li>AMR Gene Family</li>
</ul>

<h4 id="parse-rgi-results">Parse RGI Results</h4>

<h2 id="use-parse_rgipy-from-day-6-">Use parse_rgi.py from <strong><a href="https://github.com/jojyjohn28/metagenome-analysis-series/tree/main/day6-specialized-functions/">Day 6 →</a></strong></h2>

<h3 id="abricate-fast-amr-screening">ABRicate: Fast AMR Screening</h3>

<p><strong>ABRicate</strong> screens multiple AMR databases quickly.</p>

<h4 id="installation-3">Installation</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> abricate <span class="nt">-c</span> bioconda abricate
conda activate abricate

<span class="c"># Update databases</span>
abricate-get_db <span class="nt">--db</span> all
</code></pre></div></div>

<h4 id="usage-2">Usage</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda activate abricate

<span class="c"># Screen against all databases</span>
abricate genome.fa <span class="o">&gt;</span> abricate_results.tab

<span class="c"># Specific database</span>
abricate <span class="nt">--db</span> card genome.fa <span class="o">&gt;</span> card_results.tab
abricate <span class="nt">--db</span> resfinder genome.fa <span class="o">&gt;</span> resfinder_results.tab
abricate <span class="nt">--db</span> vfdb genome.fa <span class="o">&gt;</span> virulence_results.tab

<span class="c"># Batch processing</span>
abricate genomes/<span class="k">*</span>.fa <span class="o">&gt;</span> all_genomes_amr.tab

<span class="c"># Summary</span>
abricate <span class="nt">--summary</span> all_genomes_amr.tab <span class="o">&gt;</span> amr_summary.tab
</code></pre></div></div>

<p><strong>Available databases:</strong></p>

<ul>
  <li>card (CARD)</li>
  <li>resfinder (ResFinder)</li>
  <li>ncbi (NCBI AMRFinderPlus)</li>
  <li>argannot</li>
  <li>megares</li>
  <li>vfdb (Virulence factors)</li>
  <li>plasmidfinder</li>
</ul>

<hr />

<h2 id="-carbohydrate-active-enzymes-cazymes">🍬 Carbohydrate-Active Enzymes (CAZymes)</h2>

<h3 id="dbcan2-cazyme-annotation">dbCAN2: CAZyme Annotation</h3>

<p><strong>dbCAN</strong> identifies enzymes that degrade, modify, or create carbohydrates.</p>

<h4 id="installation-4">Installation</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> dbcan <span class="nt">-c</span> bioconda dbcan
conda activate dbcan

<span class="c"># Download databases</span>
dbcan_build <span class="nt">--help</span>
</code></pre></div></div>

<h4 id="usage-3">Usage</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda activate dbcan

<span class="c"># Run dbCAN</span>
run_dbcan <span class="se">\</span>
    proteins.faa <span class="se">\</span>
    protein <span class="se">\</span>
    <span class="nt">--out_dir</span> dbcan_output <span class="se">\</span>
    <span class="nt">--db_dir</span> ~/dbcan_db <span class="se">\</span>
    <span class="nt">--tools</span> all <span class="se">\</span>
    <span class="nt">--threads</span> 8

<span class="c"># Output: dbcan_output/overview.txt</span>
</code></pre></div></div>

<p><strong>CAZyme families:</strong></p>

<ul>
  <li><strong>GH</strong> - Glycoside Hydrolases (breakdown)</li>
  <li><strong>GT</strong> - Glycosyl Transferases (synthesis)</li>
  <li><strong>PL</strong> - Polysaccharide Lyases</li>
  <li><strong>CE</strong> - Carbohydrate Esterases</li>
  <li><strong>AA</strong> - Auxiliary Activities</li>
  <li><strong>CBM</strong> - Carbohydrate-Binding Modules</li>
</ul>

<h4 id="parse-dbcan-results">Parse dbCAN Results</h4>

<h2 id="use-parse_dbcanpy-from-day-6-">Use parse_dbcan.py from <strong><a href="https://github.com/jojyjohn28/metagenome-analysis-series/tree/main/day6-specialized-functions/">Day 6 →</a></strong></h2>

<h2 id="-prophages--viruses">🦠 Prophages &amp; Viruses</h2>

<h3 id="virsorter2-viral-sequence-detection">VirSorter2: Viral Sequence Detection</h3>

<p><strong>VirSorter2</strong> identifies viral sequences in genomes and metagenomes.</p>

<h4 id="installation-5">Installation</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> virsorter2 <span class="nt">-c</span> bioconda <span class="nv">virsorter</span><span class="o">=</span>2
conda activate virsorter2

<span class="c"># Setup database</span>
virsorter setup <span class="nt">-d</span> ~/virsorter2-db <span class="nt">-j</span> 4
</code></pre></div></div>

<h4 id="usage-4">Usage</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda activate virsorter2

<span class="c"># Run VirSorter2</span>
virsorter run <span class="se">\</span>
    <span class="nt">-i</span> genome.fa <span class="se">\</span>
    <span class="nt">-w</span> virsorter2_output <span class="se">\</span>
    <span class="nt">--min-length</span> 5000 <span class="se">\</span>
    <span class="nt">--min-score</span> 0.5 <span class="se">\</span>
    <span class="nt">-j</span> 8 <span class="se">\</span>
    all

<span class="c"># High-confidence viral sequences</span>
<span class="nb">cat </span>virsorter2_output/final-viral-boundary.tsv
</code></pre></div></div>

<p><strong>VirSorter2 output:</strong></p>

<ul>
  <li>Prophage regions</li>
  <li>Viral score (0-1)</li>
  <li>Boundary coordinates</li>
  <li>Gene content</li>
</ul>

<hr />

<h3 id="phaster-phage-search-tool-enhanced-release">PHASTER: PHAge Search Tool Enhanced Release</h3>

<p><strong>PHASTER</strong> identifies prophages with detailed annotation.</p>

<p><strong>Note:</strong> PHASTER is primarily a web service.</p>

<p><strong>Web interface:</strong> https://phaster.ca/</p>

<p><strong>Alternative: Use VirSorter2 + CheckV</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># CheckV for viral genome quality</span>
conda create <span class="nt">-n</span> checkv <span class="nt">-c</span> bioconda checkv
conda activate checkv

<span class="c"># Download database</span>
checkv download_database ~/checkv-db

<span class="c"># Run CheckV on VirSorter2 results</span>
checkv end_to_end <span class="se">\</span>
    virsorter2_output/final-viral-combined.fa <span class="se">\</span>
    checkv_output <span class="se">\</span>
    <span class="nt">-d</span> ~/checkv-db <span class="se">\</span>
    <span class="nt">-t</span> 8
</code></pre></div></div>

<hr />

<h2 id="️-crispr-systems">✂️ CRISPR Systems</h2>

<h3 id="crisprcasfinder-detect-crispr-cas-systems">CRISPRCasFinder: Detect CRISPR-Cas Systems</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Installation</span>
conda create <span class="nt">-n</span> crisprcasfinder <span class="nt">-c</span> bioconda crisprcasfinder
conda activate crisprcasfinder

<span class="c"># Run</span>
CRISPRCasFinder.pl <span class="se">\</span>
    <span class="nt">-in</span> genome.fa <span class="se">\</span>
    <span class="nt">-out</span> crispr_output <span class="se">\</span>
    <span class="nt">-cas</span> <span class="se">\</span>
    <span class="nt">-keep</span>
</code></pre></div></div>

<h3 id="minced-simple-crispr-detection">MinCED: Simple CRISPR Detection</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> minced <span class="nt">-c</span> bioconda minced
conda activate minced

<span class="c"># Quick CRISPR detection</span>
minced genome.fa crispr_output.txt

<span class="c"># Parse results</span>
<span class="nb">grep</span> <span class="s2">"CRISPR"</span> crispr_output.txt
</code></pre></div></div>

<p><strong>CRISPR system types:</strong></p>

<ul>
  <li>Type I (most common in bacteria)</li>
  <li>Type II (Cas9 - CRISPR editing)</li>
  <li>Type III</li>
  <li>Type IV-VI (rare)</li>
</ul>

<hr />

<h2 id="-mobile-genetic-elements">🔄 Mobile Genetic Elements</h2>

<h3 id="isfinder-insertion-sequences">ISfinder: Insertion Sequences</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Use ABRicate with custom IS database</span>
abricate <span class="nt">--db</span> isfinder genome.fa <span class="o">&gt;</span> is_results.tab

<span class="c"># Or BLAST against ISfinder database</span>
blastn <span class="se">\</span>
    <span class="nt">-query</span> genome.fa <span class="se">\</span>
    <span class="nt">-db</span> ~/isfinder_db/ISfinder <span class="se">\</span>
    <span class="nt">-outfmt</span> 6 <span class="se">\</span>
    <span class="nt">-num_threads</span> 8 <span class="se">\</span>
    <span class="o">&gt;</span> is_blast.out
</code></pre></div></div>

<h3 id="integron-finder">Integron Finder</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> integron_finder <span class="nt">-c</span> bioconda integron_finder
conda activate integron_finder

<span class="c"># Find integrons</span>
integron_finder <span class="se">\</span>
    <span class="nt">--local-max</span> <span class="se">\</span>
    <span class="nt">--cpu</span> 8 <span class="se">\</span>
    genome.fa
</code></pre></div></div>

<p><strong>Integrons contain:</strong></p>

<ul>
  <li>Integration sites</li>
  <li>Gene cassettes</li>
  <li>Often AMR genes</li>
</ul>

<hr />

<h2 id="-protein-domains--functions">🧬 Protein Domains &amp; Functions</h2>

<h3 id="interproscan-comprehensive-domain-analysis">InterProScan: Comprehensive Domain Analysis</h3>

<p><strong>InterProScan</strong> searches multiple protein signature databases.</p>

<h4 id="installation-6">Installation</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Large download - recommend using web service</span>
<span class="c"># Or install locally:</span>
wget ftp://ftp.ebi.ac.uk/pub/software/unix/iprscan/5/5.XX-XX.X/interproscan-5.XX-XX.X-64-bit.tar.gz
<span class="nb">tar</span> <span class="nt">-xzf</span> interproscan-5.XX-XX.X-64-bit.tar.gz
</code></pre></div></div>

<h4 id="usage-5">Usage</h4>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Run InterProScan</span>
interproscan.sh <span class="se">\</span>
    <span class="nt">-i</span> proteins.faa <span class="se">\</span>
    <span class="nt">-f</span> TSV,GFF3 <span class="se">\</span>
    <span class="nt">-o</span> interproscan_output <span class="se">\</span>
    <span class="nt">--cpu</span> 8 <span class="se">\</span>
    <span class="nt">--goterms</span> <span class="se">\</span>
    <span class="nt">--pathways</span>
</code></pre></div></div>

<p><strong>Databases searched:</strong></p>

<ul>
  <li>Pfam (protein families)</li>
  <li>PROSITE (patterns)</li>
  <li>PRINTS</li>
  <li>SMART</li>
  <li>TIGRFAMs</li>
  <li>PANTHER</li>
  <li>Gene3D</li>
  <li>Superfamily</li>
</ul>

<h4 id="parse-interproscan">Parse InterProScan</h4>

<h2 id="use-parse_iprpy-from-day-6-">Use parse_ipr.py from <strong><a href="https://github.com/jojyjohn28/metagenome-analysis-series/tree/main/day6-specialized-functions/">Day 6 →</a></strong></h2>

<h2 id="-comprehensive-analysis-workflow">📊 Comprehensive Analysis Workflow</h2>

<h3 id="complete-specialized-function-pipeline">Complete Specialized Function Pipeline</h3>

<p>Use comprehensive_analysis.sh from <strong><a href="https://github.com/jojyjohn28/metagenome-analysis-series/tree/main/day6-specialized-functions/">Day 6 →</a></strong></p>

<hr />

<h2 id="-comparative-analysis">📈 Comparative Analysis</h2>

<h3 id="compare-specialized-functions-across-genomes">Compare Specialized Functions Across Genomes</h3>

<p>Use compare_specialized_functions.py from <strong><a href="https://github.com/jojyjohn28/metagenome-analysis-series/tree/main/day6-specialized-functions/">Day 6 →</a></strong></p>

<h2 id="-best-practices">💡 Best Practices</h2>

<h3 id="before-analysis">Before Analysis</h3>

<ul>
  <li>✅ Have complete gene predictions (Prodigal/Prokka)</li>
  <li>✅ Use GenBank format for antiSMASH</li>
  <li>✅ Check genome quality (comp &gt;80%, cont &lt;5%)</li>
  <li>✅ Ensure databases are updated</li>
</ul>

<h3 id="during-analysis">During Analysis</h3>

<ul>
  <li>✅ Run tools in priority order (fast → slow)</li>
  <li>✅ Use multiple tools for AMR (RGI + ABRicate)</li>
  <li>✅ Save intermediate files</li>
  <li>✅ Document tool versions</li>
</ul>

<h3 id="after-analysis">After Analysis</h3>

<ul>
  <li>✅ Validate hits manually (especially AMR)</li>
  <li>✅ Check for false positives</li>
  <li>✅ Cross-reference with literature</li>
  <li>✅ Visualize results</li>
</ul>

<hr />

<h2 id="-troubleshooting">🔧 Troubleshooting</h2>

<h3 id="antismash-no-bgcs-detected">antiSMASH: No BGCs detected</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Check input format</span>
<span class="nb">head </span>genome.gbk

<span class="c"># Try with relaxed detection</span>
antismash <span class="nt">--minimal</span> ...

<span class="c"># Ensure genome is annotated</span>
</code></pre></div></div>

<h3 id="rgi-no-results">RGI: No results</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Update CARD database</span>
rgi load <span class="nt">--card_json</span> ~/card_database/card.json

<span class="c"># Try --include_loose for more hits</span>
rgi main <span class="nt">--include_loose</span> ...
</code></pre></div></div>

<h3 id="dbcan-low-cazyme-count">dbCAN: Low CAZyme count</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Use all three tools</span>
run_dbcan <span class="nt">--tools</span> all ...

<span class="c"># Check protein quality</span>
<span class="nb">grep</span> <span class="s2">"&gt;"</span> proteins.faa | <span class="nb">wc</span> <span class="nt">-l</span>
</code></pre></div></div>

<hr />

<h2 id="-expected-results">📈 Expected Results</h2>

<h3 id="typical-findings-per-genome">Typical Findings per Genome</h3>

<table>
  <thead>
    <tr>
      <th>Feature</th>
      <th>Typical Range</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>BGCs</strong></td>
      <td>5-30</td>
    </tr>
    <tr>
      <td><strong>AMR genes</strong></td>
      <td>0-50</td>
    </tr>
    <tr>
      <td><strong>CAZymes</strong></td>
      <td>50-500</td>
    </tr>
    <tr>
      <td><strong>Prophages</strong></td>
      <td>0-10</td>
    </tr>
    <tr>
      <td><strong>CRISPR arrays</strong></td>
      <td>0-5</td>
    </tr>
    <tr>
      <td><strong>Integrons</strong></td>
      <td>0-5</td>
    </tr>
  </tbody>
</table>

<h3 id="visulaizations">Visulaizations</h3>

<h4 id="-1-bgc_heatmapr---bgc-distribution">📊 1. bgc_heatmap.R - BGC Distribution</h4>

<p><strong>What it does:</strong></p>

<p>● Visualizes antiSMASH BGC distributions across genomes
● Creates 3 heatmap variants (basic, custom colors, annotated)
● Includes toy data built-in
● Automatic clustering by BGC similarity</p>

<p>Outputs:</p>

<p>● bgc_heatmap_basic.pdf - Simple clustered heatmap
● bgc_heatmap_custom.pdf - Custom gradient (white → gold → red → purple)
● bgc_heatmap_annotated.pdf - With annotations for BGC richness
● bgc_summary.csv - Summary statistics</p>

<p>Toy data included: 15 genomes × 9 BGC types</p>

<h4 id="-2-cazyme_bubble_plotr---cazyme-analysis">🍬 2. cazyme_bubble_plot.R - CAZyme Analysis</h4>

<p><strong>What it does:</strong></p>

<p>● Creates bubble plots showing CAZyme family distributions
● Bubble size = CAZyme count
● Color = Genome completion percentage
● Includes heatmap and bar plot variants
● Analyzes degradation capabilities</p>

<p>Outputs:</p>

<p>● cazyme_bubble_plot.pdf - Main bubble visualization
● cazyme_bubble_plot_alt.pdf - Alternative color scheme
● cazyme_total_barplot.pdf - Total counts by taxonomic order
● cazyme_heatmap.pdf - Clustered heatmap
● cazyme_summary.csv - Summary with degradation analysis</p>

<p>Toy data included: 12 taxonomic orders × 6 CAZyme families</p>

<p>All R-codes available in *<em><a href="https://github.com/jojyjohn28/metagenome-analysis-series/tree/main/day6-specialized-functions/">Day 6 →</a></em></p>

<h3 id="processing-time-8-core-laptop">Processing Time (8-core laptop)</h3>

<table>
  <thead>
    <tr>
      <th>Tool</th>
      <th>Single Genome</th>
      <th>10 Genomes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>antiSMASH</td>
      <td>30-60 min</td>
      <td>8-10 hrs</td>
    </tr>
    <tr>
      <td>RGI</td>
      <td>5-10 min</td>
      <td>1-2 hrs</td>
    </tr>
    <tr>
      <td>ABRicate</td>
      <td>1-2 min</td>
      <td>15-20 min</td>
    </tr>
    <tr>
      <td>dbCAN</td>
      <td>10-20 min</td>
      <td>2-3 hrs</td>
    </tr>
    <tr>
      <td>VirSorter2</td>
      <td>20-40 min</td>
      <td>5-7 hrs</td>
    </tr>
    <tr>
      <td>InterProScan</td>
      <td>1-2 hrs</td>
      <td>15-20 hrs</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="-success-checklist">✅ Success Checklist</h2>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />BGCs identified and annotated</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />AMR profile generated</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />CAZyme repertoire characterized</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Prophages detected</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />CRISPR systems identified</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Mobile elements cataloged</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Protein domains annotated</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />Comparative analysis completed</li>
</ul>

<hr />

<h2 id="-key-resources">📚 Key Resources</h2>

<h3 id="tools--databases">Tools &amp; Databases</h3>

<ul>
  <li><a href="https://antismash.secondarymetabolites.org/">antiSMASH</a></li>
  <li><a href="https://card.mcmaster.ca/">CARD</a></li>
  <li><a href="https://github.com/tseemann/abricate">ABRicate</a></li>
  <li><a href="http://bcb.unl.edu/dbCAN2/">dbCAN</a></li>
  <li><a href="https://github.com/jiarong/VirSorter2">VirSorter2</a></li>
  <li><a href="https://www.ebi.ac.uk/interpro/">InterProScan</a></li>
</ul>

<hr />

<h2 id="️-whats-next">➡️ What’s Next?</h2>

<p><strong>Congratulations!</strong> You’ve discovered the specialized capabilities of your genomes!</p>

<p>Next steps:</p>

<ul>
  <li>Comparative genomics</li>
  <li>Pangenome analysis</li>
  <li>Phylogenomics</li>
  <li>Publication!</li>
</ul>

<h4 id="repo-for-todays-code-and-other-details">Repo for today’s code and other details</h4>

<p>🔗 <strong><a href="https://github.com/jojyjohn28/metagenome-analysis-series/tree/main/day6-specialized-functions/">Day 6 →</a></strong></p>

<p>Last updated: February 2026</p>

<p><img src="/assets/img/mg_day6.png" alt="day6 mg" /></p>]]></content><author><name></name></author></entry><entry><title type="html">Day 6 — Moving Your Data: scp, rsync, SFTP, and Globus Connect</title><link href="https://jojyjohn28.github.io/blog/day6-data-transfer/" rel="alternate" type="text/html" title="Day 6 — Moving Your Data: scp, rsync, SFTP, and Globus Connect" /><published>2026-03-19T00:00:00-04:00</published><updated>2026-03-19T00:00:00-04:00</updated><id>https://jojyjohn28.github.io/blog/day6-data-transfer</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/day6-data-transfer/"><![CDATA[<h1 id="day-6-moving-your-data--scp-rsync-sftp-and-globus-connect">Day 6: Moving Your Data — scp, rsync, SFTP, and Globus Connect</h1>

<p><img src="./day6-hero.svg" alt="Data Transfer: scp, rsync, SFTP, Globus Connect" /></p>

<blockquote>
  <p><em>This is Day 6 — the final post in the series: <strong>From Laptop to HPC: Scaling Computational Biology Workflows</strong> and 🧬 Day 62 of Daily Bioinformatics from Jojy’s Desk. In <a href=".https://jojyjohn28.github.io/blog/day5-pipelines/">Day 5</a> we built reproducible pipelines with Snakemake and Nextflow. Today: the question everyone hits at the start — how do you actually get your data there?</em></p>
</blockquote>

<hr />

<h2 id="the-problem-nobody-warns-you-about">The Problem Nobody Warns You About</h2>

<p>You’ve set up your conda environment. You’ve written your SLURM script. You know how to submit jobs and run job arrays. Everything is in order.</p>

<p>Then you look at your external hard drive with 300 GB of raw sequencing reads and think:</p>

<p><em>“How do I get this onto the cluster?”</em></p>

<p>Data transfer is one of those things that seems trivial right up until you’re staring at a progress bar saying <strong>14 hours remaining</strong> — and then it fails at hour 13 because your Wi-Fi dropped. You restart. It starts from scratch.</p>

<p>There is a better way. Several better ways, in fact. This post covers every tool you’ll actually use, when to reach for each one, and how to avoid the mistakes that cost researchers hours of wasted time.</p>

<hr />

<h2 id="what-youll-need">What You’ll Need</h2>

<ul>
  <li>SSH access to your HPC (covered in Day 1)</li>
  <li>A terminal on your laptop (Linux/Mac: built-in Terminal; Windows: PowerShell or MobaXterm)</li>
  <li>For Globus: a free account at <a href="https://www.globus.org">globus.org</a></li>
</ul>

<hr />

<h2 id="the-full-toolkit-at-a-glance">The Full Toolkit at a Glance</h2>

<table>
  <thead>
    <tr>
      <th>Method</th>
      <th>Best For</th>
      <th>Resumes on Failure</th>
      <th>GUI Available</th>
      <th>Speed</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">scp</code></td>
      <td>Single files, quick uploads</td>
      <td>❌ No</td>
      <td>MobaXterm, Cyberduck</td>
      <td>Good</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">rsync</code></td>
      <td>Folders, large batches, repeat syncs</td>
      <td>✅ Yes</td>
      <td>❌ CLI only</td>
      <td>Good–Great</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">sftp</code></td>
      <td>Interactive browsing + transfer</td>
      <td>❌ No</td>
      <td>MobaXterm, FileZilla</td>
      <td>Good</td>
    </tr>
    <tr>
      <td>Globus Connect</td>
      <td>Multi-TB, HPC↔HPC, unattended</td>
      <td>✅ Yes</td>
      <td>✅ Web UI + CLI</td>
      <td>Excellent</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>💡 <strong>Quick decision rule:</strong> Small file, one-off → <code class="language-plaintext highlighter-rouge">scp</code>. Large folder or repeat syncs → <code class="language-plaintext highlighter-rouge">rsync</code>. Prefer clicking to typing → <code class="language-plaintext highlighter-rouge">sftp</code> with a GUI. Terabytes, long-distance, or must-not-fail → <strong>Globus</strong>.</p>
</blockquote>

<hr />

<h2 id="method-1-scp--the-default-workhorse">Method 1: scp — The Default Workhorse</h2>

<p><code class="language-plaintext highlighter-rouge">scp</code> (Secure Copy Protocol) works exactly like the Unix <code class="language-plaintext highlighter-rouge">cp</code> command, but over SSH. It’s the fastest way to transfer a file when you don’t need any extra features.</p>

<h3 id="basic-syntax">Basic Syntax</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ── Upload: laptop → HPC ─────────────────────────────────────────</span>

<span class="c"># Single file</span>
scp sample_R1.fastq.gz username@hpc.university.edu:~/data/

<span class="c"># Entire directory (-r flag for recursive)</span>
scp <span class="nt">-r</span> ./raw_reads/ username@hpc.university.edu:~/data/

<span class="c"># Multiple files using wildcard</span>
scp data/<span class="k">*</span>.fastq.gz username@hpc.university.edu:~/data/

<span class="c"># Specify destination filename</span>
scp local_file.txt username@hpc.university.edu:~/data/renamed_file.txt

<span class="c"># ── Download: HPC → laptop ───────────────────────────────────────</span>

<span class="c"># Single file from HPC to current directory</span>
scp username@hpc.university.edu:~/results/summary.csv ./

<span class="c"># Entire results folder</span>
scp <span class="nt">-r</span> username@hpc.university.edu:~/project/results/ ./local_results/
</code></pre></div></div>

<h3 id="useful-scp-flags">Useful scp Flags</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># -r  recursive (directories)</span>
<span class="c"># -p  preserve timestamps and permissions</span>
<span class="c"># -C  compress during transfer (useful over slow connections)</span>
<span class="c"># -v  verbose (shows progress details)</span>
<span class="c"># -P  specify port if not 22</span>

scp <span class="nt">-rCp</span> ./data/ username@hpc.university.edu:~/data/
</code></pre></div></div>

<h3 id="windows-scp-in-powershell">Windows: scp in PowerShell</h3>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># PowerShell — identical syntax to Linux/Mac</span><span class="w">
</span><span class="n">scp</span><span class="w"> </span><span class="nx">C:\Users\YourName\data\sample.fastq.gz</span><span class="w"> </span><span class="nx">username</span><span class="err">@</span><span class="nx">hpc.uni.edu:~/data/</span><span class="w">

</span><span class="c"># Upload a folder</span><span class="w">
</span><span class="n">scp</span><span class="w"> </span><span class="nt">-r</span><span class="w"> </span><span class="nx">C:\Users\YourName\raw_reads\</span><span class="w"> </span><span class="nx">username</span><span class="err">@</span><span class="nx">hpc.uni.edu:~/data/</span><span class="w">

</span><span class="c"># Download results back to Desktop</span><span class="w">
</span><span class="n">scp</span><span class="w"> </span><span class="nx">username</span><span class="err">@</span><span class="nx">hpc.uni.edu:~/results/report.html</span><span class="w"> </span><span class="nx">C:\Users\YourName\Desktop\</span><span class="w">
</span></code></pre></div></div>

<h3 id="when-scp-falls-short">When scp Falls Short</h3>

<p><code class="language-plaintext highlighter-rouge">scp</code> has one critical weakness: <strong>if the transfer fails halfway, it starts from scratch</strong>. For a 5 GB file that’s annoying. For 200 GB — that’s a 6-hour restart. Use <code class="language-plaintext highlighter-rouge">rsync</code> or Globus for anything over ~10 GB.</p>

<hr />

<h2 id="method-2-rsync--the-smart-synchroniser">Method 2: rsync — The Smart Synchroniser</h2>

<p><code class="language-plaintext highlighter-rouge">rsync</code> is the tool you reach for when <code class="language-plaintext highlighter-rouge">scp</code> isn’t enough. Its superpower: it <strong>only transfers what has changed</strong>, and it <strong>resumes interrupted transfers</strong> automatically.</p>

<p>For bioinformatics workflows — where you regularly sync updated scripts back to HPC, or pull incremental results back to your laptop — <code class="language-plaintext highlighter-rouge">rsync</code> becomes indispensable.</p>

<h3 id="core-command">Core Command</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rsync <span class="nt">-avz</span> <span class="nt">--progress</span> <span class="nb">source</span>/ destination/
</code></pre></div></div>

<p>Breaking down the flags:</p>

<table>
  <thead>
    <tr>
      <th>Flag</th>
      <th>What it does</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-a</code></td>
      <td>Archive mode: preserves permissions, timestamps, symlinks. Use this almost always.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-v</code></td>
      <td>Verbose: shows each file being transferred</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">-z</code></td>
      <td>Compress data during transfer (saves bandwidth, helpful over slow links)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--progress</code></td>
      <td>Shows per-file progress bar and speed</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--dry-run</code></td>
      <td>Preview what <em>would</em> be transferred without actually doing it</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--delete</code></td>
      <td>Delete files on destination that no longer exist on source</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">--exclude</code></td>
      <td>Skip files matching a pattern</td>
    </tr>
  </tbody>
</table>

<h3 id="uploading-to-hpc">Uploading to HPC</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Sync your raw data directory to HPC</span>
rsync <span class="nt">-avz</span> <span class="nt">--progress</span> ./raw_reads/ <span class="se">\</span>
    username@hpc.university.edu:~/project/raw_reads/

<span class="c"># Sync your scripts (quick, small files)</span>
rsync <span class="nt">-avz</span> ./scripts/ <span class="se">\</span>
    username@hpc.university.edu:~/project/scripts/

<span class="c"># Exclude large intermediate files you don't need on HPC</span>
rsync <span class="nt">-avz</span> <span class="nt">--progress</span> <span class="se">\</span>
    <span class="nt">--exclude</span> <span class="s2">"*.bam"</span> <span class="se">\</span>
    <span class="nt">--exclude</span> <span class="s2">"*.sam"</span> <span class="se">\</span>
    <span class="nt">--exclude</span> <span class="s2">"__pycache__/"</span> <span class="se">\</span>
    ./project/ <span class="se">\</span>
    username@hpc.university.edu:~/project/
</code></pre></div></div>

<h3 id="downloading-results">Downloading Results</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Pull your results folder back after jobs complete</span>
rsync <span class="nt">-avz</span> <span class="nt">--progress</span> <span class="se">\</span>
    username@hpc.university.edu:~/project/results/ <span class="se">\</span>
    ./local_results/

<span class="c"># Only download specific file types</span>
rsync <span class="nt">-avz</span> <span class="nt">--include</span> <span class="s2">"*.csv"</span> <span class="nt">--include</span> <span class="s2">"*.html"</span> <span class="se">\</span>
    <span class="nt">--exclude</span> <span class="s2">"*"</span> <span class="se">\</span>
    username@hpc.university.edu:~/project/results/ <span class="se">\</span>
    ./local_results/
</code></pre></div></div>

<h3 id="resuming-a-failed-transfer--the-key-feature">Resuming a Failed Transfer — The Key Feature</h3>

<p>This is the most important thing to know about rsync. If a transfer dies, <strong>just run the exact same command again</strong>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># First attempt — fails at 60%</span>
rsync <span class="nt">-avz</span> <span class="nt">--progress</span> ./big_dataset/ username@hpc.university.edu:~/data/
<span class="c"># Connection reset. Transferred 120/200 files.</span>

<span class="c"># Rerun — picks up exactly where it stopped</span>
rsync <span class="nt">-avz</span> <span class="nt">--progress</span> ./big_dataset/ username@hpc.university.edu:~/data/
<span class="c"># Only the remaining 80 files are transferred</span>
<span class="c"># Already-transferred files are checked and skipped</span>
</code></pre></div></div>

<p>rsync compares file sizes and timestamps (or checksums with <code class="language-plaintext highlighter-rouge">--checksum</code>). Files that already exist at the destination and haven’t changed are skipped entirely.</p>

<h3 id="dry-run-preview-before-transferring">Dry Run: Preview Before Transferring</h3>

<p>Always a good habit before a large sync:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rsync <span class="nt">-avz</span> <span class="nt">--dry-run</span> ./data/ username@hpc.university.edu:~/data/
<span class="c"># Shows exactly what would be transferred without sending anything</span>
</code></pre></div></div>

<blockquote>
  <p>⚠️ <strong>The trailing slash matters.</strong> <code class="language-plaintext highlighter-rouge">rsync ./data/</code> syncs the <em>contents</em> of <code class="language-plaintext highlighter-rouge">data/</code>. <code class="language-plaintext highlighter-rouge">rsync ./data</code> (no trailing slash) syncs the <em>folder itself</em>, creating <code class="language-plaintext highlighter-rouge">destination/data/</code> instead. This trips up almost everyone at least once.</p>
</blockquote>

<h3 id="keeping-scripts-in-sync-during-development">Keeping Scripts in Sync During Development</h3>

<p>A common workflow: develop your scripts on your laptop, rsync them to HPC, test, iterate.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create an alias for quick sync</span>
<span class="nb">alias </span>sync-hpc<span class="o">=</span><span class="s1">'rsync -avz ./scripts/ username@hpc.university.edu:~/project/scripts/'</span>

<span class="c"># One command to push your latest script changes</span>
sync-hpc
</code></pre></div></div>

<hr />

<h2 id="method-3-sftp--interactive-file-transfer">Method 3: SFTP — Interactive File Transfer</h2>

<p><code class="language-plaintext highlighter-rouge">sftp</code> (SSH File Transfer Protocol) opens an interactive session where you can browse both your local and remote filesystems before deciding what to transfer. Think of it as a terminal-based file manager.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Connect</span>
sftp username@hpc.university.edu

<span class="c"># Once inside the sftp prompt:</span>
sftp&gt; <span class="nb">ls</span>                          <span class="c"># list remote files</span>
sftp&gt; lls                         <span class="c"># list LOCAL files</span>
sftp&gt; <span class="nb">cd </span>data/                    <span class="c"># change remote directory</span>
sftp&gt; lcd ~/Downloads/            <span class="c"># change LOCAL directory</span>
sftp&gt; <span class="nb">pwd</span>                         <span class="c"># show current remote path</span>
sftp&gt; lpwd                        <span class="c"># show current local path</span>

<span class="c"># Transfers</span>
sftp&gt; put sample_R1.fastq.gz      <span class="c"># upload one file</span>
sftp&gt; get results/output.tsv      <span class="c"># download one file</span>
sftp&gt; mput <span class="k">*</span>.fastq.gz             <span class="c"># upload multiple files</span>
sftp&gt; mget results/<span class="k">*</span>.bam          <span class="c"># download multiple files</span>

sftp&gt; bye                         <span class="c"># exit</span>
</code></pre></div></div>

<h3 id="sftp-with-a-gui">sftp with a GUI</h3>

<p>Most beginners prefer a drag-and-drop interface. Three good options:</p>

<p><strong>MobaXterm (Windows — free):</strong>
Already covered in Day 1. When you SSH in, the left panel automatically shows an SFTP file browser. Drag files from Windows Explorer directly into it.</p>

<p><strong>FileZilla (Windows, Mac, Linux — free):</strong></p>

<ol>
  <li>Open FileZilla → File → Site Manager → New Site</li>
  <li>Protocol: <code class="language-plaintext highlighter-rouge">SFTP - SSH File Transfer Protocol</code></li>
  <li>Host: <code class="language-plaintext highlighter-rouge">hpc.university.edu</code>, Port: <code class="language-plaintext highlighter-rouge">22</code></li>
  <li>Logon Type: <code class="language-plaintext highlighter-rouge">Normal</code> → enter username and password</li>
  <li>Connect → drag and drop between left (local) and right (remote) panels</li>
</ol>

<p><strong>Cyberduck (Mac and Windows — free):</strong></p>

<ol>
  <li>Click Open Connection</li>
  <li>Select SFTP from the dropdown</li>
  <li>Enter your HPC hostname and credentials</li>
  <li>Browse and transfer with a familiar file manager interface</li>
</ol>

<blockquote>
  <p>💡 <strong>Data Transfer Nodes (DTNs):</strong> Many HPC clusters have dedicated <strong>data transfer nodes</strong> — separate machines optimised for file transfer with faster network connections to the outside world. Check your HPC documentation. Using <code class="language-plaintext highlighter-rouge">transfer.hpc.university.edu</code> instead of the login node for large transfers can dramatically improve speed.</p>
</blockquote>

<hr />

<h2 id="method-4-globus-connect--the-professional-standard">Method 4: Globus Connect — The Professional Standard</h2>

<p>For anything involving terabytes of data, transfers between two HPC systems, or data that absolutely must arrive intact and intact, <strong>Globus is the tool researchers rely on</strong>.</p>

<p>Globus is a cloud-based data transfer service used by virtually every major research computing facility in the world — NIH Biowulf, TACC Frontera, UK Research Data Facility, Pawsey, NCI, and hundreds of university clusters. If your institution has an HPC, it almost certainly has a Globus endpoint already configured.</p>

<h3 id="why-globus-is-different-from-everything-else">Why Globus Is Different From Everything Else</h3>

<table>
  <thead>
    <tr>
      <th>Feature</th>
      <th>scp/rsync</th>
      <th>Globus</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Runs on your laptop</td>
      <td>Yes</td>
      <td>No — runs on Globus servers</td>
    </tr>
    <tr>
      <td>Close laptop mid-transfer</td>
      <td>Cancels transfer</td>
      <td>✅ Transfer keeps running</td>
    </tr>
    <tr>
      <td>Multi-TB transfers</td>
      <td>Possible but risky</td>
      <td>✅ Designed for this</td>
    </tr>
    <tr>
      <td>Automatic retry on network failure</td>
      <td>rsync: yes</td>
      <td>✅ Automatic</td>
    </tr>
    <tr>
      <td>MD5 checksum verification</td>
      <td>Manual</td>
      <td>✅ Automatic on every file</td>
    </tr>
    <tr>
      <td>HPC endpoint to HPC endpoint</td>
      <td>Not directly</td>
      <td>✅ First-class feature</td>
    </tr>
    <tr>
      <td>GUI</td>
      <td>No (rsync)</td>
      <td>✅ Web browser</td>
    </tr>
    <tr>
      <td>Free for research</td>
      <td>Yes</td>
      <td>✅ Yes</td>
    </tr>
  </tbody>
</table>

<p>The single biggest advantage: <strong>you don’t need to keep your laptop open</strong>. Globus runs the transfer between the two endpoints (e.g., your university storage and the HPC cluster) through its own servers. You start it, close your laptop, and come back to a completed transfer.</p>

<h3 id="step-1-create-a-globus-account">Step 1: Create a Globus Account</h3>

<p>Go to <a href="https://www.globus.org">globus.org</a> and sign in with your institutional credentials (most universities are already federated). If not, create a free account.</p>

<h3 id="step-2-install-globus-connect-personal-for-your-laptop">Step 2: Install Globus Connect Personal (For Your Laptop)</h3>

<p>If you want to transfer <em>from your laptop</em>, install Globus Connect Personal:</p>

<ol>
  <li>Download from <a href="https://www.globus.org/globus-connect-personal">globus.org/globus-connect-personal</a></li>
  <li>Install and open it — it runs silently in the background</li>
  <li>Log in with your Globus account</li>
  <li>Your laptop is now a Globus endpoint</li>
</ol>

<blockquote>
  <p>If you’re transferring between two HPC systems, both of which already have Globus endpoints configured, you skip this step entirely.</p>
</blockquote>

<h3 id="step-3-find-your-hpc-endpoint">Step 3: Find Your HPC Endpoint</h3>

<p>Your institution’s HPC almost certainly already has a Globus endpoint. Find it:</p>

<ol>
  <li>Go to <a href="https://app.globus.org">app.globus.org</a></li>
  <li>Click <strong>File Manager</strong></li>
  <li>In the Collection search box, type your institution name (e.g., “University of Edinburgh”, “TACC”, “NIH”)</li>
  <li>Select the correct endpoint from the dropdown</li>
</ol>

<h3 id="step-4-transfer-files-via-the-web-interface">Step 4: Transfer Files via the Web Interface</h3>

<ol>
  <li>In the left panel: select your source endpoint (e.g., your laptop or home institution storage)</li>
  <li>Navigate to the folder you want to transfer</li>
  <li>In the right panel: select your HPC endpoint</li>
  <li>Navigate to the destination folder</li>
  <li>Select files/folders and click <strong>Start</strong></li>
</ol>

<p>That’s it. Globus handles everything:</p>

<ul>
  <li>Parallel data streams for maximum throughput</li>
  <li>Automatic retry on network interruption</li>
  <li>MD5 checksum verification on every file after transfer</li>
  <li>Email notification when complete</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Transfer initiated: 2026-03-16 09:14:22
Source:      my-laptop/Users/you/raw_data/
Destination: uni-hpc/scratch/project/raw_data/
Files:       847 files, 312 GB

[==========&gt;          ] 47% · 146 GB of 312 GB
Estimated completion: 2026-03-16 13:22:00

You can close this browser window.
Your transfer will continue running.
</code></pre></div></div>

<h3 id="step-5-globus-cli--scripting-transfers">Step 5: Globus CLI — Scripting Transfers</h3>

<p>For automated or scripted transfers, use the Globus CLI:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Install</span>
pip <span class="nb">install </span>globus-cli

<span class="c"># Authenticate</span>
globus login

<span class="c"># List your endpoints</span>
globus endpoint search <span class="s2">"your institution"</span>

<span class="c"># Transfer a folder</span>
globus transfer <span class="se">\</span>
    &lt;SOURCE_ENDPOINT_UUID&gt;:/path/to/source/ <span class="se">\</span>
    &lt;DEST_ENDPOINT_UUID&gt;:/path/to/destination/ <span class="se">\</span>
    <span class="nt">--recursive</span> <span class="se">\</span>
    <span class="nt">--sync-level</span> checksum <span class="se">\</span>
    <span class="nt">--label</span> <span class="s2">"Raw reads upload 2026-03-16"</span>

<span class="c"># Check transfer status</span>
globus task list
globus task show &lt;TASK_ID&gt;

<span class="c"># Wait for completion (useful in scripts)</span>
globus task <span class="nb">wait</span> &lt;TASK_ID&gt;
<span class="nb">echo</span> <span class="s2">"Transfer complete!"</span>
</code></pre></div></div>

<p>You can embed this in a pipeline script — trigger the Globus transfer, wait for it to complete, then kick off your SLURM jobs automatically.</p>

<h3 id="globus-for-hpc-to-hpc-transfers">Globus for HPC-to-HPC Transfers</h3>

<p>This is where Globus truly has no competition. Moving data from one cluster to another (e.g., from your home institution to a national computing facility) is trivial:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Transfer between two HPC endpoints directly</span>
<span class="c"># Neither needs to be your laptop</span>
globus transfer <span class="se">\</span>
    &lt;HOME_HPC_ENDPOINT&gt;:/scratch/project/results/ <span class="se">\</span>
    &lt;NATIONAL_HPC_ENDPOINT&gt;:/project/incoming/ <span class="se">\</span>
    <span class="nt">--recursive</span> <span class="se">\</span>
    <span class="nt">--label</span> <span class="s2">"Results to national facility"</span>
</code></pre></div></div>

<p>The transfer happens directly between the two clusters at wire speed. Your laptop is completely uninvolved.</p>

<hr />

<h2 id="choosing-the-right-tool-decision-guide">Choosing the Right Tool: Decision Guide</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>How much data?
    ├── &lt; 1 GB → scp  (simplest, no setup)
    └── &gt; 1 GB →
            Do you prefer a GUI?
                ├── YES → SFTP + MobaXterm or FileZilla
                └── NO  →
                        Will you repeat this transfer or sync regularly?
                            ├── YES → rsync  (skips unchanged files)
                            └── NO  →
                                    Is it &gt; 100 GB or HPC-to-HPC?
                                        ├── YES → Globus Connect  ✓
                                        └── NO  → rsync or scp
</code></pre></div></div>

<hr />

<h2 id="practical-patterns-for-bioinformatics">Practical Patterns for Bioinformatics</h2>

<h3 id="pattern-1-upload-raw-reads-download-results">Pattern 1: Upload raw reads, download results</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Upload raw data (do this once)</span>
rsync <span class="nt">-avz</span> <span class="nt">--progress</span> ./raw_reads/ <span class="se">\</span>
    username@hpc.university.edu:~/project/raw_reads/

<span class="c"># Submit jobs on HPC...</span>

<span class="c"># Download results when done</span>
rsync <span class="nt">-avz</span> <span class="nt">--progress</span> <span class="se">\</span>
    username@hpc.university.edu:~/project/results/ <span class="se">\</span>
    ./results/
</code></pre></div></div>

<h3 id="pattern-2-keep-scripts-in-sync-during-development">Pattern 2: Keep scripts in sync during development</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Push latest script version to HPC</span>
rsync <span class="nt">-avz</span> ./scripts/ username@hpc.university.edu:~/project/scripts/

<span class="c"># Pull back updated config or notes</span>
rsync <span class="nt">-avz</span> username@hpc.university.edu:~/project/config.yaml ./
</code></pre></div></div>

<h3 id="pattern-3-large-dataset-upload-with-globus">Pattern 3: Large dataset upload with Globus</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Start Globus transfer from terminal</span>
globus transfer <span class="se">\</span>
    &lt;LAPTOP_ENDPOINT&gt;:/Users/you/sequencing_run_2026/ <span class="se">\</span>
    &lt;HPC_ENDPOINT&gt;:/scratch/project/raw/ <span class="se">\</span>
    <span class="nt">--recursive</span> <span class="nt">--label</span> <span class="s2">"Sequencing run Mar 2026"</span>

<span class="c"># Close laptop. Come back in 3 hours.</span>
globus task list  <span class="c"># check status anytime</span>
</code></pre></div></div>

<h3 id="pattern-4-verify-transfer-integrity">Pattern 4: Verify transfer integrity</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># After rsync, verify checksums</span>
<span class="c"># On HPC:</span>
<span class="nb">md5sum </span>raw_reads/<span class="k">*</span>.fastq.gz <span class="o">&gt;</span> checksums_hpc.md5

<span class="c"># On laptop:</span>
<span class="nb">md5sum </span>raw_reads/<span class="k">*</span>.fastq.gz <span class="o">&gt;</span> checksums_local.md5

<span class="c"># Compare (should produce no output if identical)</span>
diff checksums_local.md5 checksums_hpc.md5
</code></pre></div></div>

<p>Globus does this automatically. For rsync, add <code class="language-plaintext highlighter-rouge">--checksum</code> to force checksum comparison instead of size+timestamp:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rsync <span class="nt">-avz</span> <span class="nt">--checksum</span> ./data/ username@hpc.university.edu:~/data/
</code></pre></div></div>

<hr />

<h2 id="common-mistakes-to-avoid">Common Mistakes to Avoid</h2>

<p><strong>Transferring to the login node’s home directory, which has a small quota</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ /home has limited space (often 50–100 GB)</span>
scp big_dataset/ username@hpc.university.edu:~/data/

<span class="c"># ✅ Use scratch or project storage</span>
scp big_dataset/ username@hpc.university.edu:/scratch/username/data/
</code></pre></div></div>

<p><strong>Forgetting the trailing slash with rsync</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ Creates destination/data/contents (extra nesting)</span>
rsync <span class="nt">-avz</span> ./data username@hpc.university.edu:~/project/

<span class="c"># ✅ Transfers contents directly into ~/project/</span>
rsync <span class="nt">-avz</span> ./data/ username@hpc.university.edu:~/project/data/
</code></pre></div></div>

<p><strong>Using scp for multi-GB transfers over unstable connections</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ One Wi-Fi drop = restart from scratch</span>
scp big_genome.tar.gz username@hpc.university.edu:~/data/

<span class="c"># ✅ rsync resumes where it left off</span>
rsync <span class="nt">-avz</span> <span class="nt">--progress</span> big_genome.tar.gz username@hpc.university.edu:~/data/
</code></pre></div></div>

<p><strong>Transferring through the login node instead of a DTN</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ Slow — login node is not optimised for transfers</span>
scp data.tar.gz username@hpc-login.university.edu:~/

<span class="c"># ✅ If available, use the transfer node</span>
scp data.tar.gz username@hpc-transfer.university.edu:~/scratch/
</code></pre></div></div>

<p><strong>Not compressing before transfer</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># For many small files, tar + gzip before transfer</span>
<span class="nb">tar </span>czf raw_reads.tar.gz raw_reads/
rsync <span class="nt">-avz</span> raw_reads.tar.gz username@hpc.university.edu:~/
<span class="c"># Then extract on HPC: tar xzf raw_reads.tar.gz</span>
</code></pre></div></div>

<hr />

<h2 id="try-it-yourself">Try It Yourself</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. Test scp with a small file</span>
<span class="nb">echo</span> <span class="s2">"Hello HPC"</span> <span class="o">&gt;</span> test_transfer.txt
scp test_transfer.txt username@hpc.university.edu:~/
<span class="c"># Verify it arrived</span>
ssh username@hpc.university.edu <span class="s2">"cat ~/test_transfer.txt"</span>

<span class="c"># 2. Test rsync with a folder</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> test_folder/
<span class="nb">echo</span> <span class="s2">"file 1"</span> <span class="o">&gt;</span> test_folder/a.txt
<span class="nb">echo</span> <span class="s2">"file 2"</span> <span class="o">&gt;</span> test_folder/b.txt
rsync <span class="nt">-avz</span> <span class="nt">--dry-run</span> ./test_folder/ username@hpc.university.edu:~/test_folder/
<span class="c"># If dry run looks right, remove --dry-run and run for real</span>

<span class="c"># 3. Download something back</span>
scp username@hpc.university.edu:~/test_transfer.txt ./downloaded_test.txt
<span class="nb">cat </span>downloaded_test.txt   <span class="c"># should say "Hello HPC"</span>

<span class="c"># 4. Check your storage quota on HPC</span>
ssh username@hpc.university.edu <span class="s2">"df -h </span><span class="nv">$HOME</span><span class="s2"> &amp;&amp; df -h /scratch/</span><span class="nv">$USER</span><span class="s2"> 2&gt;/dev/null"</span>
</code></pre></div></div>

<hr />

<h2 id="series-complete--you-now-have-the-full-stack">Series Complete — You Now Have the Full Stack</h2>

<p>You’ve covered the complete arc from laptop to reproducible, scalable HPC bioinformatics:</p>

<table>
  <thead>
    <tr>
      <th>Day</th>
      <th>Topic</th>
      <th>What You Can Now Do</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>Laptop vs HPC</td>
      <td>Understand the infrastructure, connect from any OS</td>
    </tr>
    <tr>
      <td>2</td>
      <td>Software installation</td>
      <td>Install tools without root, manage reproducible environments</td>
    </tr>
    <tr>
      <td>3</td>
      <td>Running jobs</td>
      <td>Write SLURM scripts, use interactive nodes, read logs</td>
    </tr>
    <tr>
      <td>4</td>
      <td>Scaling</td>
      <td>Turn 50-hour sequential runs into 15-minute parallel arrays</td>
    </tr>
    <tr>
      <td>5</td>
      <td>Pipelines</td>
      <td>Build reproducible Snakemake/Nextflow workflows</td>
    </tr>
    <tr>
      <td>6</td>
      <td>Data transfer</td>
      <td>Move any amount of data, reliably, to and from HPC</td>
    </tr>
  </tbody>
</table>

<p>The tools you use — <code class="language-plaintext highlighter-rouge">fastqc</code>, <code class="language-plaintext highlighter-rouge">bowtie2</code>, <code class="language-plaintext highlighter-rouge">kraken2</code>, <code class="language-plaintext highlighter-rouge">samtools</code> — haven’t changed. What’s changed is the infrastructure around them. And that infrastructure is what separates a researcher who can do 10 samples on their laptop from one who can do 10,000 samples on a national computing facility.</p>

<hr />

<p>Thank you for reading the series — if it was useful, please share it with a colleague who’s making the same journey. Questions? Drop a comment below.*</p>

<hr />

<p><img src="/assets/img/d6_hpc.png" alt="Laptop vs HPC: Scaling Computational Biology" /></p>]]></content><author><name></name></author><category term="HPC" /><category term="data-transfer" /><category term="globus" /><category term="rsync" /><category term="scp" /><category term="sftp" /><category term="bioinformatics" /><category term="beginners" /><category term="genomics" /><summary type="html"><![CDATA[Your pipeline is ready, your HPC account is set up — but how do you actually move 300 GB of raw sequencing reads onto the cluster? A practical guide to every data transfer method you'll use in bioinformatics, from quick one-liners to terabyte-scale transfers.]]></summary></entry><entry><title type="html">Day 5 — Reproducible Pipelines: Snakemake and Nextflow</title><link href="https://jojyjohn28.github.io/blog/day5-pipelines/" rel="alternate" type="text/html" title="Day 5 — Reproducible Pipelines: Snakemake and Nextflow" /><published>2026-03-18T00:00:00-04:00</published><updated>2026-03-18T00:00:00-04:00</updated><id>https://jojyjohn28.github.io/blog/day5-pipelines</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/day5-pipelines/"><![CDATA[<h1 id="day-5-reproducible-pipelines--snakemake-and-nextflow">Day 5: Reproducible Pipelines — Snakemake and Nextflow</h1>

<p><img src="./day5-hero.svg" alt="Reproducible Pipelines: Snakemake and Nextflow" /></p>

<blockquote>
  <p><em>This is Day 5 of a 6-part series: <strong>From Laptop to HPC: Scaling Computational Biology Workflows</strong> and 🧬 Day 61 of Daily Bioinformatics from Jojy’s Desk. In <a href="../day4-job-arrays/">Day 4</a> we scaled to 200 samples in parallel with job arrays. Today: the final evolution — workflow managers that make the whole thing reproducible.</em></p>
</blockquote>

<hr />

<h2 id="the-hidden-problem-with-shell-scripts">The Hidden Problem With Shell Scripts</h2>

<p>You’ve spent four days building a powerful HPC toolkit. You can run single jobs, handle 200 samples in parallel, load modules, manage environments. Your shell scripts work.</p>

<p>Then this happens:</p>

<blockquote>
  <p><strong>Six months later.</strong> A reviewer asks you to rerun the analysis with a slight parameter change. You open your scripts. You don’t remember which version of samtools you used. Your conda environment was updated since then. Two intermediate files are missing because a job silently failed and you didn’t notice. You rerun everything from scratch, but the results are slightly different. You don’t know why.</p>
</blockquote>

<p>Or this:</p>

<blockquote>
  <p><strong>A collaborator wants to run your pipeline on their cluster.</strong> You send them your SLURM scripts. They spend three days troubleshooting different module names, missing paths, and job submission order before giving up.</p>
</blockquote>

<p>Shell scripts are not pipelines. They’re instructions. The difference matters.</p>

<p>A <strong>workflow manager</strong> solves this by:</p>

<ul>
  <li>Tracking which outputs depend on which inputs (the <strong>dependency graph</strong>)</li>
  <li>Only re-running steps whose inputs have changed (incremental re-runs)</li>
  <li>Automatically restarting from the point of failure</li>
  <li>Explicitly documenting every tool version and parameter</li>
  <li>Running identically on laptop, HPC, and cloud — with one config change</li>
</ul>

<hr />

<h2 id="what-youll-learn-today">What You’ll Learn Today</h2>

<ul>
  <li>Why dependency tracking matters and what a DAG is</li>
  <li>How to write a <strong>Snakemake</strong> pipeline from scratch</li>
  <li>How to write a <strong>Nextflow</strong> pipeline from scratch</li>
  <li>How to run both on HPC with SLURM integration</li>
  <li>Which tool to choose for your project</li>
  <li>A GitHub repository structure that makes your pipelines shareable</li>
  <li>Publishing checklist for reproducible bioinformatics</li>
</ul>

<hr />

<h2 id="part-1-the-dependency-graph-dag">Part 1: The Dependency Graph (DAG)</h2>

<p>Before writing code, let’s understand what workflow managers actually do.</p>

<p>In a bioinformatics pipeline, outputs of one step become inputs of the next:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>raw_reads.fastq.gz
    │
    ▼
[FastQC] → quality_report.html
    │
    ▼
[Trimmomatic] → trimmed_reads.fastq.gz
    │
    ▼
[Bowtie2] → aligned.sam
    │
    ▼
[samtools sort] → aligned.bam
    │
    ▼
[samtools index] → aligned.bam.bai
    │
    ▼
[featureCounts] → counts.txt
</code></pre></div></div>

<p>This is a <strong>Directed Acyclic Graph (DAG)</strong> — a chain of steps where each depends on the previous.</p>

<p>With shell scripts, you manage this manually. With workflow managers, you declare what inputs produce what outputs, and the tool figures out the order automatically. More importantly, if <code class="language-plaintext highlighter-rouge">aligned.bam</code> already exists and <code class="language-plaintext highlighter-rouge">trimmed_reads.fastq.gz</code> hasn’t changed, the workflow skips the alignment step entirely.</p>

<hr />

<h2 id="part-2-snakemake--python-native-workflow-manager">Part 2: Snakemake — Python-Native Workflow Manager</h2>

<p>Snakemake is the most widely used workflow manager in academic bioinformatics. If you know Python, the syntax will feel natural immediately.</p>

<h3 id="core-concept-rules">Core Concept: Rules</h3>

<p>In Snakemake, you write <strong>rules</strong>. Each rule says: “given these inputs, run this command to produce these outputs.”</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># The most basic possible Snakemake rule
</span><span class="n">rule</span> <span class="n">fastqc</span><span class="p">:</span>
    <span class="nb">input</span><span class="p">:</span>  <span class="sh">"</span><span class="s">data/{sample}.fastq.gz</span><span class="sh">"</span>
    <span class="n">output</span><span class="p">:</span> <span class="sh">"</span><span class="s">qc/{sample}_fastqc.html</span><span class="sh">"</span>
    <span class="n">shell</span><span class="p">:</span>  <span class="sh">"</span><span class="s">fastqc {input} -o qc/</span><span class="sh">"</span>
</code></pre></div></div>

<p>That’s it. Snakemake figures out:</p>

<ul>
  <li>Which samples need processing (by matching the wildcard <code class="language-plaintext highlighter-rouge">{sample}</code>)</li>
  <li>Whether the output already exists (skip if yes)</li>
  <li>What order to run rules in (based on dependency chains)</li>
</ul>

<h3 id="a-complete-rna-seq-snakemake-pipeline">A Complete RNA-seq Snakemake Pipeline</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Snakefile
# ─────────────────────────────────────────────
# Complete RNA-seq pipeline: FastQC → Trim → STAR → featureCounts
# ─────────────────────────────────────────────
</span>
<span class="n">configfile</span><span class="p">:</span> <span class="sh">"</span><span class="s">config.yaml</span><span class="sh">"</span>

<span class="n">SAMPLES</span> <span class="o">=</span> <span class="n">config</span><span class="p">[</span><span class="sh">"</span><span class="s">samples</span><span class="sh">"</span><span class="p">]</span>
<span class="n">REF</span>     <span class="o">=</span> <span class="n">config</span><span class="p">[</span><span class="sh">"</span><span class="s">genome</span><span class="sh">"</span><span class="p">]</span>
<span class="n">GTF</span>     <span class="o">=</span> <span class="n">config</span><span class="p">[</span><span class="sh">"</span><span class="s">gtf</span><span class="sh">"</span><span class="p">]</span>

<span class="c1"># ── Rule all: defines the final outputs ──────
</span><span class="n">rule</span> <span class="nb">all</span><span class="p">:</span>
    <span class="nb">input</span><span class="p">:</span>
        <span class="nf">expand</span><span class="p">(</span><span class="sh">"</span><span class="s">qc/{s}_fastqc.html</span><span class="sh">"</span><span class="p">,</span>     <span class="n">sample</span><span class="o">=</span><span class="n">SAMPLES</span><span class="p">),</span>
        <span class="nf">expand</span><span class="p">(</span><span class="sh">"</span><span class="s">bam/{s}.sorted.bam</span><span class="sh">"</span><span class="p">,</span>     <span class="n">sample</span><span class="o">=</span><span class="n">SAMPLES</span><span class="p">),</span>
        <span class="nf">expand</span><span class="p">(</span><span class="sh">"</span><span class="s">bam/{s}.sorted.bam.bai</span><span class="sh">"</span><span class="p">,</span> <span class="n">sample</span><span class="o">=</span><span class="n">SAMPLES</span><span class="p">),</span>
        <span class="sh">"</span><span class="s">counts/all_samples_counts.txt</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">qc/multiqc_report.html</span><span class="sh">"</span>

<span class="c1"># ── Step 1: Quality control ──────────────────
</span><span class="n">rule</span> <span class="n">fastqc</span><span class="p">:</span>
    <span class="nb">input</span><span class="p">:</span>  <span class="sh">"</span><span class="s">data/{sample}.fastq.gz</span><span class="sh">"</span>
    <span class="n">output</span><span class="p">:</span>
        <span class="n">html</span> <span class="o">=</span> <span class="sh">"</span><span class="s">qc/{sample}_fastqc.html</span><span class="sh">"</span><span class="p">,</span>
        <span class="nb">zip</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">qc/{sample}_fastqc.zip</span><span class="sh">"</span>
    <span class="n">threads</span><span class="p">:</span> <span class="mi">4</span>
    <span class="n">log</span><span class="p">:</span>    <span class="sh">"</span><span class="s">logs/fastqc/{sample}.log</span><span class="sh">"</span>
    <span class="n">shell</span><span class="p">:</span>
        <span class="sh">"</span><span class="s">fastqc {input} -t {threads} -o qc/ &amp;&gt; {log}</span><span class="sh">"</span>

<span class="c1"># ── Step 2: Adapter trimming ─────────────────
</span><span class="n">rule</span> <span class="n">trim</span><span class="p">:</span>
    <span class="nb">input</span><span class="p">:</span>  <span class="sh">"</span><span class="s">data/{sample}.fastq.gz</span><span class="sh">"</span>
    <span class="n">output</span><span class="p">:</span> <span class="sh">"</span><span class="s">trimmed/{sample}.trimmed.fastq.gz</span><span class="sh">"</span>
    <span class="n">threads</span><span class="p">:</span> <span class="mi">4</span>
    <span class="n">log</span><span class="p">:</span>    <span class="sh">"</span><span class="s">logs/trim/{sample}.log</span><span class="sh">"</span>
    <span class="n">params</span><span class="p">:</span>
        <span class="n">adapters</span> <span class="o">=</span> <span class="n">config</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">adapters</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">TruSeq3-SE.fa</span><span class="sh">"</span><span class="p">),</span>
        <span class="n">minlen</span>   <span class="o">=</span> <span class="n">config</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">min_length</span><span class="sh">"</span><span class="p">,</span> <span class="mi">36</span><span class="p">)</span>
    <span class="n">shell</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">
        trimmomatic SE </span><span class="se">\
</span><span class="s">            -threads {threads} </span><span class="se">\
</span><span class="s">            {input} {output} </span><span class="se">\
</span><span class="s">            ILLUMINACLIP:{params.adapters}:2:30:10 </span><span class="se">\
</span><span class="s">            MINLEN:{params.minlen} </span><span class="se">\
</span><span class="s">            &amp;&gt; {log}
        </span><span class="sh">"""</span>

<span class="c1"># ── Step 3: Alignment ────────────────────────
</span><span class="n">rule</span> <span class="n">star_align</span><span class="p">:</span>
    <span class="nb">input</span><span class="p">:</span>
        <span class="n">reads</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">trimmed/{sample}.trimmed.fastq.gz</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">index</span>  <span class="o">=</span> <span class="n">REF</span>
    <span class="n">output</span><span class="p">:</span>
        <span class="n">bam</span> <span class="o">=</span> <span class="nf">temp</span><span class="p">(</span><span class="sh">"</span><span class="s">star/{sample}/Aligned.sortedByCoord.out.bam</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">threads</span><span class="p">:</span> <span class="mi">8</span>
    <span class="n">log</span><span class="p">:</span>    <span class="sh">"</span><span class="s">logs/star/{sample}.log</span><span class="sh">"</span>
    <span class="n">params</span><span class="p">:</span>
        <span class="n">outdir</span> <span class="o">=</span> <span class="sh">"</span><span class="s">star/{sample}/</span><span class="sh">"</span>
    <span class="n">shell</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">
        STAR </span><span class="se">\
</span><span class="s">            --runThreadN {threads} </span><span class="se">\
</span><span class="s">            --genomeDir {input.index} </span><span class="se">\
</span><span class="s">            --readFilesIn {input.reads} </span><span class="se">\
</span><span class="s">            --readFilesCommand zcat </span><span class="se">\
</span><span class="s">            --outSAMtype BAM SortedByCoordinate </span><span class="se">\
</span><span class="s">            --outFileNamePrefix {params.outdir} </span><span class="se">\
</span><span class="s">            &amp;&gt; {log}
        </span><span class="sh">"""</span>

<span class="c1"># ── Step 4: Sort and index ───────────────────
</span><span class="n">rule</span> <span class="n">sort_index</span><span class="p">:</span>
    <span class="nb">input</span><span class="p">:</span>  <span class="sh">"</span><span class="s">star/{sample}/Aligned.sortedByCoord.out.bam</span><span class="sh">"</span>
    <span class="n">output</span><span class="p">:</span>
        <span class="n">bam</span> <span class="o">=</span> <span class="sh">"</span><span class="s">bam/{sample}.sorted.bam</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">bai</span> <span class="o">=</span> <span class="sh">"</span><span class="s">bam/{sample}.sorted.bam.bai</span><span class="sh">"</span>
    <span class="n">threads</span><span class="p">:</span> <span class="mi">4</span>
    <span class="n">shell</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">
        samtools sort -@ {threads} {input} -o {output.bam}
        samtools index {output.bam}
        </span><span class="sh">"""</span>

<span class="c1"># ── Step 5: Feature counting ─────────────────
</span><span class="n">rule</span> <span class="n">featurecounts</span><span class="p">:</span>
    <span class="nb">input</span><span class="p">:</span>
        <span class="n">bams</span> <span class="o">=</span> <span class="nf">expand</span><span class="p">(</span><span class="sh">"</span><span class="s">bam/{s}.sorted.bam</span><span class="sh">"</span><span class="p">,</span> <span class="n">s</span><span class="o">=</span><span class="n">SAMPLES</span><span class="p">),</span>
        <span class="n">gtf</span>  <span class="o">=</span> <span class="n">GTF</span>
    <span class="n">output</span><span class="p">:</span> <span class="sh">"</span><span class="s">counts/all_samples_counts.txt</span><span class="sh">"</span>
    <span class="n">threads</span><span class="p">:</span> <span class="mi">4</span>
    <span class="n">log</span><span class="p">:</span>    <span class="sh">"</span><span class="s">logs/featurecounts.log</span><span class="sh">"</span>
    <span class="n">shell</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">
        featureCounts </span><span class="se">\
</span><span class="s">            -T {threads} </span><span class="se">\
</span><span class="s">            -a {input.gtf} </span><span class="se">\
</span><span class="s">            -o {output} </span><span class="se">\
</span><span class="s">            {input.bams} </span><span class="se">\
</span><span class="s">            &amp;&gt; {log}
        </span><span class="sh">"""</span>

<span class="c1"># ── Step 6: MultiQC summary ──────────────────
</span><span class="n">rule</span> <span class="n">multiqc</span><span class="p">:</span>
    <span class="nb">input</span><span class="p">:</span>
        <span class="nf">expand</span><span class="p">(</span><span class="sh">"</span><span class="s">qc/{s}_fastqc.html</span><span class="sh">"</span><span class="p">,</span> <span class="n">s</span><span class="o">=</span><span class="n">SAMPLES</span><span class="p">),</span>
        <span class="nf">expand</span><span class="p">(</span><span class="sh">"</span><span class="s">logs/star/{s}.log</span><span class="sh">"</span><span class="p">,</span>  <span class="n">s</span><span class="o">=</span><span class="n">SAMPLES</span><span class="p">),</span>
        <span class="sh">"</span><span class="s">logs/featurecounts.log</span><span class="sh">"</span>
    <span class="n">output</span><span class="p">:</span> <span class="sh">"</span><span class="s">qc/multiqc_report.html</span><span class="sh">"</span>
    <span class="n">shell</span><span class="p">:</span>  <span class="sh">"</span><span class="s">multiqc qc/ logs/ -o qc/</span><span class="sh">"</span>
</code></pre></div></div>

<p>The config file that drives it:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># config.yaml</span>
<span class="na">samples</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">ctrl_01</span>
  <span class="pi">-</span> <span class="s">ctrl_02</span>
  <span class="pi">-</span> <span class="s">ctrl_03</span>
  <span class="pi">-</span> <span class="s">treat_01</span>
  <span class="pi">-</span> <span class="s">treat_02</span>
  <span class="pi">-</span> <span class="s">treat_03</span>

<span class="na">genome</span><span class="pi">:</span> <span class="s2">"</span><span class="s">/scratch/references/hg38/star_index"</span>
<span class="na">gtf</span><span class="pi">:</span> <span class="s2">"</span><span class="s">/scratch/references/hg38/gencode.v44.gtf"</span>
<span class="na">adapters</span><span class="pi">:</span> <span class="s2">"</span><span class="s">TruSeq3-SE.fa"</span>
<span class="na">min_length</span><span class="pi">:</span> <span class="m">36</span>
</code></pre></div></div>

<h3 id="running-snakemake">Running Snakemake</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Install</span>
conda <span class="nb">install</span> <span class="nt">-c</span> bioconda <span class="nt">-c</span> conda-forge snakemake

<span class="c"># Dry run first — see what WOULD be executed without running anything</span>
snakemake <span class="nt">--dry-run</span>

<span class="c"># Run on your laptop (8 cores)</span>
snakemake <span class="nt">--cores</span> 8

<span class="c"># Run on HPC via SLURM (Snakemake submits each rule as a SLURM job)</span>
snakemake <span class="nt">--profile</span> slurm <span class="nt">--jobs</span> 50

<span class="c"># Visualise the DAG (requires graphviz)</span>
snakemake <span class="nt">--dag</span> | dot <span class="nt">-Tpng</span> <span class="o">&gt;</span> dag.png

<span class="c"># Force rerun everything from scratch</span>
snakemake <span class="nt">--cores</span> 8 <span class="nt">--forceall</span>

<span class="c"># Rerun only rules that depend on a changed file</span>
snakemake <span class="nt">--cores</span> 8 <span class="nt">--rerun-incomplete</span>
</code></pre></div></div>

<h3 id="running-on-hpc-with-a-slurm-profile">Running on HPC with a SLURM Profile</h3>

<p>Create a SLURM profile directory:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> ~/.config/snakemake/slurm/

<span class="nb">cat</span> <span class="o">&gt;</span> ~/.config/snakemake/slurm/config.yaml <span class="o">&lt;&lt;</span> <span class="sh">'</span><span class="no">EOF</span><span class="sh">'
executor: slurm
jobs: 100
latency-wait: 60
default-resources:
  slurm_partition: normal
  mem_mb: 8192
  runtime: 60
  cpus_per_task: 4
</span><span class="no">EOF
</span></code></pre></div></div>

<p>Now <code class="language-plaintext highlighter-rouge">snakemake --profile slurm --jobs 50</code> automatically submits each rule as its own SLURM job, with the <code class="language-plaintext highlighter-rouge">threads:</code> value used as <code class="language-plaintext highlighter-rouge">--cpus-per-task</code>.</p>

<hr />

<h2 id="part-3-nextflow--cloud-native-and-scalable">Part 3: Nextflow — Cloud-Native and Scalable</h2>

<p>Nextflow uses a different philosophy: instead of rules, it defines <strong>processes</strong> that communicate through <strong>channels</strong>. It’s more verbose than Snakemake but scales better to very large pipelines and is the preferred choice in industry and large consortia (nf-core, Seqera).</p>

<h3 id="core-concept-processes-and-channels">Core Concept: Processes and Channels</h3>

<div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// The most basic Nextflow process</span>
<span class="n">process</span> <span class="n">FASTQC</span> <span class="o">{</span>
    <span class="nl">input:</span>  <span class="n">path</span> <span class="n">reads</span>
    <span class="nl">output:</span> <span class="n">path</span> <span class="s2">"*.html"</span>
    <span class="nl">script:</span>
    <span class="s2">"""
    fastqc $reads
    """</span>
<span class="o">}</span>
</code></pre></div></div>

<p>A channel is a stream of data that flows into processes:</p>

<div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">workflow</span> <span class="o">{</span>
    <span class="c1">// Create a channel from all FASTQ files</span>
    <span class="n">reads_ch</span> <span class="o">=</span> <span class="n">Channel</span><span class="o">.</span><span class="na">fromPath</span><span class="o">(</span><span class="s2">"data/*.fastq.gz"</span><span class="o">)</span>

    <span class="c1">// Pass the channel to the process</span>
    <span class="n">FASTQC</span><span class="o">(</span><span class="n">reads_ch</span><span class="o">)</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="a-complete-rna-seq-nextflow-pipeline">A Complete RNA-seq Nextflow Pipeline</h3>

<div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// main.nf — Nextflow DSL2 RNA-seq pipeline</span>
<span class="c1">// ─────────────────────────────────────────</span>

<span class="n">nextflow</span><span class="o">.</span><span class="na">enable</span><span class="o">.</span><span class="na">dsl</span><span class="o">=</span><span class="mi">2</span>

<span class="c1">// ── Parameters ───────────────────────────</span>
<span class="n">params</span><span class="o">.</span><span class="na">reads</span>    <span class="o">=</span> <span class="s2">"data/*_{R1,R2}.fastq.gz"</span>
<span class="n">params</span><span class="o">.</span><span class="na">genome</span>   <span class="o">=</span> <span class="s2">"/scratch/references/hg38/star_index"</span>
<span class="n">params</span><span class="o">.</span><span class="na">gtf</span>      <span class="o">=</span> <span class="s2">"/scratch/references/hg38/gencode.v44.gtf"</span>
<span class="n">params</span><span class="o">.</span><span class="na">outdir</span>   <span class="o">=</span> <span class="s2">"results"</span>

<span class="c1">// ── Process: FastQC ───────────────────────</span>
<span class="n">process</span> <span class="n">FASTQC</span> <span class="o">{</span>
    <span class="n">tag</span> <span class="s2">"$sample_id"</span>
    <span class="n">publishDir</span> <span class="s2">"${params.outdir}/qc"</span><span class="o">,</span> <span class="nl">mode:</span> <span class="s1">'copy'</span>

    <span class="nl">input:</span>
    <span class="n">tuple</span> <span class="nf">val</span><span class="o">(</span><span class="n">sample_id</span><span class="o">),</span> <span class="n">path</span><span class="o">(</span><span class="n">reads</span><span class="o">)</span>

    <span class="nl">output:</span>
    <span class="n">tuple</span> <span class="nf">val</span><span class="o">(</span><span class="n">sample_id</span><span class="o">),</span> <span class="n">path</span><span class="o">(</span><span class="s2">"*.html"</span><span class="o">),</span> <span class="n">path</span><span class="o">(</span><span class="s2">"*.zip"</span><span class="o">)</span>

    <span class="nl">script:</span>
    <span class="s2">"""
    fastqc $reads -t $task.cpus
    """</span>
<span class="o">}</span>

<span class="c1">// ── Process: Trim ─────────────────────────</span>
<span class="n">process</span> <span class="n">TRIM</span> <span class="o">{</span>
    <span class="n">tag</span> <span class="s2">"$sample_id"</span>
    <span class="n">publishDir</span> <span class="s2">"${params.outdir}/trimmed"</span><span class="o">,</span> <span class="nl">mode:</span> <span class="s1">'copy'</span>

    <span class="nl">input:</span>
    <span class="n">tuple</span> <span class="nf">val</span><span class="o">(</span><span class="n">sample_id</span><span class="o">),</span> <span class="n">path</span><span class="o">(</span><span class="n">reads</span><span class="o">)</span>

    <span class="nl">output:</span>
    <span class="n">tuple</span> <span class="nf">val</span><span class="o">(</span><span class="n">sample_id</span><span class="o">),</span> <span class="n">path</span><span class="o">(</span><span class="s2">"${sample_id}.trimmed_{R1,R2}.fastq.gz"</span><span class="o">)</span>

    <span class="nl">script:</span>
    <span class="s2">"""
    trimmomatic PE \
        -threads $task.cpus \
        ${reads[0]} ${reads[1]} \
        ${sample_id}.trimmed_R1.fastq.gz /dev/null \
        ${sample_id}.trimmed_R2.fastq.gz /dev/null \
        ILLUMINACLIP:TruSeq3-PE.fa:2:30:10 MINLEN:36
    """</span>
<span class="o">}</span>

<span class="c1">// ── Process: STAR alignment ───────────────</span>
<span class="n">process</span> <span class="n">STAR</span> <span class="o">{</span>
    <span class="n">tag</span> <span class="s2">"$sample_id"</span>
    <span class="n">publishDir</span> <span class="s2">"${params.outdir}/bam"</span><span class="o">,</span> <span class="nl">mode:</span> <span class="s1">'copy'</span>
    <span class="n">cpus</span> <span class="mi">8</span>
    <span class="n">memory</span> <span class="s1">'32 GB'</span>

    <span class="nl">input:</span>
    <span class="n">tuple</span> <span class="nf">val</span><span class="o">(</span><span class="n">sample_id</span><span class="o">),</span> <span class="n">path</span><span class="o">(</span><span class="n">reads</span><span class="o">)</span>

    <span class="nl">output:</span>
    <span class="n">tuple</span> <span class="nf">val</span><span class="o">(</span><span class="n">sample_id</span><span class="o">),</span> <span class="n">path</span><span class="o">(</span><span class="s2">"${sample_id}.sorted.bam"</span><span class="o">),</span>
                          <span class="n">path</span><span class="o">(</span><span class="s2">"${sample_id}.sorted.bam.bai"</span><span class="o">)</span>

    <span class="nl">script:</span>
    <span class="s2">"""
    STAR \
        --runThreadN $task.cpus \
        --genomeDir ${params.genome} \
        --readFilesIn ${reads[0]} ${reads[1]} \
        --readFilesCommand zcat \
        --outSAMtype BAM SortedByCoordinate \
        --outFileNamePrefix ${sample_id}_

    samtools sort -@ $task.cpus ${sample_id}_Aligned.sortedByCoord.out.bam \
        -o ${sample_id}.sorted.bam
    samtools index ${sample_id}.sorted.bam
    """</span>
<span class="o">}</span>

<span class="c1">// ── Process: featureCounts ────────────────</span>
<span class="n">process</span> <span class="n">FEATURECOUNTS</span> <span class="o">{</span>
    <span class="n">publishDir</span> <span class="s2">"${params.outdir}/counts"</span><span class="o">,</span> <span class="nl">mode:</span> <span class="s1">'copy'</span>

    <span class="nl">input:</span>
    <span class="n">path</span> <span class="n">bams</span>

    <span class="nl">output:</span>
    <span class="n">path</span> <span class="s2">"all_counts.txt"</span>

    <span class="nl">script:</span>
    <span class="s2">"""
    featureCounts \
        -T $task.cpus \
        -a ${params.gtf} \
        -o all_counts.txt \
        $bams
    """</span>
<span class="o">}</span>

<span class="c1">// ── Workflow definition ───────────────────</span>
<span class="n">workflow</span> <span class="o">{</span>
    <span class="c1">// Create channel of paired-end reads</span>
    <span class="n">reads_ch</span> <span class="o">=</span> <span class="n">Channel</span>
        <span class="o">.</span><span class="na">fromFilePairs</span><span class="o">(</span><span class="n">params</span><span class="o">.</span><span class="na">reads</span><span class="o">)</span>
        <span class="o">.</span><span class="na">ifEmpty</span> <span class="o">{</span> <span class="n">error</span> <span class="s2">"No reads found: ${params.reads}"</span> <span class="o">}</span>

    <span class="c1">// Run pipeline</span>
    <span class="n">fastqc_out</span>  <span class="o">=</span> <span class="n">FASTQC</span><span class="o">(</span><span class="n">reads_ch</span><span class="o">)</span>
    <span class="n">trimmed</span>     <span class="o">=</span> <span class="n">TRIM</span><span class="o">(</span><span class="n">reads_ch</span><span class="o">)</span>
    <span class="n">aligned</span>     <span class="o">=</span> <span class="n">STAR</span><span class="o">(</span><span class="n">trimmed</span><span class="o">)</span>

    <span class="c1">// Collect all BAMs for featureCounts</span>
    <span class="n">bam_files</span>   <span class="o">=</span> <span class="n">aligned</span><span class="o">.</span><span class="na">map</span> <span class="o">{</span> <span class="n">id</span><span class="o">,</span> <span class="n">bam</span><span class="o">,</span> <span class="n">bai</span> <span class="o">-&gt;</span> <span class="n">bam</span> <span class="o">}.</span><span class="na">collect</span><span class="o">()</span>
    <span class="n">FEATURECOUNTS</span><span class="o">(</span><span class="n">bam_files</span><span class="o">)</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="running-nextflow">Running Nextflow</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Install</span>
conda <span class="nb">install</span> <span class="nt">-c</span> bioconda nextflow

<span class="c"># Test run (no data needed)</span>
nextflow run hello

<span class="c"># Run your pipeline locally</span>
nextflow run main.nf

<span class="c"># Run on HPC with SLURM</span>
nextflow run main.nf <span class="nt">-profile</span> slurm

<span class="c"># Resume from last successful step after a failure</span>
nextflow run main.nf <span class="nt">-resume</span>

<span class="c"># Run with custom parameters</span>
nextflow run main.nf <span class="se">\</span>
    <span class="nt">--reads</span> <span class="s2">"data/*_{R1,R2}.fastq.gz"</span> <span class="se">\</span>
    <span class="nt">--genome</span> /scratch/refs/hg38 <span class="se">\</span>
    <span class="nt">--outdir</span> my_results
</code></pre></div></div>

<p>Create a SLURM execution profile in <code class="language-plaintext highlighter-rouge">nextflow.config</code>:</p>

<div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// nextflow.config</span>
<span class="n">profiles</span> <span class="o">{</span>
    <span class="n">slurm</span> <span class="o">{</span>
        <span class="n">process</span><span class="o">.</span><span class="na">executor</span>       <span class="o">=</span> <span class="s1">'slurm'</span>
        <span class="n">process</span><span class="o">.</span><span class="na">queue</span>          <span class="o">=</span> <span class="s1">'normal'</span>
        <span class="n">process</span><span class="o">.</span><span class="na">clusterOptions</span> <span class="o">=</span> <span class="s1">'--account=myproject'</span>

        <span class="n">process</span> <span class="o">{</span>
            <span class="nl">withName:</span> <span class="n">STAR</span> <span class="o">{</span>
                <span class="n">cpus</span>   <span class="o">=</span> <span class="mi">8</span>
                <span class="n">memory</span> <span class="o">=</span> <span class="s1">'32 GB'</span>
                <span class="n">time</span>   <span class="o">=</span> <span class="s1">'4h'</span>
            <span class="o">}</span>
            <span class="nl">withName:</span> <span class="n">FASTQC</span> <span class="o">{</span>
                <span class="n">cpus</span>   <span class="o">=</span> <span class="mi">4</span>
                <span class="n">memory</span> <span class="o">=</span> <span class="s1">'8 GB'</span>
                <span class="n">time</span>   <span class="o">=</span> <span class="s1">'1h'</span>
            <span class="o">}</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<hr />

<h2 id="part-4-snakemake-vs-nextflow--choosing-your-tool">Part 4: Snakemake vs Nextflow — Choosing Your Tool</h2>

<table>
  <thead>
    <tr>
      <th>Feature</th>
      <th>Snakemake</th>
      <th>Nextflow</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Language</td>
      <td>Python</td>
      <td>Groovy / DSL2</td>
    </tr>
    <tr>
      <td>Learning curve</td>
      <td>Lower (if you know Python)</td>
      <td>Higher</td>
    </tr>
    <tr>
      <td>Syntax style</td>
      <td>Rule-based (input → output)</td>
      <td>Process + channel streams</td>
    </tr>
    <tr>
      <td>HPC support</td>
      <td>Excellent (profiles)</td>
      <td>Excellent (executors)</td>
    </tr>
    <tr>
      <td>Cloud support</td>
      <td>Good</td>
      <td>Excellent (AWS, GCP, Azure)</td>
    </tr>
    <tr>
      <td>Resuming failed jobs</td>
      <td><code class="language-plaintext highlighter-rouge">--rerun-incomplete</code></td>
      <td><code class="language-plaintext highlighter-rouge">-resume</code> (automatic)</td>
    </tr>
    <tr>
      <td>Community pipelines</td>
      <td>Many</td>
      <td>nf-core (industry standard)</td>
    </tr>
    <tr>
      <td>Best for</td>
      <td>Academic labs, Python users</td>
      <td>Large consortia, industry, cloud</td>
    </tr>
    <tr>
      <td>Debugging</td>
      <td>Generally easier</td>
      <td>Steeper learning curve</td>
    </tr>
  </tbody>
</table>

<p><strong>My recommendation:</strong></p>

<ul>
  <li><strong>New to workflows?</strong> → Start with Snakemake. The Python syntax is readable, the documentation is excellent, and it will handle everything in a typical academic bioinformatics project.</li>
  <li><strong>Large collaborative project / cloud / sharing with the community?</strong> → Nextflow + nf-core. The nf-core library has production-ready pipelines for RNA-seq, metagenomics, variant calling, and 100+ other workflows that you can run with a single command.</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Use a production nf-core pipeline (no writing code required)</span>
nextflow run nf-core/rnaseq <span class="se">\</span>
    <span class="nt">--input</span> samplesheet.csv <span class="se">\</span>
    <span class="nt">--genome</span> GRCh38 <span class="se">\</span>
    <span class="nt">--outdir</span> results <span class="se">\</span>
    <span class="nt">-profile</span> singularity,slurm
</code></pre></div></div>

<hr />

<h2 id="part-5-github-repository-structure">Part 5: GitHub Repository Structure</h2>

<p>A reproducible pipeline is only useful if someone else can run it. Here’s the repository structure that makes that possible:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>my-rnaseq-pipeline/
├── README.md               ← setup + usage instructions
├── config.yaml             ← parameters (samples, paths, tool settings)
├── environment.yml         ← exact conda environment (from Day 2)
├── Snakefile               ← pipeline definition
│
├── data/                   ← symlinks to raw data (never commit raw data)
│   └── .gitkeep
│
├── resources/
│   ├── TruSeq3-SE.fa       ← adapter sequences
│   └── genome_paths.yaml   ← cluster-specific reference paths
│
├── logs/                   ← SLURM job logs (gitignored)
│   └── .gitignore
│
├── results/                ← pipeline outputs (gitignored)
│   └── .gitignore
│
└── scripts/
    ├── setup_env.sh        ← create conda env + download references
    └── run_pipeline.sh     ← one-command launch script
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">run_pipeline.sh</code> wrapper makes it trivial to reproduce:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c"># run_pipeline.sh — one command to reproduce the analysis</span>

<span class="nb">set</span> <span class="nt">-euo</span> pipefail

conda activate rnaseq-pipeline
snakemake <span class="nt">--profile</span> slurm <span class="nt">--jobs</span> 100 <span class="nt">--rerun-incomplete</span>
</code></pre></div></div>

<p>Your README should answer four questions in under 60 seconds:</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gu">## Quick Start</span>

<span class="gs">**1. Clone and set up environment**</span>
git clone https://github.com/you/my-rnaseq-pipeline
conda env create -f environment.yml
conda activate rnaseq-pipeline

<span class="gs">**2. Edit config.yaml**</span>
<span class="p">
-</span> Add your sample names
<span class="p">-</span> Update genome/GTF paths for your cluster

<span class="gs">**3. Run**</span>
bash scripts/run_pipeline.sh

<span class="gs">**Expected output:**</span> results/counts/all_samples_counts.txt
<span class="gs">**Runtime:**</span> ~2 hours on 6 samples with SLURM
</code></pre></div></div>

<hr />

<h2 id="part-6-reproducibility-checklist">Part 6: Reproducibility Checklist</h2>

<p>Before you share your pipeline or submit a paper:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>□ All software versions pinned in environment.yml
□ All parameters in config.yaml (not hardcoded in scripts)
□ All paths are relative or configurable (no /home/yourname/ hardcoded)
□ README answers: install, configure, run, expected output
□ Snakefile/main.nf is committed to Git
□ At least one test dataset included or linked
□ Results directory is in .gitignore
□ Each rule/process has a log: directive
□ Pipeline tested with --dry-run before full submission
□ seff output recorded for at least one full run (resource documentation)
</code></pre></div></div>

<hr />

<h2 id="part-7-the-full-series-recap">Part 7: The Full Series Recap</h2>

<p>You’ve now covered the complete arc from laptop to reproducible HPC pipeline:</p>

<table>
  <thead>
    <tr>
      <th>Day</th>
      <th>Topic</th>
      <th>Key Skill</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>Laptop vs HPC</td>
      <td>Understanding the landscape</td>
    </tr>
    <tr>
      <td>2</td>
      <td>Software installation</td>
      <td>conda, modules, environment.yml</td>
    </tr>
    <tr>
      <td>3</td>
      <td>Running jobs</td>
      <td>SLURM, sbatch, %j, interactive nodes</td>
    </tr>
    <tr>
      <td>4</td>
      <td>Scaling</td>
      <td>Job arrays, $SLURM_ARRAY_TASK_ID</td>
    </tr>
    <tr>
      <td>5</td>
      <td>Pipelines</td>
      <td>Snakemake, Nextflow, reproducibility</td>
    </tr>
    <tr>
      <td>6</td>
      <td>Data transfer</td>
      <td>scp, rsync, Globus Connect</td>
    </tr>
  </tbody>
</table>

<p>The through-line across all six posts: your bioinformatics tools don’t change. <code class="language-plaintext highlighter-rouge">fastqc</code>, <code class="language-plaintext highlighter-rouge">bowtie2</code>, <code class="language-plaintext highlighter-rouge">samtools</code>, <code class="language-plaintext highlighter-rouge">kraken2</code> — they all work the same. What changes is the infrastructure around them: how you install them, how you run them, how you scale them, and how you make them reproducible.</p>

<hr />

<h2 id="common-mistakes-when-starting-with-workflows">Common Mistakes When Starting With Workflows</h2>

<p><strong>Putting the whole pipeline in rule all</strong></p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ❌ This defeats the purpose
</span><span class="n">rule</span> <span class="nb">all</span><span class="p">:</span>
    <span class="n">shell</span><span class="p">:</span> <span class="sh">"</span><span class="s">bash my_manual_script.sh</span><span class="sh">"</span>

<span class="c1"># ✅ Define inputs and outputs properly so Snakemake can track them
</span><span class="n">rule</span> <span class="nb">all</span><span class="p">:</span>
    <span class="nb">input</span><span class="p">:</span> <span class="nf">expand</span><span class="p">(</span><span class="sh">"</span><span class="s">results/{s}.bam</span><span class="sh">"</span><span class="p">,</span> <span class="n">s</span><span class="o">=</span><span class="n">SAMPLES</span><span class="p">)</span>
</code></pre></div></div>

<p><strong>Hardcoding paths</strong></p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ❌ Only works on your machine
</span><span class="nb">input</span><span class="p">:</span> <span class="sh">"</span><span class="s">/home/yourname/project/data/{sample}.fastq.gz</span><span class="sh">"</span>

<span class="c1"># ✅ Use config or relative paths
</span><span class="nb">input</span><span class="p">:</span> <span class="n">config</span><span class="p">[</span><span class="sh">"</span><span class="s">data_dir</span><span class="sh">"</span><span class="p">]</span> <span class="o">+</span> <span class="sh">"</span><span class="s">/{sample}.fastq.gz</span><span class="sh">"</span>
<span class="c1"># or simply:
</span><span class="nb">input</span><span class="p">:</span> <span class="sh">"</span><span class="s">data/{sample}.fastq.gz</span><span class="sh">"</span>
</code></pre></div></div>

<p><strong>Not using <code class="language-plaintext highlighter-rouge">log:</code> directives</strong></p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ❌ Errors go to the terminal and get lost
</span><span class="n">rule</span> <span class="n">fastqc</span><span class="p">:</span>
    <span class="n">shell</span><span class="p">:</span> <span class="sh">"</span><span class="s">fastqc {input}</span><span class="sh">"</span>

<span class="c1"># ✅ Always capture logs
</span><span class="n">rule</span> <span class="n">fastqc</span><span class="p">:</span>
    <span class="n">log</span><span class="p">:</span> <span class="sh">"</span><span class="s">logs/fastqc/{sample}.log</span><span class="sh">"</span>
    <span class="n">shell</span><span class="p">:</span> <span class="sh">"</span><span class="s">fastqc {input} &amp;&gt; {log}</span><span class="sh">"</span>
</code></pre></div></div>

<p><strong>Starting with Nextflow before understanding the concept</strong></p>

<p>Start with Snakemake. Get one pipeline working end-to-end. Understand the DAG concept. Then explore Nextflow when your project outgrows what Snakemake handles comfortably.</p>

<hr />

<h2 id="try-it-yourself">Try It Yourself</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. Install Snakemake</span>
conda activate bioenv
conda <span class="nb">install</span> <span class="nt">-c</span> bioconda <span class="nt">-c</span> conda-forge snakemake <span class="nt">-y</span>

<span class="c"># 2. Create a minimal test Snakefile</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> snakemake_test/<span class="o">{</span>data,qc,logs<span class="o">}</span>
<span class="nb">cd </span>snakemake_test/

<span class="c"># Create a dummy FASTQ</span>
<span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"@r1</span><span class="se">\n</span><span class="s2">ACGT</span><span class="se">\n</span><span class="s2">+</span><span class="se">\n</span><span class="s2">IIII"</span> <span class="o">&gt;</span> data/sample1.fastq.gz

<span class="nb">cat</span> <span class="o">&gt;</span> Snakefile <span class="o">&lt;&lt;</span> <span class="sh">'</span><span class="no">EOF</span><span class="sh">'
SAMPLES = ["sample1"]

rule all:
    input: expand("qc/{s}_fastqc.html", s=SAMPLES)

rule fastqc:
    input:  "data/{sample}.fastq.gz"
    output: "qc/{sample}_fastqc.html"
    log:    "logs/{sample}_fastqc.log"
    shell:  "fastqc {input} -o qc/ &amp;&gt; {log}"
</span><span class="no">EOF

</span><span class="c"># 3. Dry run</span>
snakemake <span class="nt">--dry-run</span>

<span class="c"># 4. Run</span>
snakemake <span class="nt">--cores</span> 2

<span class="c"># 5. Nothing changed — it won't rerun</span>
snakemake <span class="nt">--cores</span> 2
<span class="c"># Nothing to be done.</span>
</code></pre></div></div>

<hr />

<h2 id="summary">Summary</h2>

<ul>
  <li>Shell scripts don’t track dependencies — workflow managers do</li>
  <li><strong>Snakemake</strong> uses Python-style rules (input → output → shell). Best for Python users and academic projects</li>
  <li><strong>Nextflow</strong> uses DSL2 processes + channels. Best for large-scale, cloud, and community pipelines (nf-core)</li>
  <li>Both integrate with SLURM: submit each step as its own HPC job automatically</li>
  <li>A good GitHub repository structure + README makes your pipeline reproducible by anyone</li>
  <li>The <code class="language-plaintext highlighter-rouge">-resume</code> (Nextflow) and <code class="language-plaintext highlighter-rouge">--rerun-incomplete</code> (Snakemake) flags mean a failed job never means rerunning everything</li>
</ul>

<hr />

<h2 id="up-next">Up Next</h2>

<p><strong>Day 6: Moving Your Data — scp, rsync, and Globus Connect</strong></p>

<p>Your pipeline is reproducible. Now: how do you get 300 GB of raw reads onto the cluster in the first place? We cover every data transfer method you’ll use in practice — from quick one-liners to TB-scale transfers that run unattended.</p>

<hr />

<p><img src="/assets/img/d5_hpc.png" alt="Laptop vs HPC: Scaling Computational Biology" /></p>]]></content><author><name></name></author><category term="HPC" /><category term="Snakemake" /><category term="Nextflow" /><category term="bioinformatics" /><category term="reproducibility" /><category term="workflow" /><category term="pipeline" /><category term="beginners" /><summary type="html"><![CDATA[Shell scripts work — until they don't. Here's how to graduate from manual job submissions to proper workflow managers that track dependencies, restart from failures, scale to HPC, and make your analysis reproducible by anyone, anywhere.]]></summary></entry><entry><title type="html">Day 4 — Scaling Analysis: For Loops vs SLURM Job Arrays</title><link href="https://jojyjohn28.github.io/blog/day4-job-arrays/" rel="alternate" type="text/html" title="Day 4 — Scaling Analysis: For Loops vs SLURM Job Arrays" /><published>2026-03-17T00:00:00-04:00</published><updated>2026-03-17T00:00:00-04:00</updated><id>https://jojyjohn28.github.io/blog/day4-job-arrays</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/day4-job-arrays/"><![CDATA[<h1 id="day-4-scaling-analysis--for-loops-vs-slurm-job-arrays">Day 4: Scaling Analysis — For Loops vs SLURM Job Arrays</h1>

<blockquote>
  <p><em>This is Day 4 of a 6-part series: <strong>From Laptop to HPC: Scaling Computational Biology Workflows</strong> and 🧬 Day 60 of Daily Bioinformatics from Jojy’s Desk. In <a href="https://jojyjohn28.github.io/blog/day3-running-jobs/">Day 3</a> we learned how to submit a single job. Today we scale it: the same work, 200× faster.</em></p>
</blockquote>

<hr />

<h2 id="the-problem-with-sequential-processing">The Problem With Sequential Processing</h2>

<p>You’ve written your SLURM script. You know how to run one job. Now your supervisor hands you 200 samples and says the analysis needs to be done by Friday.</p>

<p>Your first instinct is a for loop:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for </span>sample <span class="k">in </span>data/<span class="k">*</span>.fastq.gz<span class="p">;</span> <span class="k">do
    </span>sbatch process_one_sample.sh <span class="s2">"</span><span class="nv">$sample</span><span class="s2">"</span>
<span class="k">done</span>
</code></pre></div></div>

<p>Or maybe a single job with a loop inside:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --time=50:00:00</span>

<span class="k">for </span>sample <span class="k">in </span>data/<span class="k">*</span>.fastq.gz<span class="p">;</span> <span class="k">do
    </span>fastqc <span class="s2">"</span><span class="nv">$sample</span><span class="s2">"</span> <span class="nt">-o</span> qc_results/
<span class="k">done</span>
</code></pre></div></div>

<p>Both of these work. But they’re leaving enormous potential on the table.</p>

<p>The for loop approach submits 200 separate jobs manually. The single-job-with-loop approach runs everything sequentially — sample 2 waits for sample 1 to finish, sample 3 waits for sample 2, and so on.</p>

<p>With 200 samples at 15 minutes each, you’re looking at <strong>50 hours of wall time</strong>.</p>

<p>Job arrays run all 200 simultaneously. Wall time: <strong>15 minutes</strong>.</p>

<p>Same total CPU hours. Completely different calendar time.</p>

<hr />

<h2 id="what-youll-learn-today">What You’ll Learn Today</h2>

<ul>
  <li>Why sequential processing is the wrong default for large datasets</li>
  <li>What a SLURM job array actually is and how it works</li>
  <li>How <code class="language-plaintext highlighter-rouge">$SLURM_ARRAY_TASK_ID</code> maps to your sample files</li>
  <li>How to control concurrency — running 20 at a time instead of all 200</li>
  <li>How to handle failed tasks, rerun subsets, and read per-task logs</li>
  <li>Real patterns: paired-end reads, sample sheets, parameter sweeps</li>
  <li>A benchmark comparison: for loop vs job array</li>
</ul>

<hr />

<h2 id="part-1-what-is-a-job-array">Part 1: What Is a Job Array?</h2>

<p>A <strong>SLURM job array</strong> is a single <code class="language-plaintext highlighter-rouge">sbatch</code> submission that creates multiple identical jobs, each identified by a unique <strong>task ID</strong> — an integer from 1 to N.</p>

<p>Every task runs the same script, but gets a different task ID through the environment variable <code class="language-plaintext highlighter-rouge">$SLURM_ARRAY_TASK_ID</code>. Your script uses that ID to figure out which sample it should process.</p>

<p>Think of it like this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sbatch --array=1-200 process.sh
    ↓
SLURM creates 200 jobs:
  Job 84600_1   → process.sh  (SLURM_ARRAY_TASK_ID=1)
  Job 84600_2   → process.sh  (SLURM_ARRAY_TASK_ID=2)
  Job 84600_3   → process.sh  (SLURM_ARRAY_TASK_ID=3)
  ...
  Job 84600_200 → process.sh  (SLURM_ARRAY_TASK_ID=200)

All 200 run in parallel across available nodes.
</code></pre></div></div>

<p>The <strong>array job ID</strong> (84600) is shared across all tasks. Each task has its own <strong>task ID</strong> (1–200). Together they form a unique identifier: <code class="language-plaintext highlighter-rouge">84600_1</code>, <code class="language-plaintext highlighter-rouge">84600_2</code>, etc.</p>

<hr />

<h2 id="part-2-your-first-job-array">Part 2: Your First Job Array</h2>

<p>Let’s convert a for loop into a job array step by step.</p>

<h3 id="starting-point-the-for-loop">Starting Point: The For Loop</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c"># run_loop.sh — processes samples sequentially</span>

module load fastqc/0.12.1
<span class="nb">mkdir</span> <span class="nt">-p</span> qc_results/

<span class="k">for </span>sample <span class="k">in </span>data/<span class="k">*</span>.fastq.gz<span class="p">;</span> <span class="k">do
    </span>fastqc <span class="s2">"</span><span class="nv">$sample</span><span class="s2">"</span> <span class="nt">-o</span> qc_results/
<span class="k">done</span>

<span class="c"># ⏱ 200 samples × 15 min = 50 hours wall time</span>
</code></pre></div></div>

<h3 id="step-1-add-the-sbatch-array-directive">Step 1: Add the #SBATCH –array directive</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --job-name=fastqc_array</span>
<span class="c">#SBATCH --array=1-200              # create 200 tasks</span>
<span class="c">#SBATCH --output=logs/%A_%a.out    # %A=array ID, %a=task ID</span>
<span class="c">#SBATCH --error=logs/%A_%a.err</span>
<span class="c">#SBATCH --time=00:30:00            # per-task time limit</span>
<span class="c">#SBATCH --mem=4G                   # per-task memory</span>
<span class="c">#SBATCH --cpus-per-task=4</span>

module purge
module load fastqc/0.12.1
<span class="nb">mkdir</span> <span class="nt">-p</span> qc_results/ logs/

<span class="c"># Step 2: Use $SLURM_ARRAY_TASK_ID to pick one sample</span>
<span class="nv">SAMPLES</span><span class="o">=(</span>data/<span class="k">*</span>.fastq.gz<span class="o">)</span>           <span class="c"># bash array of all files</span>
<span class="nv">SAMPLE</span><span class="o">=</span><span class="k">${</span><span class="nv">SAMPLES</span><span class="p">[</span><span class="nv">$SLURM_ARRAY_TASK_ID</span><span class="p"> - 1]</span><span class="k">}</span>   <span class="c"># pick the Nth one</span>

<span class="nb">echo</span> <span class="s2">"Task </span><span class="nv">$SLURM_ARRAY_TASK_ID</span><span class="s2"> processing: </span><span class="nv">$SAMPLE</span><span class="s2">"</span>

fastqc <span class="s2">"</span><span class="nv">$SAMPLE</span><span class="s2">"</span> <span class="se">\</span>
       <span class="nt">-t</span> <span class="nv">$SLURM_CPUS_PER_TASK</span> <span class="se">\</span>
       <span class="nt">-o</span> qc_results/

<span class="nb">echo</span> <span class="s2">"Done: </span><span class="si">$(</span><span class="nb">date</span><span class="si">)</span><span class="s2">"</span>

<span class="c"># ⚡ 200 tasks run in parallel → 15 min wall time</span>
</code></pre></div></div>

<p>Submit it:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sbatch run_array.sh
<span class="c"># Submitted batch job 84600</span>
</code></pre></div></div>

<p>That one command launches 200 jobs. All of them start as soon as compute nodes become available.</p>

<h3 id="what-does-slurm_array_task_id---1-mean">What Does $SLURM_ARRAY_TASK_ID - 1 Mean?</h3>

<p>Bash arrays are <strong>0-indexed</strong> (first element is index 0), but SLURM arrays start at <strong>1</strong> by default. Subtracting 1 aligns them:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SLURM_ARRAY_TASK_ID=1  →  index 0  →  data/sample_001.fastq.gz
SLURM_ARRAY_TASK_ID=2  →  index 1  →  data/sample_002.fastq.gz
SLURM_ARRAY_TASK_ID=3  →  index 2  →  data/sample_003.fastq.gz
...
SLURM_ARRAY_TASK_ID=200 → index 199 → data/sample_200.fastq.gz
</code></pre></div></div>

<p>If you prefer to start your array at 0:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#SBATCH --array=0-199</span>
<span class="nv">SAMPLE</span><span class="o">=</span><span class="k">${</span><span class="nv">SAMPLES</span><span class="p">[</span><span class="nv">$SLURM_ARRAY_TASK_ID</span><span class="p">]</span><span class="k">}</span>   <span class="c"># no subtraction needed</span>
</code></pre></div></div>

<hr />

<h2 id="part-3-monitoring-job-arrays">Part 3: Monitoring Job Arrays</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># See all your array tasks</span>
squeue <span class="nt">-u</span> <span class="nv">$USER</span>

<span class="c"># The output shows each task separately:</span>
<span class="c"># JOBID        PARTITION  NAME           USER  ST  TIME   NODES</span>
<span class="c"># 84600_1      normal     fastqc_array   you   R   0:03   1</span>
<span class="c"># 84600_2      normal     fastqc_array   you   R   0:03   1</span>
<span class="c"># 84600_[3-200]normal     fastqc_array   you   PD  0:00   1</span>

<span class="c"># Compact view (groups pending tasks in brackets)</span>
squeue <span class="nt">-u</span> <span class="nv">$USER</span> <span class="nt">-t</span> all

<span class="c"># Check one specific task</span>
scontrol show job 84600_42

<span class="c"># Get efficiency for one task</span>
seff 84600_42
</code></pre></div></div>

<h3 id="reading-per-task-log-files">Reading Per-Task Log Files</h3>

<p>Because you used <code class="language-plaintext highlighter-rouge">%A_%a</code> in your output filename, every task has its own log:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>logs/84600_1.out    <span class="c"># task 1</span>
logs/84600_2.out    <span class="c"># task 2</span>
...
logs/84600_200.out  <span class="c"># task 200</span>

<span class="c"># Check if any tasks failed</span>
<span class="nb">grep</span> <span class="nt">-l</span> <span class="s2">"error</span><span class="se">\|</span><span class="s2">Error</span><span class="se">\|</span><span class="s2">FAILED"</span> logs/84600_<span class="k">*</span>.err

<span class="c"># Check a specific task's output</span>
<span class="nb">cat </span>logs/84600_42.out

<span class="c"># See how many tasks completed successfully</span>
<span class="nb">grep</span> <span class="nt">-l</span> <span class="s2">"Done:"</span> logs/84600_<span class="k">*</span>.out | <span class="nb">wc</span> <span class="nt">-l</span>
</code></pre></div></div>

<hr />

<h2 id="part-4-controlling-concurrency--the-n-throttle">Part 4: Controlling Concurrency — The %N Throttle</h2>

<p>Sometimes you don’t want all 200 tasks running at once. Reasons include:</p>

<ul>
  <li>The cluster has limited nodes and you don’t want to monopolise them</li>
  <li>A shared database or reference file has read limits</li>
  <li>You want to be a polite cluster citizen</li>
</ul>

<p>Control maximum simultaneous tasks with <code class="language-plaintext highlighter-rouge">%N</code>:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Run at most 20 tasks at a time</span>
<span class="c">#SBATCH --array=1-200%20</span>

<span class="c"># Run at most 50 tasks at a time</span>
<span class="c">#SBATCH --array=1-200%50</span>

<span class="c"># Run them all (default)</span>
<span class="c">#SBATCH --array=1-200</span>
</code></pre></div></div>

<p>With <code class="language-plaintext highlighter-rouge">--array=1-200%20</code>, SLURM starts 20 tasks, and as each one finishes, it immediately starts the next pending one. The queue self-manages — you don’t need to babysit it.</p>

<hr />

<h2 id="part-5-rerunning-failed-tasks">Part 5: Rerunning Failed Tasks</h2>

<p>In real workflows, some tasks fail. Maybe a sample had corrupted data. Maybe a node went down mid-job. Job arrays make it trivial to rerun only the failures.</p>

<h3 id="finding-which-tasks-failed">Finding Which Tasks Failed</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Check exit codes for all tasks</span>
sacct <span class="nt">-j</span> 84600 <span class="nt">--format</span><span class="o">=</span>JobID,State,ExitCode

<span class="c"># Output:</span>
<span class="c"># 84600_1     COMPLETED  0:0</span>
<span class="c"># 84600_2     COMPLETED  0:0</span>
<span class="c"># 84600_5     FAILED     1:0   ← this one failed</span>
<span class="c"># 84600_42    FAILED     1:0   ← and this one</span>
<span class="c"># ...</span>

<span class="c"># Quick one-liner to list just the failed task IDs</span>
sacct <span class="nt">-j</span> 84600 <span class="nt">--format</span><span class="o">=</span>JobID,State <span class="nt">-n</span> | <span class="se">\</span>
    <span class="nb">awk</span> <span class="s1">'$2=="FAILED" {print $1}'</span> | <span class="se">\</span>
    <span class="nb">sed</span> <span class="s1">'s/84600_//'</span>
<span class="c"># outputs: 5, 42</span>
</code></pre></div></div>

<h3 id="rerunning-specific-tasks">Rerunning Specific Tasks</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Rerun only tasks 5 and 42</span>
<span class="c">#SBATCH --array=5,42</span>

<span class="c"># Rerun a range of tasks</span>
<span class="c">#SBATCH --array=10-20</span>

<span class="c"># Combine: specific tasks plus a range</span>
<span class="c">#SBATCH --array=5,42,100-110</span>
</code></pre></div></div>

<p>Or cancel a specific task that’s running but producing bad output:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Cancel one task</span>
scancel 84600_42

<span class="c"># Cancel all pending tasks in the array (let running ones finish)</span>
scancel <span class="nt">-t</span> PENDING 84600

<span class="c"># Cancel the entire array</span>
scancel 84600
</code></pre></div></div>

<hr />

<h2 id="part-6-real-bioinformatics-patterns">Part 6: Real Bioinformatics Patterns</h2>

<h3 id="pattern-1-paired-end-fastq-files">Pattern 1: Paired-End FASTQ Files</h3>

<p>Most sequencing data comes in pairs (R1 and R2). Your array needs to handle both files for each sample.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --array=1-100</span>
<span class="c">#SBATCH --job-name=align_pe</span>
<span class="c">#SBATCH --output=logs/%A_%a.out</span>
<span class="c">#SBATCH --cpus-per-task=8</span>
<span class="c">#SBATCH --mem=16G</span>
<span class="c">#SBATCH --time=02:00:00</span>

module purge
module load bowtie2/2.5.1 samtools/1.17

<span class="c"># Build arrays of R1 and R2 files</span>
<span class="nv">R1_FILES</span><span class="o">=(</span>data/<span class="k">*</span>_R1.fastq.gz<span class="o">)</span>
<span class="nv">R2_FILES</span><span class="o">=(</span>data/<span class="k">*</span>_R2.fastq.gz<span class="o">)</span>

<span class="nv">R1</span><span class="o">=</span><span class="k">${</span><span class="nv">R1_FILES</span><span class="p">[</span><span class="nv">$SLURM_ARRAY_TASK_ID</span><span class="p"> - 1]</span><span class="k">}</span>
<span class="nv">R2</span><span class="o">=</span><span class="k">${</span><span class="nv">R2_FILES</span><span class="p">[</span><span class="nv">$SLURM_ARRAY_TASK_ID</span><span class="p"> - 1]</span><span class="k">}</span>

<span class="c"># Derive sample name from R1 filename</span>
<span class="nv">SAMPLE</span><span class="o">=</span><span class="si">$(</span><span class="nb">basename</span> <span class="s2">"</span><span class="nv">$R1</span><span class="s2">"</span> _R1.fastq.gz<span class="si">)</span>

<span class="nb">echo</span> <span class="s2">"Processing sample: </span><span class="nv">$SAMPLE</span><span class="s2">"</span>
<span class="nb">echo</span> <span class="s2">"R1: </span><span class="nv">$R1</span><span class="s2">"</span>
<span class="nb">echo</span> <span class="s2">"R2: </span><span class="nv">$R2</span><span class="s2">"</span>

<span class="nb">mkdir</span> <span class="nt">-p</span> alignments/

bowtie2 <span class="se">\</span>
    <span class="nt">-x</span> ref/genome <span class="se">\</span>
    <span class="nt">-1</span> <span class="s2">"</span><span class="nv">$R1</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">-2</span> <span class="s2">"</span><span class="nv">$R2</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">-p</span> <span class="nv">$SLURM_CPUS_PER_TASK</span> <span class="se">\</span>
    2&gt; alignments/<span class="k">${</span><span class="nv">SAMPLE</span><span class="k">}</span>.log | <span class="se">\</span>
samtools <span class="nb">sort</span> <span class="se">\</span>
    -@ <span class="nv">$SLURM_CPUS_PER_TASK</span> <span class="se">\</span>
    <span class="nt">-o</span> alignments/<span class="k">${</span><span class="nv">SAMPLE</span><span class="k">}</span>.bam

samtools index alignments/<span class="k">${</span><span class="nv">SAMPLE</span><span class="k">}</span>.bam

<span class="nb">echo</span> <span class="s2">"Alignment complete: </span><span class="si">$(</span><span class="nb">date</span><span class="si">)</span><span class="s2">"</span>
</code></pre></div></div>

<h3 id="pattern-2-sample-sheet-csv">Pattern 2: Sample Sheet (CSV)</h3>

<p>For complex projects, maintain a CSV sample sheet instead of relying on filename patterns:</p>

<pre><code class="language-csv">sample_id,r1_path,r2_path,condition
ctrl_01,data/ctrl_01_R1.fastq.gz,data/ctrl_01_R2.fastq.gz,control
ctrl_02,data/ctrl_02_R1.fastq.gz,data/ctrl_02_R2.fastq.gz,control
treat_01,data/treat_01_R1.fastq.gz,data/treat_01_R2.fastq.gz,treatment
</code></pre>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --array=2-51    # rows 2-51 of the CSV (row 1 = header)</span>
<span class="c">#SBATCH --job-name=align_sheet</span>
<span class="c">#SBATCH --output=logs/%A_%a.out</span>
<span class="c">#SBATCH --cpus-per-task=8</span>
<span class="c">#SBATCH --mem=16G</span>
<span class="c">#SBATCH --time=02:00:00</span>

module purge
module load bowtie2/2.5.1 samtools/1.17

<span class="nv">SHEET</span><span class="o">=</span><span class="s2">"samples.csv"</span>

<span class="c"># Extract this task's row from the CSV</span>
<span class="nv">ROW</span><span class="o">=</span><span class="si">$(</span><span class="nb">awk</span> <span class="nt">-F</span><span class="s1">','</span> <span class="s2">"NR==</span><span class="nv">$SLURM_ARRAY_TASK_ID</span><span class="s2">"</span> <span class="s2">"</span><span class="nv">$SHEET</span><span class="s2">"</span><span class="si">)</span>

<span class="nv">SAMPLE</span><span class="o">=</span><span class="si">$(</span><span class="nb">echo</span> <span class="s2">"</span><span class="nv">$ROW</span><span class="s2">"</span> | <span class="nb">cut</span> <span class="nt">-d</span><span class="s1">','</span> <span class="nt">-f1</span><span class="si">)</span>
<span class="nv">R1</span><span class="o">=</span><span class="si">$(</span><span class="nb">echo</span>     <span class="s2">"</span><span class="nv">$ROW</span><span class="s2">"</span> | <span class="nb">cut</span> <span class="nt">-d</span><span class="s1">','</span> <span class="nt">-f2</span><span class="si">)</span>
<span class="nv">R2</span><span class="o">=</span><span class="si">$(</span><span class="nb">echo</span>     <span class="s2">"</span><span class="nv">$ROW</span><span class="s2">"</span> | <span class="nb">cut</span> <span class="nt">-d</span><span class="s1">','</span> <span class="nt">-f3</span><span class="si">)</span>
<span class="nv">COND</span><span class="o">=</span><span class="si">$(</span><span class="nb">echo</span>   <span class="s2">"</span><span class="nv">$ROW</span><span class="s2">"</span> | <span class="nb">cut</span> <span class="nt">-d</span><span class="s1">','</span> <span class="nt">-f4</span><span class="si">)</span>

<span class="nb">echo</span> <span class="s2">"Sample: </span><span class="nv">$SAMPLE</span><span class="s2">  |  Condition: </span><span class="nv">$COND</span><span class="s2">"</span>

<span class="nb">mkdir</span> <span class="nt">-p</span> alignments/

bowtie2 <span class="nt">-x</span> ref/genome <span class="nt">-1</span> <span class="s2">"</span><span class="nv">$R1</span><span class="s2">"</span> <span class="nt">-2</span> <span class="s2">"</span><span class="nv">$R2</span><span class="s2">"</span> <span class="se">\</span>
        <span class="nt">-p</span> <span class="nv">$SLURM_CPUS_PER_TASK</span> <span class="se">\</span>
    | samtools <span class="nb">sort</span> -@ <span class="nv">$SLURM_CPUS_PER_TASK</span> <span class="se">\</span>
      <span class="nt">-o</span> alignments/<span class="k">${</span><span class="nv">SAMPLE</span><span class="k">}</span>.bam

samtools index alignments/<span class="k">${</span><span class="nv">SAMPLE</span><span class="k">}</span>.bam
</code></pre></div></div>

<h3 id="pattern-3-parameter-sweep">Pattern 3: Parameter Sweep</h3>

<p>Job arrays aren’t just for processing different samples. They’re perfect for testing a range of parameters:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --array=1-10</span>
<span class="c">#SBATCH --job-name=param_sweep</span>
<span class="c">#SBATCH --output=logs/sweep_%A_%a.out</span>
<span class="c">#SBATCH --cpus-per-task=4</span>
<span class="c">#SBATCH --mem=8G</span>

module load kraken2/2.1.3

<span class="c"># Test 10 different confidence thresholds (0.1 to 1.0)</span>
<span class="nv">CONFIDENCE</span><span class="o">=</span><span class="si">$(</span><span class="nb">echo</span> <span class="s2">"scale=1; </span><span class="nv">$SLURM_ARRAY_TASK_ID</span><span class="s2"> / 10"</span> | bc<span class="si">)</span>

<span class="nb">echo</span> <span class="s2">"Testing confidence threshold: </span><span class="nv">$CONFIDENCE</span><span class="s2">"</span>

<span class="nb">mkdir</span> <span class="nt">-p</span> results/sweep_<span class="k">${</span><span class="nv">CONFIDENCE</span><span class="k">}</span>/

kraken2 <span class="se">\</span>
    <span class="nt">--db</span> /scratch/databases/kraken2_standard <span class="se">\</span>
    <span class="nt">--confidence</span> <span class="s2">"</span><span class="nv">$CONFIDENCE</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">--report</span> results/sweep_<span class="k">${</span><span class="nv">CONFIDENCE</span><span class="k">}</span>/report.txt <span class="se">\</span>
    data/test_sample.fastq.gz <span class="se">\</span>
    <span class="o">&gt;</span> results/sweep_<span class="k">${</span><span class="nv">CONFIDENCE</span><span class="k">}</span>/output.txt

<span class="nb">echo</span> <span class="s2">"Confidence </span><span class="nv">$CONFIDENCE</span><span class="s2"> done: </span><span class="si">$(</span><span class="nb">date</span><span class="si">)</span><span class="s2">"</span>
</code></pre></div></div>

<hr />

<h2 id="part-7-the-full-benchmark">Part 7: The Full Benchmark</h2>

<p>Here’s the comparison across all three approaches for 200 samples, 15 minutes processing time each:</p>

<table>
  <thead>
    <tr>
      <th>Approach</th>
      <th>Wall Time</th>
      <th>CPU Hours</th>
      <th>Notes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Laptop for loop</td>
      <td>~50 hours</td>
      <td>50</td>
      <td>Sequential, laptop tied up</td>
    </tr>
    <tr>
      <td>Single HPC job with loop</td>
      <td>~50 hours</td>
      <td>50</td>
      <td>Better resources, still sequential</td>
    </tr>
    <tr>
      <td>HPC job array (200 tasks)</td>
      <td>~15 minutes</td>
      <td>50</td>
      <td>All tasks run simultaneously</td>
    </tr>
    <tr>
      <td>HPC job array (throttled 50%)</td>
      <td>~30 minutes</td>
      <td>50</td>
      <td>Max 100 tasks at once</td>
    </tr>
  </tbody>
</table>

<p>The total CPU hours are identical in every case — you’re not doing less work. The difference is pure <strong>parallelism</strong>. Job arrays don’t reduce compute; they compress wall time.</p>

<p>This matters enormously in practice:</p>

<ul>
  <li>A 50-hour sequential job must finish without interruption</li>
  <li>If it crashes at hour 48, you lose everything and restart</li>
  <li>A 15-minute array job is easy to rerun; each task is independent</li>
  <li>Failures affect one task, not the whole analysis</li>
</ul>

<hr />

<h2 id="part-8-common-mistakes-and-how-to-fix-them">Part 8: Common Mistakes and How to Fix Them</h2>

<h3 id="array-size-doesnt-match-number-of-files">Array size doesn’t match number of files</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ If you have 200 files but array=1-199</span>
<span class="c"># Task 200 is never run — one sample missed silently</span>

<span class="c"># ✅ Count your files first</span>
<span class="nb">ls </span>data/<span class="k">*</span>.fastq.gz | <span class="nb">wc</span> <span class="nt">-l</span>
<span class="c"># 200</span>

<span class="c">#SBATCH --array=1-200   # matches exactly</span>
</code></pre></div></div>

<h3 id="off-by-one-errors">Off-by-one errors</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ SLURM starts at 1, bash arrays at 0</span>
<span class="nv">SAMPLES</span><span class="o">=(</span>data/<span class="k">*</span>.fastq.gz<span class="o">)</span>
<span class="nv">SAMPLE</span><span class="o">=</span><span class="k">${</span><span class="nv">SAMPLES</span><span class="p">[</span><span class="nv">$SLURM_ARRAY_TASK_ID</span><span class="p">]</span><span class="k">}</span>   <span class="c"># task 1 → index 1 (skips sample 1)</span>

<span class="c"># ✅ Always subtract 1 when using --array=1-N</span>
<span class="nv">SAMPLE</span><span class="o">=</span><span class="k">${</span><span class="nv">SAMPLES</span><span class="p">[</span><span class="nv">$SLURM_ARRAY_TASK_ID</span><span class="p"> - 1]</span><span class="k">}</span>   <span class="c"># task 1 → index 0 (correct)</span>
</code></pre></div></div>

<h3 id="requesting-too-many-resources-per-task">Requesting too many resources per task</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ Overkill — wastes queue priority</span>
<span class="c">#SBATCH --mem=64G     # for a FastQC job that uses 2 GB</span>

<span class="c"># ✅ Profile one sample first with an interactive node</span>
<span class="c"># then set realistic limits</span>
<span class="c">#SBATCH --mem=4G</span>
</code></pre></div></div>

<h3 id="not-checking-for-silent-failures">Not checking for silent failures</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ Job shows COMPLETED but output is wrong</span>
<span class="c"># FastQC might fail on a corrupted file but exit 0</span>

<span class="c"># ✅ Add explicit checks</span>
<span class="k">if</span> <span class="o">[[</span> <span class="o">!</span> <span class="nt">-f</span> <span class="s2">"qc_results/</span><span class="k">${</span><span class="nv">SAMPLE</span><span class="k">}</span><span class="s2">_fastqc.html"</span> <span class="o">]]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">echo</span> <span class="s2">"ERROR: output not created for </span><span class="nv">$SAMPLE</span><span class="s2">"</span>
    <span class="nb">exit </span>1
<span class="k">fi</span>
</code></pre></div></div>

<h3 id="forgetting-to-create-output-and-log-directories">Forgetting to create output and log directories</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ Job fails: cannot open output file</span>
<span class="c">#SBATCH --output=logs/%A_%a.out</span>
<span class="c"># → logs/ doesn't exist</span>

<span class="c"># ✅ Create directories in your pre-submission step</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> logs/ qc_results/ alignments/
sbatch run_array.sh
</code></pre></div></div>

<hr />

<h2 id="try-it-yourself">Try It Yourself</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. Create 10 test files</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> test_array/data/ test_array/logs/
<span class="nb">cd </span>test_array/

<span class="c"># Generate 10 small fake FASTQ files for testing</span>
<span class="k">for </span>i <span class="k">in</span> <span class="si">$(</span><span class="nb">seq</span> <span class="nt">-w</span> 1 10<span class="si">)</span><span class="p">;</span> <span class="k">do
    </span><span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"@read1</span><span class="se">\n</span><span class="s2">ACGT</span><span class="se">\n</span><span class="s2">+</span><span class="se">\n</span><span class="s2">IIII"</span> <span class="o">&gt;</span> data/sample_<span class="k">${</span><span class="nv">i</span><span class="k">}</span>.fastq
<span class="k">done</span>

<span class="c"># 2. Write the array script</span>
<span class="nb">cat</span> <span class="o">&gt;</span> run_array.sh <span class="o">&lt;&lt;</span> <span class="sh">'</span><span class="no">EOF</span><span class="sh">'
#!/bin/bash
#SBATCH --job-name=test_array
#SBATCH --array=1-10
#SBATCH --output=logs/%A_%a.out
#SBATCH --time=00:05:00
#SBATCH --mem=500M
#SBATCH --cpus-per-task=1

SAMPLES=(data/*.fastq)
SAMPLE=</span><span class="k">${</span><span class="nv">SAMPLES</span><span class="p">[</span><span class="nv">$SLURM_ARRAY_TASK_ID</span><span class="p"> - 1]</span><span class="k">}</span><span class="sh">

echo "Task </span><span class="nv">$SLURM_ARRAY_TASK_ID</span><span class="sh"> → </span><span class="nv">$SAMPLE</span><span class="sh">"
echo "Lines in file: </span><span class="si">$(</span><span class="nb">wc</span> <span class="nt">-l</span> &lt; <span class="nv">$SAMPLE</span><span class="si">)</span><span class="sh">"
sleep 10
echo "Done: </span><span class="si">$(</span><span class="nb">date</span><span class="si">)</span><span class="sh">"
</span><span class="no">EOF

</span><span class="c"># 3. Submit</span>
sbatch run_array.sh

<span class="c"># 4. Watch all 10 tasks run simultaneously</span>
watch squeue <span class="nt">-u</span> <span class="nv">$USER</span>

<span class="c"># 5. Check outputs</span>
<span class="nb">ls </span>logs/
<span class="nb">grep</span> <span class="s2">"Task"</span> logs/<span class="k">*</span>.out
</code></pre></div></div>

<hr />

<h2 id="summary">Summary</h2>

<ul>
  <li><strong>For loops</strong> are sequential — simple to write but all samples wait in line</li>
  <li><strong>Job arrays</strong> run one task per sample, all in parallel — same total compute, fraction of the wall time</li>
  <li><code class="language-plaintext highlighter-rouge">$SLURM_ARRAY_TASK_ID</code> is the key: each task uses it to select its own input file</li>
  <li>Use <code class="language-plaintext highlighter-rouge">%A_%a</code> in output filenames for per-task logs (array job ID + task ID)</li>
  <li>Throttle with <code class="language-plaintext highlighter-rouge">--array=1-200%50</code> to limit concurrent tasks</li>
  <li>Rerun only failed tasks by specifying their IDs: <code class="language-plaintext highlighter-rouge">--array=5,42</code></li>
  <li>200 samples × 15 min = 50 hours sequential → 15 minutes parallel — that’s the single biggest win in HPC bioinformatics</li>
</ul>

<hr />

<h2 id="up-next">Up Next</h2>

<p><strong>Day 5: Reproducible Pipelines — Snakemake and Nextflow</strong></p>

<p>You can now run any analysis on HPC at scale. But shell scripts and job arrays have a hidden problem: they don’t track dependencies. If step 2 depends on step 1 finishing, and step 1 fails halfway through, your script doesn’t know. In Day 5, we introduce workflow managers that solve this — and make your science reproducible.</p>

<hr />

<p><img src="/assets/img/d4-hpc.png" alt="Laptop vs HPC: Scaling Computational Biology" /></p>]]></content><author><name></name></author><category term="HPC" /><category term="SLURM" /><category term="job-arrays" /><category term="bioinformatics" /><category term="parallelism" /><category term="scaling" /><category term="genomics" /><category term="beginners" /><summary type="html"><![CDATA[200 samples × 15 minutes = 50 hours sequential. Or 15 minutes parallel. Here's exactly how job arrays work, why they are the single biggest productivity unlock on HPC, and every pattern you need to handle real-world datasets.]]></summary></entry><entry><title type="html">Day 3 — Running Jobs: Terminal, Bash Loops, and SLURM</title><link href="https://jojyjohn28.github.io/blog/day3-running-jobs/" rel="alternate" type="text/html" title="Day 3 — Running Jobs: Terminal, Bash Loops, and SLURM" /><published>2026-03-16T00:00:00-04:00</published><updated>2026-03-16T00:00:00-04:00</updated><id>https://jojyjohn28.github.io/blog/day3-running-jobs</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/day3-running-jobs/"><![CDATA[<h1 id="day-3-running-jobs--terminal-bash-loops-and-slurm">Day 3: Running Jobs — Terminal, Bash Loops, and SLURM</h1>

<p><img src="./day3-hero.svg" alt="Running Jobs: Terminal vs SLURM" /></p>

<blockquote>
  <p><em>This is Day 3 of a 6-part series: <strong>From Laptop to HPC: Scaling Computational Biology Workflows</strong> and 🧬 Day 59 of Daily Bioinformatics from Jojy’s Desk.. In <a href="../day1-laptop-vs-hpc/">Day 1</a> we covered what HPC is. In <a href="../day2-software-installation/">Day 2</a> we covered software installation. Today: the biggest mindset shift — how to actually run things.</em></p>
</blockquote>

<hr />

<h2 id="the-moment-that-confuses-everyone">The Moment That Confuses Everyone</h2>

<p>You’ve logged into the HPC. Your conda environment is ready. You type:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>fastqc sample_R1.fastq.gz <span class="nt">-o</span> qc_results/
</code></pre></div></div>

<p>And it… runs. The output scrolls past. You think: <em>“That wasn’t so bad.”</em></p>

<p>Then you get an email from your sysadmin:</p>

<blockquote>
  <p><em>“Please do not run compute jobs on the login node. Use the job scheduler.”</em></p>
</blockquote>

<p>What did you do wrong? What even is a login node? What’s a job scheduler? And what does “submit a job” actually mean?</p>

<p>This post answers all of it — from scratch.</p>

<hr />

<h2 id="what-youll-learn-today">What You’ll Learn Today</h2>

<ul>
  <li>The difference between a laptop, a login node, and a compute node</li>
  <li>Why you must <strong>never</strong> run jobs on the login node</li>
  <li>How to run things on your <strong>laptop</strong> — direct commands and bash loops</li>
  <li>How to use an <strong>interactive node</strong> on HPC for testing</li>
  <li>How to write and submit your first <strong>SLURM batch script</strong></li>
  <li>What <code class="language-plaintext highlighter-rouge">%j</code> means in output filenames — demystified</li>
  <li>The full picture: queued vs running jobs</li>
</ul>

<hr />

<h2 id="part-1-the-three-environments--laptop-login-node-compute-node">Part 1: The Three Environments — Laptop, Login Node, Compute Node</h2>

<p>Before anything else, you need to understand the geography of an HPC cluster. There are three distinct places where code can run, and they are <strong>not interchangeable</strong>.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>┌────────────────────────────────────────────────────────────────────┐
│                         HPC CLUSTER                                 │
│                                                                     │
│   You (SSH in)                                                      │
│       │                                                             │
│       ▼                                                             │
│  ┌──────────────┐    sbatch job.sh    ┌──────────────────────────┐ │
│  │  LOGIN NODE  │ ─────────────────▶  │    SLURM SCHEDULER       │ │
│  │              │                     │  (manages the queue)     │ │
│  │ - Edit files │                     └────────────┬─────────────┘ │
│  │ - Submit jobs│                                  │ allocates     │
│  │ - Check queue│                                  ▼               │
│  │              │                     ┌──────────────────────────┐ │
│  │ ❌ NO compute│                     │     COMPUTE NODES        │ │
│  └──────────────┘                     │  node001, node002, ...   │ │
│                                       │  - Run your actual jobs  │ │
│                                       │  - 64–128 CPUs each      │ │
│                                       │  - 256 GB–4 TB RAM each  │ │
│                                       └──────────────────────────┘ │
└────────────────────────────────────────────────────────────────────┘
</code></pre></div></div>

<h3 id="the-login-node-gateway-not-workbench">The Login Node: Gateway, Not Workbench</h3>

<p>The login node is the machine you land on when you SSH into the cluster. It’s <strong>shared by every user simultaneously</strong> — dozens or hundreds of people editing files, checking jobs, and transferring data all at the same time.</p>

<p>Its job is to be a gateway, not a workhorse.</p>

<p>When you run a heavy computation on the login node, here’s what happens:</p>

<ul>
  <li>You consume CPU and RAM that everyone shares</li>
  <li>Other users’ terminals slow down or freeze</li>
  <li>The admins get an alert and find your process</li>
  <li>Your process gets killed — possibly mid-analysis, losing all progress</li>
  <li>You get a politely stern email</li>
</ul>

<blockquote>
  <p>💡 <strong>The rule is simple:</strong> On the login node you may: edit files, write scripts, transfer files, submit jobs, and check job status. That’s it. Anything that does computation goes through the scheduler.</p>
</blockquote>

<h3 id="compute-nodes-where-the-work-happens">Compute Nodes: Where the Work Happens</h3>

<p>Compute nodes are the powerful machines that actually run your jobs. You never SSH directly into them during normal use — the scheduler allocates them to your jobs automatically.</p>

<p>When your job is allocated a compute node, it gets exclusive use of the requested CPUs and RAM for the duration of the job. Nobody else touches those resources while your job runs.</p>

<hr />

<h2 id="part-2-running-things-on-your-laptop">Part 2: Running Things on Your Laptop</h2>

<p>On your laptop, there’s no scheduler. Commands run immediately, use your local resources, and block your terminal until they finish. This is fine for small work and essential for development and testing.</p>

<h3 id="direct-command-one-sample">Direct Command: One Sample</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Run directly — blocks terminal until done</span>
fastqc sample_R1.fastq.gz <span class="nt">-o</span> qc_results/

<span class="c"># Run in the background so you can keep using the terminal</span>
fastqc sample_R1.fastq.gz <span class="nt">-o</span> qc_results/ &amp;

<span class="c"># Check what's running in background</span>
<span class="nb">jobs</span>

<span class="c"># Bring it back to foreground</span>
<span class="nb">fg</span>
</code></pre></div></div>

<h3 id="bash-for-loop-many-samples-on-laptop">Bash For Loop: Many Samples on Laptop</h3>

<p>When you have multiple samples, the simplest approach on a laptop is a for loop:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c"># run_fastqc_loop.sh — processes samples one by one</span>

<span class="nb">mkdir</span> <span class="nt">-p</span> qc_results/

<span class="k">for </span>sample <span class="k">in </span>data/<span class="k">*</span>.fastq.gz<span class="p">;</span> <span class="k">do
    </span><span class="nv">name</span><span class="o">=</span><span class="si">$(</span><span class="nb">basename</span> <span class="s2">"</span><span class="nv">$sample</span><span class="s2">"</span> .fastq.gz<span class="si">)</span>
    <span class="nb">echo</span> <span class="s2">"Processing: </span><span class="nv">$name</span><span class="s2">"</span>
    fastqc <span class="s2">"</span><span class="nv">$sample</span><span class="s2">"</span> <span class="nt">-o</span> qc_results/
<span class="k">done

</span><span class="nb">echo</span> <span class="s2">"All done!"</span>
</code></pre></div></div>

<p>Run it:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bash run_fastqc_loop.sh

<span class="c"># Or make it executable and run directly</span>
<span class="nb">chmod</span> +x run_fastqc_loop.sh
./run_fastqc_loop.sh
</code></pre></div></div>

<p><strong>What’s actually happening:</strong> Each sample is processed fully before the next one starts. If you have 200 samples and each takes 5 minutes, you’re looking at <strong>16 hours of sequential processing</strong> — your laptop fan running at full speed the whole time.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Rough timing estimate</span>
<span class="nb">echo</span> <span class="s2">"Estimated time: </span><span class="k">$((</span> <span class="m">200</span> <span class="o">*</span> <span class="m">5</span> <span class="k">))</span><span class="s2"> minutes = </span><span class="k">$((</span> <span class="m">200</span> <span class="o">*</span> <span class="m">5</span> <span class="o">/</span> <span class="m">60</span> <span class="k">))</span><span class="s2"> hours"</span>
<span class="c"># Estimated time: 1000 minutes = 16 hours</span>
</code></pre></div></div>

<p>A loop is fine for 10 samples. For 50+, you need parallelism — which is exactly what job arrays give you (covered in Day 4). But first, let’s get a single job running on HPC properly.</p>

<hr />

<h2 id="part-3-why-cant-i-just-run-it-on-hpc">Part 3: Why Can’t I Just Run It on HPC?</h2>

<p>The short answer: you can — but only in the right place.</p>

<p>The confusion comes from the fact that <strong>the login node feels like a normal Linux machine</strong>. You can type commands, they run, output appears. It feels identical to your laptop.</p>

<p>But you’re sharing that machine’s CPUs and RAM with every logged-in user. Imagine 80 researchers simultaneously deciding to align their FASTQ files on that one machine. It would grind to a halt within seconds.</p>

<p>The solution is to route all computation through <strong>SLURM</strong> — the job scheduler.</p>

<h3 id="what-does-slurm-actually-do">What Does SLURM Actually Do?</h3>

<p>SLURM (<strong>S</strong>imple <strong>L</strong>inux <strong>U</strong>tility for <strong>R</strong>esource <strong>M</strong>anagement) is software that:</p>

<ol>
  <li>Maintains a <strong>queue</strong> of pending jobs from all users</li>
  <li>Monitors which <strong>compute nodes</strong> are free</li>
  <li>Allocates free nodes to queued jobs based on requested resources and priority</li>
  <li>Starts your job on the allocated node</li>
  <li>Returns the node to the pool when your job finishes</li>
</ol>

<p>When you “submit a job”, you’re adding it to SLURM’s queue. SLURM decides when and where it runs.</p>

<hr />

<h2 id="part-4-interactive-nodes--hpc-for-testing">Part 4: Interactive Nodes — HPC for Testing</h2>

<p>Before writing a full batch script, you often want to test your commands interactively — just like on your laptop, but on a compute node with proper resources.</p>

<p>That’s what interactive sessions are for.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Request an interactive session on a compute node</span>
srun <span class="nt">--cpus-per-task</span><span class="o">=</span>4 <span class="nt">--mem</span><span class="o">=</span>8G <span class="nt">--time</span><span class="o">=</span>01:00:00 <span class="nt">--pty</span> bash

<span class="c"># Shorter version (flags vary by cluster — check your HPC docs)</span>
srun <span class="nt">-c</span> 4 <span class="nt">--mem</span><span class="o">=</span>8G <span class="nt">-t</span> 1:00:00 <span class="nt">--pty</span> bash

<span class="c"># On some clusters, use salloc instead</span>
salloc <span class="nt">--cpus-per-task</span><span class="o">=</span>4 <span class="nt">--mem</span><span class="o">=</span>8G <span class="nt">--time</span><span class="o">=</span>01:00:00
</code></pre></div></div>

<p>Once SLURM allocates you a node, your prompt changes:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>user@login01:~$ srun --cpus-per-task=4 --mem=8G --pty bash
srun: job 84530 queued and waiting for resources
srun: job 84530 has been allocated resources

user@node099:~$   # ← you're now on a compute node!
</code></pre></div></div>

<p>Now you can run commands exactly like on your laptop — they execute on the compute node, using its resources:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Test your pipeline on one sample</span>
fastqc test_sample.fastq.gz <span class="nt">-t</span> 4 <span class="nt">-o</span> qc_test/

<span class="c"># Test a memory-intensive tool</span>
kraken2 <span class="nt">--db</span> /scratch/databases/kraken2_standard <span class="se">\</span>
        <span class="nt">--report</span> test.report <span class="se">\</span>
        test_sample.fastq.gz

<span class="c"># When done, exit back to the login node</span>
<span class="nb">exit</span>
</code></pre></div></div>

<blockquote>
  <p>💡 <strong>Interactive sessions are for testing, not production.</strong> If you close your terminal or lose your connection, the session ends and anything running stops. For real analyses, use batch jobs (below).</p>
</blockquote>

<hr />

<h2 id="part-5-your-first-slurm-batch-script">Part 5: Your First SLURM Batch Script</h2>

<p>A batch job is a shell script with special <code class="language-plaintext highlighter-rouge">#SBATCH</code> comments at the top. These comments tell SLURM what resources your job needs. Everything that isn’t a <code class="language-plaintext highlighter-rouge">#SBATCH</code> line is just normal bash.</p>

<h3 id="anatomy-of-a-slurm-script">Anatomy of a SLURM Script</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c"># ↑ Always the first line — tells the system this is a bash script</span>

<span class="c"># ──── SLURM directives ────────────────────────────────────────────</span>
<span class="c">#SBATCH --job-name=fastqc_run      # Name shown in squeue</span>
<span class="c">#SBATCH --output=logs/fastqc_%j.out  # Stdout log (%j = job ID)</span>
<span class="c">#SBATCH --error=logs/fastqc_%j.err   # Stderr log (errors go here)</span>
<span class="c">#SBATCH --time=01:00:00            # Max wall time: 1 hour</span>
<span class="c">#SBATCH --mem=8G                   # RAM requested</span>
<span class="c">#SBATCH --cpus-per-task=4          # CPU cores requested</span>
<span class="c">#SBATCH --partition=normal         # Which queue/partition to use</span>

<span class="c"># ──── Your actual script starts here ──────────────────────────────</span>
<span class="c"># Create output directory</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> qc_results/
<span class="nb">mkdir</span> <span class="nt">-p</span> logs/

<span class="c"># Load software (always do this — terminal modules don't carry over)</span>
module purge
module load fastqc/0.12.1

<span class="c"># Run the analysis</span>
<span class="nb">echo</span> <span class="s2">"Job started: </span><span class="si">$(</span><span class="nb">date</span><span class="si">)</span><span class="s2">"</span>
<span class="nb">echo</span> <span class="s2">"Running on node: </span><span class="si">$(</span><span class="nb">hostname</span><span class="si">)</span><span class="s2">"</span>

fastqc data/sample_R1.fastq.gz <span class="se">\</span>
       <span class="nt">-t</span> <span class="nv">$SLURM_CPUS_PER_TASK</span> <span class="se">\ </span>  <span class="c"># use exactly what was allocated</span>
       <span class="nt">-o</span> qc_results/

<span class="nb">echo</span> <span class="s2">"Job finished: </span><span class="si">$(</span><span class="nb">date</span><span class="si">)</span><span class="s2">"</span>
</code></pre></div></div>

<h3 id="what-is-j-and-why-does-it-matter">What Is %j and Why Does It Matter?</h3>

<p>Look at this line:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#SBATCH --output=logs/fastqc_%j.out</span>
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">%j</code> is a <strong>SLURM placeholder</strong> that gets replaced with the actual job ID when the job runs. So if SLURM assigns your job ID 84521, the output file becomes:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>logs/fastqc_84521.out
</code></pre></div></div>

<p><strong>Why this matters:</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Without %j — every run overwrites the same file</span>
<span class="c">#SBATCH --output=logs/fastqc.out   # ← run 1 and run 2 both write here</span>

<span class="c"># With %j — every run gets its own file</span>
<span class="c">#SBATCH --output=logs/fastqc_%j.out</span>
<span class="c"># run 1 → logs/fastqc_84521.out</span>
<span class="c"># run 2 → logs/fastqc_84522.out</span>
<span class="c"># run 3 → logs/fastqc_84523.out</span>
</code></pre></div></div>

<p>Without <code class="language-plaintext highlighter-rouge">%j</code>, if you resubmit a job because something went wrong, the new output overwrites the old one. You lose the evidence of what failed. With <code class="language-plaintext highlighter-rouge">%j</code>, you have a permanent, unique log for every job you’ve ever run.</p>

<p>Other useful SLURM placeholders:</p>

<table>
  <thead>
    <tr>
      <th>Placeholder</th>
      <th>Expands to</th>
      <th>Use case</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">%j</code></td>
      <td>Job ID</td>
      <td>Single jobs</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">%A</code></td>
      <td>Array job ID</td>
      <td>Job arrays (Day 4)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">%a</code></td>
      <td>Array task ID</td>
      <td>Job arrays (Day 4)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">%N</code></td>
      <td>Node name</td>
      <td>Debugging node issues</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">%x</code></td>
      <td>Job name</td>
      <td>Alternative to job ID</td>
    </tr>
  </tbody>
</table>

<p>So for job arrays you’ll often write:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#SBATCH --output=logs/%x_%A_%a.out</span>
<span class="c"># becomes: logs/fastqc_84600_42.out</span>
</code></pre></div></div>

<h3 id="key-sbatch-directives-reference">Key #SBATCH Directives Reference</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#SBATCH --job-name=myanalysis      # Name in squeue (keep it short)</span>
<span class="c">#SBATCH --output=logs/job_%j.out   # Stdout (create logs/ dir first!)</span>
<span class="c">#SBATCH --error=logs/job_%j.err    # Stderr — check this when jobs fail</span>
<span class="c">#SBATCH --time=02:00:00            # Format: HH:MM:SS or D-HH:MM:SS</span>
<span class="c">#SBATCH --mem=16G                  # Total RAM (or --mem-per-cpu=4G)</span>
<span class="c">#SBATCH --cpus-per-task=8          # CPU threads per task</span>
<span class="c">#SBATCH --ntasks=1                 # Number of parallel tasks (usually 1)</span>
<span class="c">#SBATCH --partition=normal         # Partition/queue (check your HPC docs)</span>
<span class="c">#SBATCH --mail-type=END,FAIL       # Email you when job ends or fails</span>
<span class="c">#SBATCH --mail-user=you@uni.edu    # Your email address</span>
</code></pre></div></div>

<blockquote>
  <p>⚠️ <strong>Important:</strong> Create your log directory before submitting. If <code class="language-plaintext highlighter-rouge">logs/</code> doesn’t exist, SLURM can’t write the output file and your job fails immediately with a confusing error.</p>
</blockquote>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> logs/   <span class="c"># Do this once before your first sbatch</span>
</code></pre></div></div>

<hr />

<h2 id="part-6-submitting-and-monitoring-your-job">Part 6: Submitting and Monitoring Your Job</h2>

<h3 id="submitting">Submitting</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sbatch run_fastqc.sh

<span class="c"># SLURM responds with:</span>
<span class="c"># Submitted batch job 84521</span>
</code></pre></div></div>

<p>That number (84521) is your <strong>job ID</strong>. Write it down. You’ll need it to check status, read logs, or cancel the job.</p>

<h3 id="checking-job-status">Checking Job Status</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># See all your jobs</span>
squeue <span class="nt">-u</span> <span class="nv">$USER</span>

<span class="c"># See all jobs on the cluster</span>
squeue

<span class="c"># See more detail on a specific job</span>
scontrol show job 84521
</code></pre></div></div>

<p>The output of <code class="language-plaintext highlighter-rouge">squeue</code> looks like this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JOBID   PARTITION  NAME        USER    ST  TIME    NODES  NODELIST
84521   normal     fastqc_run  you     R   0:42    1      node042
84522   normal     align_r1    you     R   0:18    1      node043
84523   normal     align_r2    you     PD  0:00    1      (Resources)
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">ST</code> column is the most important. The main states you’ll see:</p>

<table>
  <thead>
    <tr>
      <th>State</th>
      <th>Code</th>
      <th>Meaning</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Running</td>
      <td><code class="language-plaintext highlighter-rouge">R</code></td>
      <td>Your job is executing on a compute node</td>
    </tr>
    <tr>
      <td>Pending</td>
      <td><code class="language-plaintext highlighter-rouge">PD</code></td>
      <td>Waiting in the queue for resources</td>
    </tr>
    <tr>
      <td>Completing</td>
      <td><code class="language-plaintext highlighter-rouge">CG</code></td>
      <td>Finishing up, cleaning up</td>
    </tr>
    <tr>
      <td>Failed</td>
      <td><code class="language-plaintext highlighter-rouge">F</code></td>
      <td>Job exited with an error</td>
    </tr>
    <tr>
      <td>Completed</td>
      <td><code class="language-plaintext highlighter-rouge">CD</code></td>
      <td>Finished successfully</td>
    </tr>
    <tr>
      <td>Cancelled</td>
      <td><code class="language-plaintext highlighter-rouge">CA</code></td>
      <td>You or an admin cancelled it</td>
    </tr>
  </tbody>
</table>

<h3 id="what-happens-between-pending-and-running">What Happens Between Pending and Running?</h3>

<p>When you submit a job, it enters the <strong>pending</strong> state. SLURM looks at your requested resources (CPUs, RAM, time) and finds a compute node that:</p>

<ol>
  <li>Has enough free CPUs and RAM</li>
  <li>Can fit your requested time within scheduling constraints</li>
  <li>Matches your requested partition</li>
</ol>

<p>Once a suitable node is found, your job transitions to <strong>running</strong>. This can take anywhere from seconds (cluster is quiet) to hours or days (cluster is busy, you requested a lot of resources).</p>

<p>You can estimate your wait time:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Show estimated start time for pending jobs</span>
squeue <span class="nt">-u</span> <span class="nv">$USER</span> <span class="nt">--start</span>
</code></pre></div></div>

<h3 id="reading-your-output-files">Reading Your Output Files</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Watch the output file live as the job runs</span>
<span class="nb">tail</span> <span class="nt">-f</span> logs/fastqc_84521.out

<span class="c"># Read the full output after completion</span>
<span class="nb">cat </span>logs/fastqc_84521.out

<span class="c"># Check for errors</span>
<span class="nb">cat </span>logs/fastqc_84521.err

<span class="c"># Get a summary of resources used after job completes</span>
seff 84521
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">seff</code> command is especially useful. It tells you:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Job ID: 84521
State: COMPLETED (exit code 0)
Cores: 4
CPU Utilized: 00:03:12
CPU Efficiency: 80.00%
Memory Utilized: 2.34 GB
Memory Efficiency: 29.25% of 8.00 GB
</code></pre></div></div>

<p>If your CPU efficiency is very low, you requested too many cores. If memory efficiency is very low, you requested too much RAM. Use this to tune your future jobs — both for your own queue priority and to be a good citizen of a shared resource.</p>

<h3 id="cancelling-a-job">Cancelling a Job</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Cancel a specific job</span>
scancel 84521

<span class="c"># Cancel all your jobs</span>
scancel <span class="nt">-u</span> <span class="nv">$USER</span>

<span class="c"># Cancel all your pending jobs only</span>
scancel <span class="nt">-u</span> <span class="nv">$USER</span> <span class="nt">-t</span> PENDING
</code></pre></div></div>

<hr />

<h2 id="part-7-from-laptop-loop-to-hpc-batch--side-by-side">Part 7: From Laptop Loop to HPC Batch — Side by Side</h2>

<p>Here’s the same task — running FastQC on many samples — in all three approaches:</p>

<h3 id="approach-1-laptop-bash-loop-sequential">Approach 1: Laptop bash loop (sequential)</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c"># run_local.sh — works on laptop, slow for many samples</span>

<span class="k">for </span>sample <span class="k">in </span>data/<span class="k">*</span>.fastq.gz<span class="p">;</span> <span class="k">do
    </span>fastqc <span class="s2">"</span><span class="nv">$sample</span><span class="s2">"</span> <span class="nt">-o</span> qc_results/
<span class="k">done</span>
<span class="c"># 200 samples × 5 min = 16 hours, one core at a time</span>
</code></pre></div></div>

<h3 id="approach-2-hpc-batch-script-single-job-all-samples">Approach 2: HPC batch script (single job, all samples)</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --job-name=fastqc_all</span>
<span class="c">#SBATCH --output=logs/fastqc_all_%j.out</span>
<span class="c">#SBATCH --error=logs/fastqc_all_%j.err</span>
<span class="c">#SBATCH --time=08:00:00       # allow enough time for all samples</span>
<span class="c">#SBATCH --mem=16G</span>
<span class="c">#SBATCH --cpus-per-task=8</span>

module purge
module load fastqc/0.12.1

<span class="nb">mkdir</span> <span class="nt">-p</span> qc_results/ logs/

<span class="c"># Still sequential, but on a powerful compute node</span>
<span class="k">for </span>sample <span class="k">in </span>data/<span class="k">*</span>.fastq.gz<span class="p">;</span> <span class="k">do
    </span>fastqc <span class="s2">"</span><span class="nv">$sample</span><span class="s2">"</span> <span class="nt">-t</span> <span class="nv">$SLURM_CPUS_PER_TASK</span> <span class="nt">-o</span> qc_results/
<span class="k">done</span>

<span class="c"># 200 samples on 8 CPUs — still sequential per sample,</span>
<span class="c"># but each sample now uses 8 threads</span>
</code></pre></div></div>

<h3 id="approach-3-hpc-job-array-truly-parallel--day-4-preview">Approach 3: HPC job array (truly parallel — Day 4 preview)</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --job-name=fastqc_array</span>
<span class="c">#SBATCH --output=logs/fastqc_%A_%a.out</span>
<span class="c">#SBATCH --array=1-200           # 200 jobs, all running at once</span>
<span class="c">#SBATCH --cpus-per-task=4</span>
<span class="c">#SBATCH --mem=4G</span>
<span class="c">#SBATCH --time=00:30:00</span>

module purge
module load fastqc/0.12.1

<span class="c"># Each job processes exactly one sample</span>
<span class="nv">SAMPLES</span><span class="o">=(</span>data/<span class="k">*</span>.fastq.gz<span class="o">)</span>
<span class="nv">SAMPLE</span><span class="o">=</span><span class="k">${</span><span class="nv">SAMPLES</span><span class="p">[</span><span class="nv">$SLURM_ARRAY_TASK_ID</span><span class="p"> - 1]</span><span class="k">}</span>

fastqc <span class="s2">"</span><span class="nv">$SAMPLE</span><span class="s2">"</span> <span class="nt">-t</span> <span class="nv">$SLURM_CPUS_PER_TASK</span> <span class="nt">-o</span> qc_results/

<span class="c"># 200 jobs run simultaneously — wall time ~5 minutes total</span>
</code></pre></div></div>

<p>We’ll go deep on job arrays in Day 4. For now, the key insight: a single SLURM batch script is already a huge upgrade over a laptop loop because you can request dedicated resources, your laptop is freed up, and the job runs even if you close your laptop.</p>

<hr />

<h2 id="common-mistakes-to-avoid">Common Mistakes to Avoid</h2>

<p><strong>Not creating the logs directory</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ Job fails instantly — can't write to logs/</span>
<span class="c">#SBATCH --output=logs/job_%j.out</span>
<span class="c"># → logs/ doesn't exist → sbatch error</span>

<span class="c"># ✅ Create it before submitting</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> logs/
sbatch myjob.sh
</code></pre></div></div>

<p><strong>Requesting too much time “just in case”</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ Lower priority, longer to start</span>
<span class="c">#SBATCH --time=7-00:00:00   # 7 days for a 2-hour job</span>

<span class="c"># ✅ Estimate reasonably + add 20% buffer</span>
<span class="c">#SBATCH --time=02:30:00</span>
</code></pre></div></div>

<p><strong>Forgetting to load modules in the script</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ Works in terminal, fails in sbatch — modules don't transfer</span>
module load samtools/1.17      <span class="c"># in your terminal</span>
sbatch myjob.sh                <span class="c"># job doesn't have samtools</span>

<span class="c"># ✅ Load inside the script</span>
module purge
module load samtools/1.17
</code></pre></div></div>

<p><strong>Using $HOME for large data</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ $HOME is usually small quota (50–100 GB)</span>
<span class="c">#SBATCH --chdir=$HOME/data/</span>

<span class="c"># ✅ Use scratch storage for large files</span>
<span class="c">#SBATCH --chdir=/scratch/$USER/project/</span>
</code></pre></div></div>

<p><strong>Not checking seff after jobs</strong></p>

<p>Always run <code class="language-plaintext highlighter-rouge">seff &lt;jobid&gt;</code> after a job completes. It’s 10 seconds of work that tells you exactly how to tune your resource requests — which means shorter queue wait times for your next job.</p>

<hr />

<h2 id="try-it-yourself">Try It Yourself</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. Create a minimal test script</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> ~/test_job/logs/
<span class="nb">cd</span> ~/test_job/

<span class="nb">cat</span> <span class="o">&gt;</span> test.sh <span class="o">&lt;&lt;</span> <span class="sh">'</span><span class="no">EOF</span><span class="sh">'
#!/bin/bash
#SBATCH --job-name=hello_hpc
#SBATCH --output=logs/hello_%j.out
#SBATCH --time=00:05:00
#SBATCH --mem=1G
#SBATCH --cpus-per-task=1

echo "Hello from node: </span><span class="si">$(</span><span class="nb">hostname</span><span class="si">)</span><span class="sh">"
echo "My job ID is: </span><span class="nv">$SLURM_JOB_ID</span><span class="sh">"
echo "Date: </span><span class="si">$(</span><span class="nb">date</span><span class="si">)</span><span class="sh">"
sleep 30
echo "Done!"
</span><span class="no">EOF

</span><span class="c"># 2. Submit it</span>
sbatch test.sh
<span class="c"># Submitted batch job 84560</span>

<span class="c"># 3. Watch it run</span>
squeue <span class="nt">-u</span> <span class="nv">$USER</span>

<span class="c"># 4. Read the output (once state = R or CG)</span>
<span class="nb">cat </span>logs/hello_84560.out

<span class="c"># 5. Check resource usage</span>
seff 84560
</code></pre></div></div>

<hr />

<h2 id="summary">Summary</h2>

<ul>
  <li>The <strong>login node</strong> is a shared gateway — never run compute there. Use it only for editing files, submitting jobs, and checking status</li>
  <li>On your <strong>laptop</strong>, commands run directly. Bash for loops work but are sequential — fine for small datasets, slow for 50+ samples</li>
  <li><strong>Interactive nodes</strong> (<code class="language-plaintext highlighter-rouge">srun --pty bash</code>) give you a compute node for testing, exactly like your laptop but with HPC resources</li>
  <li><strong>SLURM batch scripts</strong> (<code class="language-plaintext highlighter-rouge">sbatch</code>) are shell scripts with <code class="language-plaintext highlighter-rouge">#SBATCH</code> headers that tell the scheduler what resources your job needs</li>
  <li><code class="language-plaintext highlighter-rouge">%j</code> in output filenames is replaced by the job ID — always use it so every run has its own unique log file</li>
  <li>After every job, run <code class="language-plaintext highlighter-rouge">seff &lt;jobid&gt;</code> to see how efficiently you used your requested resources</li>
</ul>

<hr />

<h2 id="up-next">Up Next</h2>

<p><strong>Day 4: Scaling Analysis — For Loops vs Job Arrays</strong></p>

<p>You now know how to run one job properly. In Day 4, we scale it. We’ll take that bash for loop processing 200 samples in 16 hours and turn it into a SLURM job array that finishes in 15 minutes — with the same amount of total compute, just running in parallel.</p>

<hr />

<p><img src="/assets/img/d3_hpc.png" alt="Laptop vs HPC: Scaling Computational Biology" /></p>]]></content><author><name></name></author><category term="HPC" /><category term="SLURM" /><category term="sbatch" /><category term="bioinformatics" /><category term="beginners" /><category term="job-scheduler" /><category term="bash" /><category term="computational-biology" /><summary type="html"><![CDATA[The biggest mindset shift in HPC: you don't run commands, you submit jobs. Why the login node is off-limits, what actually happens when you type sbatch, and how to go from a laptop bash loop to a proper SLURM script.]]></summary></entry><entry><title type="html">Day 2 — Software Installation: sudo vs module load vs conda</title><link href="https://jojyjohn28.github.io/blog/day2-software-installation/" rel="alternate" type="text/html" title="Day 2 — Software Installation: sudo vs module load vs conda" /><published>2026-03-13T00:00:00-04:00</published><updated>2026-03-13T00:00:00-04:00</updated><id>https://jojyjohn28.github.io/blog/day2-software-installation</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/day2-software-installation/"><![CDATA[<h1 id="day-2-software-installation--sudo-vs-module-load-vs-conda">Day 2: Software Installation — sudo vs module load vs conda</h1>

<blockquote>
  <p><em>This is Day 2 of a 6-part series: <strong>From Laptop to HPC: Scaling Computational Biology Workflows</strong> and 🧬 Day 58 of Daily Bioinformatics from Jojy’s Desk. In <a href="https://jojyjohn28.github.io/blog/day1-laptop-vs-hpc/">Day 1</a> we covered what HPC is and why bioinformatics needs it. Today: the first thing that breaks when you log in.</em></p>
</blockquote>

<hr />

<h2 id="the-sudo-story">The sudo Story</h2>

<p>Picture this. You’ve just logged into an HPC cluster for the first time. You need <code class="language-plaintext highlighter-rouge">samtools</code>. On your terminal, you’d type:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get <span class="nb">install </span>samtools
</code></pre></div></div>

<p>So you type exactly that on the HPC. And get this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sudo: Permission denied
</code></pre></div></div>

<p>Your first instinct: <em>“Something’s wrong with my account. Let me email IT.”</em></p>

<p>Nothing is wrong with your account.</p>

<p><strong>You don’t have root access on HPC — and that is completely intentional.</strong></p>

<p>Here’s why, and here’s what to do instead.</p>

<hr />

<h2 id="what-youll-learn-today">What You’ll Learn Today</h2>

<p>By the end of this post you’ll understand:</p>

<ul>
  <li>What <code class="language-plaintext highlighter-rouge">sudo</code> actually does, and why it’s dangerous on a shared system</li>
  <li>How to use <strong>environment modules</strong> — the HPC-native way to load software</li>
  <li>How to use <strong>conda</strong> to install anything you want, without root access</li>
  <li>How to write an <code class="language-plaintext highlighter-rouge">environment.yml</code> file so your analysis is reproducible on any machine</li>
</ul>

<hr />

<h2 id="part-1-what-does-sudo-actually-mean">Part 1: What Does sudo Actually Mean?</h2>

<p><code class="language-plaintext highlighter-rouge">sudo</code> stands for <strong>“superuser do”</strong>. When you run a command with <code class="language-plaintext highlighter-rouge">sudo</code>, you’re telling the operating system: <em>run this as the root user — the most powerful account on the system.</em></p>

<p><strong>On your laptop, that’s fine. You own the machine. If you accidentally break something, it affects only you.</strong></p>

<p>But on an HPC cluster, you are sharing infrastructure with potentially <strong>hundreds or thousands of other users</strong>. The cluster might be running active jobs for colleagues in your lab, researchers at other institutions, and automated pipelines that have been running for weeks.</p>

<p>If any user had root access and ran:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get upgrade
</code></pre></div></div>

<p>…they could upgrade every system package on every node. That could:</p>

<ul>
  <li>Break every other user’s software environment instantly</li>
  <li>Crash jobs that have been running for 3 days</li>
  <li>Render the entire cluster unusable until an admin spends hours fixing it</li>
</ul>

<p>This isn’t hypothetical. It’s exactly why HPC sysadmins remove sudo access entirely. It’s not a punishment. It’s responsible infrastructure.</p>

<blockquote>
  <p>💡 <strong>The rule:</strong> On HPC, the root account belongs to system administrators only. Your account is a <strong>regular user account</strong> — powerful enough to do everything you need, but sandboxed to protect everyone else.</p>
</blockquote>

<hr />

<h2 id="part-2-the-hpc-native-solution--environment-modules">Part 2: The HPC-Native Solution — Environment Modules</h2>

<p>Most HPC clusters come with a system called <strong>environment modules</strong> (sometimes called <code class="language-plaintext highlighter-rouge">lmod</code> or just <code class="language-plaintext highlighter-rouge">module</code>). System administrators pre-install common research software — samtools, bowtie2, STAR, Python, R, FastQC — and make them available through the module system.</p>

<p>You don’t install these tools. You <strong>load</strong> them when you need them.</p>

<h3 id="exploring-whats-available">Exploring What’s Available</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># See everything that's installed on the HPC</span>
module avail

<span class="c"># Filter by name (much more useful on real clusters)</span>
module avail samtools
module avail python
module avail kraken
</code></pre></div></div>

<p>On most academic clusters this will show a long list like:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>----------------- /software/modulefiles ----------------
samtools/1.15    samtools/1.17    samtools/1.18
python/3.9       python/3.11      python/3.12
bowtie2/2.4.5    bowtie2/2.5.1
fastqc/0.11.9    fastqc/0.12.1
kraken2/2.1.2    kraken2/2.1.3
star/2.7.10      star/2.7.11
</code></pre></div></div>

<h3 id="loading-and-using-a-module">Loading and Using a Module</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Load a specific version</span>
module load samtools/1.17

<span class="c"># Check it works</span>
samtools <span class="nt">--version</span>
<span class="c"># samtools 1.17</span>

<span class="c"># Load multiple tools</span>
module load samtools/1.17
module load bowtie2/2.5.1
module load fastqc/0.12.1

<span class="c"># See what you currently have loaded</span>
module list

<span class="c"># Unload a single module</span>
module unload samtools/1.17

<span class="c"># Unload everything and start fresh</span>
module purge
</code></pre></div></div>

<h3 id="why-version-numbers-matter">Why Version Numbers Matter</h3>

<p>Notice that you load <code class="language-plaintext highlighter-rouge">samtools/1.17</code> not just <code class="language-plaintext highlighter-rouge">samtools</code>. This is intentional and important. Different versions of the same tool can produce <strong>different results</strong> — a known issue in bioinformatics reproducibility. Always specify the version, and always record it in your methods section.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Vague — which version did you use? Nobody knows.</span>
module load samtools

<span class="c"># Explicit — anyone can reproduce this</span>
module load samtools/1.17
</code></pre></div></div>

<h3 id="loading-modules-in-job-scripts">Loading Modules in Job Scripts</h3>

<p>When you submit jobs to SLURM (covered in Day 3), your job script needs to load its own modules. Modules loaded in your terminal session are <strong>not automatically inherited</strong> by submitted jobs.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c">#SBATCH --job-name=align</span>
<span class="c">#SBATCH --cpus-per-task=8</span>
<span class="c">#SBATCH --mem=16G</span>

<span class="c"># Always load modules explicitly in job scripts</span>
module purge                    <span class="c"># start clean</span>
module load samtools/1.17
module load bowtie2/2.5.1

<span class="c"># Now run your analysis</span>
bowtie2 <span class="nt">-x</span> ref/genome <span class="nt">-U</span> sample.fastq.gz | samtools <span class="nb">sort</span> <span class="nt">-o</span> output.bam
</code></pre></div></div>

<h3 id="when-modules-fall-short">When Modules Fall Short</h3>

<p>The module system is great, but it has a gap: it only contains software that <strong>your HPC administrators have chosen to install</strong>. If you need:</p>

<ul>
  <li>A niche Python package (<code class="language-plaintext highlighter-rouge">scikit-bio</code>, <code class="language-plaintext highlighter-rouge">empress</code>, a custom analysis tool)</li>
  <li>A specific version not available in modules</li>
  <li>Something you developed yourself</li>
  <li>A tool that was just released last week</li>
</ul>

<p>…you need a different solution. That’s where <code class="language-plaintext highlighter-rouge">conda</code> comes in.</p>

<hr />

<h2 id="part-3-conda--your-personal-package-manager">Part 3: conda — Your Personal Package Manager</h2>

<p><code class="language-plaintext highlighter-rouge">conda</code> is a package manager that works entirely in <strong>user space</strong> — meaning it installs software into your home directory, not system directories. No root access required. It works identically on your laptop and on HPC.</p>

<p>If you haven’t used conda before, think of it like this:</p>

<blockquote>
  <p><strong>conda creates isolated “boxes” of software</strong> called environments. Each box has its own Python version and its own set of packages. You switch between boxes depending on which project you’re working on. Boxes can’t interfere with each other.</p>
</blockquote>

<p>This isolation is important. Without it:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Without environments, all packages pile up together</span>
pip <span class="nb">install </span><span class="nv">numpy</span><span class="o">==</span>1.24      <span class="c"># project A needs this</span>
pip <span class="nb">install </span><span class="nv">numpy</span><span class="o">==</span>1.26      <span class="c"># project B needs this — now project A breaks</span>
</code></pre></div></div>

<p>With conda environments, each project gets its own independent copy of everything.</p>

<h3 id="step-1-install-conda-if-you-havent-already">Step 1: Install conda (If You Haven’t Already)</h3>

<p>On your <strong>laptop</strong>, download and install <a href="https://docs.conda.io/en/latest/miniconda.html">Miniconda</a> — the lightweight version of conda.</p>

<p>On <strong>HPC</strong>, conda is often pre-installed or available as a module:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Check if conda is available on HPC</span>
which conda           <span class="c"># if this returns a path, you're good</span>
conda <span class="nt">--version</span>

<span class="c"># If not, load it as a module</span>
module load anaconda3
<span class="c"># or</span>
module load miniconda3
</code></pre></div></div>

<h3 id="step-2-create-your-first-environment">Step 2: Create Your First Environment</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create a new environment named 'bioenv' with Python 3.11</span>
conda create <span class="nt">-n</span> bioenv <span class="nv">python</span><span class="o">=</span>3.11

<span class="c"># You'll be asked to confirm: type 'y' and press Enter</span>
<span class="c"># Proceed ([y]/n)? y</span>
</code></pre></div></div>

<p>What just happened? conda created a brand new, isolated Python 3.11 installation in your home directory. Nothing on the system was changed.</p>

<h3 id="step-3-activate-the-environment">Step 3: Activate the Environment</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Activate (switch into) your environment</span>
conda activate bioenv

<span class="c"># Your prompt changes to show which environment is active:</span>
<span class="c"># (bioenv) user@hpc:~$</span>
</code></pre></div></div>

<p>That <code class="language-plaintext highlighter-rouge">(bioenv)</code> prefix tells you: everything you install now goes into this environment.</p>

<h3 id="step-4-install-your-bioinformatics-tools">Step 4: Install Your Bioinformatics Tools</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Install tools from the bioconda channel (bioinformatics-specific packages)</span>
conda <span class="nb">install</span> <span class="nt">-c</span> bioconda <span class="nt">-c</span> conda-forge samtools bowtie2 fastqc

<span class="c"># Install Python packages the same way</span>
conda <span class="nb">install</span> <span class="nt">-c</span> conda-forge biopython pandas matplotlib

<span class="c"># Or use pip inside your conda environment (fine for packages not on conda)</span>
pip <span class="nb">install </span>multiqc
</code></pre></div></div>

<blockquote>
  <p>💡 <strong>What is bioconda?</strong> It’s a community-maintained collection of bioinformatics tools packaged for conda. It has over 8,000 packages including virtually every bioinformatics tool you’ll ever need. Always add <code class="language-plaintext highlighter-rouge">-c bioconda -c conda-forge</code> when installing bioinformatics software.</p>
</blockquote>

<h3 id="step-5-verify-everything-works">Step 5: Verify Everything Works</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Still inside (bioenv)</span>
samtools <span class="nt">--version</span>
<span class="c"># samtools 1.17</span>

fastqc <span class="nt">--version</span>
<span class="c"># FastQC v0.12.1</span>

python <span class="nt">-c</span> <span class="s2">"import biopython; print('biopython OK')"</span>
<span class="c"># biopython OK</span>
</code></pre></div></div>

<h3 id="switching-between-environments">Switching Between Environments</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Deactivate current environment</span>
conda deactivate

<span class="c"># Create a second environment for a different project</span>
conda create <span class="nt">-n</span> rnaseq <span class="nv">python</span><span class="o">=</span>3.11
conda activate rnaseq
conda <span class="nb">install</span> <span class="nt">-c</span> bioconda star hisat2 salmon

<span class="c"># Switch back to the first</span>
conda deactivate
conda activate bioenv
</code></pre></div></div>

<p>Your two environments are completely independent. Installing something in <code class="language-plaintext highlighter-rouge">rnaseq</code> has zero effect on <code class="language-plaintext highlighter-rouge">bioenv</code>.</p>

<hr />

<h2 id="part-4-the-environmentyml-file--reproducibility-unlocked">Part 4: The environment.yml File — Reproducibility Unlocked</h2>

<p>Here’s a scenario that happens all the time:</p>

<blockquote>
  <p>You run your metagenomics pipeline, get results, write the paper. Three months later a reviewer asks you to re-run with a slight modification. You open your laptop. Python updated itself. A package auto-upgraded. The results are slightly different. The reviewer is unhappy.</p>
</blockquote>

<p>The fix is an <code class="language-plaintext highlighter-rouge">environment.yml</code> file. It’s a plain text file that records every package and its exact version in your environment.</p>

<h3 id="creating-an-environmentyml">Creating an environment.yml</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Activate your environment first</span>
conda activate bioenv

<span class="c"># Export the full environment spec to a file</span>
conda <span class="nb">env export</span> <span class="o">&gt;</span> environment.yml

<span class="c"># What it looks like:</span>
<span class="nb">cat </span>environment.yml
</code></pre></div></div>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">bioenv</span>
<span class="na">channels</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">bioconda</span>
  <span class="pi">-</span> <span class="s">conda-forge</span>
  <span class="pi">-</span> <span class="s">defaults</span>
<span class="na">dependencies</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">python=3.11.0</span>
  <span class="pi">-</span> <span class="s">samtools=1.17</span>
  <span class="pi">-</span> <span class="s">bowtie2=2.5.1</span>
  <span class="pi">-</span> <span class="s">fastqc=0.12.1</span>
  <span class="pi">-</span> <span class="s">biopython=1.83</span>
  <span class="pi">-</span> <span class="s">pandas=2.1.0</span>
  <span class="pi">-</span> <span class="s">matplotlib=3.8.0</span>
  <span class="pi">-</span> <span class="na">pip</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">multiqc==1.18</span>
</code></pre></div></div>

<h3 id="recreating-the-environment-anywhere">Recreating the Environment Anywhere</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># On a new machine, new HPC account, or a colleague's laptop:</span>
conda <span class="nb">env </span>create <span class="nt">-f</span> environment.yml

<span class="c"># Activate it</span>
conda activate bioenv

<span class="c"># Everything is identical — same versions, same behaviour</span>
</code></pre></div></div>

<p>This single file is what turns your analysis from <em>“it worked on my machine”</em> into <strong>reproducible science</strong>.</p>

<h3 id="a-minimal-environmentyml-better-practice">A Minimal environment.yml (Better Practice)</h3>

<p>The full export above includes every dependency of every dependency. That can cause issues when recreating across different operating systems. A cleaner approach is to write a minimal file with just your direct dependencies:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># environment.yml — keep this in your project git repo</span>
<span class="na">name</span><span class="pi">:</span> <span class="s">metagenomics-pipeline</span>
<span class="na">channels</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">bioconda</span>
  <span class="pi">-</span> <span class="s">conda-forge</span>
<span class="na">dependencies</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">python=3.11</span>
  <span class="pi">-</span> <span class="s">samtools=1.17</span>
  <span class="pi">-</span> <span class="s">bowtie2=2.5.1</span>
  <span class="pi">-</span> <span class="s">fastqc=0.12.1</span>
  <span class="pi">-</span> <span class="s">kraken2=2.1.3</span>
  <span class="pi">-</span> <span class="s">biopython=1.83</span>
  <span class="pi">-</span> <span class="s">pandas&gt;=2.0</span>
  <span class="pi">-</span> <span class="na">pip</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">multiqc==1.18</span>
</code></pre></div></div>

<p>Commit this file to your project’s Git repository. Anyone who clones your repo can recreate your exact environment in one command.</p>

<hr />

<h2 id="the-full-decision-tree">The Full Decision Tree</h2>

<p>When you need software on HPC, ask yourself:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Is it available as a module?
    ├── YES → module load toolname/version  ✓
    └── NO  → Is it on bioconda/conda-forge?
                  ├── YES → conda install -c bioconda toolname  ✓
                  └── NO  → pip install inside conda environment  ✓
                             (or contact your HPC admin to add a module)
</code></pre></div></div>

<p>In practice, for bioinformatics:</p>

<ul>
  <li><strong>Core alignment/assembly tools</strong> (samtools, bowtie2, STAR, GATK) → usually available as modules AND conda</li>
  <li><strong>Python packages</strong> (pandas, scikit-bio, biopython) → conda or pip</li>
  <li><strong>R packages</strong> → <code class="language-plaintext highlighter-rouge">install.packages()</code> or <code class="language-plaintext highlighter-rouge">BiocManager::install()</code> inside a loaded R module</li>
  <li><strong>Very new or niche tools</strong> → conda or build from source in your home directory</li>
</ul>

<hr />

<h2 id="common-mistakes-to-avoid">Common Mistakes to Avoid</h2>

<p><strong>Installing packages outside an environment</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ Bad — installs into your base conda, pollutes everything</span>
conda <span class="nb">install </span>samtools

<span class="c"># ✅ Good — always activate your environment first</span>
conda activate bioenv
conda <span class="nb">install </span>samtools
</code></pre></div></div>

<p><strong>Forgetting to load modules in your job script</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ This will fail — modules from your terminal don't carry over</span>
sbatch myjob.sh

<span class="c"># ✅ Always put module load commands inside the script itself</span>
</code></pre></div></div>

<p><strong>Mixing conda channels inconsistently</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ Can cause version conflicts</span>
conda <span class="nb">install</span> <span class="nt">-c</span> bioconda samtools
conda <span class="nb">install</span> <span class="nt">-c</span> defaults python

<span class="c"># ✅ Be explicit and consistent with channels</span>
conda <span class="nb">install</span> <span class="nt">-c</span> bioconda <span class="nt">-c</span> conda-forge samtools python
</code></pre></div></div>

<p><strong>Using <code class="language-plaintext highlighter-rouge">conda update --all</code> carelessly</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ⚠️ This upgrades everything in the environment — can break your pipeline</span>
conda update <span class="nt">--all</span>

<span class="c"># ✅ Update specific packages when you need to</span>
conda update samtools
</code></pre></div></div>

<hr />

<h2 id="try-it-yourself">Try It Yourself</h2>

<p>If you have HPC access, run these commands now:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. Explore what modules are available</span>
module avail 2&gt;&amp;1 | <span class="nb">grep</span> <span class="nt">-i</span> samtools

<span class="c"># 2. Load a module and check it</span>
module load samtools/1.17
samtools <span class="nt">--version</span>
module purge

<span class="c"># 3. Create a test conda environment</span>
conda create <span class="nt">-n</span> testenv <span class="nv">python</span><span class="o">=</span>3.11 <span class="nt">-y</span>
conda activate testenv
conda <span class="nb">install</span> <span class="nt">-c</span> bioconda fastqc <span class="nt">-y</span>
fastqc <span class="nt">--version</span>

<span class="c"># 4. Export it</span>
conda <span class="nb">env export</span> <span class="o">&gt;</span> testenv.yml
<span class="nb">cat </span>testenv.yml

<span class="c"># 5. Clean up</span>
conda deactivate
conda <span class="nb">env </span>remove <span class="nt">-n</span> testenv
</code></pre></div></div>

<hr />

<h2 id="summary">Summary</h2>

<ul>
  <li><code class="language-plaintext highlighter-rouge">sudo</code> is deliberately disabled on HPC — one user with root could break the system for everyone</li>
  <li><strong>Environment modules</strong> (<code class="language-plaintext highlighter-rouge">module load</code>) give you access to software pre-installed by HPC admins — always specify the version</li>
  <li><strong>conda</strong> lets you install anything you want without root access, works identically on laptop and HPC</li>
  <li><strong><code class="language-plaintext highlighter-rouge">environment.yml</code></strong> records your exact environment so any analysis can be reproduced by anyone, anywhere</li>
  <li>The golden workflow: test your conda environment on your laptop, then recreate it on HPC with <code class="language-plaintext highlighter-rouge">conda env create -f environment.yml</code></li>
</ul>

<hr />

<h2 id="up-next">Up Next</h2>

<p><strong>Day 3: Running One Job — Terminal vs SLURM</strong></p>

<p>Your software is installed and your environment is ready. Now: how do you actually run something on HPC? Spoiler — you can’t just type <code class="language-plaintext highlighter-rouge">fastqc sample.fastq.gz</code> and hit Enter. We’ll cover why, and walk through writing your first SLURM job script from scratch.</p>

<hr />

<p><img src="/assets/img/d2-hpc.png" alt="Laptop vs HPC: Scaling Computational Biology" /></p>]]></content><author><name></name></author><category term="HPC" /><category term="conda" /><category term="modules" /><category term="sudo" /><category term="bioinformatics" /><category term="beginners" /><category term="reproducibility" /><category term="computational-biology" /><summary type="html"><![CDATA[The first real shock of HPC life: you can't install software the way you're used to. Here's why, and three progressively better solutions — explained step by step for beginners.]]></summary></entry><entry><title type="html">Day 1 — Laptop vs HPC: Why Bioinformatics Needs Both</title><link href="https://jojyjohn28.github.io/blog/day1-laptop-vs-hpc/" rel="alternate" type="text/html" title="Day 1 — Laptop vs HPC: Why Bioinformatics Needs Both" /><published>2026-03-12T00:00:00-04:00</published><updated>2026-03-12T00:00:00-04:00</updated><id>https://jojyjohn28.github.io/blog/day1-laptop-vs-hpc</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/day1-laptop-vs-hpc/"><![CDATA[<h1 id="day-1-laptop-vs-hpc--why-bioinformatics-needs-both">Day 1: Laptop vs HPC — Why Bioinformatics Needs Both</h1>

<blockquote>
  <p><em>This is Day 1 of a 6-part series: <strong>From Laptop to HPC: Scaling Computational Biology Workflows</strong>. Each post covers a real challenge you’ll face when moving from local development to high-performance computing. No prior HPC experience needed</em> and 🧬 <strong>Day 58</strong> of Daily Bioinformatics from Jojy’s Desk Dataset:</p>
</blockquote>

<hr />

<h2 id="it-started-with-a-crash">It Started With a Crash</h2>

<p>I was processing a microbiome dataset — 50 samples, nothing extraordinary. I fired up my script, walked to the kitchen to make coffee, came back, and saw this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Killed: out of memory
RAM used: 31.8 GB / 32 GB
</code></pre></div></div>

<p>My laptop had literally run out of memory trying to do bioinformatics work that, in the field, is considered <em>routine</em>.</p>

<p>That moment is what most computational biologists experience eventually. Your laptop is powerful, but genomics data doesn’t care about your RAM. And that’s exactly why you need to understand both worlds: <strong>your local machine</strong> and <strong>an HPC cluster</strong>.</p>

<p>By the end of this post, you’ll know exactly what each one is, when to use which, and why they work together — not against each other.</p>

<hr />

<h2 id="what-youll-need-for-this-series">What You’ll Need for This Series</h2>

<p>Before we dive in, here’s what’s assumed:</p>

<ul>
  <li>You know basic command line (you can <code class="language-plaintext highlighter-rouge">cd</code>, <code class="language-plaintext highlighter-rouge">ls</code>, and run a script)</li>
  <li>You have some bioinformatics background (you know what FASTQ, FASTA, or BAM files are)</li>
  <li>You do <strong>not</strong> need any prior HPC or cluster experience — that’s what this series is for</li>
</ul>

<hr />

<h2 id="what-is-a-local-machine">What Is a Local Machine?</h2>

<p>Your laptop or workstation is your <strong>personal computing environment</strong>. When you type a command, it runs immediately. When you install software with <code class="language-plaintext highlighter-rouge">sudo apt-get install</code>, it installs on <em>your</em> system. You own it completely.</p>

<p>Here’s a realistic picture of what a modern research laptop looks like:</p>

<table>
  <thead>
    <tr>
      <th>Resource</th>
      <th>Typical Range</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>RAM (Memory)</td>
      <td>8 – 32 GB</td>
    </tr>
    <tr>
      <td>CPU cores</td>
      <td>4 – 16</td>
    </tr>
    <tr>
      <td>Storage</td>
      <td>500 GB – 4 TB</td>
    </tr>
    <tr>
      <td>Root access (<code class="language-plaintext highlighter-rouge">sudo</code>)</td>
      <td>✅ Yes</td>
    </tr>
    <tr>
      <td>Job scheduler</td>
      <td>❌ None — you run commands directly</td>
    </tr>
    <tr>
      <td>Cost</td>
      <td>One-time, yours forever</td>
    </tr>
  </tbody>
</table>

<p><strong>The laptop is great for:</strong></p>

<ul>
  <li>Writing and testing scripts</li>
  <li>Exploring small datasets</li>
  <li>Developing analysis pipelines</li>
  <li>Running quick quality checks</li>
</ul>

<p><strong>The laptop struggles with:</strong></p>

<ul>
  <li>Datasets larger than your available RAM</li>
  <li>Jobs that take days to run</li>
  <li>Processing dozens or hundreds of samples at once</li>
  <li>Analyses that need specialized hardware (high-memory nodes, GPU)</li>
</ul>

<blockquote>
  <p>💡 <strong>Think of your laptop like a personal car.</strong> Fast, flexible, goes wherever you point it. But you can’t move 10,000 packages with it.</p>
</blockquote>

<hr />

<h2 id="what-is-an-hpc-cluster">What Is an HPC Cluster?</h2>

<p>An <strong>HPC (High-Performance Computing) cluster</strong> is a collection of many powerful computers — called <strong>nodes</strong> — connected together via a high-speed network and managed by a central system.</p>

<p>When you log into an HPC, you’re not logging into one computer. You’re logging into a shared ecosystem of computing resources that can be allocated to your jobs on demand.</p>

<p>Here’s what a typical academic HPC cluster looks like:</p>

<table>
  <thead>
    <tr>
      <th>Resource</th>
      <th>Typical Range</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>RAM per node</td>
      <td>256 GB – 4 TB</td>
    </tr>
    <tr>
      <td>CPU cores per node</td>
      <td>32 – 128</td>
    </tr>
    <tr>
      <td>Number of nodes</td>
      <td>100 – 1,000+</td>
    </tr>
    <tr>
      <td>Total storage</td>
      <td>Petabytes (shared filesystem)</td>
    </tr>
    <tr>
      <td>Root access (<code class="language-plaintext highlighter-rouge">sudo</code>)</td>
      <td>❌ No</td>
    </tr>
    <tr>
      <td>Job scheduler</td>
      <td>✅ Yes (SLURM, PBS, LSF)</td>
    </tr>
    <tr>
      <td>Cost</td>
      <td>Shared institutional resource</td>
    </tr>
  </tbody>
</table>

<blockquote>
  <p>💡 <strong>Think of an HPC like a shipping fleet.</strong> You don’t own it. You book capacity. It can move massive loads in parallel that no personal car ever could.</p>
</blockquote>

<h3 id="how-does-access-work">How Does Access Work?</h3>

<p>You typically access HPC clusters through:</p>

<ol>
  <li><strong>SSH</strong> — a secure terminal connection: <code class="language-plaintext highlighter-rouge">ssh username@hpc.university.edu</code></li>
  <li><strong>A login node</strong> — a shared entry point where you prepare and submit jobs (not where you run them — more on this in Day 3!)</li>
  <li><strong>A job scheduler</strong> — software like SLURM that manages who gets what resources and when</li>
</ol>

<hr />

<h2 id="why-does-bioinformatics-specifically-need-hpc">Why Does Bioinformatics <em>Specifically</em> Need HPC?</h2>

<p>Not every field runs into this problem. A data scientist might work comfortably with gigabytes of tabular data for years. Bioinformatics is different because of the nature of the data itself.</p>

<h3 id="the-data-is-enormous">The Data Is Enormous</h3>

<table>
  <thead>
    <tr>
      <th>Data Type</th>
      <th>Typical Size per Sample</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Whole Genome Sequencing (WGS)</td>
      <td>50 – 100+ GB raw reads</td>
    </tr>
    <tr>
      <td>Metagenomics (gut microbiome)</td>
      <td>5 – 20 GB per sample</td>
    </tr>
    <tr>
      <td>RNA-seq (transcriptomics)</td>
      <td>1 – 5 GB per sample</td>
    </tr>
    <tr>
      <td>Single-cell RNA-seq</td>
      <td>10 – 50 GB per experiment</td>
    </tr>
  </tbody>
</table>

<p>Now multiply any of those by 100 samples. You’re looking at terabytes of data, and processing each sample can require gigabytes of RAM <em>simultaneously</em>.</p>

<h3 id="reference-databases-are-huge">Reference Databases Are Huge</h3>

<p>Tools like Kraken2 (metagenomic classification) or STAR (RNA-seq alignment) require loading enormous reference databases into memory:</p>

<ul>
  <li>Kraken2 standard database: <strong>~100 GB RAM</strong> just to load</li>
  <li>STAR human genome index: <strong>~30 GB RAM</strong></li>
  <li>DIAMOND protein database: <strong>~200+ GB RAM</strong></li>
</ul>

<p>Your laptop never had a chance.</p>

<h3 id="many-samples--many-jobs">Many Samples = Many Jobs</h3>

<p>In a real study, you don’t process one sample. You process cohorts:</p>

<ul>
  <li>50 patients? That’s 50 alignment jobs.</li>
  <li>200 microbiome samples? 200 assembly + annotation pipelines.</li>
  <li>1,000-sample GWAS study? You need parallel computing or you’ll be waiting months.</li>
</ul>

<p>Running these sequentially on a laptop isn’t just slow — it’s often practically impossible.</p>

<hr />

<h2 id="head-to-head-comparison">Head-to-Head Comparison</h2>

<p>Here’s the full picture side by side:</p>

<table>
  <thead>
    <tr>
      <th>Feature</th>
      <th>Your Laptop</th>
      <th>HPC Cluster</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>RAM</td>
      <td>16–32 GB</td>
      <td>256 GB – 4 TB per node</td>
    </tr>
    <tr>
      <td>CPUs</td>
      <td>4–16 cores</td>
      <td>32–128+ cores per node</td>
    </tr>
    <tr>
      <td>Storage</td>
      <td>1–4 TB</td>
      <td>Petabyte-scale shared FS</td>
    </tr>
    <tr>
      <td>Root access</td>
      <td>✅ <code class="language-plaintext highlighter-rouge">sudo</code> works</td>
      <td>❌ No <code class="language-plaintext highlighter-rouge">sudo</code></td>
    </tr>
    <tr>
      <td>Install software</td>
      <td><code class="language-plaintext highlighter-rouge">apt-get</code>, <code class="language-plaintext highlighter-rouge">pip</code> freely</td>
      <td><code class="language-plaintext highlighter-rouge">module load</code> or <code class="language-plaintext highlighter-rouge">conda</code></td>
    </tr>
    <tr>
      <td>Run a command</td>
      <td>Just type it</td>
      <td>Submit a job script</td>
    </tr>
    <tr>
      <td>Run 200 samples</td>
      <td>Days (sequential)</td>
      <td>Hours (parallel)</td>
    </tr>
    <tr>
      <td>Internet access</td>
      <td>Full</td>
      <td>Often restricted</td>
    </tr>
    <tr>
      <td>Cost</td>
      <td>Your machine</td>
      <td>Shared institutional</td>
    </tr>
    <tr>
      <td>Best for</td>
      <td>Development, testing</td>
      <td>Production runs</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="you-need-both--heres-why">You Need Both — Here’s Why</h2>

<p>A common mistake beginners make is thinking HPC <em>replaces</em> their laptop. It doesn’t. They serve fundamentally different purposes.</p>

<p><strong>Use your laptop for:</strong></p>

<ul>
  <li>Writing scripts and code</li>
  <li>Testing on a small subset of data (5–10 samples)</li>
  <li>Visualizing results in R or Python</li>
  <li>Quick data exploration</li>
</ul>

<p><strong>Use HPC for:</strong></p>

<ul>
  <li>Running validated pipelines on full datasets</li>
  <li>Any job needing &gt;32 GB RAM</li>
  <li>Parallelizing across many samples</li>
  <li>Long-running jobs (hours to days)</li>
</ul>

<p>The workflow that actually works in practice looks like this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Write script on laptop
          ↓
Test on 2–3 samples locally
          ↓
Transfer data + script to HPC
          ↓
Submit job to scheduler
          ↓
Download/analyze results locally
</code></pre></div></div>

<p>This back-and-forth is not a limitation — it’s the correct workflow.</p>

<hr />

<h2 id="a-note-on-what-changes-and-what-doesnt">A Note on What Changes (and What Doesn’t)</h2>

<p>Moving to HPC doesn’t mean learning an entirely new way to do bioinformatics. The tools are the same: <code class="language-plaintext highlighter-rouge">samtools</code>, <code class="language-plaintext highlighter-rouge">bowtie2</code>, <code class="language-plaintext highlighter-rouge">fastqc</code>, <code class="language-plaintext highlighter-rouge">kraken2</code>. The concepts are the same. What changes is:</p>

<ol>
  <li><strong>How you install software</strong> — no <code class="language-plaintext highlighter-rouge">sudo</code>, but there are solutions (Day 2)</li>
  <li><strong>How you run jobs</strong> — not directly, but through a scheduler (Day 3)</li>
  <li><strong>How you scale</strong> — not for loops, but job arrays (Day 4)</li>
  <li><strong>How you structure pipelines</strong> — towards reproducibility (Day 5)</li>
</ol>

<p>Each of those changes is the topic of the next four posts in this series.</p>

<hr />

<h2 id="common-beginner-mistakes-on-day-one">Common Beginner Mistakes on Day One</h2>

<p>Watch out for these when you first log into an HPC:</p>

<ul>
  <li><strong>Running compute jobs on the login node</strong> — this is like blocking the office entrance to do your work. Don’t do it. (Covered in Day 3)</li>
  <li><strong>Trying to <code class="language-plaintext highlighter-rouge">sudo apt-get install</code></strong> — you’ll get “Permission denied”. There’s a better way. (Covered in Day 2)</li>
  <li><strong>Expecting interactive terminal behavior</strong> — HPC jobs run in the background. You submit and wait. (Day 3)</li>
  <li><strong>Not checking available storage quotas</strong> — most HPC systems have per-user storage limits. Check early.</li>
</ul>

<hr />

<h2 id="connecting-from-windows-powershell-and-mobaxterm">Connecting from Windows: PowerShell and MobaXterm</h2>

<p>If you’re on a Mac or Linux laptop, SSH just works in your terminal. If you’re on <strong>Windows</strong>, you have two excellent options — and you only need to pick one.</p>

<h3 id="option-1-windows-powershell-built-in-no-install-needed">Option 1: Windows PowerShell (Built-in, No Install Needed)</h3>

<p>Modern Windows 10 and 11 include a built-in SSH client. Open PowerShell (search for it in the Start menu) and connect exactly like you would on Linux:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Connect to your HPC</span><span class="w">
</span><span class="n">ssh</span><span class="w"> </span><span class="nx">username</span><span class="err">@</span><span class="nx">hpc.yourinstitution.edu</span><span class="w">

</span><span class="c"># Transfer a file to HPC</span><span class="w">
</span><span class="n">scp</span><span class="w"> </span><span class="nx">myfile.fastq.gz</span><span class="w"> </span><span class="nx">username</span><span class="err">@</span><span class="nx">hpc.yourinstitution.edu:~/data/</span><span class="w">

</span><span class="c"># Transfer a folder from HPC back to your Windows machine</span><span class="w">
</span><span class="n">scp</span><span class="w"> </span><span class="nt">-r</span><span class="w"> </span><span class="nx">username</span><span class="err">@</span><span class="nx">hpc.yourinstitution.edu:~/results/</span><span class="w"> </span><span class="nx">C:\Users\YourName\Desktop\</span><span class="w">
</span></code></pre></div></div>

<p>PowerShell SSH is clean, fast, and requires zero setup. For most day-to-day HPC work, it’s all you need.</p>

<blockquote>
  <p>💡 <strong>Check your SSH version:</strong> Open PowerShell and type <code class="language-plaintext highlighter-rouge">ssh -V</code>. If you see <code class="language-plaintext highlighter-rouge">OpenSSH_8</code> or higher, you’re good to go. If not, update Windows or install OpenSSH from Settings → Optional Features.</p>
</blockquote>

<h3 id="option-2-mobaxterm-recommended-for-beginners">Option 2: MobaXterm (Recommended for Beginners)</h3>

<p><a href="https://mobaxterm.mobatek.net/">MobaXterm</a> is a free Windows application that bundles SSH, file transfer, and a graphical interface all in one. It’s the most popular HPC client among Windows-based researchers for good reason.</p>

<p><strong>Why researchers love MobaXterm:</strong></p>

<ul>
  <li><strong>Built-in file browser</strong> — drag and drop files between your laptop and HPC</li>
  <li><strong>Graphical display (X11)</strong> — run GUI tools on the HPC and see them on your Windows screen</li>
  <li><strong>Saved sessions</strong> — save your HPC login details so you don’t retype them every time</li>
  <li><strong>Multiple tabs</strong> — open several HPC connections simultaneously</li>
  <li><strong>SFTP sidebar</strong> — automatically shows your HPC files in a panel on the left</li>
</ul>

<p><strong>Getting started with MobaXterm:</strong></p>

<ol>
  <li>Download the free Home Edition from <a href="https://mobaxterm.mobatek.net/">mobaxterm.mobatek.net</a></li>
  <li>Open it and click <strong>Session</strong> → <strong>SSH</strong></li>
  <li>Enter your HPC hostname (e.g., <code class="language-plaintext highlighter-rouge">hpc.yourinstitution.edu</code>) and username</li>
  <li>Click OK — you’re in</li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Once connected in MobaXterm, everything works the same as Linux/Mac</span>
ssh username@hpc.yourinstitution.edu   <span class="c"># or use the GUI session manager</span>

<span class="c"># The left panel automatically opens an SFTP browser</span>
<span class="c"># Drag files from your Windows desktop straight into ~/data/</span>
</code></pre></div></div>

<h3 id="which-should-you-use">Which Should You Use?</h3>

<table>
  <thead>
    <tr>
      <th> </th>
      <th>PowerShell</th>
      <th>MobaXterm</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Install required</td>
      <td>❌ No</td>
      <td>✅ Yes (free)</td>
    </tr>
    <tr>
      <td>File transfer GUI</td>
      <td>❌ Command only</td>
      <td>✅ Drag and drop</td>
    </tr>
    <tr>
      <td>X11 / GUI forwarding</td>
      <td>⚠️ Needs extra setup</td>
      <td>✅ Built-in</td>
    </tr>
    <tr>
      <td>Best for</td>
      <td>Quick connections, scripting</td>
      <td>Beginners, file management</td>
    </tr>
  </tbody>
</table>

<p>If you’re just getting started and you’re on Windows, <strong>download MobaXterm</strong>. It removes a lot of friction in the early days. Once you’re comfortable with the command line, PowerShell works great for quick connections.</p>

<hr />

<h2 id="try-it-yourself">Try It Yourself</h2>

<p>If you already have HPC access, try these commands right now:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Log in (replace with your actual HPC address)</span>
ssh username@hpc.yourinstitution.edu

<span class="c"># Check the system</span>
<span class="nb">hostname</span>                    <span class="c"># What node am I on?</span>
<span class="nb">cat</span> /proc/cpuinfo | <span class="nb">grep</span> <span class="s2">"model name"</span> | <span class="nb">head</span> <span class="nt">-1</span>   <span class="c"># CPU info</span>
free <span class="nt">-h</span>                     <span class="c"># Available memory</span>
<span class="nb">df</span> <span class="nt">-h</span> <span class="nv">$HOME</span>                 <span class="c"># Your storage quota</span>

<span class="c"># See what compute nodes look like</span>
sinfo                       <span class="c"># Show available partitions (SLURM)</span>
</code></pre></div></div>

<p>Don’t worry if <code class="language-plaintext highlighter-rouge">sinfo</code> shows a lot of unfamiliar output. By Day 3, it’ll make complete sense.</p>

<hr />

<h2 id="summary">Summary</h2>

<ul>
  <li>Your <strong>laptop</strong> is fast, flexible, and you have full control — but it has hard limits on RAM and can’t run hundreds of jobs in parallel</li>
  <li>An <strong>HPC cluster</strong> gives you enormous compute power but requires learning new tools and workflows</li>
  <li>Bioinformatics specifically hits laptop limits because of large reference databases, multi-GB sample files, and the need to process large cohorts in parallel</li>
  <li>You need <strong>both</strong>: laptop for development and testing, HPC for production runs</li>
  <li>Moving to HPC changes <em>how</em> you run things — not the bioinformatics tools themselves</li>
</ul>

<hr />

<h2 id="up-next">Up Next</h2>

<p><strong>Day 2: Software Installation — <code class="language-plaintext highlighter-rouge">sudo</code> vs <code class="language-plaintext highlighter-rouge">module load</code> vs <code class="language-plaintext highlighter-rouge">conda</code></strong></p>

<p>We’ll tackle the first real surprise of HPC life: you can’t install software the way you’re used to. No <code class="language-plaintext highlighter-rouge">sudo</code>. No <code class="language-plaintext highlighter-rouge">apt-get</code>. But there are elegant solutions — and one of them works identically on both your laptop and HPC.</p>

<hr />

<p><em>Questions or corrections? Drop a comment below — I read every one.</em></p>

<hr />

<p><img src="/assets/img/hpc-d1.png" alt="Laptop vs HPC: Scaling Computational Biology" /></p>]]></content><author><name></name></author><category term="HPC" /><category term="bioinformatics" /><category term="genomics" /><category term="metagenomics" /><category term="beginners" /><category term="SLURM" /><category term="computational-biology" /><category term="linux" /><summary type="html"><![CDATA[Your laptop crashed on a 50-sample microbiome run. Here's what that means, why it happens, and how HPC clusters solve it — explained from scratch for beginners.]]></summary></entry><entry><title type="html">Day 8 — From Raw Data to Paper Figures: A Complete Microbiome Stats Workflow</title><link href="https://jojyjohn28.github.io/blog/day8-complete-microbiome-stats-workflow/" rel="alternate" type="text/html" title="Day 8 — From Raw Data to Paper Figures: A Complete Microbiome Stats Workflow" /><published>2026-03-11T00:00:00-04:00</published><updated>2026-03-11T00:00:00-04:00</updated><id>https://jojyjohn28.github.io/blog/day8-complete-microbiome-stats-workflow</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/day8-complete-microbiome-stats-workflow/"><![CDATA[<h4 id="day-7--from-raw-data-to-paper-figures-a-complete-microbiome-stats-workflow">Day 7 — From Raw Data to Paper Figures: A Complete Microbiome Stats Workflow</h4>

<p>*Day 7 of 7 in the series <strong>Applied Statistics for Microbiome &amp; Genomics Data.*</strong> &amp; 🧬 Day 57 of Daily Bioinformatics from Jojy’s Desk
<em>Dataset: 61 MAGs × 27 metagenomes, Chesapeake Bay + Delaware Bay.</em>
<em>Paper: John et al. 2026, ISME Communications — Functional redundancy and metabolic flexibility of microbial communities in two Mid-Atlantic bays.</em>
<em>All R code: <a href="https://github.com/jojyjohn28/microbiome_stats">github.com/jojyjohn28/microbiome_stats</a></em></p>

<hr />

<p>Days 1–6 introduced six methods. Each was presented as its own lesson. But in a real analysis — one that ends in a published paper — they don’t run in isolation. They run in sequence, each answer feeding the next question.</p>

<p>This is the post I wish I had before starting. Not “here is how PERMANOVA works,” but: <strong>here is the exact order you run everything, the biological question that justifies each step, and what you do when each test tells you something unexpected.</strong></p>

<p>The workflow below is the one I used for our recent ISME Communications paper. I’ll show you how each method connects, where the Mantel test fits as a bridge between community structure and environment, how I structure the stats section of a paper, and end with a reusable checklist for every future project.</p>

<hr />

<h4 id="the-core-principle-let-the-biology-drive-the-statistics">The Core Principle: Let the Biology Drive the Statistics</h4>

<p>The single most common mistake in microbiome data analysis is running every available test and reporting the ones that are significant. This produces p-value mining, not science.</p>

<p>Every step in this workflow is anchored to a biological question. If you cannot write the question in plain English before running the test, do not run it.</p>

<p>The questions I started with, directly from our paper’s hypotheses:</p>

<ol>
  <li>Does community composition differ across environmental conditions (season, salinity, bay)?</li>
  <li>Which environmental gradients structure the community — and how much variance do they explain?</li>
  <li>Is community dissimilarity correlated with environmental dissimilarity?</li>
  <li>Which individual MAGs are enriched under which conditions, and how large are those effects?</li>
  <li>How does MAG abundance change continuously along salinity and temperature gradients?</li>
  <li>Do MAGs form coherent ecological guilds, and which environmental driver controls each guild?</li>
  <li>What environmental variables predict functional redundancy, and does the answer differ between potential and expressed FRed?</li>
</ol>

<p>One question, one method. Seven questions, seven methods.</p>

<hr />

<h4 id="the-full-workflow">The Full Workflow</h4>

<h5 id="stage-1--data-preparation-before-any-statistics">Stage 1 — Data preparation (before any statistics)</h5>

<p>This stage has no p-values. It is entirely about understanding your data before you model it. Skip it and every downstream result is suspect.</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">library</span><span class="p">(</span><span class="n">vegan</span><span class="p">)</span><span class="w">
</span><span class="n">library</span><span class="p">(</span><span class="n">ggplot2</span><span class="p">)</span><span class="w">
</span><span class="n">library</span><span class="p">(</span><span class="n">patchwork</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── Load ──────────────────────────────────────────────────────</span><span class="w">
</span><span class="n">metadata</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">read.csv</span><span class="p">(</span><span class="s2">"../day1/data/metadata.csv"</span><span class="p">,</span><span class="w">
                     </span><span class="n">header</span><span class="o">=</span><span class="kc">TRUE</span><span class="p">,</span><span class="w"> </span><span class="n">row.names</span><span class="o">=</span><span class="m">1</span><span class="p">)</span><span class="w">
</span><span class="n">mag_raw</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">read.csv</span><span class="p">(</span><span class="s2">"../day1/data/mag_abundance.csv"</span><span class="p">,</span><span class="w">
                     </span><span class="n">header</span><span class="o">=</span><span class="kc">TRUE</span><span class="p">,</span><span class="w"> </span><span class="n">row.names</span><span class="o">=</span><span class="m">1</span><span class="p">)</span><span class="w">
</span><span class="n">mag_t</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">t</span><span class="p">(</span><span class="n">mag_raw</span><span class="p">)</span><span class="w">               </span><span class="c1"># samples × MAGs</span><span class="w">

</span><span class="c1">## ── Always align rows before anything else ────────────────────</span><span class="w">
</span><span class="n">metadata</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">metadata</span><span class="p">[</span><span class="n">match</span><span class="p">(</span><span class="n">rownames</span><span class="p">(</span><span class="n">mag_t</span><span class="p">),</span><span class="w"> </span><span class="n">rownames</span><span class="p">(</span><span class="n">metadata</span><span class="p">)),</span><span class="w"> </span><span class="p">]</span><span class="w">
</span><span class="n">stopifnot</span><span class="p">(</span><span class="nf">all</span><span class="p">(</span><span class="n">rownames</span><span class="p">(</span><span class="n">mag_t</span><span class="p">)</span><span class="w"> </span><span class="o">==</span><span class="w"> </span><span class="n">rownames</span><span class="p">(</span><span class="n">metadata</span><span class="p">)))</span><span class="w">
</span><span class="n">cat</span><span class="p">(</span><span class="s2">"n ="</span><span class="p">,</span><span class="w"> </span><span class="n">nrow</span><span class="p">(</span><span class="n">mag_t</span><span class="p">),</span><span class="w"> </span><span class="s2">"| MAGs ="</span><span class="p">,</span><span class="w"> </span><span class="n">ncol</span><span class="p">(</span><span class="n">mag_t</span><span class="p">),</span><span class="w"> </span><span class="s2">"\n"</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── Assess sparsity ───────────────────────────────────────────</span><span class="w">
</span><span class="n">zero_frac</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="nf">sum</span><span class="p">(</span><span class="n">mag_t</span><span class="w"> </span><span class="o">==</span><span class="w"> </span><span class="m">0</span><span class="p">)</span><span class="w"> </span><span class="o">/</span><span class="w"> </span><span class="nf">length</span><span class="p">(</span><span class="n">mag_t</span><span class="p">)</span><span class="w">
</span><span class="n">cat</span><span class="p">(</span><span class="s2">"Zero fraction:"</span><span class="p">,</span><span class="w"> </span><span class="nf">round</span><span class="p">(</span><span class="n">zero_frac</span><span class="w"> </span><span class="o">*</span><span class="w"> </span><span class="m">100</span><span class="p">,</span><span class="w"> </span><span class="m">1</span><span class="p">),</span><span class="w"> </span><span class="s2">"%\n"</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── Distribution check ────────────────────────────────────────</span><span class="w">
</span><span class="c1"># Visualise raw abundance distribution — expect right-skewed</span><span class="w">
</span><span class="n">hist</span><span class="p">(</span><span class="n">log1p</span><span class="p">(</span><span class="n">as.vector</span><span class="p">(</span><span class="n">mag_t</span><span class="p">)),</span><span class="w"> </span><span class="n">breaks</span><span class="o">=</span><span class="m">50</span><span class="p">,</span><span class="w">
     </span><span class="n">xlab</span><span class="o">=</span><span class="s2">"log(abundance+1)"</span><span class="p">,</span><span class="w"> </span><span class="n">main</span><span class="o">=</span><span class="s2">"MAG abundance distribution"</span><span class="p">)</span><span class="w">
</span></code></pre></div></div>

<p>What you are checking:</p>

<p>▶ Does the row alignment hold? If metadata and abundance rows are out of order, every downstream result will be silently wrong.</p>

<p>▶ How sparse is the matrix? Our MAG data had ~68% zeros — common for genome-resolved metagenomics. This rules out parametric tests on raw counts.</p>

<p>▶ What does the abundance distribution look like? Right-skewed with zero inflation confirms you need Hellinger or log transformation before any Euclidean-distance-based method.</p>

<hr />

<h5 id="stage-2--community-composition-ordination-rdadbrda">Stage 2 — Community composition: ordination (RDA/dbRDA)</h5>

<p><strong>Biological question:</strong> Which environmental variables explain the most variation in overall community structure, and in what direction do they pull the community in multivariate space?</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## ── Hellinger transformation ──────────────────────────────────</span><span class="w">
</span><span class="n">hell</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">decostand</span><span class="p">(</span><span class="n">mag_t</span><span class="p">,</span><span class="w"> </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"hellinger"</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── RDA: unconstrained first to see variance structure ────────</span><span class="w">
</span><span class="n">pca</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">rda</span><span class="p">(</span><span class="n">hell</span><span class="p">)</span><span class="w">
</span><span class="n">screeplot</span><span class="p">(</span><span class="n">pca</span><span class="p">,</span><span class="w"> </span><span class="n">bstick</span><span class="o">=</span><span class="kc">TRUE</span><span class="p">)</span><span class="w">          </span><span class="c1"># how many axes worth keeping?</span><span class="w">

</span><span class="c1">## ── dbRDA: constrain on environmental variables ───────────────</span><span class="w">
</span><span class="n">env_vars</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">metadata</span><span class="p">[,</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="s2">"Salinity"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Temperature"</span><span class="p">,</span><span class="w"> </span><span class="s2">"ChlA"</span><span class="p">,</span><span class="w">
                          </span><span class="s2">"Bacterial_Production"</span><span class="p">)]</span><span class="w">
</span><span class="n">env_sc</span><span class="w">   </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">scale</span><span class="p">(</span><span class="n">env_vars</span><span class="p">)</span><span class="w">

</span><span class="n">dbrda</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">dbrda</span><span class="p">(</span><span class="n">hell</span><span class="w"> </span><span class="o">~</span><span class="w"> </span><span class="n">.</span><span class="p">,</span><span class="w"> </span><span class="n">data</span><span class="o">=</span><span class="n">as.data.frame</span><span class="p">(</span><span class="n">env_sc</span><span class="p">),</span><span class="w">
                </span><span class="n">dist</span><span class="o">=</span><span class="s2">"bray"</span><span class="p">)</span><span class="w">
</span><span class="n">anova</span><span class="p">(</span><span class="n">dbrda</span><span class="p">,</span><span class="w"> </span><span class="n">by</span><span class="o">=</span><span class="s2">"margin"</span><span class="p">,</span><span class="w"> </span><span class="n">permutations</span><span class="o">=</span><span class="m">999</span><span class="p">)</span><span class="w">
</span><span class="n">RsquareAdj</span><span class="p">(</span><span class="n">dbrda</span><span class="p">)</span><span class="w">
</span></code></pre></div></div>

<p>In our paper, the dbRDA confirmed that season (operating through its correlated gradients — temperature and salinity) structured the community along the first constrained axis, with bay identity adding a secondary axis. This plot became Figure 1B and defined the framing for all downstream tests: <strong>the dominant gradient is seasonal/salinity, not spatial.</strong></p>

<p>Once you have the ordination, the next question becomes: is that visual separation statistically supported?</p>

<hr />

<h5 id="stage-3--permanova-is-the-separation-real">Stage 3 — PERMANOVA: is the separation real?</h5>

<p><strong>Biological question:</strong> Do communities differ significantly between predefined groups, and how much of total variance is attributable to each factor?</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## ── Bray-Curtis dissimilarity ─────────────────────────────────</span><span class="w">
</span><span class="n">bc</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">vegdist</span><span class="p">(</span><span class="n">hell</span><span class="p">,</span><span class="w"> </span><span class="n">method</span><span class="o">=</span><span class="s2">"bray"</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── Always check dispersion first ────────────────────────────</span><span class="w">
</span><span class="c1"># If groups differ in spread, PERMANOVA p-values can be misleading</span><span class="w">
</span><span class="n">disp</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">betadisper</span><span class="p">(</span><span class="n">bc</span><span class="p">,</span><span class="w"> </span><span class="n">metadata</span><span class="o">$</span><span class="n">Season</span><span class="p">)</span><span class="w">
</span><span class="n">anova</span><span class="p">(</span><span class="n">disp</span><span class="p">)</span><span class="w">          </span><span class="c1"># want p &gt; 0.05</span><span class="w">

</span><span class="c1">## ── PERMANOVA ─────────────────────────────────────────────────</span><span class="w">
</span><span class="n">set.seed</span><span class="p">(</span><span class="m">42</span><span class="p">)</span><span class="w">
</span><span class="n">perm</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">adonis2</span><span class="p">(</span><span class="n">bc</span><span class="w"> </span><span class="o">~</span><span class="w"> </span><span class="n">Season</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">Bay</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">Size_Fraction</span><span class="p">,</span><span class="w">
                 </span><span class="n">data</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">metadata</span><span class="p">,</span><span class="w">
                 </span><span class="n">permutations</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">999</span><span class="p">,</span><span class="w">
                 </span><span class="n">by</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"margin"</span><span class="p">)</span><span class="w">
</span><span class="n">print</span><span class="p">(</span><span class="n">perm</span><span class="p">)</span><span class="w">
</span><span class="c1"># Season: R² = 0.325, p = 0.001 ***</span><span class="w">
</span><span class="c1"># Bay:    R² = 0.049, p = 0.121 ns</span><span class="w">
</span><span class="c1"># SF:     R² = 0.019, p = 0.550 ns</span><span class="w">
</span></code></pre></div></div>

<p>The PERMANOVA result (Season R² = 0.325, p = 0.001) does two things: it validates the ordination visually, and it anchors the variance explained number in the paper. We can now say — with a defensible test — that <strong>season explains 32.5% of variation in MAG community composition</strong>, more than bay identity and size fraction combined.</p>

<p>But PERMANOVA treats season as a discrete label. It cannot tell you <em>which</em> environmental gradient along the seasonal axis drives the pattern. That requires the Mantel test.</p>

<hr />

<h5 id="stage-4--mantel-test-the-bridge-between-community-and-environment">Stage 4 — Mantel test: the bridge between community and environment</h5>

<p><strong>Biological question:</strong> Is the dissimilarity between communities correlated with the dissimilarity between their environmental contexts? If so, which environmental distance matrix best predicts community distance?</p>

<p>This is the step most microbiome workflows skip. It matters because PERMANOVA tests group membership; the Mantel test tests gradients. The two can give different answers, and both are informative.</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">library</span><span class="p">(</span><span class="n">vegan</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── Community distance ────────────────────────────────────────</span><span class="w">
</span><span class="n">bc</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">vegdist</span><span class="p">(</span><span class="n">hell</span><span class="p">,</span><span class="w"> </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"bray"</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── Environmental distance matrices ──────────────────────────</span><span class="w">
</span><span class="c1"># Single-variable: does salinity distance predict community distance?</span><span class="w">
</span><span class="n">sal_dist</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">dist</span><span class="p">(</span><span class="n">scale</span><span class="p">(</span><span class="n">metadata</span><span class="o">$</span><span class="n">Salinity</span><span class="p">))</span><span class="w">
</span><span class="n">temp_dist</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">dist</span><span class="p">(</span><span class="n">scale</span><span class="p">(</span><span class="n">metadata</span><span class="o">$</span><span class="n">Temperature</span><span class="p">))</span><span class="w">

</span><span class="c1"># Multivariate: full environmental dissimilarity</span><span class="w">
</span><span class="n">env_full</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">scale</span><span class="p">(</span><span class="n">metadata</span><span class="p">[,</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="s2">"Salinity"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Temperature"</span><span class="p">,</span><span class="w">
                                  </span><span class="s2">"ChlA"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Bacterial_Production"</span><span class="p">,</span><span class="w">
                                  </span><span class="s2">"Nitrate"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Phosphate"</span><span class="p">)])</span><span class="w">
</span><span class="n">env_dist</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">dist</span><span class="p">(</span><span class="n">env_full</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── Mantel tests ──────────────────────────────────────────────</span><span class="w">
</span><span class="n">set.seed</span><span class="p">(</span><span class="m">42</span><span class="p">)</span><span class="w">
</span><span class="n">mantel</span><span class="p">(</span><span class="n">bc</span><span class="p">,</span><span class="w"> </span><span class="n">sal_dist</span><span class="p">,</span><span class="w">  </span><span class="n">method</span><span class="o">=</span><span class="s2">"pearson"</span><span class="p">,</span><span class="w"> </span><span class="n">permutations</span><span class="o">=</span><span class="m">9999</span><span class="p">)</span><span class="w">
</span><span class="c1"># r = 0.61  p = 0.0001</span><span class="w">
</span><span class="n">mantel</span><span class="p">(</span><span class="n">bc</span><span class="p">,</span><span class="w"> </span><span class="n">temp_dist</span><span class="p">,</span><span class="w"> </span><span class="n">method</span><span class="o">=</span><span class="s2">"pearson"</span><span class="p">,</span><span class="w"> </span><span class="n">permutations</span><span class="o">=</span><span class="m">9999</span><span class="p">)</span><span class="w">
</span><span class="c1"># r = 0.58  p = 0.0001</span><span class="w">
</span><span class="n">mantel</span><span class="p">(</span><span class="n">bc</span><span class="p">,</span><span class="w"> </span><span class="n">env_dist</span><span class="p">,</span><span class="w">  </span><span class="n">method</span><span class="o">=</span><span class="s2">"pearson"</span><span class="p">,</span><span class="w"> </span><span class="n">permutations</span><span class="o">=</span><span class="m">9999</span><span class="p">)</span><span class="w">
</span><span class="c1"># r = 0.70  p = 0.0001</span><span class="w">

</span><span class="c1">## ── Partial Mantel: salinity after controlling for temperature ─</span><span class="w">
</span><span class="n">mantel.partial</span><span class="p">(</span><span class="n">bc</span><span class="p">,</span><span class="w"> </span><span class="n">sal_dist</span><span class="p">,</span><span class="w"> </span><span class="n">temp_dist</span><span class="p">,</span><span class="w">
               </span><span class="n">method</span><span class="o">=</span><span class="s2">"pearson"</span><span class="p">,</span><span class="w"> </span><span class="n">permutations</span><span class="o">=</span><span class="m">9999</span><span class="p">)</span><span class="w">
</span><span class="c1"># r = 0.31  p = 0.008</span><span class="w">
</span><span class="c1"># Salinity still explains unique variance in community structure</span><span class="w">
</span><span class="c1"># even after removing the shared variance with temperature</span><span class="w">
</span></code></pre></div></div>

<p>The Mantel test confirmed that community distance is strongly correlated with environmental distance (r = 0.70, p &lt; 0.0001). The partial Mantel test showed that salinity explains unique variance in community structure (r = 0.31, p = 0.008) beyond its correlation with temperature — directly supporting the regression results from Day 5.</p>

<p><strong>How to report a Mantel test in a paper:</strong></p>

<blockquote>
  <p>“Bray-Curtis community dissimilarity was significantly correlated with multivariate environmental distance (Mantel r = 0.70, p &lt; 0.001, 9999 permutations). Partial Mantel analysis showed that salinity retained a significant partial correlation with community dissimilarity after controlling for temperature (r = 0.31, p = 0.008), consistent with salinity as an independent driver of community composition.”</p>
</blockquote>

<hr />

<h5 id="stage-5--differential-abundance-which-mags-how-big">Stage 5 — Differential abundance: which MAGs, how big</h5>

<p><strong>Biological question:</strong> Which individual MAGs are enriched in one condition versus another, and what is the magnitude of that enrichment?</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">library</span><span class="p">(</span><span class="n">coin</span><span class="p">)</span><span class="w">    </span><span class="c1"># wilcox_test() with exact p-values</span><span class="w">

</span><span class="n">mag_log</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">log1p</span><span class="p">(</span><span class="n">mag_t</span><span class="p">)</span><span class="w">
</span><span class="n">seasons</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">factor</span><span class="p">(</span><span class="n">metadata</span><span class="o">$</span><span class="n">Season</span><span class="p">)</span><span class="w">
</span><span class="n">mag_names</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">colnames</span><span class="p">(</span><span class="n">mag_log</span><span class="p">)</span><span class="w">

</span><span class="n">results_wilcox</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">do.call</span><span class="p">(</span><span class="n">rbind</span><span class="p">,</span><span class="w"> </span><span class="n">lapply</span><span class="p">(</span><span class="n">mag_names</span><span class="p">,</span><span class="w"> </span><span class="k">function</span><span class="p">(</span><span class="n">mag</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="w">
  </span><span class="n">x_spring</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">mag_log</span><span class="p">[</span><span class="n">seasons</span><span class="w"> </span><span class="o">==</span><span class="w"> </span><span class="s2">"Spring"</span><span class="p">,</span><span class="w"> </span><span class="n">mag</span><span class="p">]</span><span class="w">
  </span><span class="n">x_summer</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">mag_log</span><span class="p">[</span><span class="n">seasons</span><span class="w"> </span><span class="o">==</span><span class="w"> </span><span class="s2">"Summer"</span><span class="p">,</span><span class="w"> </span><span class="n">mag</span><span class="p">]</span><span class="w">

  </span><span class="n">wt</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">wilcox.test</span><span class="p">(</span><span class="n">x_summer</span><span class="p">,</span><span class="w"> </span><span class="n">x_spring</span><span class="p">,</span><span class="w"> </span><span class="n">exact</span><span class="o">=</span><span class="kc">FALSE</span><span class="p">)</span><span class="w">
  </span><span class="n">n1</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="nf">length</span><span class="p">(</span><span class="n">x_summer</span><span class="p">);</span><span class="w"> </span><span class="n">n2</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="nf">length</span><span class="p">(</span><span class="n">x_spring</span><span class="p">)</span><span class="w">
  </span><span class="n">U</span><span class="w">     </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">wt</span><span class="o">$</span><span class="n">statistic</span><span class="w">
  </span><span class="n">delta</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="p">(</span><span class="n">U</span><span class="w"> </span><span class="o">/</span><span class="w"> </span><span class="p">(</span><span class="n">n1</span><span class="w"> </span><span class="o">*</span><span class="w"> </span><span class="n">n2</span><span class="p">))</span><span class="w"> </span><span class="o">*</span><span class="w"> </span><span class="m">2</span><span class="w"> </span><span class="o">-</span><span class="w"> </span><span class="m">1</span><span class="w">   </span><span class="c1"># Cliff's delta</span><span class="w">

  </span><span class="n">data.frame</span><span class="p">(</span><span class="n">MAG</span><span class="w">       </span><span class="o">=</span><span class="w"> </span><span class="n">mag</span><span class="p">,</span><span class="w">
             </span><span class="n">p_value</span><span class="w">   </span><span class="o">=</span><span class="w"> </span><span class="n">wt</span><span class="o">$</span><span class="n">p.value</span><span class="p">,</span><span class="w">
             </span><span class="n">cliffs_d</span><span class="w">  </span><span class="o">=</span><span class="w"> </span><span class="n">delta</span><span class="p">,</span><span class="w">
             </span><span class="n">direction</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">ifelse</span><span class="p">(</span><span class="n">delta</span><span class="w"> </span><span class="o">&gt;</span><span class="w"> </span><span class="m">0</span><span class="p">,</span><span class="w"> </span><span class="s2">"Summer"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Spring"</span><span class="p">))</span><span class="w">
</span><span class="p">}))</span><span class="w">

</span><span class="n">results_wilcox</span><span class="o">$</span><span class="n">q_value</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">p.adjust</span><span class="p">(</span><span class="n">results_wilcox</span><span class="o">$</span><span class="n">p_value</span><span class="p">,</span><span class="w">
                                    </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"BH"</span><span class="p">)</span><span class="w">
</span><span class="n">n_sig</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="nf">sum</span><span class="p">(</span><span class="n">results_wilcox</span><span class="o">$</span><span class="n">q_value</span><span class="w"> </span><span class="o">&lt;</span><span class="w"> </span><span class="m">0.05</span><span class="p">)</span><span class="w">
</span><span class="n">cat</span><span class="p">(</span><span class="s2">"Season-significant MAGs (q&lt;0.05):"</span><span class="p">,</span><span class="w"> </span><span class="n">n_sig</span><span class="p">,</span><span class="w"> </span><span class="s2">"of"</span><span class="p">,</span><span class="w">
    </span><span class="nf">length</span><span class="p">(</span><span class="n">mag_names</span><span class="p">),</span><span class="w"> </span><span class="s2">"\n"</span><span class="p">)</span><span class="w">
</span><span class="c1"># 40 of 61</span><span class="w">

</span><span class="c1"># Top hits — all Summer-enriched, Cliff's δ = +1.00 (perfect separation)</span><span class="w">
</span><span class="n">head</span><span class="p">(</span><span class="n">results_wilcox</span><span class="p">[</span><span class="n">order</span><span class="p">(</span><span class="n">results_wilcox</span><span class="o">$</span><span class="n">q_value</span><span class="p">),</span><span class="w"> </span><span class="p">],</span><span class="w"> </span><span class="m">10</span><span class="p">)</span><span class="w">
</span></code></pre></div></div>

<p>The Wilcoxon + Cliff’s delta combination answers a question PERMANOVA cannot: not just “do seasons differ” but “which MAGs are responsible and how completely are they partitioned?” All top 40 season-significant MAGs were Summer-enriched. Cliff’s δ = +1.00 for the top hits means complete rank separation — not a single Spring sample exceeded a Summer sample in abundance for those MAGs.</p>

<p>In the paper, this result connected directly to the FRed analysis: MAGs enriched in summer were part of the high-FRed community driving the seasonal FRed increase.</p>

<hr />

<h5 id="stage-6--regression-how-much-does-abundance-change-per-unit-environment">Stage 6 — Regression: how much does abundance change per unit environment</h5>

<p><strong>Biological question:</strong> How does MAG abundance change quantitatively as a continuous environmental variable changes, and does that relationship hold after controlling for correlated predictors?</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## ── Fit additive models for all 61 MAGs ──────────────────────</span><span class="w">
</span><span class="n">results_reg</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">do.call</span><span class="p">(</span><span class="n">rbind</span><span class="p">,</span><span class="w"> </span><span class="n">lapply</span><span class="p">(</span><span class="n">mag_names</span><span class="p">,</span><span class="w"> </span><span class="k">function</span><span class="p">(</span><span class="n">mag</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="w">
  </span><span class="n">df_m</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">cbind</span><span class="p">(</span><span class="n">metadata</span><span class="p">,</span><span class="w"> </span><span class="n">abundance</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">mag_log</span><span class="p">[,</span><span class="w"> </span><span class="n">mag</span><span class="p">])</span><span class="w">
  </span><span class="n">m</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">lm</span><span class="p">(</span><span class="n">abundance</span><span class="w"> </span><span class="o">~</span><span class="w"> </span><span class="n">Salinity</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">Temperature</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">Season_num</span><span class="p">,</span><span class="w">
              </span><span class="n">data</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">df_m</span><span class="p">)</span><span class="w">
  </span><span class="n">sm</span><span class="w">   </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">summary</span><span class="p">(</span><span class="n">m</span><span class="p">)</span><span class="w">
  </span><span class="n">cf</span><span class="w">   </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">coef</span><span class="p">(</span><span class="n">sm</span><span class="p">)</span><span class="w">
  </span><span class="n">data.frame</span><span class="p">(</span><span class="w">
    </span><span class="n">MAG</span><span class="w">       </span><span class="o">=</span><span class="w"> </span><span class="n">mag</span><span class="p">,</span><span class="w">
    </span><span class="n">R2</span><span class="w">        </span><span class="o">=</span><span class="w"> </span><span class="n">sm</span><span class="o">$</span><span class="n">r.squared</span><span class="p">,</span><span class="w">
    </span><span class="n">adj_R2</span><span class="w">    </span><span class="o">=</span><span class="w"> </span><span class="n">sm</span><span class="o">$</span><span class="n">adj.r.squared</span><span class="p">,</span><span class="w">
    </span><span class="n">beta_sal</span><span class="w">  </span><span class="o">=</span><span class="w"> </span><span class="n">cf</span><span class="p">[</span><span class="s2">"Salinity"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Estimate"</span><span class="p">],</span><span class="w">
    </span><span class="n">p_sal</span><span class="w">     </span><span class="o">=</span><span class="w"> </span><span class="n">cf</span><span class="p">[</span><span class="s2">"Salinity"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Pr(&gt;|t|)"</span><span class="p">],</span><span class="w">
    </span><span class="n">beta_temp</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">cf</span><span class="p">[</span><span class="s2">"Temperature"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Estimate"</span><span class="p">],</span><span class="w">
    </span><span class="n">p_temp</span><span class="w">    </span><span class="o">=</span><span class="w"> </span><span class="n">cf</span><span class="p">[</span><span class="s2">"Temperature"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Pr(&gt;|t|)"</span><span class="p">]</span><span class="w">
  </span><span class="p">)</span><span class="w">
</span><span class="p">}))</span><span class="w">

</span><span class="n">results_reg</span><span class="o">$</span><span class="n">q_sal</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">p.adjust</span><span class="p">(</span><span class="n">results_reg</span><span class="o">$</span><span class="n">p_sal</span><span class="p">,</span><span class="w">  </span><span class="s2">"BH"</span><span class="p">)</span><span class="w">
</span><span class="n">results_reg</span><span class="o">$</span><span class="n">q_temp</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">p.adjust</span><span class="p">(</span><span class="n">results_reg</span><span class="o">$</span><span class="n">p_temp</span><span class="p">,</span><span class="w"> </span><span class="s2">"BH"</span><span class="p">)</span><span class="w">

</span><span class="n">cat</span><span class="p">(</span><span class="s2">"Salinity-significant MAGs (q&lt;0.05):"</span><span class="p">,</span><span class="w">
    </span><span class="nf">sum</span><span class="p">(</span><span class="n">results_reg</span><span class="o">$</span><span class="n">q_sal</span><span class="w"> </span><span class="o">&lt;</span><span class="w"> </span><span class="m">0.05</span><span class="p">),</span><span class="w"> </span><span class="s2">"\n"</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── For our paper: regression of FRed against environment ─────</span><span class="w">
</span><span class="c1"># (from John et al. 2026, Table 1 and Table S5)</span><span class="w">
</span><span class="c1"># Potential FRed ~ temperature + season + salinity + size_fraction</span><span class="w">
</span><span class="n">m_fred</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">lm</span><span class="p">(</span><span class="n">Potential_FRed</span><span class="w"> </span><span class="o">~</span><span class="w"> </span><span class="n">Temperature</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">Season</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">Salinity</span><span class="w"> </span><span class="o">+</span><span class="w">
               </span><span class="n">Size_Fraction</span><span class="p">,</span><span class="w"> </span><span class="n">data</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">metadata</span><span class="p">)</span><span class="w">
</span><span class="n">summary</span><span class="p">(</span><span class="n">m_fred</span><span class="p">)</span><span class="w">
</span><span class="c1"># Adj R² = 0.76, F = 8.52, p &lt; 0.0001</span><span class="w">
</span><span class="c1"># Temperature: p &lt; 0.007 ***</span><span class="w">
</span><span class="c1"># Season:      p &lt; 0.001 ***</span><span class="w">
</span><span class="c1"># Salinity:    p = 0.037 *</span><span class="w">
</span><span class="c1"># Size Frac:   p &lt; 0.001 ***</span><span class="w">

</span><span class="c1"># Expressed FRed ~ salinity + ammonium</span><span class="w">
</span><span class="n">m_exp</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">lm</span><span class="p">(</span><span class="n">Expressed_FRed</span><span class="w"> </span><span class="o">~</span><span class="w"> </span><span class="n">Salinity</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">Ammonium</span><span class="p">,</span><span class="w"> </span><span class="n">data</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">metadata</span><span class="p">)</span><span class="w">
</span><span class="n">summary</span><span class="p">(</span><span class="n">m_exp</span><span class="p">)</span><span class="w">
</span><span class="c1"># Adj R² = 0.39, F = 5.01, p &lt; 0.012</span><span class="w">
</span></code></pre></div></div>

<p>This regression framework — the same one from Day 5, applied here to functional redundancy rather than just individual MAG abundance — is one of the core results in the paper. Potential FRed was explained by a model that accounted for 76% of variance, with temperature, season, salinity, and size fraction all as significant predictors. Expressed FRed was harder to predict (38% variance explained), with salinity and ammonium as the only significant drivers — a biologically interpretable result that points to nutrient limitation suppressing non-essential metabolic expression.</p>

<hr />

<h5 id="stage-7--wgcna-from-individual-mags-to-ecological-guilds">Stage 7 — WGCNA: from individual MAGs to ecological guilds</h5>

<p><strong>Biological question:</strong> Do the MAGs significant in previous steps form coherent co-abundance modules, and which single environmental driver controls each module as a unit?</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">library</span><span class="p">(</span><span class="n">WGCNA</span><span class="p">)</span><span class="w">
</span><span class="n">options</span><span class="p">(</span><span class="n">stringsAsFactors</span><span class="o">=</span><span class="kc">FALSE</span><span class="p">)</span><span class="w">
</span><span class="n">enableWGCNAThreads</span><span class="p">()</span><span class="w">

</span><span class="n">hell</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">decostand</span><span class="p">(</span><span class="n">mag_t</span><span class="p">,</span><span class="w"> </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"hellinger"</span><span class="p">)</span><span class="w">

</span><span class="c1"># Soft threshold selection → β = 6</span><span class="w">
</span><span class="n">adjacency</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">adjacency</span><span class="p">(</span><span class="n">hell</span><span class="p">,</span><span class="w"> </span><span class="n">power</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">6</span><span class="p">,</span><span class="w"> </span><span class="n">type</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"signed"</span><span class="p">)</span><span class="w">
</span><span class="n">TOM</span><span class="w">       </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">TOMsimilarity</span><span class="p">(</span><span class="n">adjacency</span><span class="p">,</span><span class="w"> </span><span class="n">TOMType</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"signed"</span><span class="p">)</span><span class="w">
</span><span class="n">dissTOM</span><span class="w">   </span><span class="o">&lt;-</span><span class="w"> </span><span class="m">1</span><span class="w"> </span><span class="o">-</span><span class="w"> </span><span class="n">TOM</span><span class="w">

</span><span class="n">TaxaTree</span><span class="w">     </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">hclust</span><span class="p">(</span><span class="n">as.dist</span><span class="p">(</span><span class="n">dissTOM</span><span class="p">),</span><span class="w"> </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"average"</span><span class="p">)</span><span class="w">
</span><span class="n">dynamicMods</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">cutreeDynamic</span><span class="p">(</span><span class="n">TaxaTree</span><span class="p">,</span><span class="w"> </span><span class="n">distM</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">dissTOM</span><span class="p">,</span><span class="w">
                               </span><span class="n">deepSplit</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">2</span><span class="p">,</span><span class="w"> </span><span class="n">minClusterSize</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">5</span><span class="p">)</span><span class="w">
</span><span class="n">moduleColors</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">labels2colors</span><span class="p">(</span><span class="n">dynamicMods</span><span class="p">)</span><span class="w">
</span><span class="n">merge</span><span class="w">        </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">mergeCloseModules</span><span class="p">(</span><span class="n">hell</span><span class="p">,</span><span class="w"> </span><span class="n">moduleColors</span><span class="p">,</span><span class="w">
                                   </span><span class="n">cutHeight</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">0.25</span><span class="p">)</span><span class="w">
</span><span class="n">moduleColors</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">merge</span><span class="o">$</span><span class="n">colors</span><span class="w">

</span><span class="c1"># Eigengene–trait correlation</span><span class="w">
</span><span class="n">MEs</span><span class="w">         </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">orderMEs</span><span class="p">(</span><span class="n">moduleEigengenes</span><span class="p">(</span><span class="n">hell</span><span class="p">,</span><span class="w"> </span><span class="n">moduleColors</span><span class="p">)</span><span class="o">$</span><span class="n">eigengenes</span><span class="p">)</span><span class="w">
</span><span class="n">modTraitCor</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">cor</span><span class="p">(</span><span class="n">MEs</span><span class="p">,</span><span class="w"> </span><span class="n">datTraits</span><span class="p">,</span><span class="w"> </span><span class="n">use</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"p"</span><span class="p">)</span><span class="w">
</span><span class="c1"># Blue module (41 MAGs): r = -0.97 with Temperature</span><span class="w">
</span><span class="c1"># Brown module (4 MAGs):  r = -0.89 with Salinity</span><span class="w">
</span></code></pre></div></div>

<p>WGCNA revealed that 41 of the 40 Wilcoxon-significant MAGs belong to a single module — the blue module — with a module eigengene that correlates at r = −0.97 with temperature. What appeared as 40 independent seasonal signals in Stage 5 is, in network terms, one coordinated guild responding to one driver. This is the kind of biological synthesis that only becomes visible when you move from individual tests to the full workflow.</p>

<hr />

<h5 id="stage-8--correlation-analysis-of-functional-redundancy">Stage 8 — Correlation analysis of functional redundancy</h5>

<p><strong>Biological question:</strong> Is functional redundancy correlated with species richness and functional diversity? Do potential and expressed FRed respond differently to environmental variables?</p>

<p>This stage is specific to our paper but illustrates a general principle: after the community-level and taxon-level analyses are complete, you sometimes need a dedicated analysis for derived ecological metrics.</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## ── FRed was computed using the Adiv package ─────────────────</span><span class="w">
</span><span class="c1"># FRed = uniqueness() from Adiv, based on Euclidean distance</span><span class="w">
</span><span class="c1"># between MAG trait profiles (187 metabolic genes)</span><span class="w">

</span><span class="n">library</span><span class="p">(</span><span class="n">adiv</span><span class="p">)</span><span class="w">

</span><span class="c1"># Trait table: MAGs × metabolic marker genes (METABOLIC V4.0 HMM hits)</span><span class="w">
</span><span class="c1"># One row per MAG, one column per gene (binary or count)</span><span class="w">
</span><span class="n">trait_table</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">read.csv</span><span class="p">(</span><span class="s2">"results/metabolic_trait_table.csv"</span><span class="p">,</span><span class="w">
                         </span><span class="n">row.names</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">1</span><span class="p">)</span><span class="w">
</span><span class="n">trait_dist</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">dist</span><span class="p">(</span><span class="n">trait_table</span><span class="p">,</span><span class="w"> </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"euclidean"</span><span class="p">)</span><span class="w">

</span><span class="c1"># FRed per sample: weighted by MAG abundance</span><span class="w">
</span><span class="c1"># uniqueness() = 1 - average uniqueness → ranges 0 (all distinct)</span><span class="w">
</span><span class="c1">#                                                 to 1 (all identical)</span><span class="w">
</span><span class="c1"># fred_results: samples × FRed values</span><span class="w">

</span><span class="c1">## ── FRed correlations ─────────────────────────────────────────</span><span class="w">
</span><span class="c1"># Pearson r: FRed vs species richness</span><span class="w">
</span><span class="n">cor.test</span><span class="p">(</span><span class="n">fred_df</span><span class="o">$</span><span class="n">Potential_FRed</span><span class="p">,</span><span class="w"> </span><span class="n">fred_df</span><span class="o">$</span><span class="n">Species_Richness</span><span class="p">,</span><span class="w">
         </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"pearson"</span><span class="p">)</span><span class="w">
</span><span class="c1"># r = 0.47, R² = 0.22, p = 0.037</span><span class="w">

</span><span class="c1"># Pearson r: FRed vs functional diversity (Q)</span><span class="w">
</span><span class="n">cor.test</span><span class="p">(</span><span class="n">fred_df</span><span class="o">$</span><span class="n">Potential_FRed</span><span class="p">,</span><span class="w"> </span><span class="n">fred_df</span><span class="o">$</span><span class="n">Q</span><span class="p">,</span><span class="w">
         </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"pearson"</span><span class="p">)</span><span class="w">
</span><span class="c1"># r = 0.13, R² = 0.02, p = 0.58   (non-significant)</span><span class="w">

</span><span class="c1">## ── Key environmental correlations ───────────────────────────</span><span class="w">
</span><span class="c1"># Table 1 from paper:</span><span class="w">
</span><span class="c1"># Potential FRed ~ Temperature: PA: R²=0.651, p=.041</span><span class="w">
</span><span class="c1">#                               FL: R²=0.912, p=.0001</span><span class="w">
</span><span class="c1"># Expressed FRed ~ Salinity:    PA: R²=0.654, p=.04</span><span class="w">
</span><span class="c1"># Expressed FRed ~ Nitrate:     FL: R²=−0.949, p=.005</span><span class="w">

</span><span class="c1">## ── Wilcoxon tests: season and size fraction effects ──────────</span><span class="w">
</span><span class="n">wilcox.test</span><span class="p">(</span><span class="n">Potential_FRed</span><span class="w"> </span><span class="o">~</span><span class="w"> </span><span class="n">Season</span><span class="p">,</span><span class="w"> </span><span class="n">data</span><span class="o">=</span><span class="n">fred_df</span><span class="p">)</span><span class="w">
</span><span class="c1"># p &lt; 0.002: FRed higher in Summer than Spring in both bays</span><span class="w">

</span><span class="n">wilcox.test</span><span class="p">(</span><span class="n">Potential_FRed</span><span class="w"> </span><span class="o">~</span><span class="w"> </span><span class="n">Size_Fraction</span><span class="p">,</span><span class="w"> </span><span class="n">data</span><span class="o">=</span><span class="n">fred_df</span><span class="p">)</span><span class="w">
</span><span class="c1"># p &lt; 0.04: PA community higher FRed than FL community</span><span class="w">
</span></code></pre></div></div>

<p>The FRed correlation analysis produced a biologically nuanced result: FRed is moderately correlated with species richness (r = 0.47) but not with functional diversity (r = 0.13). This means communities with more species tend toward more redundancy, but communities with higher functional diversity are not necessarily more redundant — a distinction with direct implications for ecosystem stability under perturbation.</p>

<hr />

<h2 id="how-i-structure-the-statistics-section-of-a-paper">How I Structure the Statistics Section of a Paper</h2>

<p>After running the above workflow, the challenge becomes translating it into a methods section that is reproducible and a results section that tells a coherent story. Here is the exact structure I used for John et al. 2026.</p>

<p><strong>Methods — order of presentation:</strong></p>

<ol>
  <li>Data preprocessing and transformation (Hellinger, log1p — justify each)</li>
  <li>Ordination (RDA/dbRDA — state the dissimilarity metric and transformation)</li>
  <li>Community-level tests (betadisper + PERMANOVA — always report both)</li>
  <li>Mantel tests (state number of permutations, correction method)</li>
  <li>Differential abundance (Wilcoxon + Cliff’s delta + BH correction — report n significant / n tested)</li>
  <li>Regression (model formula, diagnostics run, whether VIF was checked)</li>
  <li>Network analysis (WGCNA — β value, cutHeight, minClusterSize — all three)</li>
  <li>FRed quantification (Adiv package, trait table construction, normalization)</li>
  <li>FRed statistics (linear regression, Wilcoxon, correlation — state which variables were removed for collinearity)</li>
</ol>

<p><strong>Results — order of presentation:</strong></p>

<p>Lead with the community-level result (PERMANOVA: Season R² = 0.325). This orients the reader — the community differs by season, and season explains more variance than any other factor. Then zoom in: Mantel confirms it tracks the environmental gradient. Then zoom in further: Wilcoxon identifies the individual drivers. Then zoom out conceptually: WGCNA shows those drivers are one coherent guild. Then shift to the functional layer: regression on FRed shows how the community-level pattern translates to ecosystem process stability.</p>

<p>The flow is: <strong>community → environment bridge → taxa → guilds → function.</strong></p>

<hr />

<h2 id="the-reusable-analysis-checklist">The Reusable Analysis Checklist</h2>

<p>Copy this into every new project. Check each box before moving to the next step.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PRE-ANALYSIS
□ Row alignment: metadata and abundance rows match (stopifnot)
□ Zero fraction reported: know your sparsity before choosing tests
□ Abundance distribution visualised: skewness, outliers
□ Transformation chosen with justification (not just "we used Hellinger")
□ Biological questions written out in plain English — one per test

COMMUNITY STRUCTURE
□ PCoA or RDA plotted — visual inspection before any tests
□ betadisper checked before PERMANOVA — dispersion not significantly different
□ PERMANOVA run with marginal SS (by="margin") — not sequential
□ R² and p reported for every factor, not just significant ones
□ Pairwise PERMANOVA only run if overall test is significant

ENVIRONMENTAL LINKAGE
□ Mantel test run: community distance ~ environmental distance
□ Partial Mantel run for key predictors to isolate independent effects
□ Number of permutations stated (≥999; ≥9999 for final publication)

DIFFERENTIAL ABUNDANCE
□ BH FDR correction applied across all tests, not per comparison
□ Effect size (Cliff's delta or similar) reported alongside p-value
□ Direction of enrichment stated explicitly
□ n significant / n tested reported (not just "many were significant")

REGRESSION
□ Response variable log-transformed and distribution checked
□ VIF checked — no predictor &gt; 5–10
□ All four diagnostic plots examined (residuals, QQ, scale-location, histogram)
□ Shapiro-Wilk and Breusch-Pagan formal tests run
□ Coefficients translated into biological units (not just β = 0.27)
□ Adjusted R² reported, not R²

WGCNA
□ Soft threshold chosen from scale-free topology plot — not copied from tutorial
□ mergeCloseModules run — cutHeight justified
□ Grey module investigated, not discarded
□ Module-trait heatmap is the primary results figure, not the dendrogram alone

REPORTING
□ Every p-value accompanied by effect size
□ Multiple testing correction method stated for each set of tests
□ R package versions recorded (sessionInfo())
□ Seed set for all permutation tests (set.seed())
□ Code deposited before submission
</code></pre></div></div>

<hr />

<h2 id="common-pitfalls-of-the-full-workflow">Common Pitfalls of the Full Workflow</h2>

<p>▶ <strong>Running every test available.</strong> If you run PERMANOVA, Wilcoxon, regression, Mantel, and WGCNA without a connecting hypothesis, you are generating a list of numbers, not a scientific argument. Each test should answer a question the previous test raised.</p>

<p>▶ <strong>Using PERMANOVA without betadisper.</strong> PERMANOVA tests centroid differences and dispersion differences simultaneously. If your groups differ in spread rather than location, the significant p-value is misleading. Always run <code class="language-plaintext highlighter-rouge">betadisper</code> first and report the result, even if it is non-significant.</p>

<p>▶ <strong>Treating the Mantel test as redundant.</strong> “We already have PERMANOVA” is not a reason to skip Mantel. PERMANOVA tests discrete group membership. Mantel tests whether the continuous gradient in community composition tracks the continuous gradient in environment. These answer different questions and can produce opposite answers in the same dataset.</p>

<p>▶ <strong>Reporting R² without translating coefficients.</strong> R² = 0.76 in the FRed model sounds impressive. But the biological interpretation — temperature alone accounts for the dominant share of that variance (p &lt; 0.007, FL fraction R² = 0.91) — is what makes the result ecologically meaningful. Always translate.</p>

<p>▶ <strong>Skipping the FRed correlation with richness.</strong> In our data, FRed correlated with species richness (r = 0.47) but not with functional diversity (r = 0.13). If we had only reported the regression model, we would have missed this: more diverse communities are not necessarily more functionally diverse — they are more redundant. That distinction matters for resilience prediction.</p>

<p>▶ <strong>Concluding that high FRed = high resilience.</strong> It does not, necessarily. High FRed means many taxa share functions — but if a perturbation selects against the shared function itself (rather than individual taxa), high FRed provides no buffer. Our paper explicitly discusses this: low FRed spring communities, which show higher functional diversity, may actually be more vulnerable to functional losses despite their diversity.</p>

<hr />

<h2 id="the-complete-series-in-one-paragraph">The Complete Series in One Paragraph</h2>

<p>Day 1 established why normal statistics fail on microbiome data. Day 2 showed how to visualise community structure with ordination. Day 3 tested whether that structure is real with PERMANOVA. Day 4 identified which individual MAGs drive it with Wilcoxon and Cliff’s delta. Day 5 quantified how strongly abundance tracks continuous gradients with regression. Day 6 revealed that most of those MAGs form one coherent guild using WGCNA. Day 7 connects all of it into the analytical workflow that produced a published paper — and gives you the checklist to repeat it on your own data.</p>

<p>The dataset was 61 MAGs, 27 metagenomes, two estuaries, two seasons. The workflow is not specific to this dataset. Every step applies to 16S amplicon data, shotgun metagenomics, metatranscriptomics, or any table of samples × features × environmental metadata.</p>

<p>The statistics are the last thing you design. The biological questions come first.</p>

<hr />

<h2 id="what-i-would-do-differently">What I Would Do Differently</h2>

<p>▶ Run the Mantel test earlier. We computed it during revision, not during the initial analysis. It would have anchored the regression results more clearly from the start.</p>

<p>▶ Pre-register the analysis plan before looking at the data. Deciding between PERMANOVA and Mantel after seeing that one gives p = 0.04 and the other p = 0.06 is not rigorous. Write the analysis plan first.</p>

<p>▶ Save <code class="language-plaintext highlighter-rouge">sessionInfo()</code> output with every script. Package versions in WGCNA changed between R 4.2 and 4.3 in ways that silently affected TOM computation. We caught this during review. A saved <code class="language-plaintext highlighter-rouge">sessionInfo()</code> would have caught it earlier.</p>

<hr />

<p><em>Code, data, all results: <a href="https://github.com/jojyjohn28/microbiome_stats">github.com/jojyjohn28/microbiome_stats</a></em></p>

<p><em>All R code and data: <a href="https://github.com/jojyjohn28/microbiome_stats">github.com/jojyjohn28/microbiome_stats</a></em><br />
_ Read full paper here : <a href="https://academic.oup.com/ismecommun/advance-article/doi/10.1093/ismeco/ycag021/8454622">Functional Redudancy_John et al,2026</a></p>

<hr />

<p><img src="/assets/img/d8-summary.png" alt="WGCNA MAG Co-Abundance Networks" /></p>]]></content><author><name></name></author><category term="workflow" /><category term="PERMANOVA" /><category term="RDA" /><category term="regression" /><category term="WGCNA" /><category term="Mantel" /><category term="functional-redundancy" /><category term="metagenomics" /><category term="R" /><summary type="html"><![CDATA[Every method from Days 1–7, assembled into the actual order you run them, with the biological question that justifies each step — and the checklist you copy into every new project.]]></summary></entry><entry><title type="html">Day 7 — Correlation Analysis &amp;amp; Mantel Test: Linking MAGs to Environment</title><link href="https://jojyjohn28.github.io/blog/day7-correlation-mantel-test-microbiome/" rel="alternate" type="text/html" title="Day 7 — Correlation Analysis &amp;amp; Mantel Test: Linking MAGs to Environment" /><published>2026-03-10T00:00:00-04:00</published><updated>2026-03-10T00:00:00-04:00</updated><id>https://jojyjohn28.github.io/blog/day7-correlation-mantel-test-microbiome</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/day7-correlation-mantel-test-microbiome/"><![CDATA[<h4 id="day-7--correlation-analysis--mantel-test-linking-mags-to-environment">Day 7 — Correlation Analysis &amp; Mantel Test: Linking MAGs to Environment</h4>

<p>*Day 7 of 8 in the series <strong>Applied Statistics for Microbiome &amp; Genomics Data.*</strong> &amp; 🧬 Day 57 of Daily Bioinformatics from Jojy’s Desk
<em>Dataset: 61 MAGs × 27 metagenomes, Chesapeake Bay + Delaware Bay.</em>
<em>All R code: <a href="https://github.com/jojyjohn28/microbiome_stats">github.com/jojyjohn28/microbiome_stats</a></em></p>

<hr />

<p>Days 2–4 asked whether communities differ between groups. Day 5 asked how abundance changes along a gradient. Day 6 found that most of the signal collapses into a few ecological guilds.</p>

<p>Today we ask two complementary questions that tie everything together:</p>

<ol>
  <li><strong>Which environmental variables does each MAG track?</strong> — Spearman correlation, run individually between every MAG and every environmental measurement.</li>
  <li><strong>Does community dissimilarity scale with environmental dissimilarity?</strong> — the Mantel test, which works on distance matrices rather than raw values and makes no assumption about which group a sample belongs to.</li>
</ol>

<p>These are not the same question. The first is taxon-level. The second is community-level and continuous. Both answers matter.</p>

<hr />

<h4 id="why-spearman-not-pearson">Why Spearman, Not Pearson?</h4>

<p>MAG abundances are right-skewed, zero-inflated, and span several orders of magnitude. Environmental variables like bacterial production and cell counts have the same distribution problem. Pearson correlation assumes both variables are approximately normally distributed — that assumption fails here almost every time.</p>

<p>Spearman converts values to ranks before computing the correlation. It asks: <em>when Salinity increases from sample to sample, does Pelagibacterales rank consistently higher?</em> No distributional assumptions required.</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">library</span><span class="p">(</span><span class="n">Hmisc</span><span class="p">)</span><span class="w">
</span><span class="c1"># rcorr() returns r matrix and p-value matrix in one call</span><span class="w">
</span><span class="n">corr_env</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">rcorr</span><span class="p">(</span><span class="n">as.matrix</span><span class="p">(</span><span class="n">env_df</span><span class="p">),</span><span class="w"> </span><span class="n">type</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"spearman"</span><span class="p">)</span><span class="w">
</span><span class="n">corr_mat</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">corr_env</span><span class="o">$</span><span class="n">r</span><span class="w">
</span><span class="n">p_values</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">corr_env</span><span class="o">$</span><span class="n">P</span><span class="w">
</span></code></pre></div></div>

<p>The result is a symmetric matrix: each cell is the Spearman r between two variables, and a matching matrix gives p-values. Apply BH correction across all MAG–environment pairs before calling anything significant.</p>

<hr />

<h4 id="part-a-environmental-variable-correlations-panel-a">Part A: Environmental variable correlations (Panel A)</h4>

<p>Before correlating MAGs with environment, correlate the environmental variables with each other. This is not optional — if Salinity and Temperature are r = 0.85 with each other, you cannot interpret a MAG that correlates with both as responding to two independent drivers. It may be responding to one.</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">library</span><span class="p">(</span><span class="n">ggcorrplot</span><span class="p">)</span><span class="w">

</span><span class="n">ggcorrplot</span><span class="p">(</span><span class="n">corr_mat_env</span><span class="p">,</span><span class="w">
           </span><span class="n">hc.order</span><span class="w">  </span><span class="o">=</span><span class="w"> </span><span class="kc">TRUE</span><span class="p">,</span><span class="w">        </span><span class="c1"># cluster similar variables together</span><span class="w">
           </span><span class="n">type</span><span class="w">      </span><span class="o">=</span><span class="w"> </span><span class="s2">"lower"</span><span class="p">,</span><span class="w">
           </span><span class="n">lab</span><span class="w">       </span><span class="o">=</span><span class="w"> </span><span class="kc">TRUE</span><span class="p">,</span><span class="w">
           </span><span class="n">p.mat</span><span class="w">     </span><span class="o">=</span><span class="w"> </span><span class="n">pval_mat_env</span><span class="p">,</span><span class="w">
           </span><span class="n">sig.level</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">0.05</span><span class="p">,</span><span class="w">
           </span><span class="n">insig</span><span class="w">     </span><span class="o">=</span><span class="w"> </span><span class="s2">"blank"</span><span class="p">,</span><span class="w">     </span><span class="c1"># hide non-significant cells</span><span class="w">
           </span><span class="n">colors</span><span class="w">    </span><span class="o">=</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="s2">"#2166ac"</span><span class="p">,</span><span class="w"> </span><span class="s2">"#f7f7f7"</span><span class="p">,</span><span class="w"> </span><span class="s2">"#b2182b"</span><span class="p">))</span><span class="w">
</span></code></pre></div></div>

<p>In our dataset, Salinity and Temperature are strongly positively correlated (r ≈ 0.80) — both increase together along the spring-to-summer estuarine gradient. Nitrate is strongly negatively correlated with both (high Nitrate in cold, low-salinity spring samples). Silicate follows a similar pattern. Bacterial Production and Cell Count co-vary positively with Temperature.</p>

<p>This collinearity structure directly explains why Day 5 regression found that Temperature and Season added no independent explanatory power once Salinity was in the model — they share the same underlying gradient.</p>

<hr />

<h4 id="part-b-magenvironment-correlations-panel-b">Part B: MAG–environment correlations (Panel B)</h4>

<p>With the collinearity structure understood, correlate each of the 61 MAGs against each of the 9 environmental variables. That is 549 tests — BH FDR correction across all of them is mandatory.</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># rcorr on the combined matrix, then extract the MAG × env block</span><span class="w">
</span><span class="n">combined</span><span class="w">     </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">cbind</span><span class="p">(</span><span class="n">mag_log</span><span class="p">,</span><span class="w"> </span><span class="n">env_df</span><span class="p">)</span><span class="w">
</span><span class="n">corr_all</span><span class="w">     </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">rcorr</span><span class="p">(</span><span class="n">as.matrix</span><span class="p">(</span><span class="n">combined</span><span class="p">),</span><span class="w"> </span><span class="n">type</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"spearman"</span><span class="p">)</span><span class="w">
</span><span class="n">mag_env_r</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">corr_all</span><span class="o">$</span><span class="n">r</span><span class="p">[</span><span class="m">1</span><span class="o">:</span><span class="n">n_mags</span><span class="p">,</span><span class="w"> </span><span class="p">(</span><span class="n">n_mags</span><span class="m">+1</span><span class="p">)</span><span class="o">:</span><span class="p">(</span><span class="n">n_mags</span><span class="o">+</span><span class="n">n_env</span><span class="p">)]</span><span class="w">
</span><span class="n">mag_env_p</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">corr_all</span><span class="o">$</span><span class="n">P</span><span class="p">[</span><span class="m">1</span><span class="o">:</span><span class="n">n_mags</span><span class="p">,</span><span class="w"> </span><span class="p">(</span><span class="n">n_mags</span><span class="m">+1</span><span class="p">)</span><span class="o">:</span><span class="p">(</span><span class="n">n_mags</span><span class="o">+</span><span class="n">n_env</span><span class="p">)]</span><span class="w">

</span><span class="c1"># BH correction</span><span class="w">
</span><span class="n">mag_env_q</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">matrix</span><span class="p">(</span><span class="n">p.adjust</span><span class="p">(</span><span class="n">as.vector</span><span class="p">(</span><span class="n">mag_env_p</span><span class="p">),</span><span class="w"> </span><span class="n">method</span><span class="o">=</span><span class="s2">"BH"</span><span class="p">),</span><span class="w">
                        </span><span class="n">nrow</span><span class="o">=</span><span class="n">n_mags</span><span class="p">,</span><span class="w"> </span><span class="n">ncol</span><span class="o">=</span><span class="n">n_env</span><span class="p">)</span><span class="w">
</span></code></pre></div></div>

<table>
  <tbody>
    <tr>
      <td>The heatmap (Panel B) shows the top 20 MAGs by maximum</td>
      <td>r</td>
      <td>. The pattern mirrors what PERMANOVA and Wilcoxon found: Salinity and Temperature dominate. Pelagibacterales and Rhodobacterales MAGs show strong positive Salinity correlations; Burkholderiales and Nanopelagicales show negative. Nitrate is a near-mirror of Temperature for the same MAGs — unsurprisingly, given the collinearity in Panel A.</td>
    </tr>
  </tbody>
</table>

<p><strong>What correlation adds over Wilcoxon:</strong> Wilcoxon told you which MAGs are enriched in one season. Correlation tells you <em>which environmental variable along that seasonal gradient</em> each MAG is actually tracking, and whether the relationship holds continuously across all 27 samples rather than just between two group centroids.</p>

<hr />

<h4 id="part-c--d-the-mantel-test">Part C &amp; D: The Mantel Test</h4>

<p>The Mantel test works at the community level, not the taxon level. It asks: <em>do pairs of samples that are environmentally similar also have similar community composition?</em></p>

<p>It computes two distance matrices:</p>

<ul>
  <li><strong>Community distance:</strong> Bray-Curtis dissimilarity between all pairs of samples</li>
  <li><strong>Environmental distance:</strong> Euclidean distance between sample environmental profiles</li>
</ul>

<p>Then it correlates these two matrices using a permutation test (shuffle one matrix’s labels, recompute the correlation, repeat 9999 times to build a null distribution).</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">library</span><span class="p">(</span><span class="n">vegan</span><span class="p">)</span><span class="w">

</span><span class="n">hell</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">decostand</span><span class="p">(</span><span class="n">mag_t</span><span class="p">,</span><span class="w"> </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"hellinger"</span><span class="p">)</span><span class="w">
</span><span class="n">bc_dist</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">vegdist</span><span class="p">(</span><span class="n">hell</span><span class="p">,</span><span class="w"> </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"bray"</span><span class="p">)</span><span class="w">
</span><span class="n">env_dist</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">dist</span><span class="p">(</span><span class="n">scale</span><span class="p">(</span><span class="n">env_df</span><span class="p">),</span><span class="w"> </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"euclidean"</span><span class="p">)</span><span class="w">

</span><span class="n">set.seed</span><span class="p">(</span><span class="m">42</span><span class="p">)</span><span class="w">
</span><span class="n">mantel_full</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">mantel</span><span class="p">(</span><span class="n">bc_dist</span><span class="p">,</span><span class="w"> </span><span class="n">env_dist</span><span class="p">,</span><span class="w">
                       </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"pearson"</span><span class="p">,</span><span class="w"> </span><span class="n">permutations</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">9999</span><span class="p">)</span><span class="w">
</span><span class="n">print</span><span class="p">(</span><span class="n">mantel_full</span><span class="p">)</span><span class="w">
</span></code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Mantel statistic r: 0.70
Significance: 1e-04

Upper quantiles of permutations (null model):
   90%    95%  97.5%    99%
0.0895 0.1193 0.1486 0.1830

Permutation: free
Number of permutations: 9999
</code></pre></div></div>

<p>r = 0.70, p &lt; 0.0001. The full environmental distance matrix predicts 49% of the variance in community dissimilarity (r² = 0.49).</p>

<p><strong>Partial Mantel — isolating salinity from temperature:</strong></p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">sal_dist</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">dist</span><span class="p">(</span><span class="n">scale</span><span class="p">(</span><span class="n">env_df</span><span class="p">[[</span><span class="s2">"Salinity"</span><span class="p">]]))</span><span class="w">
</span><span class="n">temp_dist</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">dist</span><span class="p">(</span><span class="n">scale</span><span class="p">(</span><span class="n">env_df</span><span class="p">[[</span><span class="s2">"Temperature"</span><span class="p">]]))</span><span class="w">

</span><span class="n">mantel.partial</span><span class="p">(</span><span class="n">bc_dist</span><span class="p">,</span><span class="w"> </span><span class="n">sal_dist</span><span class="p">,</span><span class="w"> </span><span class="n">temp_dist</span><span class="p">,</span><span class="w">
               </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"pearson"</span><span class="p">,</span><span class="w"> </span><span class="n">permutations</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">9999</span><span class="p">)</span><span class="w">
</span><span class="c1"># r = 0.31, p = 0.008</span><span class="w">
</span></code></pre></div></div>

<p>After controlling for the shared variance with Temperature, Salinity still retains a significant partial correlation with community dissimilarity (r = 0.31, p = 0.008). This means Salinity is not just a proxy for Temperature — it carries independent information about which communities co-occur.</p>

<p><strong>What Mantel adds over PERMANOVA:</strong> PERMANOVA asked whether communities in Spring differ from Summer (a discrete group test). The Mantel test asked whether the degree of community difference scales continuously with the degree of environmental difference. The two can give different answers; both are informative. In this dataset they agree: environmental gradients, particularly salinity, continuously structure community composition.</p>

<hr />

<h2 id="the-r-code-complete">The R Code (complete)</h2>

<p>All code for both correlation analyses and the Mantel test, including the 4-panel figure, is in the Day 7 script at:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/Jojy_Research_Sync/.../day7-correlation/day7_correlation_mantel.R
</code></pre></div></div>

<p>Figure panels:</p>

<ul>
  <li><strong>A</strong> — Environmental variable Spearman correlation heatmap (<code class="language-plaintext highlighter-rouge">ggcorrplot</code>)</li>
  <li><strong>B</strong> — Top 20 MAG × environment Spearman heatmap with BH-corrected significance stars</li>
  <li><strong>C</strong> — Mantel r bar chart per environmental variable (blue = p &lt; 0.05)</li>
  <li><strong>D</strong> — Mantel permutation null distribution with observed r marked</li>
</ul>

<hr />

<h2 id="results-summary">Results Summary</h2>

<table>
  <thead>
    <tr>
      <th>Test</th>
      <th>Result</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Env–env collinearity</td>
      <td>Salinity–Temperature r = 0.80, Nitrate negatively correlated with both</td>
    </tr>
    <tr>
      <td>Salinity–MAG significant (q&lt;0.05)</td>
      <td>~28 MAGs</td>
    </tr>
    <tr>
      <td>Temperature–MAG significant</td>
      <td>~26 MAGs</td>
    </tr>
    <tr>
      <td>Nitrate–MAG significant</td>
      <td>~18 MAGs</td>
    </tr>
    <tr>
      <td>Full Mantel (community ~ environment)</td>
      <td>r = 0.70, p &lt; 0.0001</td>
    </tr>
    <tr>
      <td>Partial Mantel (Salinity | Temperature)</td>
      <td>r = 0.31, p = 0.008</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="common-pitfalls">Common Pitfalls</h2>

<p>▶ <strong>Using Pearson on skewed data.</strong> MAG abundances are not normally distributed. Pearson r will be distorted by a handful of outlier samples. Always use Spearman for field ecological data.</p>

<p>▶ <strong>Not correcting for multiple tests.</strong> 61 MAGs × 9 env variables = 549 tests. At α = 0.05 you expect ~27 false positives by chance alone. BH correction is the minimum requirement.</p>

<p>▶ <strong>Interpreting correlated env variables as independent drivers.</strong> If Salinity and Temperature both correlate significantly with a MAG, check the env–env heatmap first. If they are collinear, you cannot attribute the signal to both independently without a regression that controls for each.</p>

<p>▶ <strong>Confusing Mantel with PERMANOVA.</strong> PERMANOVA partitions variance by group labels. Mantel measures matrix correlation — it has no concept of groups. Use both; they answer different questions.</p>

<hr />

<h2 id="key-takeaways">Key Takeaways</h2>

<p>▶ Spearman correlation translates community-level patterns (season drives composition) into taxon-level mechanism (Pelagibacterales tracks Salinity, r = +0.83; Burkholderiales tracks Nitrate, r = +0.76).</p>

<p>▶ The env–env correlation heatmap is the prerequisite step before any MAG–env correlation — it tells you which drivers are independent and which share the same underlying gradient.</p>

<p>▶ The Mantel test (r = 0.70) confirms that the degree of environmental difference between any two samples predicts the degree of community difference. The community is not randomly distributed across the environmental landscape.</p>

<p>▶ The partial Mantel test shows Salinity retains an independent community-structuring signal beyond its correlation with Temperature — directly supporting the regression result from Day 5.</p>

<hr />

<h2 id="whats-next">What’s Next</h2>

<p>Day 8: <strong>Putting it all together</strong> — connecting Days 1–7 into a coherent analytical workflow, how each method answers a different biological question, and a reusable analysis checklist for your own projects.</p>

<hr />

<p><em>Code, data, all results: <a href="https://github.com/jojyjohn28/microbiome_stats">github.com/jojyjohn28/microbiome_stats</a></em></p>

<p><em>All R code and data: <a href="https://github.com/jojyjohn28/microbiome_stats">github.com/jojyjohn28/microbiome_stats</a></em><br />
<em>Found this useful? Share it with someone learning microbiome statistics.</em></p>

<hr />

<p><img src="/assets/img/d7_corr.png" alt="Correlation and Mantel Test" /></p>]]></content><author><name></name></author><category term="correlation" /><category term="Mantel" /><category term="Spearman" /><category term="distance-matrices" /><category term="metagenomics" /><category term="R" /><summary type="html"><![CDATA[Spearman correlation asks which environmental variables each MAG tracks. The Mantel test asks something deeper — does community dissimilarity itself scale with environmental dissimilarity?]]></summary></entry><entry><title type="html">Day 6 — WGCNA for Microbiome Co-Abundance Networks: From Individual MAGs to Modules</title><link href="https://jojyjohn28.github.io/blog/day6-wgcna-mag-coabundance-networks/" rel="alternate" type="text/html" title="Day 6 — WGCNA for Microbiome Co-Abundance Networks: From Individual MAGs to Modules" /><published>2026-03-09T00:00:00-04:00</published><updated>2026-03-09T00:00:00-04:00</updated><id>https://jojyjohn28.github.io/blog/day6-wgcna-mag-coabundance-networks</id><content type="html" xml:base="https://jojyjohn28.github.io/blog/day6-wgcna-mag-coabundance-networks/"><![CDATA[<h4 id="day-6--wgcna-for-microbiome-co-abundance-networks-from-individual-mags-to-modules">Day 6 — WGCNA for Microbiome Co-Abundance Networks: From Individual MAGs to Modules</h4>

<p>*Day 6 of 7 in the series <strong>Applied Statistics for Microbiome &amp; Genomics Data.*</strong> &amp; 🧬 Day 55 of Daily Bioinformatics from Jojy’s Desk
<em>Dataset: 61 MAGs × 27 metagenomes, Chesapeake Bay + Delaware Bay.</em>
<em>All R code: <a href="https://github.com/jojyjohn28/microbiome_stats">github.com/jojyjohn28/microbiome_stats</a></em></p>

<hr />

<p>Day 4 gave you a list: 40 of 61 MAGs are significantly enriched in one season over the other, Wilcoxon q &lt; 0.05. That list is useful, but it raises an immediate follow-up question — are those 40 MAGs 40 independent stories, or do they form a smaller number of coherent ecological guilds that rise and fall together?</p>

<p>WGCNA (Weighted Gene Co-expression Network Analysis) answers that question. It was designed for gene expression — find groups of genes that are co-regulated across samples and link them to external traits. The translation to MAG co-abundance is direct and underrepresented in the metagenomics literature: instead of genes co-expressed across RNA-seq samples, you have MAGs that co-occur in abundance across metagenomes.</p>

<blockquote>
  <p><em>How many distinct ecological modules exist in this MAG community? Which environmental gradient drives each one?</em></p>
</blockquote>

<p>This is the shift from asking “which MAG?” to asking “which guild?” — and the answer is almost always more interpretable.</p>

<hr />

<h4 id="why-not-just-cluster-on-abundance">Why Not Just Cluster on Abundance?</h4>

<p>You could compute a Pearson correlation matrix between MAGs and do hierarchical clustering on it. WGCNA does something smarter in two ways.</p>

<p><strong>Soft thresholding</strong> raises the correlation matrix to a power β before building the network. Low correlations (noise) are suppressed toward zero; high correlations (real co-occurrence) are preserved. The network becomes sparse and scale-free, which is the empirically observed architecture of biological interaction networks.</p>

<p><strong>Topological Overlap (TOM)</strong> then refines the adjacency. Two MAGs get a high TOM score not just if they directly co-occur, but if they share many co-occurrence partners in common — a network-aware similarity that is far more robust to noise than pairwise correlation alone. In a metagenome with 27 samples, TOM substantially reduces the chance that two MAGs appear co-abundant simply because both happened to be rare in the same subset of samples.</p>

<hr />

<h4 id="the-conceptual-pipeline">The Conceptual Pipeline</h4>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Raw abundances
      ↓  Hellinger transformation
MAG × MAG Pearson correlation matrix
      ↓  Signed soft thresholding  (adjacency = |0.5 + 0.5·r|^β)
Weighted adjacency matrix
      ↓  Topological Overlap Matrix (TOM)
1 − TOM  →  hierarchical clustering  →  modules
      ↓  PCA of each module's abundance matrix
Module eigengenes  →  correlate with environmental traits
</code></pre></div></div>

<p>Each step has a specific reason. The Hellinger transformation handles compositionality and zero-inflation. The signed network preserves whether two MAGs co-occur positively or negatively. TOM handles noise. The eigengene reduces each module to a single representative profile.</p>

<hr />

<h4 id="before-you-start-filter-and-transform">Before You Start: Filter and Transform</h4>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">library</span><span class="p">(</span><span class="n">vegan</span><span class="p">)</span><span class="w">
</span><span class="n">library</span><span class="p">(</span><span class="n">WGCNA</span><span class="p">)</span><span class="w">
</span><span class="n">library</span><span class="p">(</span><span class="n">ggplot2</span><span class="p">)</span><span class="w">
</span><span class="n">library</span><span class="p">(</span><span class="n">patchwork</span><span class="p">)</span><span class="w">
</span><span class="n">library</span><span class="p">(</span><span class="n">reshape2</span><span class="p">)</span><span class="w">

</span><span class="n">options</span><span class="p">(</span><span class="n">stringsAsFactors</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="kc">FALSE</span><span class="p">)</span><span class="w">
</span><span class="n">enableWGCNAThreads</span><span class="p">()</span><span class="w">

</span><span class="c1">## ── 1. Load data ──────────────────────────────────────────────</span><span class="w">
</span><span class="n">metadata</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">read.csv</span><span class="p">(</span><span class="s2">"../day1/data/metadata.csv"</span><span class="p">,</span><span class="w"> </span><span class="n">header</span><span class="o">=</span><span class="kc">TRUE</span><span class="p">,</span><span class="w"> </span><span class="n">row.names</span><span class="o">=</span><span class="m">1</span><span class="p">,</span><span class="w"> </span><span class="n">sep</span><span class="o">=</span><span class="s2">","</span><span class="p">)</span><span class="w">
</span><span class="n">mag_raw</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">read.csv</span><span class="p">(</span><span class="s2">"../day1/data/mag_abundance.csv"</span><span class="p">,</span><span class="w"> </span><span class="n">header</span><span class="o">=</span><span class="kc">TRUE</span><span class="p">,</span><span class="w"> </span><span class="n">row.names</span><span class="o">=</span><span class="m">1</span><span class="p">,</span><span class="w"> </span><span class="n">sep</span><span class="o">=</span><span class="s2">","</span><span class="p">)</span><span class="w">
</span><span class="n">mag_t</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">t</span><span class="p">(</span><span class="n">mag_raw</span><span class="p">)</span><span class="w">

</span><span class="n">metadata</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">metadata</span><span class="p">[</span><span class="n">match</span><span class="p">(</span><span class="n">rownames</span><span class="p">(</span><span class="n">mag_t</span><span class="p">),</span><span class="w"> </span><span class="n">rownames</span><span class="p">(</span><span class="n">metadata</span><span class="p">)),</span><span class="w"> </span><span class="p">]</span><span class="w">
</span><span class="n">stopifnot</span><span class="p">(</span><span class="nf">all</span><span class="p">(</span><span class="n">rownames</span><span class="p">(</span><span class="n">mag_t</span><span class="p">)</span><span class="w"> </span><span class="o">==</span><span class="w"> </span><span class="n">rownames</span><span class="p">(</span><span class="n">metadata</span><span class="p">)))</span><span class="w">

</span><span class="c1">## ── 2. Hellinger transformation ───────────────────────────────</span><span class="w">
</span><span class="c1"># Hellinger: square root of relative abundance</span><span class="w">
</span><span class="c1"># Reduces influence of dominant taxa, handles compositionality</span><span class="w">
</span><span class="n">hell</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">decostand</span><span class="p">(</span><span class="n">mag_t</span><span class="p">,</span><span class="w"> </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"hellinger"</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── 3. Check data quality ─────────────────────────────────────</span><span class="w">
</span><span class="n">gsg</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">goodSamplesGenes</span><span class="p">(</span><span class="n">hell</span><span class="p">,</span><span class="w"> </span><span class="n">verbose</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">3</span><span class="p">)</span><span class="w">
</span><span class="k">if</span><span class="w"> </span><span class="p">(</span><span class="o">!</span><span class="n">gsg</span><span class="o">$</span><span class="n">allOK</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="w">
  </span><span class="n">hell</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">hell</span><span class="p">[</span><span class="n">gsg</span><span class="o">$</span><span class="n">goodSamples</span><span class="p">,</span><span class="w"> </span><span class="n">gsg</span><span class="o">$</span><span class="n">goodGenes</span><span class="p">]</span><span class="w">
  </span><span class="n">cat</span><span class="p">(</span><span class="s2">"Removed"</span><span class="p">,</span><span class="w"> </span><span class="nf">sum</span><span class="p">(</span><span class="o">!</span><span class="n">gsg</span><span class="o">$</span><span class="n">goodSamples</span><span class="p">),</span><span class="w"> </span><span class="s2">"samples,"</span><span class="p">,</span><span class="w">
      </span><span class="nf">sum</span><span class="p">(</span><span class="o">!</span><span class="n">gsg</span><span class="o">$</span><span class="n">goodGenes</span><span class="p">),</span><span class="w"> </span><span class="s2">"MAGs\n"</span><span class="p">)</span><span class="w">
</span><span class="p">}</span><span class="w">
</span><span class="c1"># With 61 MAGs and 27 samples, gsg$allOK should return TRUE</span><span class="w">
</span></code></pre></div></div>

<hr />

<h4 id="step-1-choose-the-soft-threshold-β">Step 1: Choose the Soft Threshold (β)</h4>

<p>The soft threshold is the most consequential decision in a WGCNA analysis. Pick it too low and you get a dense, noisy network where almost everything connects to everything. Pick it too high and you kill real signal.</p>

<p>The criterion is empirical: find the lowest β where the network approximates a scale-free topology — specifically, where the R² of a log–log regression of connectivity against frequency reaches 0.80. Simultaneously, mean connectivity should still be above zero (otherwise the network has collapsed entirely).</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## ── 4. Soft threshold selection ───────────────────────────────</span><span class="w">
</span><span class="n">powers</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="m">1</span><span class="o">:</span><span class="m">20</span><span class="p">)</span><span class="w">
</span><span class="n">sft</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">pickSoftThreshold</span><span class="p">(</span><span class="w">
            </span><span class="n">hell</span><span class="p">,</span><span class="w">
            </span><span class="n">powerVector</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">powers</span><span class="p">,</span><span class="w">
            </span><span class="n">networkType</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"signed"</span><span class="p">,</span><span class="w">
            </span><span class="n">verbose</span><span class="w">     </span><span class="o">=</span><span class="w"> </span><span class="m">5</span><span class="w">
          </span><span class="p">)</span><span class="w">

</span><span class="c1"># Inspect the table — look for lowest power where SFT.R.sq &gt; 0.80</span><span class="w">
</span><span class="n">print</span><span class="p">(</span><span class="n">sft</span><span class="o">$</span><span class="n">fitIndices</span><span class="p">)</span><span class="w">

</span><span class="c1"># For this dataset: β = 6 (local R² peak, mean connectivity still &gt; 0)</span><span class="w">
</span><span class="n">softPower</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="m">6</span><span class="w">
</span></code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  Power SFT.R.sq   slope  mean.k. median.k. max.k.
      1    0.370   4.210    26.2     ...
      6    0.105  -0.090     5.5     ...   ← chosen: local peak, connectivity &gt; 0
     12    0.467  -1.100     2.7
</code></pre></div></div>

<p>For small datasets (n &lt; 30 samples), the R² curve is noisy and may never cleanly reach 0.80. Do not force β = 30 just to hit the threshold — you will collapse the network. Accept the best local peak in the 6–12 range and note it explicitly in your methods.</p>

<hr />

<p><img src="/assets/img/wgcna-1.png" alt="WGCNA MAG Co-Abundance Networks" /></p>

<h4 id="step-2-adjacency-matrix--tom">Step 2: Adjacency Matrix → TOM</h4>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## ── 5. Build weighted adjacency ───────────────────────────────</span><span class="w">
</span><span class="c1"># Signed network: adjacency = (0.5 + 0.5 × Pearson r)^β</span><span class="w">
</span><span class="c1"># This preserves the sign of co-occurrence (positive vs negative)</span><span class="w">
</span><span class="n">adjacency</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">adjacency</span><span class="p">(</span><span class="n">hell</span><span class="p">,</span><span class="w"> </span><span class="n">power</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">softPower</span><span class="p">,</span><span class="w"> </span><span class="n">type</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"signed"</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── 6. Topological Overlap Matrix ─────────────────────────────</span><span class="w">
</span><span class="c1"># TOM accounts for shared neighbourhood structure, not just pairwise r</span><span class="w">
</span><span class="c1"># TOM_ij = (Σ_u a_iu · a_uj) / (min(k_i, k_j) + 1 − a_ij)</span><span class="w">
</span><span class="n">TOM</span><span class="w">     </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">TOMsimilarity</span><span class="p">(</span><span class="n">adjacency</span><span class="p">,</span><span class="w"> </span><span class="n">TOMType</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"signed"</span><span class="p">)</span><span class="w">
</span><span class="n">dissTOM</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="m">1</span><span class="w"> </span><span class="o">-</span><span class="w"> </span><span class="n">TOM</span><span class="w">
</span></code></pre></div></div>

<p>The TOM computation is the slow step. For 61 MAGs it runs in seconds. For datasets with thousands of OTUs it can take hours — save the object and never recompute it unnecessarily.</p>

<hr />

<h4 id="step-3-module-detection">Step 3: Module Detection</h4>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## ── 7. Hierarchical clustering on TOM dissimilarity ──────────</span><span class="w">
</span><span class="n">TaxaTree</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">hclust</span><span class="p">(</span><span class="n">as.dist</span><span class="p">(</span><span class="n">dissTOM</span><span class="p">),</span><span class="w"> </span><span class="n">method</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"average"</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── 8. Dynamic tree cut ───────────────────────────────────────</span><span class="w">
</span><span class="c1"># minClusterSize: minimum MAGs per module</span><span class="w">
</span><span class="c1"># deepSplit = 2: moderately sensitive splitting</span><span class="w">
</span><span class="n">minModuleSize</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="m">5</span><span class="w">

</span><span class="n">dynamicMods</span><span class="w">   </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">cutreeDynamic</span><span class="p">(</span><span class="w">
                   </span><span class="n">dendro</span><span class="w">            </span><span class="o">=</span><span class="w"> </span><span class="n">TaxaTree</span><span class="p">,</span><span class="w">
                   </span><span class="n">distM</span><span class="w">             </span><span class="o">=</span><span class="w"> </span><span class="n">dissTOM</span><span class="p">,</span><span class="w">
                   </span><span class="n">deepSplit</span><span class="w">         </span><span class="o">=</span><span class="w"> </span><span class="m">2</span><span class="p">,</span><span class="w">
                   </span><span class="n">pamRespectsDendro</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="kc">FALSE</span><span class="p">,</span><span class="w">
                   </span><span class="n">minClusterSize</span><span class="w">    </span><span class="o">=</span><span class="w"> </span><span class="n">minModuleSize</span><span class="w">
                 </span><span class="p">)</span><span class="w">
</span><span class="n">dynamicColors</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">labels2colors</span><span class="p">(</span><span class="n">dynamicMods</span><span class="p">)</span><span class="w">
</span><span class="n">cat</span><span class="p">(</span><span class="s2">"Initial modules:\n"</span><span class="p">);</span><span class="w"> </span><span class="n">print</span><span class="p">(</span><span class="n">table</span><span class="p">(</span><span class="n">dynamicColors</span><span class="p">))</span><span class="w">

</span><span class="c1">## ── 9. Merge similar modules ──────────────────────────────────</span><span class="w">
</span><span class="c1"># cutHeight = 0.25 → merge modules whose eigengenes correlate &gt; 0.75</span><span class="w">
</span><span class="n">merge</span><span class="w">        </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">mergeCloseModules</span><span class="p">(</span><span class="n">hell</span><span class="p">,</span><span class="w"> </span><span class="n">dynamicColors</span><span class="p">,</span><span class="w">
                                   </span><span class="n">cutHeight</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">0.25</span><span class="p">,</span><span class="w"> </span><span class="n">verbose</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">3</span><span class="p">)</span><span class="w">
</span><span class="n">moduleColors</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">merge</span><span class="o">$</span><span class="n">colors</span><span class="w">
</span><span class="n">mergedMEs</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">merge</span><span class="o">$</span><span class="n">newMEs</span><span class="w">

</span><span class="n">cat</span><span class="p">(</span><span class="s2">"After merging:\n"</span><span class="p">);</span><span class="w"> </span><span class="n">print</span><span class="p">(</span><span class="n">table</span><span class="p">(</span><span class="n">moduleColors</span><span class="p">))</span><span class="w">
</span><span class="c1"># grey = unassigned MAGs that fit no module cleanly (expected with small n)</span><span class="w">
</span></code></pre></div></div>

<p>The merge step is not optional. WGCNA’s dynamic tree cut is intentionally aggressive — it will produce many small modules that are nearly identical in their eigengene profiles. <code class="language-plaintext highlighter-rouge">mergeCloseModules</code> collapses any two modules whose eigengenes correlate above <code class="language-plaintext highlighter-rouge">1 − cutHeight = 0.75</code>. Skip this and you inflate your module count with redundant, non-replicable groups.</p>

<hr />

<h4 id="step-4-module-eigengenes-and-environmental-correlations">Step 4: Module Eigengenes and Environmental Correlations</h4>

<p>An eigengene is the first principal component of all MAGs within a module — a single representative abundance profile across your 27 samples. It lets you ask: does this guild as a whole track temperature? Salinity? Bacterial production?</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## ── 10. Recalculate module eigengenes ─────────────────────────</span><span class="w">
</span><span class="n">MEs0</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">moduleEigengenes</span><span class="p">(</span><span class="n">hell</span><span class="p">,</span><span class="w"> </span><span class="n">moduleColors</span><span class="p">)</span><span class="o">$</span><span class="n">eigengenes</span><span class="w">
</span><span class="n">MEs</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">orderMEs</span><span class="p">(</span><span class="n">MEs0</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── 11. Select numerical traits ───────────────────────────────</span><span class="w">
</span><span class="n">trait_cols</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="s2">"Salinity"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Temperature"</span><span class="p">,</span><span class="w"> </span><span class="s2">"ChlA"</span><span class="p">,</span><span class="w">
                </span><span class="s2">"Bacterial_Production"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Nitrate"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Phosphate"</span><span class="p">)</span><span class="w">
</span><span class="n">datTraits</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">metadata</span><span class="p">[,</span><span class="w"> </span><span class="n">intersect</span><span class="p">(</span><span class="n">trait_cols</span><span class="p">,</span><span class="w"> </span><span class="nf">names</span><span class="p">(</span><span class="n">metadata</span><span class="p">))]</span><span class="w">
</span><span class="n">datTraits</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">data.frame</span><span class="p">(</span><span class="n">lapply</span><span class="p">(</span><span class="n">datTraits</span><span class="p">,</span><span class="w"> </span><span class="n">as.numeric</span><span class="p">))</span><span class="w">
</span><span class="n">rownames</span><span class="p">(</span><span class="n">datTraits</span><span class="p">)</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">rownames</span><span class="p">(</span><span class="n">metadata</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── 12. Pearson correlation + p-values ────────────────────────</span><span class="w">
</span><span class="n">nSamples</span><span class="w">       </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">nrow</span><span class="p">(</span><span class="n">hell</span><span class="p">)</span><span class="w">
</span><span class="n">modTraitCor</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">cor</span><span class="p">(</span><span class="n">MEs</span><span class="p">,</span><span class="w"> </span><span class="n">datTraits</span><span class="p">,</span><span class="w"> </span><span class="n">use</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"p"</span><span class="p">)</span><span class="w">
</span><span class="n">modTraitPvalue</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">corPvalueStudent</span><span class="p">(</span><span class="n">modTraitCor</span><span class="p">,</span><span class="w"> </span><span class="n">nSamples</span><span class="p">)</span><span class="w">

</span><span class="c1">## ── 13. Module membership (MM) and trait significance (GS) ───</span><span class="w">
</span><span class="c1"># MM: how central each MAG is to its own module (correlation with eigengene)</span><span class="w">
</span><span class="c1"># GS: how significantly each MAG correlates with a trait of interest</span><span class="w">
</span><span class="n">sal</span><span class="w">      </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">as.data.frame</span><span class="p">(</span><span class="n">datTraits</span><span class="o">$</span><span class="n">Salinity</span><span class="p">);</span><span class="w"> </span><span class="nf">names</span><span class="p">(</span><span class="n">sal</span><span class="p">)</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="s2">"Salinity"</span><span class="w">
</span><span class="n">modNames</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">substring</span><span class="p">(</span><span class="nf">names</span><span class="p">(</span><span class="n">MEs</span><span class="p">),</span><span class="w"> </span><span class="m">3</span><span class="p">)</span><span class="w">

</span><span class="n">ModuleMembership</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">as.data.frame</span><span class="p">(</span><span class="n">cor</span><span class="p">(</span><span class="n">hell</span><span class="p">,</span><span class="w"> </span><span class="n">MEs</span><span class="p">,</span><span class="w"> </span><span class="n">use</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"p"</span><span class="p">))</span><span class="w">
</span><span class="nf">names</span><span class="p">(</span><span class="n">ModuleMembership</span><span class="p">)</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">paste</span><span class="p">(</span><span class="s2">"MM"</span><span class="p">,</span><span class="w"> </span><span class="n">modNames</span><span class="p">,</span><span class="w"> </span><span class="n">sep</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">""</span><span class="p">)</span><span class="w">

</span><span class="n">TraitSignificance</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">as.data.frame</span><span class="p">(</span><span class="n">cor</span><span class="p">(</span><span class="n">hell</span><span class="p">,</span><span class="w"> </span><span class="n">sal</span><span class="p">,</span><span class="w"> </span><span class="n">use</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"p"</span><span class="p">))</span><span class="w">
</span><span class="nf">names</span><span class="p">(</span><span class="n">TraitSignificance</span><span class="p">)</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="s2">"GS.Salinity"</span><span class="w">

</span><span class="c1"># MAGs with high MM (&gt; 0.8) AND high GS (&gt; 0.5) are hub MAGs —</span><span class="w">
</span><span class="c1"># central to their module AND most predictive of your environmental variable</span><span class="w">
</span></code></pre></div></div>

<hr />

<h4 id="the-figure-panels-ac">The Figure (Panels A–C)</h4>

<p>Three panels, horizontal layout, assembled with patchwork.</p>

<p><strong>Panel A</strong> — MAG dendrogram based on TOM dissimilarity, with module color bar below. This is the core clustering result: how the 61 MAGs group into ecological guilds based on co-abundance patterns, not taxonomy.</p>

<p><strong>Panel B</strong> — Topological Overlap Matrix, reordered by module. Bright block structure along the diagonal confirms that the modules are real — MAGs within a module genuinely co-occur at a higher rate than MAGs from different modules.</p>

<p><strong>Panel C</strong> — Module-trait correlation heatmap. Each cell shows the Pearson r and p-value between a module eigengene and an environmental variable. This is the biological punchline: which environmental gradient drives each guild.</p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## ── 14. Panel A: Dendrogram with color bar ────────────────────</span><span class="w">
</span><span class="c1"># Extract leaf order from the clustering tree</span><span class="w">
</span><span class="n">leaf_order</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">TaxaTree</span><span class="o">$</span><span class="n">order</span><span class="w">
</span><span class="n">n_mags</span><span class="w">     </span><span class="o">&lt;-</span><span class="w"> </span><span class="nf">length</span><span class="p">(</span><span class="n">moduleColors</span><span class="p">)</span><span class="w">

</span><span class="c1"># Color bar dataframe (one row per MAG, ordered by dendrogram)</span><span class="w">
</span><span class="n">cbar_df</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">data.frame</span><span class="p">(</span><span class="w">
  </span><span class="n">x</span><span class="w">     </span><span class="o">=</span><span class="w"> </span><span class="nf">seq_along</span><span class="p">(</span><span class="n">leaf_order</span><span class="p">),</span><span class="w">
  </span><span class="n">color</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">moduleColors</span><span class="p">[</span><span class="n">leaf_order</span><span class="p">]</span><span class="w">
</span><span class="p">)</span><span class="w">

</span><span class="c1"># Dendrogram — convert hclust to ggplot-compatible format</span><span class="w">
</span><span class="c1"># Using ggdendro for clean rendering</span><span class="w">
</span><span class="n">library</span><span class="p">(</span><span class="n">ggdendro</span><span class="p">)</span><span class="w">
</span><span class="n">dend_data</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">dendro_data</span><span class="p">(</span><span class="n">TaxaTree</span><span class="p">,</span><span class="w"> </span><span class="n">type</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"rectangle"</span><span class="p">)</span><span class="w">

</span><span class="n">p_dend</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">ggdendrogram</span><span class="p">(</span><span class="n">TaxaTree</span><span class="p">,</span><span class="w"> </span><span class="n">rotate</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="kc">FALSE</span><span class="p">,</span><span class="w"> </span><span class="n">labels</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="kc">FALSE</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">labs</span><span class="p">(</span><span class="n">title</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"MAG Dendrogram (TOM dissimilarity)"</span><span class="p">,</span><span class="w">
       </span><span class="n">x</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"MAGs"</span><span class="p">,</span><span class="w"> </span><span class="n">y</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"Dissimilarity (1 − TOM)"</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">theme_classic</span><span class="p">(</span><span class="n">base_size</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">11</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">theme</span><span class="p">(</span><span class="w">
    </span><span class="n">axis.text.x</span><span class="w">  </span><span class="o">=</span><span class="w"> </span><span class="n">element_blank</span><span class="p">(),</span><span class="w">
    </span><span class="n">axis.ticks.x</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">element_blank</span><span class="p">(),</span><span class="w">
    </span><span class="n">plot.title</span><span class="w">   </span><span class="o">=</span><span class="w"> </span><span class="n">element_text</span><span class="p">(</span><span class="n">face</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"bold"</span><span class="p">,</span><span class="w"> </span><span class="n">size</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">11</span><span class="p">)</span><span class="w">
  </span><span class="p">)</span><span class="w">

</span><span class="c1"># Color bar below</span><span class="w">
</span><span class="n">p_cbar</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">ggplot</span><span class="p">(</span><span class="n">cbar_df</span><span class="p">,</span><span class="w"> </span><span class="n">aes</span><span class="p">(</span><span class="n">x</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">x</span><span class="p">,</span><span class="w"> </span><span class="n">y</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">1</span><span class="p">,</span><span class="w"> </span><span class="n">fill</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">color</span><span class="p">))</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">geom_tile</span><span class="p">()</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">scale_fill_identity</span><span class="p">()</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">theme_void</span><span class="p">()</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">theme</span><span class="p">(</span><span class="n">plot.margin</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">margin</span><span class="p">(</span><span class="m">0</span><span class="p">,</span><span class="w"> </span><span class="m">0</span><span class="p">,</span><span class="w"> </span><span class="m">0</span><span class="p">,</span><span class="w"> </span><span class="m">0</span><span class="p">))</span><span class="w">

</span><span class="c1"># Stack dendrogram on top of color bar</span><span class="w">
</span><span class="n">p_A</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">p_dend</span><span class="w"> </span><span class="o">/</span><span class="w"> </span><span class="n">p_cbar</span><span class="w"> </span><span class="o">+</span><span class="w"> </span><span class="n">plot_layout</span><span class="p">(</span><span class="n">heights</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="m">10</span><span class="p">,</span><span class="w"> </span><span class="m">1</span><span class="p">))</span><span class="w">
</span></code></pre></div></div>

<p><img src="/assets/img/wgcna-2.png" alt="WGCNA MAG Co-Abundance Networks" /></p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## ── 15. Panel B: TOM heatmap ──────────────────────────────────</span><span class="w">
</span><span class="c1"># Reorder TOM by dendrogram leaf order</span><span class="w">
</span><span class="n">tom_vals</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="m">1</span><span class="w"> </span><span class="o">-</span><span class="w"> </span><span class="n">dissTOM</span><span class="w">                       </span><span class="c1"># TOM (similarity)</span><span class="w">
</span><span class="n">tom_plot</span><span class="w">  </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">tom_vals</span><span class="p">[</span><span class="n">leaf_order</span><span class="p">,</span><span class="w"> </span><span class="n">leaf_order</span><span class="p">]</span><span class="w">   </span><span class="c1"># reorder rows and cols</span><span class="w">
</span><span class="n">tom_plot7</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">tom_plot</span><span class="o">^</span><span class="m">7</span><span class="w">                         </span><span class="c1"># power for visual contrast</span><span class="w">
</span><span class="n">diag</span><span class="p">(</span><span class="n">tom_plot7</span><span class="p">)</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="kc">NA</span><span class="w">

</span><span class="c1"># Melt to long format for ggplot2</span><span class="w">
</span><span class="n">tom_df</span><span class="w">          </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">melt</span><span class="p">(</span><span class="n">tom_plot7</span><span class="p">,</span><span class="w"> </span><span class="n">varnames</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="s2">"MAG_x"</span><span class="p">,</span><span class="w"> </span><span class="s2">"MAG_y"</span><span class="p">),</span><span class="w"> </span><span class="n">value.name</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"TOM7"</span><span class="p">)</span><span class="w">
</span><span class="n">tom_df</span><span class="o">$</span><span class="n">MAG_x</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="nf">as.numeric</span><span class="p">(</span><span class="n">tom_df</span><span class="o">$</span><span class="n">MAG_x</span><span class="p">)</span><span class="w">
</span><span class="n">tom_df</span><span class="o">$</span><span class="n">MAG_y</span><span class="w">    </span><span class="o">&lt;-</span><span class="w"> </span><span class="nf">as.numeric</span><span class="p">(</span><span class="n">tom_df</span><span class="o">$</span><span class="n">MAG_y</span><span class="p">)</span><span class="w">

</span><span class="n">p_B</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">ggplot</span><span class="p">(</span><span class="n">tom_df</span><span class="p">,</span><span class="w"> </span><span class="n">aes</span><span class="p">(</span><span class="n">x</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">MAG_x</span><span class="p">,</span><span class="w"> </span><span class="n">y</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">MAG_y</span><span class="p">,</span><span class="w"> </span><span class="n">fill</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">TOM7</span><span class="p">))</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">geom_raster</span><span class="p">(</span><span class="n">interpolate</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="kc">FALSE</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">scale_fill_gradientn</span><span class="p">(</span><span class="w">
    </span><span class="n">colours</span><span class="w">  </span><span class="o">=</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="s2">"#f8fafc"</span><span class="p">,</span><span class="w"> </span><span class="s2">"#fde68a"</span><span class="p">,</span><span class="w"> </span><span class="s2">"#f59e0b"</span><span class="p">,</span><span class="w"> </span><span class="s2">"#dc2626"</span><span class="p">,</span><span class="w"> </span><span class="s2">"#7f1d1d"</span><span class="p">),</span><span class="w">
    </span><span class="n">na.value</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"white"</span><span class="p">,</span><span class="w">
    </span><span class="n">name</span><span class="w">     </span><span class="o">=</span><span class="w"> </span><span class="s2">"TOM⁷"</span><span class="w">
  </span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">labs</span><span class="p">(</span><span class="n">title</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"Topological Overlap Matrix (TOM)"</span><span class="p">,</span><span class="w">
       </span><span class="n">x</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"MAGs"</span><span class="p">,</span><span class="w"> </span><span class="n">y</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"MAGs"</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">theme_classic</span><span class="p">(</span><span class="n">base_size</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">11</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">theme</span><span class="p">(</span><span class="w">
    </span><span class="n">axis.text</span><span class="w">     </span><span class="o">=</span><span class="w"> </span><span class="n">element_blank</span><span class="p">(),</span><span class="w">
    </span><span class="n">axis.ticks</span><span class="w">    </span><span class="o">=</span><span class="w"> </span><span class="n">element_blank</span><span class="p">(),</span><span class="w">
    </span><span class="n">plot.title</span><span class="w">    </span><span class="o">=</span><span class="w"> </span><span class="n">element_text</span><span class="p">(</span><span class="n">face</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"bold"</span><span class="p">,</span><span class="w"> </span><span class="n">size</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">11</span><span class="p">),</span><span class="w">
    </span><span class="n">legend.position</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"right"</span><span class="w">
  </span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">coord_fixed</span><span class="p">()</span><span class="w">
</span></code></pre></div></div>

<p><img src="/assets/img/wgcna-3.png" alt="WGCNA MAG Co-Abundance Networks" /></p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## ── 16. Panel C: Module-trait heatmap ────────────────────────</span><span class="w">
</span><span class="c1"># Reshape correlation matrix to long format</span><span class="w">
</span><span class="n">cor_df</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">melt</span><span class="p">(</span><span class="n">modTraitCor</span><span class="p">,</span><span class="w"> </span><span class="n">varnames</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="s2">"Module"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Trait"</span><span class="p">),</span><span class="w">
               </span><span class="n">value.name</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"r"</span><span class="p">)</span><span class="w">
</span><span class="n">pval_df</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">melt</span><span class="p">(</span><span class="n">modTraitPvalue</span><span class="p">,</span><span class="w"> </span><span class="n">varnames</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="s2">"Module"</span><span class="p">,</span><span class="w"> </span><span class="s2">"Trait"</span><span class="p">),</span><span class="w">
                </span><span class="n">value.name</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"p"</span><span class="p">)</span><span class="w">
</span><span class="n">heatmap_df</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">merge</span><span class="p">(</span><span class="n">cor_df</span><span class="p">,</span><span class="w"> </span><span class="n">pval_df</span><span class="p">)</span><span class="w">

</span><span class="c1"># Significance labels</span><span class="w">
</span><span class="n">heatmap_df</span><span class="o">$</span><span class="n">label</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">with</span><span class="p">(</span><span class="n">heatmap_df</span><span class="p">,</span><span class="w">
  </span><span class="n">ifelse</span><span class="p">(</span><span class="n">p</span><span class="w"> </span><span class="o">&lt;</span><span class="w"> </span><span class="m">0.001</span><span class="p">,</span><span class="w"> </span><span class="n">paste0</span><span class="p">(</span><span class="nf">round</span><span class="p">(</span><span class="n">r</span><span class="p">,</span><span class="w"> </span><span class="m">2</span><span class="p">),</span><span class="w"> </span><span class="s2">"\n***"</span><span class="p">),</span><span class="w">
  </span><span class="n">ifelse</span><span class="p">(</span><span class="n">p</span><span class="w"> </span><span class="o">&lt;</span><span class="w"> </span><span class="m">0.01</span><span class="p">,</span><span class="w">  </span><span class="n">paste0</span><span class="p">(</span><span class="nf">round</span><span class="p">(</span><span class="n">r</span><span class="p">,</span><span class="w"> </span><span class="m">2</span><span class="p">),</span><span class="w"> </span><span class="s2">"\n**"</span><span class="p">),</span><span class="w">
  </span><span class="n">ifelse</span><span class="p">(</span><span class="n">p</span><span class="w"> </span><span class="o">&lt;</span><span class="w"> </span><span class="m">0.05</span><span class="p">,</span><span class="w">  </span><span class="n">paste0</span><span class="p">(</span><span class="nf">round</span><span class="p">(</span><span class="n">r</span><span class="p">,</span><span class="w"> </span><span class="m">2</span><span class="p">),</span><span class="w"> </span><span class="s2">"\n*"</span><span class="p">),</span><span class="w">
                    </span><span class="n">paste0</span><span class="p">(</span><span class="nf">round</span><span class="p">(</span><span class="n">r</span><span class="p">,</span><span class="w"> </span><span class="m">2</span><span class="p">),</span><span class="w"> </span><span class="s2">"\np="</span><span class="p">,</span><span class="w"> </span><span class="nf">round</span><span class="p">(</span><span class="n">p</span><span class="p">,</span><span class="w"> </span><span class="m">2</span><span class="p">))))))</span><span class="w">

</span><span class="c1"># Clean up module names for display (remove "ME" prefix)</span><span class="w">
</span><span class="n">heatmap_df</span><span class="o">$</span><span class="n">Module</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">gsub</span><span class="p">(</span><span class="s2">"^ME"</span><span class="p">,</span><span class="w"> </span><span class="s2">""</span><span class="p">,</span><span class="w"> </span><span class="n">heatmap_df</span><span class="o">$</span><span class="n">Module</span><span class="p">)</span><span class="w">

</span><span class="c1"># Trait display labels</span><span class="w">
</span><span class="n">trait_display</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="w">
  </span><span class="n">Salinity</span><span class="w">             </span><span class="o">=</span><span class="w"> </span><span class="s2">"Salinity\n(psu)"</span><span class="p">,</span><span class="w">
  </span><span class="n">Temperature</span><span class="w">          </span><span class="o">=</span><span class="w"> </span><span class="s2">"Temperature\n(°C)"</span><span class="p">,</span><span class="w">
  </span><span class="n">ChlA</span><span class="w">                 </span><span class="o">=</span><span class="w"> </span><span class="s2">"Chl-a\n(μg/L)"</span><span class="p">,</span><span class="w">
  </span><span class="n">Bacterial_Production</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"Bacterial\nProduction"</span><span class="p">,</span><span class="w">
  </span><span class="n">Nitrate</span><span class="w">              </span><span class="o">=</span><span class="w"> </span><span class="s2">"Nitrate\n(μM)"</span><span class="p">,</span><span class="w">
  </span><span class="n">Phosphate</span><span class="w">            </span><span class="o">=</span><span class="w"> </span><span class="s2">"Phosphate\n(μM)"</span><span class="w">
</span><span class="p">)</span><span class="w">
</span><span class="n">heatmap_df</span><span class="o">$</span><span class="n">Trait</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">recode</span><span class="p">(</span><span class="nf">as.character</span><span class="p">(</span><span class="n">heatmap_df</span><span class="o">$</span><span class="n">Trait</span><span class="p">),</span><span class="w">
                            </span><span class="o">!!!</span><span class="n">trait_display</span><span class="p">)</span><span class="w">

</span><span class="n">p_C</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="n">ggplot</span><span class="p">(</span><span class="n">heatmap_df</span><span class="p">,</span><span class="w"> </span><span class="n">aes</span><span class="p">(</span><span class="n">x</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">Trait</span><span class="p">,</span><span class="w"> </span><span class="n">y</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">Module</span><span class="p">,</span><span class="w"> </span><span class="n">fill</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">r</span><span class="p">))</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">geom_tile</span><span class="p">(</span><span class="n">colour</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"white"</span><span class="p">,</span><span class="w"> </span><span class="n">linewidth</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">0.8</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">geom_text</span><span class="p">(</span><span class="n">aes</span><span class="p">(</span><span class="n">label</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">label</span><span class="p">),</span><span class="w"> </span><span class="n">size</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">3</span><span class="p">,</span><span class="w"> </span><span class="n">lineheight</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">0.9</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">scale_fill_gradientn</span><span class="p">(</span><span class="w">
    </span><span class="n">colours</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="s2">"#2166ac"</span><span class="p">,</span><span class="w"> </span><span class="s2">"#92c5de"</span><span class="p">,</span><span class="w"> </span><span class="s2">"#f7f7f7"</span><span class="p">,</span><span class="w"> </span><span class="s2">"#f4a582"</span><span class="p">,</span><span class="w"> </span><span class="s2">"#b2182b"</span><span class="p">),</span><span class="w">
    </span><span class="n">limits</span><span class="w">  </span><span class="o">=</span><span class="w"> </span><span class="nf">c</span><span class="p">(</span><span class="m">-1</span><span class="p">,</span><span class="w"> </span><span class="m">1</span><span class="p">),</span><span class="w">
    </span><span class="n">name</span><span class="w">    </span><span class="o">=</span><span class="w"> </span><span class="s2">"Pearson r"</span><span class="w">
  </span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">labs</span><span class="p">(</span><span class="n">title</span><span class="w">    </span><span class="o">=</span><span class="w"> </span><span class="s2">"Module–Trait Relationships"</span><span class="p">,</span><span class="w">
       </span><span class="n">subtitle</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"r value · * p&lt;0.05  ** p&lt;0.01  *** p&lt;0.001"</span><span class="p">,</span><span class="w">
       </span><span class="n">x</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="kc">NULL</span><span class="p">,</span><span class="w"> </span><span class="n">y</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="kc">NULL</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">theme_classic</span><span class="p">(</span><span class="n">base_size</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">11</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">theme</span><span class="p">(</span><span class="w">
    </span><span class="n">axis.text.x</span><span class="w">  </span><span class="o">=</span><span class="w"> </span><span class="n">element_text</span><span class="p">(</span><span class="n">size</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">9</span><span class="p">),</span><span class="w">
    </span><span class="n">axis.text.y</span><span class="w">  </span><span class="o">=</span><span class="w"> </span><span class="n">element_text</span><span class="p">(</span><span class="n">size</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">10</span><span class="p">,</span><span class="w"> </span><span class="n">face</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"bold"</span><span class="p">),</span><span class="w">
    </span><span class="n">plot.title</span><span class="w">   </span><span class="o">=</span><span class="w"> </span><span class="n">element_text</span><span class="p">(</span><span class="n">face</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"bold"</span><span class="p">,</span><span class="w"> </span><span class="n">size</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">11</span><span class="p">),</span><span class="w">
    </span><span class="n">plot.subtitle</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">element_text</span><span class="p">(</span><span class="n">size</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">8.5</span><span class="p">,</span><span class="w"> </span><span class="n">colour</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"grey50"</span><span class="p">),</span><span class="w">
    </span><span class="n">legend.position</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"right"</span><span class="w">
  </span><span class="p">)</span><span class="w">
</span></code></pre></div></div>

<p><img src="/assets/img/wgcna-4.png" alt="WGCNA MAG Co-Abundance Networks" /></p>

<div class="language-r highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## ── 17. Assemble figure ───────────────────────────────────────</span><span class="w">
</span><span class="n">combined_fig</span><span class="w"> </span><span class="o">&lt;-</span><span class="w"> </span><span class="p">(</span><span class="n">p_A</span><span class="w"> </span><span class="o">|</span><span class="w"> </span><span class="n">p_B</span><span class="w"> </span><span class="o">|</span><span class="w"> </span><span class="n">p_C</span><span class="p">)</span><span class="w"> </span><span class="o">+</span><span class="w">
  </span><span class="n">plot_annotation</span><span class="p">(</span><span class="w">
    </span><span class="n">title</span><span class="w">    </span><span class="o">=</span><span class="w"> </span><span class="s2">"Day 6 — WGCNA: MAG Co-Abundance Modules &amp; Environmental Drivers"</span><span class="p">,</span><span class="w">
    </span><span class="n">subtitle</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"61 MAGs  ·  27 metagenomes  ·  Signed Pearson  ·  β=6  ·  TOM clustering  ·  Hellinger transformation"</span><span class="p">,</span><span class="w">
    </span><span class="n">tag_levels</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"A"</span><span class="w">
  </span><span class="p">)</span><span class="w"> </span><span class="o">&amp;</span><span class="w">
  </span><span class="n">theme</span><span class="p">(</span><span class="n">plot.tag</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">element_text</span><span class="p">(</span><span class="n">face</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"bold"</span><span class="p">,</span><span class="w"> </span><span class="n">size</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">13</span><span class="p">))</span><span class="w">

</span><span class="c1">## ── 18. Save ──────────────────────────────────────────────────</span><span class="w">
</span><span class="n">dir.create</span><span class="p">(</span><span class="s2">"figures"</span><span class="p">,</span><span class="w"> </span><span class="n">showWarnings</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="kc">FALSE</span><span class="p">)</span><span class="w">
</span><span class="n">ggsave</span><span class="p">(</span><span class="w">
  </span><span class="s2">"~/Jojy_Research_Sync/website_assets/projects/microbiome_stats/day6-wgcna/figures/day6_wgcna_figure.tiff"</span><span class="p">,</span><span class="w">
  </span><span class="n">combined_fig</span><span class="p">,</span><span class="w">
  </span><span class="n">width</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">18</span><span class="p">,</span><span class="w"> </span><span class="n">height</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">7</span><span class="p">,</span><span class="w"> </span><span class="n">dpi</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="m">300</span><span class="p">,</span><span class="w"> </span><span class="n">compression</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"lzw"</span><span class="w">
</span><span class="p">)</span><span class="w">
</span></code></pre></div></div>

<hr />

<h2 id="results-from-our-data">Results from Our Data</h2>

<p>Running WGCNA on 61 MAGs across 27 samples — with β = 6, <code class="language-plaintext highlighter-rouge">minClusterSize = 5</code>, and merge cutHeight = 0.25 — produces <strong>4 modules</strong> (plus 3 grey unassigned MAGs):</p>

<table>
  <thead>
    <tr>
      <th>Module</th>
      <th>n MAGs</th>
      <th>Key trait</th>
      <th>r</th>
      <th>p</th>
      <th>Interpretation</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>brown</td>
      <td>4</td>
      <td>Salinity (−)</td>
      <td>−0.89</td>
      <td>&lt; 0.001</td>
      <td>Low-salinity specialists</td>
    </tr>
    <tr>
      <td>blue</td>
      <td>41</td>
      <td>Temperature (−)</td>
      <td>−0.97</td>
      <td>&lt; 0.001</td>
      <td>Cool-water / Spring guild</td>
    </tr>
    <tr>
      <td>green</td>
      <td>6</td>
      <td>Bacterial Production (+)</td>
      <td>+0.71</td>
      <td>&lt; 0.001</td>
      <td>Productivity-linked guild</td>
    </tr>
    <tr>
      <td>orange</td>
      <td>7</td>
      <td>Salinity (+)</td>
      <td>+0.57</td>
      <td>0.002</td>
      <td>High-salinity guild</td>
    </tr>
  </tbody>
</table>

<p>▶ <strong>Panel A (dendrogram)</strong> shows the blue module dominating the left branch — 41 MAGs clustering together under a single broad clade in the TOM tree. The four smaller modules (brown, green, orange) appear as distinct tight sub-branches on the right. Three grey MAGs sit as isolated leaves with no clear affiliation.</p>

<p>▶ <strong>Panel B (TOM heatmap)</strong> confirms that the block structure is real. The large dark block corresponding to the blue module shows genuinely elevated topological overlap among those 41 MAGs — they share co-occurrence partners, not just pairwise correlations. Smaller blocks for brown, green, and orange are visible but compact.</p>

<p>▶ <strong>Panel C (module-trait heatmap)</strong> is the biological punchline. The blue module eigengene correlates with Temperature at r = <strong>−0.97</strong> (p &lt; 0.001) and with Bacterial Production at r = <strong>−0.81</strong> (p &lt; 0.001). These are your spring/cool-water taxa. The brown module inverts this: r = <strong>−0.89</strong> with Salinity, meaning peak abundance at low salinity — freshwater-influenced, oligohaline specialists. The orange module is the mirror: r = <strong>+0.57</strong> with Salinity, enriched in high-salinity conditions.</p>

<p>▶ <strong>What WGCNA adds over Day 4 Wilcoxon results.</strong> Day 4 found 40 individual MAGs significant for season — a list. WGCNA shows that 41 of those MAGs form one coherent module, share a single eigengene, and are all driven by the same environmental signal (temperature). That 41-MAG blue module is not 41 independent stories. It is one story told by 41 organisms: the cool-water spring community that collapses as summer warms the estuary.</p>

<hr />

<h2 id="common-pitfalls">Common Pitfalls</h2>

<p>▶ <strong>Wrong soft threshold.</strong> Using β = 30 to force R² ≥ 0.80 on a small dataset destroys network structure. Use the pickSoftThreshold plot; accept the best local peak above 0.80, or the inflection in the R² curve if 0.80 is never reached cleanly.</p>

<p>▶ <strong>Skipping mergeCloseModules.</strong> WGCNA’s initial cut is aggressive. Without merging, you will report 8 modules when 4 are the correct answer, and several will fail to replicate in an independent dataset because they represent noise splits of the same underlying guild.</p>

<p>▶ <strong>Using an unsigned network.</strong> <code class="language-plaintext highlighter-rouge">type = "unsigned"</code> treats positive and negative co-abundance as equivalent — ecologically wrong. A MAG that always disappears when another appears is not in the same guild as one that tracks it positively. Always use <code class="language-plaintext highlighter-rouge">type = "signed"</code>.</p>

<p>▶ <strong>Ignoring grey.</strong> Grey MAGs are not failures. They are MAGs whose co-abundance pattern does not fit any identified module cleanly. Investigate them separately — they may represent cosmopolitan taxa with context-dependent associations, or genuinely rare MAGs with insufficient signal.</p>

<p>▶ <strong>Over-interpreting small modules.</strong> A module of 4 MAGs in 27 samples is not robust. Use <code class="language-plaintext highlighter-rouge">minClusterSize = 5</code> as a floor and treat modules with n &lt; 8 with skepticism. They may be real, but they will not replicate without a larger n.</p>

<hr />

<h2 id="key-takeaways">Key Takeaways</h2>

<p>▶ WGCNA moves you from 61 individual tests to 4 interpretable ecological modules. The blue module (41 MAGs, r = −0.97 with Temperature) is a result invisible when testing MAGs one at a time — but immediately apparent when you look at the community as a co-abundance network.</p>

<p>▶ The soft threshold β is the most consequential parameter you will set. It controls network sparsity. Use the scale-free topology criterion and the pickSoftThreshold plot to choose it — never copy a number from a tutorial without checking it on your own data.</p>

<p>▶ TOM is not just a correlation matrix with extra steps. By incorporating shared network neighbourhood structure, it is substantially more robust to noise than pairwise Pearson r — critical when n is small and every sample counts.</p>

<p>▶ The module eigengene is the bridge between network structure and biology. It compresses an entire guild into one abundance profile per sample, letting you correlate the guild as a unit with any continuous environmental variable.</p>

<p>▶ The module-trait heatmap is the take-home figure. It answers the question you started with: not which MAG is significant, but which ecological guild responds to which environmental driver.</p>

<hr />

<h2 id="whats-next">What’s Next</h2>

<p>Day 7: <strong>Correlation Analysis &amp; Mantel Test</strong>: Linking MAGs to Environment</p>

<hr />

<p><em>Code, data, all results: <a href="https://github.com/jojyjohn28/microbiome_stats">github.com/jojyjohn28/microbiome_stats</a></em></p>

<p><em>All R code and data: <a href="https://github.com/jojyjohn28/microbiome_stats">github.com/jojyjohn28/microbiome_stats</a></em><br />
<em>Found this useful? Share it with someone learning microbiome statistics.</em></p>

<hr />

<p><img src="/assets/img/wgcna-all.png" alt="WGCNA MAG Co-Abundance Networks" /></p>]]></content><author><name></name></author><category term="WGCNA" /><category term="co-expression" /><category term="network" /><category term="modules" /><category term="eigengenes" /><category term="metagenomics" /><category term="R" /><summary type="html"><![CDATA[WGCNA moves you from 61 individual Wilcoxon tests to a handful of ecological guilds — each with a name, a size, and a direct relationship to the environmental gradient that drives it.]]></summary></entry></feed>